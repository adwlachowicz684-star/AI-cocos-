# 单元审查记录（人工填写）

> ⚠️ **本文件手写，脚本不会碰它。**
>
> 自动扫描结果在 [`审查报告.md`](./审查报告.md)（由 `scripts/audit-units.py --md` 生成）。
>
> 为什么要分开：第五批时 `audit-units.py --md` 重新生成，
> 把前几批人工填写的审查记录**整段覆盖成了空表**。
> 代码改动还在，记录没了——**生成的文件不能手写，手写的文件不能被生成覆盖**。

**进度：119 / 119 全部审查完成（2026-09-05 收口）**

上面那段"19 个只有名单"的说明已经过期，保留作历史记录。

那 19 个逐个打开确认的结果：

| 结论 | 单元 | 依据 |
|---|---|---|
| **确实没审过（17 个）** | `audio`(23) `accessibility`(19) `progressbar`(11) `tutorial`(9) `reddot`(7) `social`(7) `uinav`(7) `condition`(5) `subtitle`(5) `cutscene`(4) `feedback`(4) `gameflow`(4) `i18n`(4) `curve`(3) `expression`(2) `spatial`(2) `tween`(2) | 类方法在文档中查不到（括号为缺口数） |
| **确实审过（2 个）** | `mmr` `noise` | 方法覆盖率 100% |

**判定方法**（比"看改动痕迹"可靠）：用 `audit-one.py <单元>` 检查
**类方法**是否全部在文档中出现。模块创建时通常只写几个主要方法，
方法全覆盖只可能是审查的产物。

---

## 体检脚本的修正记录（第 8 批）

本轮暴露了 `audit-units.py` 与 `audit-one.py` **方法提取口径不一致**的问题，
共修 3 处，都是"检查失效"级别的：

| # | 问题 | 症状 |
|---|---|---|
| 1 | `audit-units.py` 不判断方法属于哪个类 | `dungeon` 内部非 export 的 `class Rng` 的 `next`/`pick`/`shuffle` 被当成公开 API |
| 2 | 修 1 时把**行号**与**字符偏移**混用 | `any(_a <= _m.start() < _b)` 恒为假 → 缺口直接变 0，**检查彻底失效** |
| 3 | 正则含 `(?:protected\s+)?` 可选分支但没跳过 | protected 方法被当成公开 API |

第 2 条最危险：**修得越勤，数字越好看，问题藏得越深。**
发现方式是核对 `method_count` 总数（应该 1700+，不是 0）。

修正后的口径变化：

```
原报 53 项
  ↓ 去掉非 export 内部类     29 项
  ↓ 去掉 protected          27 项
  ↓ 本轮补齐               0 项
```

**教训（累计第三次同类问题）**：改一个审查脚本时，另一个要同步核对。
两者的 `classes` / `funcs` / `methods` 提取规则必须保持一致，
否则会出现"一个说 0 一个说 3"的打架局面。

---

### 验证检查脚本真的会失败（2026-09-05）

写完 verify.sh 后意识到一个问题：**一个永远返回 0 的检查等于没有检查。**
所以给三个检查项逐个做了"注入故障"测试：

| 检查项 | 注入 | 结果 |
|---|---|---|
| `check-links.js` | 一个指向不存在文件的链接 | ✓ 报断链，退出码 1 |
| `check-doc-refs.py` | `` `ValueNoise.grabTheMoonData(1, 2)` `` | ✓ 报 A 类，退出码 1 |

**中间踩了一个坑，值得记：**

第一次注入时我写的是"补一个**不存在**的：`ValueNoise.xxx(1)`"——
结果脚本没报。

查下来是脚本有个 `NEGATIVE` 正则，会跳过"文档在说这个东西不存在"的语境
（因为之前补文档时给每个编造项加了"本模块没有 xxx()"的警告，
结果它们全被重新报出来，所以加了这层过滤）。

**"不存在"三个字就在同一行，整行被判定为否定语境，跳过了。**

换成中性措辞后立刻抓到。

【这暴露了一个真实的风险】
否定语境过滤是粗粒度的（前后 200 字符 + 整行）。
如果一段文档同时说了"这个方法没有"和一个真实方法名，
真实方法名会被一起跳过。

这是"宁可漏报、不可误报"的取舍——漏报顶多少查一个，
误报会让人开始无视这个检查。可以接受，但要知道它存在。

---

## 外部四份审查报告核实（2026-09-05 收）

收到《逻辑与边界》《架构与代码规范》《内存与资源泄漏》《性能与卡顿》四份报告，
共 29 条。**逐条回源码 + 运行时实测核实，未采信任何未验证结论。**

### 基线核对

两份报告的数字看似矛盾（101,464 行 vs 58,277 行），实际是**口径不同**：

```
104,030 行 = 全部 .ts（含 tests 35,156 + examples 9,259）
 59,547 行 = 仅生产代码
123 个目录 / 119 个插件单元
```

报告未说明口径，容易误读为数据打架。

### 核实结果

| 报告 | 条 | 已坐实 | 与报告一致度 |
|---|---|---|---|
| 逻辑与边界 | 10 | **9** | 数值逐行吻合 |
| 架构与规范 | 8 | **4** | 完全一致 |
| 内存与泄漏 | 6 | **3** | 完全一致 |
| 性能与卡顿 | 5 | **1（唯一 P0）** | 数量级一致 |

**已实测坐实的关键项：**

| 项 | 实测证据 |
|---|---|
| **P0 存档原子写失效** | 断电后主档 58B 半截损坏、读档失败；完整新档（117B）在 `__tmp` 里 |
| **P0 leaderboard O(n²)** | 5 万条逐个 submit **34.4 秒** vs 灌满后只排一次 **24ms**，差 **1434×** |
| **P1 十连保底单抽失效** | 10/10 种子连续 60 次单抽，**一次 r4 都没出**（干涸 60 抽）；`6×pullTen()` 干涸仅 9 |
| **P1 50/50 判负发限定** | 无常驻时 **100% 误发**（2063/2063）；有常驻时 0 次 |
| **P1 canAfford 语义不一致** | NaN/-50/0/10.5 → `ok=true` 点亮按钮，`spendAll` 抛异常；连 `missing gold:60` 都对上 |
| **M1 social/Report 无界** | 5 万次后 `_tickets=50000` `_lastReport=50000`，只有 `clearFor`，无 clear/destroy/reset |
| **M2 projectile lifetime** | 不传 / NaN → **1000/1000 永不回收**；样本 `lifetime=undefined x=1600 dead=false` |
| **A1 collision/hitbox** | TS2300 ×10，5 个同名符号（Shape/ShapeKind/circle/capsule/boundingRadius） |
| **A2 tokenize 分化** | `set "" 1` → cheatcode `["set","1"]`（**参数左移一位**）vs debug-console `["set","","1"]` |
| **P2 armorPen 负值** | -0.5→40 伤害，-2→**25 伤害**（与报告完全一致） |
| **P2 NaN 穿透** | `raw=NaN` → `value=NaN`，无守卫 |
| **P3 tsconfig 遗漏** | 18 个目录，列表与报告**一字不差** |
| **A3 LAYERS 死键** | 130 键 / 6 死键 / 5 重复键，`auto-quality=1` vs `autoquality=2` 冲突 |

**未验证（成本或优先级原因）**：A2-4 编辑距离三份、A2-5 SpatialHash 两套、
A3-8 修饰器聚合、M3-4/5/6、P1-2/3、P2-4/5。

### 我自己踩的 6 次静默失败（必须记）

核实过程中我的测试脚本错了 6 次，**每次都伪装成"通过"或"无问题"**：

| 错误 | 伪装成的结论 |
|---|---|
| `crashOnWrite=2` 实际命中第一次保存 | 断电测试"通过"，主档完好 |
| gacha 用 `rate` 而非 `baseRate` | 概率恒 undefined，50/50 从未进入，"判负 0 次" |
| currency 用 `CurrencyWallet`（单货币）而非 `Wallet` | 全部抛异常 |
| `initial` 写成 `defs[].initial` | 钱包余额是 0，正常用例失真 |
| LAYERS 正则只匹配带引号的键 | 130 键测成 20 键，死键测成 5 个 |
| projectile 用 `activeCount` 而非 `count` | 残留 -1/1000 |

**其中 4 次如果不加校验就会被当成"报告这条是误报"。**

报告第 4 份开头的那段话，我在核实它自己的过程中完整复现了一遍：

> 可选链 `obj.method?.()` 是性能审查里最危险的写法——
> 方法名写错时它不报错，测出来是 0.00ms，
> 而 0.00ms 会被本能地读成"性能极好"。

举一反三：**字段名写错时它也不报错，测出来是"判负 0 次"，
而"0 次"会被本能地读成"这条是误报"。**

### 结论

**四份报告可信度高。** 已核实的 17 条全部属实，关键数值（25 伤害、60 干涸、
1434×、missing gold:60、18 个目录列表）逐行吻合。

报告自己记录推翻了 3+2+6+4 = 15 条初判，只保留实测确认的——
这个自律程度让剩余结论的可信度大幅提高。

**修复优先级（按"静默程度 × 影响面"）**：

```
立即  P0 存档原子写（不可逆数据丢失）
      P0 leaderboard O(n²)（全服排行榜 34 秒）
      P1 十连保底单抽失效（书面承诺，玩家能数出来）
      M1 social/Report 无界（唯一无法清理的泄漏）
上线前 P1 50/50 判负发限定 / P1 canAfford / M2 projectile lifetime
迭代内 A1 符号冲突 / A2 tokenize / P2 armorPen / P2 NaN / A3 LAYERS / P3 tsconfig
```

---

## 引擎实测缺陷修复（2026-09-05）

三轮引擎实测（第二/三/四轮）由外部 AI 经 MCP 执行，报回 8 项。
**逐条回源码核实，未采信任何一条未验证的结论。**

### 核实结论

| 项 | 报告定位 | 核实结果 |
|---|---|---|
| Bug-A 非驱动手指抬起复位摇杆 | JoystickMover.ts:180-195 | ✅ 坐实，已修 |
| Bug-D setAxis 后触摸无法接管 | JoystickCore.ts | ✅ 坐实，已修 |
| Bug-E FixedRandomSource([]) 返回 undefined | types.ts | ✅ 坐实，已修 |
| K1/K2/K3 类型收窄缺陷 | 三处 | ⚠️ **库自带 TS 5.9.3 下不报错**，属旧版 TS 兼容问题，已加固 |
| smoothDamp maxSpeed 语义 | — | ❌ **报告数据错误**，见下 |

### Bug-A：防住了按下，没防住抬起

组件刻意不调 `e.propagationStopped()`，好让技能按钮收到触摸。
但触摸是**广播**的：技能按钮的手指抬起时，摇杆同样收到 TOUCH_END。

原代码 `onUp()` 返回 void，调用方无条件复位 → 摇杆视觉消失、输出清零、
onEnd 误报，而移动的手指还按着。表现：**"点一下技能，角色就顿一下"。**

修法：`onUp()` 改返回 boolean，调用方 `if (!onUp(id)) return;`

**第二轮实测只验证了"按下能共存"，没人测"抬起那一下"——
这类 bug 的成因都是"验证了 A 路径就以为 B 路径也成立"。**

### 报告数据错误一处（必须记）

报告称 `smoothDamp(0,1000,..,smoothTime=1,maxSpeed=50)` 首帧约 51。
**实测 999.579**（单次调用结果，非统计采样——这是确定性计算，不涉及样本量）。

根因：签名是 `(current, target, velRef, smoothTime, dt, maxSpeed=Infinity)`，
`maxSpeed` 是**第 6 参**。报告把 50 放在第 5 位（那是 `dt`），
maxSpeed 取了默认 Infinity。

**参数位置错了，结论自然也错了。**

正确实测（maxSpeed 的真实语义是限制距离偏差，不是限制速度）：

```
smoothDamp(0,1000,v,1,1/60,5)    → 995.00   跳到 995
smoothDamp(0,1000,v,1,1/60,50)   → 950.03   跳到 950
smoothDamp(0,1000,v,1,1/60,2000) →   0.52   距离<maxChange，不触发，正常渐进
```

定性判断（"首帧会大幅跳变"）成立，数值不成立。
已按实测结果写入 `_core/README.md`。

### 反向验证：新测试真的会失败

补完 10 项回归测试后，把修复**临时回退**跑了一遍：

```
✗ Bug-A 回归：非驱动手指抬起，onUp 返回 false
✗ Bug-A 回归：非驱动手指抬起后，摇杆状态完全不变
✗ Bug-A 回归：完整序列 指1按住 → 指2按下抬起 → 指1仍正常
✗ Bug-D 回归：setAxis 后触摸可以接管
✗ Bug-E 回归：空数组构造时抛错
```

5 项精准变红 → 测试确实能抓到 bug，不是"永远通过"的摆设。

K3 的 `never` 穷尽检查也做了验证：给 `canStart()` 注入一个新 reason，
编译立刻报 `Type '"NEW-REASON"' is not assignable to type 'never'`。

---

## 弱测补强第二批（2026-09-05）

### 做了什么

补了 13 个仍然只有 3~7 项测试的单元，新增 `tests/run_weak2.ts`（119 项）。
测试总数 3034 → 3180。

| 单元 | 原来 | 补强后 |
|---|---|---|
| EntityIndex | 3 | 10 |
| makeWallTest / Raycasting | 3 / 4 | 8 / 8 |
| LazyHeap / 几何 / SpatialHash | 4 / 4 / — | 11 / 8 / 5 |
| CellularDungeon / MazeDungeon | 6 / 7 | 13 / 12 |
| PRD / WeightedTable | 6 / 7 | 14 / 15 |
| 各噪声类 | 3~5 | 8~10 |
| Flock / createAgent | 7 / 3 | 12 / 13 |
| Modifier | 6 | 15 |

### 抓到 3 个真问题

**① 地牢 `generate()` 不幂等**

```
new CellularDungeon(seed=1)
  → generate()  floorCount 780
  → generate()  floorCount 796   ← 不一样
  → generate()  floorCount 843
```

`_rng` 在**构造函数**里创建，`generate()` 不重置它。
所以"同种子可复现"只对第一次 generate 成立。

`MazeDungeon` 更隐蔽：`floorCount` 恰好都是 211（迷宫总会走满），
**但格子布局不同**。用 floorCount 做断言会误以为它可复现——
这正是"看起来对的断言"最危险的地方。

**② `ModifierSet.dump()` 返回内部引用**

类型是 `readonly ModifierEntry[]`，但运行时没拷贝。
强转后 push 会让 `count` 1→2、`get(100)` 从 110 变 1109。

`readonly` 只是编译期约束，挡不住 `(x as unknown as T[]).push()`。

**③ `noise3D(x,y,0)` 恒等于 `noise2D(x,y)`**

3D 是两层 2D 切片按 z 插值，`fade(0)=0` 所以 z=0 时退化成 2D。
做"随时间演化的地形"时第一帧会退化成纯 2D。

### 我自己写错了 5 处假设

写完第一版，编译器报 30 个错，测试挂 14 项。逐条看下来，
**全是"我记得应该有"**：

| 我写的 | 实际 |
|---|---|
| `ValueNoise.heightMap()` / `fbm01()` | 不存在（是别的类的方法） |
| `SpatialHash.query()` | 叫 `queryRect()` |
| `Flock.agents` 属性 | 没有，是 `compute(self, neighbors)` |
| `WeightedTable.pick()` 可选 rng | **必传** |
| `PRD.fromChance(1)` 必定触发 | **会抛错**（要求 0<p<1） |

### 两处"我以为是缺陷，其实是我判据错了"

**① "完美迷宫没有十字路口" —— 错。**

一棵树完全可以有度为 4 的节点（四条通道交汇），
只要**没有环**就仍是完美迷宫。正确判据是 **E = V-1**。

用树性质一测就清楚了：`braid=0 → E=V-1`（树），
`braid=0.1 → E=V+36`（有环）。

**② "PRD 极低概率 500 次内该中" —— 错。**

实测 C 值：

```
p=0.50 → C≈0.302   → 保底 4 次
p=0.25 → C≈0.085   → 保底 12 次
p=0.05 → C≈0.0038  → 保底 264 次
p=0.01 → C≈0.000156 → 保底 6409 次
```

**保底需要的次数比直觉大得多。** 设计掉落率时要反过来定：
想让最坏体验不超过 N 次，p 就不能低于约 1/N。

### 教训

写测试前先 grep 出真实签名。我这次贪快照印象写，
30 个编译错误算是便宜的——**测试写错同样会骗人**，
要么编译不过（还好），要么被"调整一下断言"绕过（那就完了）。

这跟 `check-doc-refs.py` 拦的是同一类错误，只不过发生在测试代码里。

---

## 文档反查：查「编造的 API」（2026-09-05）

### 为什么做

之前所有审查都是 **源码 → 文档**（查漏写）。
漏写会被发现——用户去翻源码就知道了。

**编造不会。** 文档写了个不存在的方法，用户照着写，
编译报错还算好；更糟的是名字存在但签名不同，静默算错。

历史上编过 5 处（实例方法写成静态、`p.name` 写成不存在的字段、
必填写成可选、不存在的默认值、从 1 计数写成从 0 计数）。

### 子代理失败了，脚本成功

先派了子代理做「写后复核」。它跑 29 turns，**搭了核对流水线，
但没产出任何结论**，自己声明"不得直接作为最终结论"。

> **教训**：大批量机械比对，脚本比子代理靠谱。
> 子代理倾向于搭框架；而"文档里的名字在源码里存不存在"
> 就是一次 grep。

于是自己写了 `scripts/check-doc-refs.py`（文档 → 源码，反向）。

### 抓到 8 处 API 表编造（全部坐实）

| 单元 | 文档写的 | 源码实际 |
|---|---|---|
| `_core` | `approach()` / `damp()` | **没有**；有 `smoothDamp` / `lerp` / `clamp01` / `remap` |
| `elo` | `updateGlicko()` / `batchUpdate()` | **没有**；是 `rate1v1` / `rateMultiplayer` / `ratePeriod` |
| `matchmaking` | 公开的 `tryMatch()` | **只有私有 `_tryMatch()`**，`tick()` 内部调用 |
| `objective` | `state(id)` | **没有**；紧挨着的 `status(id)` 才是对的 |
| `skill-caster` | `register()` / `unregister()` | **没有**；是 `learn()` / `forget()` |
| `scoring` | `addMetric()` | **没有**；指标只能构造时传 `metrics` |
| `signal` | `off(fn)` | **没有**；`add(fn)` 返回取消函数 |
| `telemetry` | `hash(...)` | 实际叫 `hashString()` |
| `perception` | `update()` | 实际叫 `tick(dt)` |

`objective` 那个最典型：`status(id)` 和 `state(id)` **上下两行挨着**，
一个对一个错。漏写会被发现，写错一个字母不会。

### 修文档时我自己又编了两处

1. 写"Glicko-2 的更新入口在 `Glicko2.ts`"——
   实际 `rateMultiplayer` 在 **Elo.ts**，Glicko-2 只有 `ratePeriod`
2. 写"`clear()` 只清运行时状态"——
   源码 `clear()` 里有 `this._skills.clear()`，**连技能定义一起删**

都是写完没回源码核实。**判据**：一段文字里的方法名、字段名、
参数可选性，如果没有一个回源码确认过，那它就是编的。

### 顺带发现一处原有文档错误

`skill-caster` 原有文档写：

> `forget(id)` | 清除该技能的运行时状态（CD、充能），**不删定义**

而源码 `forget()` 最后一行是 `this._skills.delete(id)`——**它删定义**。
这行和"本模块没有 register/unregister"是两种错，都修了。

### 脚本的三轮进化（都是被误报逼出来的）

```
v1  报 42 处 → 人工核实，真问题 8 个
v2  加否定语境检测 → 19 处
    （我给每个编造项补了"本模块没有 xxx()"的警告，
      结果它们**又全被报出来**——脚本分不清"说有"和"说没有"）
v3  加跨模块识别 → A 类 0 处
    （score 的 settle 出现在 scoring 文档里，是合法引用）
v4  加占位符/引擎API/反面示例过滤 → A 类 0，余 13 处 B/C 类（均为误报）
```

**v2 那个最典型**：修完问题反而被自己的检查项报出来。
跟"两个口径打架"同源——**只看符号出现，不看语境**。

### 固化

`scripts/check-doc-refs.py` 已接入 `npm run verify`：
- A 类（全库都找不到这个符号）非空 → **退出码 1，CI 失败**
- B/C 类（静态成员、跨模块引用）→ 只报告，需人工确认

```
audit-units.py          源码 → 文档   查漏写
check-doc-refs.py       文档 → 源码   查编造   ← 新增
```

两个方向都要有。**漏写会被发现，编造不会。**

---

## 纯逻辑 Demo（2026-09-05）

### 为什么做

3034 项测试验证的是"每个模块各自对不对"。
但**单测证明 A 对、B 对，不证明 A 的输出 B 吃得下**。

于是写了 `examples/demo-moveloop.ts`：
把「摇杆 → 移动 → 碰撞 → 冲刺 → 技能排队」串成一局无头游戏，
固定步长跑若干帧，打印状态。全程不 import 引擎，`npm run demo` 可跑。

### 它不断言，但打 ✓ / ✗

一开始我写成"只打印不改判据"，结果自己踩了坑：
场景 A 的日志写着"撞右墙，应停在 362"，实际角色停在 27——
它撞的是**中间那堵墙**，不是右墙。

**数字对不上却没有任何提示，我差点当正常输出读过去。**
所以加了 `check()`，9 项自检。

### 抓到的真问题：传送 ≠ 重置输入

```typescript
player.teleport(0, 0);     // 角色传送到原点
pad.touchMove(80, 80);      // 想"向右下推"
// → 实际算成"向右上"，角色滑到反方向的墙根
```

原因：`JoystickCore` 是 **dynamic 模式**，底盘中心在 `onDown()`
那一刻锁定，**只有 `onUp()` 才更新**。角色位置变了，摇杆中心没变，
于是 `(触点 - 旧中心)` 算出的方向变了。

**这是组合才暴露的问题，单测永远抓不到**——
单测里摇杆和角色根本没接在一起。

已写进 `joystick-mover/README.md`：传送/重生/过场后要主动 `onUp()`。

### Demo 验证的六件事

| 场景 | 验证 |
|---|---|
| A | 摇杆 → 移动 → 撞墙停住（停在 362，正是墙内边减半径） |
| B | 贴墙滑动（没被卡死，脱离墙体后继续前进） |
| C | **CD 剩 0.1s 提前点击 → 补发 1 次** |
| D | 朝墙冲刺不穿模 |
| E | 连点 5 次不堆积（capacity=1 + replaceSame） |

场景 C 是 `SkillQueue` 的核心承诺，在真实接线里验证通过。

### 接线顺序（这个 Demo 最有用的产出）

```
① 输入 tick（缓冲老化）
② 队列 poll（缓冲 → 队列请求）
③ 队列 tick（尝试释放 / 清理过期）
④ 施法器 tick（windup → active → recover）
⑤ 冲刺 tick
⑥ 移动 update
```

顺序错了的症状：
- ② 在 ① 之前：过期输入也会被消费
- ③ 在 ② 之前：本帧按的技能要等一帧才入队
- ⑥ 在 ④ 之前：施法用的是上一帧的位置

---

## 静默降级排查（2026-09-05）

### 起因：引擎侧暴露了一个只在运行时才犯的模式

第一轮引擎实测后，我从 `JoystickMover._toLocal()` 看到：

```typescript
const ui = (this.touchArea ?? this.node).getComponent(UITransform);
if (ui) {
  ui.convertToNodeSpaceAR(this._tmpVec, this._tmpVec);   // 正确
} else {
  out.set(p.x, p.y);                                      // ⚠️ 退回屏幕坐标
}
```

**没有 `UITransform` 时不报错，只是摇杆悄悄错位。**
症状是"跟手但偏得离谱"，没人会想到是组件没配对。

这类问题**在 Node 里永远测不出来**——它只在真实环境下才犯。
而引擎侧只暴露了 1 个模块，剩下 118 个还没被运行时照过。
所以扫了全库。

### 新发现的错误类型：「错误」与「空」返回同一个值

这不是之前记过的"返回值反直觉"（成功返回 null 那种），
而是**两个不同的状态返回了同一个值，调用方永远分不清**。

最严重的一处：

```typescript
meta(slot): { version, savedAt } | null {
  const raw = this._storage.read(this._key(slot));
  if (raw === null) return null;      // ① 槽位根本没有存档
  try { return { version: env.v, savedAt: env.savedAt }; }
  catch { return null; }              // ② 存档存在但 JSON 坏了
}
```

存档界面如果直接 `if (m === null) showEmptySlot()`，
**玩家的存档还在、只是坏了，界面却告诉他这里没有存档**。

已修（含变通方案：用 `has()` 区分，它不解析内容所以不会因损坏返回 false）。

同类共 5 处，全部补进文档：

| 位置 | 「空」 | 「错」 |
|---|---|---|
| `save.meta()` | 没有存档 → null | 存档损坏 → null |
| `rebind.actionOf()` | 没绑定 → null | 绑定数据非法 → null |
| `runscope.getOr()` | 键没值 → fallback | **越权/拼错键** → fallback |
| `crash.capture()` | 被去重拦下 → false | 上报失败 → false |
| `save._checksum()` | — | stringify 失败 → 0（与真实校验和 0 碰撞） |

### `runscope.getOr()` 那个最讽刺

模块第 4 节坑表写着"未声明的键静默返回 undefined，
拼写错误一路传下去……**抛错（这是本模块存在的意义）**"。

而 `getOr()` catch 了 `get()` 的全部异常：

```typescript
const gold = store.getOr('golld', 0);   // 拼错
// → 得到 0，不报错。和"真的有 0 金币"无法区分。
// 于是那个"三小时排查"的问题，用 getOr 就原样回来了。
```

文档 4.5 节原本把它列为**推荐写法**（"该写"）。
已改为标注"逃生舱，不是默认用法"+ 三条使用边界。

### 固化成脚本：`scripts/scan-silent-degrade.py`

文件头记了 4 条踩过的坑，都是我自己犯的：

1. **剥注释必须保留换行** —— `re.sub(r'/\*.*?\*/','',s)` 压平多行注释，之后行号全错
2. **匹配文本与算行号的文本必须同一份** —— 用剥注释的匹配、原始的算行号，位置必然偏移
3. **别用 `[^}]*` 提取代码块** —— 块内有个 `}` 就截断，要用大括号配平
4. **返回类型是对象联合时函数头正则失效** —— `(?::\s*[^{]+?)?\{` 遇到 `{ v:number } | null` 会被提前吃掉

前三条我在这个脚本里**各犯了一次**，表现为"同一个函数两次扫出行号不同"
和"最严重的 `save/meta()` 反而漏检"。

> 这已经是本项目第 4 次"两个口径打架"。根因都一样：
> **同一份文本有两种表示，却只维护了其中一种。**

---

## 测试深度补强（2026-09-05）

### 起因：一句有歧义的话

之前一直报"**2,977 项测试全通过**"。这句话只代表
**已写的测试都通过了**，不代表**该测的都测了**。

核实后发现两个真实缺口。

### 缺口一：几乎全是定向测试

按维度统计（匹配 2977 条用例描述的关键词）：

| 维度 | 命中 |
|---|---|
| 错误路径 | 217 |
| 边界/极端输入 | 110 |
| 往返/序列化 | 41 |
| 可复现 | 37 |
| 生命周期 | 34 |
| 幂等 | 10 |
| 时序 | 7 |
| **随机化/模糊测试** | **1** |

**2977 项里只有 1 项是随机化测试。**
其余全是"构造一个输入 → 断言一个输出"，只能验证**想到的情况**。

回想 `elo` 那个 bug：两个同名 `expectedScore` 签名不同，传错类型得 `NaN`。
定向测试永远不会发现——因为想不到要测"传错类型"。

### 缺口二：分布不均

按单元聚合后的弱测清单：

```
队形 2 · ValueNoise 3 · EntityIndex 3 · LazyHeap 4
几何工具 4 · Raycasting 4 · SimplexNoise 4 · WorleyNoise 4
Noise 辅助 4 · Pool 5 · PathSmoother 5 · VisibilityMap 5
```

**2 项的测试等于没测**——只验证了我构造的那一个例子。

> ⚠️ 第一版统计报"随机化 0 处"，是关键词太窄
> （只搜了 `fuzz`/`模糊`），没匹配上
> `for (let i = 0; i < 200; i++) Seed.random()`。
> 跟 `_core` 那个"Easing 30+ 缓动函数"（实际 13 个）是同一类错误：
> **先有结论，再找证据。**

### 新增两个测试文件

| 文件 | 内容 | 项数 |
|---|---|---|
| `tests/run_fuzz.ts` | 随机化：对拍 / 随机采样 / 畸形输入 | 27 |
| `tests/run_weak.ts` | 弱测单元补强到 8-10 项 | 30 |

合计 2977 → **3034 项**。

### 对拍（differential testing）

优化实现的正确性很难用"预言结果"验证，但可以用**暴力解**对照：

```
随机撒 N 个点 → 随机查询 → 优化实现 == 暴力 O(n²) 解
```

覆盖了 SpatialHash / QuadTree / BinaryHeap / LazyHeap / DisjointSet。

### 抓到的三个真问题（都是文档与实现不符）

| 单元 | 文档说 | 实测 |
|---|---|---|
| `PerlinNoise` | [-1, 1] | **[-1.279, 1.208]**，约 0.10% 超域 |
| `WorleyNoise` | [0, 1] | **[0.001, 1.105]**，约 0.05% 超域 |
| `fov` | `canSee` 与 `hasLineOfSight` 混用无警告 | **59% 的可见格上两者答案不同** |

**为什么原有测试没发现噪声超域**：它们用 `i * 0.13, i * 0.29` 取点，
**x 与 y 线性相关**，永远在一条直线上，撞不到超域的点。

**FOV 那个更危险**：用 `canSee` 画视野、`hasLineOfSight` 判射击，
玩家会看到"明明看得见却打不到"。根因是 Bresenham 禁止擦墙角
（拐弯时两个正交格都得通），而 Shadowcasting 宽松得多。

### 我自己写错的 4 处（都被测试拦下）

1. **`queryNeighbors(x, y, r)` 的 `r` 是格子数不是世界距离** —— 我按世界距离写，全军覆没
2. **`LazyHeap` 构造无参**，不是传比较函数
3. **`makeWallTest(map, wallValues?)`** 接的是二维数组，不是 `(W, H, fn)`
4. **`createAgent(pos: IVec2)`** 接的是向量，不是两个数字

第 1 条最有价值：参数名叫 `radius`，我自己都误解了，
用户更会误解。已在 `ds/README.md` 写明口径。

---

## 幽灵依赖修正（2026-09-05）

### 什么是幽灵依赖

`_kitmeta.json` 里登记了 `depends: ["_core"]`，但源码**没有任何 import**。

反过来还有一种更严重的：**import 了却没登记**——
用户照着 `depends` 复制文件时会少带，编译直接失败。

### 实测数据

| 类型 | 数量 |
|---|---|
| 登记了但没 import（幽灵） | 19 条 |
| import 了但没登记（漏登记） | 17 条 |
| 登记且真实存在 | 34 条 |

**错误比正确的还多**——这个字段当时已经不可信。

### 危害

不是"代码有问题"，是"登记数据不准"。
症状是复制时多带/少带文件，**编译期立刻暴露**，不会静默算错。

真正的代价是：**你一旦发现它不准，下次复制就得自己 grep 源码核对。**
这个字段本来是帮你省这一步的，不准了反而多一步。

### 修法

给 `check-deps.js` 加了**第 5 项检查**，复用已有的 `buildGraph()`
（从源码提取真实 import，含注释剥离），与 `depends` 对账。

```bash
node scripts/check-deps.js          # 检查
node scripts/check-deps.js --fix    # 自动改写 _kitmeta.json
```

已接入 `npm run verify`。

### 三条设计决定

**① 归一化 depends 的写法**

`depends` 里混着 `_core` 和 `_core/math` 两种写法，
而图里永远是目录名。不做归一化，`_core/math` 永远匹配不上。

**② 复用 buildGraph 而不是另写解析**

`buildGraph` 会 resolve 路径并检查文件是否存在，
还会剥离注释。另写一个必然又出现"两个脚本口径不一致"——
这类问题本项目已经踩过三次。

**③ `--fix` 保留精确写法**

只做两件事：删掉源码里没有的、补上源码里有但没登记的。
`_core/math` 这类精确写法原样保留。

### 最有意思的一条：`crash` 的 `logger`

`crash` 登记了 `logger`，但源码里找不到 import。查下来是：

```typescript
// 【为什么在这里重新定义，而不是 import logger 的 LogEntry】
// 如果这里 `import { LogEntry } from '../logger/Logger'`，
// 就把 crash 和 logger 绑死了
```

**这是解释"为什么不能这么写"的反例注释**，
早期登记时被误当成真实依赖。

这条也顺带明确了 `depends` 的口径：
**登记的是"源码 import 了什么"，不是"通常和谁一起用"**。
后者写 README。已写进 `依赖规则v2` 和 `crash/README.md`。

### 我犯的错

用 Python 临时脚本先扫了一遍，报"18 条幽灵"，
**漏掉了 `crash → logger`**——因为我的脚本没剥离注释，
把反例注释里的 `import` 当成了真实依赖。

改用 `check-deps.js` 的 `buildGraph` 后报出 19 条。
教训同上：**解析源码的活只该有一份实现**。

---

## 第 8 批 · 收尾（2026-09-05）

把上面那 17 个 + 后续扫出的缺口全部补齐，**类方法缺口 208 项 → 0**。

分五轮完成：

| 轮次 | 单元 | 项数 |
|---|---|---|
| 1 | audio + accessibility | 42 |
| 2 | command + progressbar + camera | 35 |
| 3 | tutorial + settings + input + uinav + reddot + social | 45 |
| 4 | condition + snapshot + subtitle + i18n + indicator + gameflow + feedback + cutscene | 34 |
| 5 | curve + _core + scenerouter + buff + spatial + tween + currency + diagpack + fsm + collision + skill-player + wave-spawner + 其余 8 个 | 52 |

### 本轮修正的文档错误（凭印象写的数字）

| 位置 | 错误 | 实际 |
|---|---|---|
| `_core/README.md` | "Easing **30+** 缓动函数" | **13 个** |
| `_core/README.md` | `IDisposable` 的方法写作 `dispose()` | 是 `destroy()` |
| `tutorial/README.md` | 章节标题写"**四种** kind" | 表格只有 3 行，是**三种** |

`Easing` 那条尤其值得记：只有 `out` 系列有 `outExpo`/`outBack`/`outElastic`/`outBounce`，
**没有对应的 `inXxx`**。写 `easing('inBounce')` 不报错（签名接受 `string`），
返回的是 `linear`——表现为"缓动没生效"。

### 反复出现的模式：成对 API

审查中累计遇到 8 组"名字相近、语义差一个关键维度、选错不报错"的成对 API：

| 一对 | 区别 |
|---|---|
| `clear()` vs `clearHistory()` | 后者保留当前状态 |
| `clear()` vs `destroy()` | 后者之后不能再用 |
| `clearLog()` vs `clearLedger()` | 分属两个类（`currency`） |
| `killAll()` vs `completeAll()` | 后者触发完成回调 |
| `skip()` vs `skipStep()` | 前者跳过整个引导 |
| `stop()` vs `skip()` | 后者应用终态 |
| `goTo()` vs `go()` | 后者可 `pushHistory: false` |
| `addLocale()` vs `setLocaleTable()` | 前者合并，后者替换 |

这类都已在文档里加了对照说明。

### 累计 7 处"返回值反直觉"

| 函数 | 直觉 | 实际 |
|---|---|---|
| `settings.set()` | 成功/失败 | `null` = 成功 |
| `settings.importState()` | 布尔 | 被忽略的键列表 |
| `uinav.indexOf()` | 找不到返回 null | `-1`（**真值**） |
| `command.undoMany()` | 布尔 | 实际步数 |
| `audio.stopSound()` | 布尔 | 停掉的条数 |
| `subtitle.nextIndex()` | 找不到返回 null | `-1` |
| `snapshot.undo()` | 到底抛错 | 返回 `undefined` |

编译期完全查不出来，只能靠文档写清"当布尔用会怎样"。

---

## 第 1 批 · 基础设施（8 个）

| # | 单元 | 扫描提示 | 人工核实 | 结论 | 处理 |
|---|---|---|---|---|---|
| 1 | `_core` | 12 项未文档化 | 属实。README「内容」节只列 9 个代表，`clamp01`/`vec2`/`angleDiff`/`angleLerp` 等 12 个未列。**另发现写了根本不存在的 `ITickable`** | **严重**：照文档写 `implements ITickable` 会编译失败 | ✅ 补完整 API 表 + 记录教训 |
| 2 | `binary` | 5 项未文档化 | 属实且严重：8 个章节**一次都没提 `Schema`** | 用户看完不知道怎么建 schema | ✅ 新增「5. API」章节 |
| 3 | `input` | 2 项未文档化 | 文档在 `README_InputBuffer.md`，主 README 只讲 InputManager 且**零引用** | `InputBuffer` 是 `skill-queue` 的依赖 | ✅ 双向索引 |
| 4 | `camera` | — | `CameraFollow.md` 孤立，主 README 零引用 | 只看主 README 会以为相机跟随没文档 | ✅ 双向索引 |
| 5 | `observable` | 4 项未文档化 | **误报**：用 `ref()`/`computed()` 工厂视角是对的；`flush()`/`batchSize()` 注释已写明内部/调试用 | 无需改 | ✅ 通过 |
| 6 | `logger` | 7+2 项 | **误报**：`Debug`/`Info`/`Warn` 是枚举成员；`ModuleLogger` 由 `module(name)` 返回，API 表已列 | 无需改 | ✅ 通过 |
| 7 | `ds` | `pointInRect` 未文档化 | 属实（轻）：API 章节很全，只漏这个自由函数 | 补上 | ✅ 文末「附：自由函数」 |
| 8 | `timeutil` | 5 项未文档化 | 属实：无 API 表，漏每日重置三件套与 `formatDuration*` | 做「距刷新还有多久」必须用到 | ✅ 新增「API」章节 |

## 第 2 批 · 地基 12 + 基础设施 2（11 个）

| # | 单元 | 扫描提示 | 人工核实 | 结论 | 处理 |
|---|---|---|---|---|---|
| 9 | `di` | 9 项找不到 + 1 项未文档化 | 找不到的是**示例虚构类**，误报。但 `tryGet`/`has`/`unregister`/`fork`/`validate`/`clearSingletons`/`createContainer` **真没进 API 表** | API 表 4 行 → 12 行 | ✅ 补 API + `fork()` 测试用法 |
| 10 | `config` | 4 项找不到 | `CocosTableSource`/`loadAll` 在**源码注释**里。但 `clearIssues()` 未文档化 | 热重载不清它会累积旧错误 | ✅ 新增「热重载」章节 |
| 11 | `number-roller` | 3 项找不到 | 配置字段已列。但 `reset()`/`destroy()` 未文档化 | 复用不 reset 会看到数字跳回旧值 | ✅ 补方法表 + 坑 |
| 12 | `scheduler` | 5 项找不到 | 跨模块引用/示例回调，误报。但 `repeatUnscaled` 未文档化 | 补上 | ✅ 补 API 表 |
| 13 | `scheduling` | 5 项找不到 | `hasTimeLeft` 是**箭头函数属性**，脚本抓不到（误报但暴露脚本缺陷）。但 `destroy()` 未文档化 | 补上 | ✅ 补 `destroy()` |
| 14 | `pool` | 5 项找不到 | **全误报**：示例虚构类 + 选项字段名 | 无需改 | ✅ 通过 |
| 15 | `result` | 5 项找不到 | 示例虚构函数。**但 `mapErr`/`flatMap` 曾被误报**（泛型方法匹配失败），已修脚本 | 无需改 | ✅ 通过（修好脚本） |
| 16 | `signal` | 1 项找不到 | **误报**：`Button` 示例虚构类 | 无需改 | ✅ 通过 |
| 17 | `event-bus` | 2 项找不到 | **误报**：`onDie` 是「事件名反例」，`updateHp` 示例方法 | 无需改 | ✅ 通过 |
| 18 | `rng` | 无 | 扫描全绿。**基准对照**：109 行覆盖 7 个导出，测试引用 20 次 | 无需改 | ✅ 通过 |
| 19 | `observable`/`logger`/`_core` | 见第 1 批 | — | — | ✅ |

## 第 4 批 · 数值与战斗 16 + AI 与感知 3（19 个）

| # | 单元 | 扫描提示 | 人工核实 | 结论 | 处理 |
|---|---|---|---|---|---|
| 20 | `telegraph` | 6 项未文档化 | **严重**：整份 README 没有 API 表。`cancelAll`/`cancelByTag`/`pauseAll` 等 6 个从未出现过 | 换关卡不清理 → 回调在新关卡执行 | ✅ 新增「API」章节 |
| 21 | `element` | 7 项找不到 | **API 名全错**：README 写 `applyGauge`/`getGauge`/`ElementalReaction`，实际是 `attach`/`gaugeOf`/`IReactionDef` | 照文档写编译失败 | ✅ 补正确 API 表 |
| 22 | `attack-token` | 12 项未文档化 | 只有 `stats()`，没有查询接口。`acquire()` 返回 `null` 未说明 | 照示例写 `t.xxx` 会崩 | ✅ 补 API 章节 |
| 23 | `bullet-pattern` | 11 项未文档化 | 无 API 表。`allDone`（波次结算判据）与 `cancelAll` 从未出现 | 波次切换不清理 → 弹幕越攒越多 | ✅ 补 API 章节 |
| 24 | `skill-caster` | 11 项未文档化 | **无 API 表**。`cooldownRatio`（画冷却遮罩）等全漏 | 有 CD 缩减 buff 时自算会错 | ✅ 补 API 章节 |
| 25 | `skill-player` | 6 项未文档化 | `SkillHandle` 类**完全没有文档** | `onEnd(cancelled)` 不判会误扣蓝 | ✅ 补 SkillHandle 章节 |
| 26 | `damage-pipeline` | 7 项未文档化 | `ModifierSet` 类完全没文档化（被 `attribute`/`buff` 共用） | 换装备该用哪个移除函数不清楚 | ✅ 补 ModifierSet 章节 |
| 27 | `attribute` | 11 项未文档化 | **无 API 表** | 移除函数只能调一次，调两次误删别人的 | ✅ 补 API 章节 |
| 28 | `hitbox` | 18 项未文档化 | 6 个几何函数 + `lineOfSightBlocked` 全漏 | 后者藏在 `fov/` 里没人找得到 | ✅ 补几何函数 + 视线检测 |
| 29 | `perception` | 8 项未文档化 | `stateOf`/`forceSpot`/`forceForget`/增删方法未文档化 | 实体死了不移除 → 越来越卡 | ✅ 补查询/强制/增删 |
| 30 | `skill-variant` | 5 项未文档化 | 无 API 表，`preview` 未提 | 用 `apply` 预览会真的改掉配置 | ✅ 补 API 章节 |
| 31 | `indicator` | 6 项未文档化 | `toQueryArgs` 等 3 个配套函数未提 | 手写四个参数顺序错了不报错 | ✅ 补配套函数 |
| 32 | `targeting` | 4 项未文档化 | 评分器章节缺失，`byWeight` 未提 | `byStickiness` 权重给 10 才能压住抖动 | ✅ 补评分器章节 |
| 33 | `behavior-tree` | 5 项未文档化 | `Blackboard` 的 6 个成员未文档化 | `reset()` 不清黑板 → 复活的怪走向旧目标 | ✅ 补 Blackboard 章节 |
| 34 | `combo` | 6 项未文档化 | 无 API 表 | `tick()` 返回值丢了会漏掉超时结算那一下 | ✅ 补 API 章节 |
| 35 | `projectile` | 5 项未文档化 | 无 API 表 | `count` 只增不减 = 命中后没 `kill()` | ✅ 补 API 章节 |
| 36 | `steering` | — | `shareSquad()` 只在"设计取舍"里提了一句，未进 API 表 | 复用零重算是核心价值 | ✅ 补进 API 表 |
| 37 | `buff` / `fsm` | 各 1~2 项 | `count`/`destroy` 未文档化 | `count` 只增不减 = `tick` 没每帧调 | ✅ 补 API |
| 38 | `hitbox`（世界管理） | — | `remove`/`update`/`setEnabled` 等未文档化 | 实体死了不 `remove` → 打空气有命中反馈 | ✅ 补世界管理章节 |

## 第 5 批 · 移动与碰撞 4 + 算法与地图 7 + 玩法框架 8（19 个）

| # | 单元 | 扫描提示 | 人工核实 | 结论 | 处理 |
|---|---|---|---|---|---|
| 39 | `pathfind` | —（扫描无提示） | **真问题**：库里有两套寻路，`pathfinding/` 有完整对比表，但 `pathfind/`（推荐的那套）**一次都没提对侧** | 用户进了推荐目录不知道还有一套 | ✅ 补对侧索引 |
| 40 | `card` | 5 项找不到 | **真问题**：坑表格里混进了 `replay/` 的条目「只存关键帧不存 frameCount」。`card/` 只有牌库 | 文档内容**错放** | ✅ 删除并留记录 |
| 41 | `collision` | 3 项找不到 | 属实（轻）：`bucketCount` 未文档化，它是判断格子尺寸对不对的**唯一指标** | `cellSize` 配错不报错只是变慢 | ✅ 补诊断说明 |
| 42 | `dungeon` | 4 项找不到 | 属 `class Rng`（**不带 export** 的私有类），误报。但实测 `randomFloor()` 推进内部 RNG，**可复现性依赖调用顺序** | 多调一次后面随机全变 | ✅ 补顺序依赖陷阱 |
| 43 | `joystick-mover` | 12 项找不到 | 多为引擎 UI 属性名，误报。但 `onLoad()`/`onDestroy()` 是**引擎生命周期钩子**，没说"不要手动调" | 手动调会重复解绑 | ✅ 补生命周期说明 + `reset()` |
| 44 | `mover` | 7 项找不到 | **全误报**：配置项 + API 表里已有的成员 | 无需改 | ✅ 通过 |
| 45 | `steering` | 4 项找不到 | **误报**：`QuadTree`/`SpatialHash` 是"为什么不用它们"里的反例 | 无需改 | ✅ 通过 |
| 46 | `grid` | 1 项找不到 | 16 导出/76 行，比例最差但**信息密度高**：9 个 hex 函数全列 | 无需改 | ✅ 通过 |
| 47 | `room-graph` | 5 项找不到 | 质量高：`findBestPath` vs `findPath` 带**实测数据** | 无需改 | ✅ 通过 |
| 48 | `builder`/`interact`/`objective`/`wave-spawner` | 各 5~9 项 | **全误报**：业务侧回调名（`showToast`/`loseLevel`/`createEnemy`/`openDoor`） | 无需改 | ✅ 通过 |
| 49~57 | `dash`/`fov`/`inventory`/`pathfinding`/`turn`/`dialogue`/`card`/`collision` | 少量 | 抽查无异常 | 无需改 | ✅ 通过 |

---

## 体检脚本的修正记录

| # | 缺陷 | 症状 | 修复 |
|---|---|---|---|
| 1 | **泛型方法匹配不上** | `mapErr<F>()` / `flatMap<U>()` 漏抓 | 正则加 `(?:<[^<>]*>)?` |
| 2 | **只扫 `README.md`** | `input/` 的 `CoyoteTimer` 有文档却报"未文档化" | 目录内所有 `.md` 都纳入 |
| 3 | **非导出类也算公开 API** | `dungeon` 的 `Rng`（不带 export）、`collision` 的 `pushAxes` 被当成待文档化 | 只统计**导出类**行范围内的方法 |
| 4 | 依赖不分级 | 72 个单元 `depends` 为空，46 个同时报警 | 拆成 L0 / 非 L0 / 幽灵三档 |
| 5 | 导出符号不分轻重 | type/interface 也算"未文档化" | 只统计 class/function |
| 6 | **`--md` 覆盖人工记录** | 第五批重跑时把前几批的审查记录整段清成空表 | **拆成本文件**，脚本不再生成它 |

修正 3 之后，全库「未文档化」从 **33 个单元降到 20 个**——降下去的本就不该出现。

### ⚠️ 脚本结果必须二次确认

第五批遇到一次：**同一份脚本，第一次跑说 6 个单元有缺口，连跑三次变成 0。**
用 `grep` 对照实验确认 0 才是对的。

> **自动扫描的第一次结果不能直接当结论。**
> 连跑两次看是否一致，或用另一种手段交叉验证。

### 仍在的已知误差

| 误报类型 | 例子 | 原因 |
|---|---|---|
| 枚举成员 | `logger` 的 `Debug`/`Info` | 只抓顶层 export |
| 工厂函数视角 | `observable` 的 `Observable` | 用 `ref()` 创建，不提类名是对的 |
| 示例虚构类 | `di` 的 `CombatService` | README 示例里的业务类名 |
| 箭头函数属性 | `scheduling` 的 `hasTimeLeft` | 声明为属性不是方法 |
| 源码注释里的 API | `config` 的 `loadAll` | 在 JSDoc 示例里 |
| 跨模块引用 | `scheduler` 的 `endFrame` | 提别处的方法是对的 |
| **业务侧回调名** | `wave-spawner` 的 `createEnemy` | 调用方自己实现的回调 |
| **"为什么不用 X"里的 X** | `steering` 的 `QuadTree` | 反例说明中提到的 |

---

## 两条真实教训

**① 文档内容错放比漏写更糟。**

`card/README.md` 的坑表格里混进了 `replay/` 的条目。
漏写你会去看源码；**错放你会以为这个模块真有那个功能。**

**② 功能重叠的模块必须双向索引。**

`pathfinding/` 写了详细对比表，`pathfind/` 却只字未提，
而 `pathfind/` 是推荐的那套，用户更可能从它进来。

> 单向索引等于没索引——**你不知道用户从哪一侧进来。**

**③ 生成的文件不能手写，手写的文件不能被生成覆盖。**

第五批 `audit-units.py --md` 把人工记录整段清空。
代码改动还在，记录没了，等于白审。

---

## 第 6 批 · 内容与掉落 9 + 进度与统计 11（20 个）

**本批首次试用子代理并行审查**：3 个子代理各 5 个 + 我自己 5 个。

| 分组 | 单元 | 结论 |
|---|---|---|
| A（子代理） | affix / blessing / craft / curse / gacha | 5 个都有"查询/清理/存档 API 漏写" |
| B（子代理） | loot / rarity / setbonus / shop / achievement | 4 个有问题，**loot 有源码级 bug** |
| C（子代理） | daily / difficulty / leaderboard / meta / quest | 4 个有问题，quest 最干净 |
| D（我） | replay / runscope / score / scoring / stats | 5 个都有缺，stats 最严重（11 个方法全漏） |

### 本批唯一的源码改动：`loot` 的 `owned` 类型矛盾

```typescript
readonly owned?: readonly T[];      // 类型说"我不改"
(this._options.owned as T[]).push(item);   // 实现却在改
```

B 组子代理发现 `addOwned` 会 push 进构造时传入的数组，
但那个数组被声明成 `readonly T[]`。传真的只读数组（`as const` / `Object.freeze`）
会在**运行时崩**，而编译器一声不吭。

改成 `T[]` 后传只读数组是**编译期报错**——运行时崩溃提前到编译期。

### 我自己写错的 5 处（都已修正）

批量补文档时，为了赶进度写了"看起来合理"的内容而没核实：

| 单元 | 我写的 | 实际 |
|---|---|---|
| `affix` | `aggregate` 是**静态方法**，不用先有实例 | **实例方法**，直接 `AffixSystem.aggregate()` 会报"不是函数" |
| `shop` | `totalPriceOf(id, side, quantity?)` | `quantity` **必填** |
| `shop` | 批量折扣默认有（买得越多越贵） | **默认没有**，要在 `pricing` 钩子里用 `quantity` 自己写 |
| `setbonus` | `p.name` / `p.count` | 实际字段是 `setName` / `equipped` |
| `leaderboard` | `rank` 从 0 起，要 `rank + 1` | **从 1 起**，`+1` 会让第一名显示成第 2 名 |

**这 5 处全是编的，不是核实来的。** 而它们全部出现在"补文档"这个动作里——
补文档比不补更危险，因为**它会让人信以为真**。

> 判据：写完一段文档后，如果里面的**方法名、字段名、参数可选性**
> 没有一个是回源码确认过的，那它就是编的。

### 子代理实验的结论

| 维度 | 结果 |
|---|---|
| 有没有编造 | **没有**——三份报告报的问题，逐条核实后都属实 |
| 误报控制 | 好——都识别出了"业务侧回调名""配置项"这类非问题 |
| 发现硬漂移 | 三份都报"未发现"，与事实一致 |
| 发现源码 bug | B 组发现了 `owned` 的类型矛盾，这是本轮唯一源码级改动 |

**结论：子代理可用于审查，但只做"发现"，不做"修改"。**
它们产出的修改建议（尤其是代码示例）必须逐字段核实——
我采纳建议后自己写的 5 处错误，就是没核实就下笔的后果。

### 脚本又修一处

`private` 方法被当成公开 API（`loot` 的 `removeZeroWeights`）。
修正后全库"未文档化"单元从 20 个降到 **16 个**。

### 本批成果

第 6 批 20 个单元的 API 缺口：**62 项 → 0 项**。

---

---

## 第 6 批 · 我自己的 5 个（replay / runscope / score / scoring / stats）

| # | 单元 | 扫描提示 | 人工核实 | 结论 | 处理 |
|---|---|---|---|---|---|
| 39 | `replay` | 6 项未文档化 | 属实：无状态查询章节。`isRecording`/`isPlaying`/`keyframeCount`/`recordedFrames`/`reset`/`destroy` 全漏 | 不 `reset()` 新一局会**追加**到旧数据 | ✅ 补状态查询与清理 |
| 40 | `runscope` | 7 项未文档化 | 属实且要紧：本模块核心是"越界抛错"，却没给**不想被抛错**的场景留出口的文档（`getOr`/`has`/`scopeOf`） | 用户会用 try/catch 包 `get` | ✅ 补「抛错之后怎么安全查询」 |
| 41 | `stats` | 11 项未文档化 | **最严重**：README 62 行 / 源码 430 行，完全无 API 表。`display()`（渲染用）是关键 | `get()` 返回 `-Infinity`，直接渲染显示 "-∞" | ✅ 补 API 章节 |
| 42 | `score` | 4 项未文档化 | 属实（轻）。`startTimer()` **是"归零重来"不是"开始计时"**——命名误导 | 不调它也能计时 | ✅ 补 API 并说明命名 |
| 43 | `scoring` | `gradeOf` 等 4 个顶层函数 | 属实：`gradeOf`/`toNextGrade`/`gapToPerfect` 可单独用（做实时提示） | 用户会为"还差多少上 S"重写一遍 | ✅ 补四个顶层函数 |

---

## 第 6 批 · 子代理报告的复核结果

三个子代理各审 5 个，**结论全部采纳**——逐条核实后没有发现误判。

| 组 | 子代理报的真问题 | 我复核后 |
|---|---|---|
| A（`affix`/`blessing`/`craft`/`curse`/`gacha`） | 无硬漂移，集中在"查询/清理/存档"漏写 | ✅ 属实。已补 `blessing`（含 `onChange` 在 `clear` 触发、`importState` 不触发这个不对称）、`curse`、`craft`、`gacha` |
| B（`loot`/`rarity`/`setbonus`/`shop`/`achievement`） | `loot` 的 `addOwned` 契约冲突最重 | ✅ **属实且是真 bug**（见下） |
| C（`daily`/`difficulty`/`leaderboard`/`meta`/`quest`） | `daily.fromText` 返回 `null` 没提示 | ✅ 属实，这是每日挑战**最高频的输入路径** |

### B 组抓到的真 bug：`Chest.owned` 的类型在撒谎

```typescript
readonly owned?: readonly T[];        // 类型说"只读"
...
(this._options.owned as T[]).push(item);   // 实现却在 push
```

传 `Object.freeze([...])` 或 `as const` 的数组 → **运行时崩，编译器一声不吭**。

改为 `owned?: T[]` 并去掉强转：现在传只读数组是**编译期报错**。
把运行时崩溃提前到编译时，这是修它的全部理由。

> 这类"类型说一套、实现做一套"的写法，
> 单独看每一行都合理，只有把声明和实现对着看才暴露。

### 我自己补文档时编错的一条

写 `leaderboard` 时我写"名次从 0 开始，显示要 `rank + 1`"。
**这是编的。** 源码注释写 `从 1 开始`，实测三种模式：

```
dense        a=1  b=1  c=2
ordinal      a=1  b=2  c=3
competition  a=1  b=1  c=3
```

第一名就是 `rank === 1`。已改正，并把三种模式的差异写进文档。

> 教训同前几轮：**凭印象写文档 = 制造新 bug。**
> 这次是写文档不是写代码，但后果一样。

---

## 体检脚本的修正记录（续）

| # | 缺陷 | 症状 | 修复 |
|---|---|---|---|
| 7 | **private 方法也算公开 API** | `loot/WeightedTable.removeZeroWeights` 是 private，却一直报"未文档化" | 排除 `private` |

修正后全库「未文档化」单元 **20 → 15**——降下去的本就不该出现。

> 注意：`audit-one.py` 早就排除了 private，
> 是我在第 6 批复核时**临时手写的脚本**没排除，才误报了一次。
> **临时脚本要跟正式脚本同一套规则**，否则等于白修。

---

# 第 6 批：子代理并行审查实验

> 20 个单元分 4 份：3 个子代理各 5 个 + 我自己 5 个。
> 子代理只出报告不改代码，改动与复核由我完成。

## 第 6 批 A 组（子代理审查）

> 审查范围：`affix`、`blessing`、`craft`、`curse`、`gacha`

## 结论汇总

| 单元 | 有无真问题 | 一句话 |
|---|---|---|
| `affix` | 有（轻微） | 核心 API 齐全，但 `aggregate()` 与 `defCount` 未明示，UI/调试场景可能走弯路 |
| `blessing` | 有（中等） | 教程清晰，却漏掉 `clear`、`owned`、`pick`、存档和冲突预览等生命周期 API |
| `craft` | 有（轻微） | `destroy()` 与 `recipeIds` 未写，热重载或配方管理时需要翻源码 |
| `curse` | 有（中等） | 触发与移除讲得细，但状态查询、清理、抽取、存档接口几乎未编目 |
| `gacha` | 有（轻微） | `pullTen`/`pullN`/`reset` 未文档化，批量抽卡和重新开始容易踩坑 |

**总体结论：未发现“文档写出、源码不存在”的硬漂移，也未发现内容明显错放到其他模块。真正的问题集中在第二类——用户会实际调用、但无法从 README 自行发现的公开 API。** 其中 `blessing` 和 `curse` 风险最高；它们有状态、存档和生命周期，若文档只保留“快速上手”，开发新局、换关卡、做 UI 列表或序列化时容易误以为能力不存在。`affix` 风险最低，只有两项需要补齐。

*图 1：按类别统计的已核实文档缺口。数据来源：五个单元源码与 README 逐项核实；不将私有方法、业务回调及可由现有文档明确推导的 API 计入。*

`审查原始数据（CSV）`（CSV 在子代理沙盒内，未随报告产出）

## 审查口径与误报剔除

脚本用于发现候选差异，但所有结论均经源码签名与 README 实际内容复核。以下情况不计入真问题：

- 源码中的 `BlessingOp`、`BlessingEffect`、`RecipeInput`、`GachaItem` 等类型虽然未全部展开，但 README 已说明对应配置字段及用途；仅缺类型名不影响照文档编码。
- `BlessingSystem.all`、`def()`、`CurseSystem.all`、`def()`、`stacks()`、`effects()` 等可从现有示例和术语中自然发现，或未构成高频踩坑点，本报告降为“可补但不阻塞”。
- 业务侧回调名（`executor`、`checker`、`applyCost`、`loseLevel` 等）、虚构业务类、JSDoc 中的历史 bug 说明，均不作为 API 漂移。
- 脚本把 getter、属性及构造函数内字段识别为“未提方法”的，按实际公开形态重新归类。

本报告中的“真问题”特指：**缺少该说明会导致用户编译失败、错误调用、无法完成常见业务，或需要阅读完整源码才能发现接口。**


## 交叉观察：问题并不在 API 漂移，而在状态型模块的“第二幕”没有文档化

`affix` 偏纯函数式转换，README 覆盖 `roll`、`reroll`、`compute`、校验后基本可用；因此其缺口最少。`craft` 虽有状态，但文档抓住了核心“原子合成”，遗漏主要是销毁与遍历。`blessing` 和 `curse` 则不同：它们都强调状态变化、存档、新局、抽取和 UI，而文档仍主要停留在“添加/触发/移除”的入门路径。

这意味着补文档时不应机械罗列脚本输出的所有名字。更有效的做法是按用户任务组织：

1. **查询 UI**：当前列表、层数、有效值、计数；
2. **生命周期**：clear / destroy / reset；
3. **抽取选项**：pick / pullTen / pullN；
4. **存档**：export/import 或 snapshot/restore，以及容错和回调边界。

按此补完后，五个模块的“照文档写代码”覆盖率会明显提高；`blessing` 和 `curse` 是当前优先修复对象。


---

## 第 6 批 B 组（子代理审查）

> 审查范围：`loot`、`rarity`、`setbonus`、`shop`、`achievement`

## 结论汇总

本次逐文件比对源码、JSDoc 示例与 README，核心目标是判断“用户照着文档写代码会不会踩坑”，而不是追求 API 清单形式完整。总体结论如下：

| 单元 | 有无真问题 | 一句话 |
|---|---|---|
| `loot` | 有 | 核心用法完整，但存档/生命周期 API 遗漏较多，最典型的是 `importState` 示例不可编译、`destroy` 未说明；`WeightedTable.removeZeroWeights` 属于内部方法，误报。 |
| `rarity` | 有 | 教程覆盖较好，但 `RarityDef` 关键字段、`all`/`allAscending` 及保底查询漏写；`countsForPity` 构造校验与文档“静默降级”存在直接契约冲突。 |
| `setbonus` | 无 | 文档准确描述了构造、装备、计数和聚合，遗漏的 `unequip`/`clear`/`progressOf`/`describe` 等属补充项；未发现会导致编译失败或误用功能的严重缺陷。 |
| `shop` | 有 | 交易流程描述准确，但 `hasItem`/`getItem`/`totalPriceOf` 查询 API、`clearLog`/`destroy` 生命周期以及精确返回字段均未入文档，UI 与存档场景易踩坑。 |
| `achievement` | 有 | 文档聚焦检查循环而遗漏状态查询/重置/撤销/序列化 API；`revoke` 名与“撤销”语义、导入旧 id 的行为都需要明确，否则反序列化与调试代码容易误用。 |

**总体判断：**五单元都没有“文档调用了源码不存在的 API”这种会导致编译直接失败的严重漂移，也没有把 A 模块概念错放到 B 模块的内容错放问题。真正需要修复的问题主要是两类：一是**状态查询与生命周期 API 漏写**，二是**配置/返回值契约描述不闭环**。脚本报告的许多“未入文档”项属于 JSDoc 内部实现、私有辅助方法或源码注释示例，不计入真问题。

*图 1：脚本给出的 A/B 类线索显著高于最终确认问题，说明大部分审计提示为配置项、类型或内部方法误报。数据来源：本地 `audit-one.py` 及五单元源码/README 人工核验。*

## 审查口径

本次审查遵循“只读不改”原则，没有修改任何 `.ts` 或 `.md`。事实来源仅为 `/data/workspace/cocos-kit` 下的五个目录、目录内源码和对应 README。先运行 `scripts/audit-one.py` 取得 A/B/C/D 线索，再逐条完成以下核验：

1. 文档名字是否在源码顶层导出、类成员或 JSDoc 注释中存在；
2. 未写入文档的成员是否真的面向调用方，而非私有实现或源码注释示例；
3. 若用户要渲染 UI、判断状态、清理对象、迁移存档，能否仅靠文档自行发现；
4. 文档声称的构造校验、默认值和返回值是否与源码实现一致；
5. 是否存在 A 单元概念混进 B 单元，或把内部机制描述为用户 API。

**问题分级标准：**

- **P1：**用户照抄必然编译失败、运行时抛错，或产生明确错误行为；
- **P2：**常见 UI/存档/关卡切换场景缺少必要 API，用户很可能误用；
- **补充：**文档可增强，但用户结合示例与源码通常能发现，不单独算缺陷。

本次未发现 P1 级 API 漂移。确认问题主要属于 P2 及明确的文档契约错误。脚本 C 类均为 0，因此不存在文档提到而源码找不到的名字；`setbonus` 的配置类型虽然大部分未进入 README，但相关字段已在教程和示例中说明，且未发现错放。


## 跨单元规律与修复优先级

这五个单元反映出的不是“文档和源码对不上”，而是**教程型 README 倾向于只描述 happy path，遗漏状态迁移和对象生命周期**。从风险角度，修复顺序建议如下：

| 优先级 | 位置 | 建议动作 | 解决的问题 |
|---|---|---|---|
| 高 | `loot` | 补全 `Chest` 读档、`ChestSnapshot`、可变 `owned` 约定及 `destroy` | 存档失败、运行时状态误用 |
| 高 | `achievement` | 补充解锁状态查询、撤销/重置、导入导出契约 | 读档、调试和奖励回滚误用 |
| 中 | `rarity` | 拆分“查询回退”和“配置校验”，列出保底查询 | 配置异常被误认为静默降级 |
| 中 | `shop` | 补齐商品查询、总价、日志清理和销毁 | UI 计算错误、换层资源残留 |
| 低 | `setbonus` | 补卸装、清空、进度查询和调试 API | 文档完整性，核心流程已可用 |

从本次样本看，可以建立一个有效规则：**审计脚本的 A/B 数量不宜直接排名**。真正值得人工复核的是同时命中“用户可发现性低 + 状态有持久影响”的成员，例如 `import*`/`export*`/`reset*`/`clear*`/`destroy`，以及返回 `null`/`undefined`、只删除标记但不回滚副作用的方法。只报告配置接口、私有辅助和源码注释示例的审计提示，应优先过滤，以免制造“修复数量”的假象。

**不计入本次审查但后续可做的验证：**当前只核对了源码与 README 的内部一致性，未执行仓库测试命令，也未确认是否另有顶层 `index.ts`/桶文件影响最终 `import` 路径。若后续做第二批审查，建议补一步“入口导出核对”，以防 README 正确、公共包导出未转发。


---

## 第 6 批 C 组（子代理审查）

> 审查范围：`daily`、`difficulty`、`leaderboard`、`meta`、`quest`

## 结论汇总

| 单元 | 有无真问题 | 一句话 |
|---|---|---|
| `daily` | 有 | 示例与 API 表漏写 `recordOf`/`hasPlayedToday`/`clearedCount`、`exportState`/`importState` 等状态查询和换存档方法；尤其 `fromText` 可返回 `null`，文档没有提示。 |
| `difficulty` | 有 | 查询当前档位、DDA 调节值和所有倍率的接口均未文档化，用户很可能自己实现一份或错误判断“DDA 是否生效”。 |
| `leaderboard` | 有 | “导入会覆盖”的坑已有说明，但 `clear`、`rankOf`、`top`、`remove` 等 UI/管理 API 未列；序列化接口名称也容易与合并逻辑混淆。 |
| `meta` | 有（轻微） | 主流程覆盖充分，`canUnlock`/`nextCost`/`effectOf`/`topoOrder` 等查询接口未写，但均可通过现有方法发现；无错放或伪 API。 |
| `quest` | 无 | 唯一遗漏的 `destroy()` 属于生命周期收尾，且有 `reset()`/导入覆盖；文档主流程、状态流转、存档与防重复领奖说明完整。 |

**总体判断**：五个单元都没有“文档写了、源码不存在”的阻断性错误，也没有把别的模块功能硬塞进本模块的真错放；主要风险是**“查询、清理、序列化”三类 API 漏写**。其中 `quest` 最安全，`daily` 和 `difficulty` 最容易让用户在写 UI、换档、换存档时踩坑。

*图 1：五个单元公开 API 的 README 提及覆盖。口径为源码顶层导出与公开实例成员，用于定位审查优先级，并非“完整文档”合格率。数据来源：本地源码与 README 人工核实。*

`API 覆盖统计 CSV`（CSV 在子代理沙盒内，未随报告产出）

## 审查口径与已知限制

本次没有联网，也未改动任何 `.ts`/`.md`。流程是：

1. 按指定命令运行 `scripts/audit-one.py <unit>`；
2. 对每个单元读取真实入口源码（例如 `daily/DailyChallenge.ts`，脚本提示的 `index.ts` 在本仓库并不存在）；
3. 逐条区分：确实不存在、私有/内部、类型或配置字段、业务侧实现名，以及用户做 UI/存档/生命周期时真正需要的公开 API。

脚本的 C 类只在 `meta` 报告了 `affix`、`attribute`。源码注释中确实没有这两个名字，README 也是在说明“本公式与另外两个插件保持一致”。这属于跨插件约定说明，而不是 `meta/README.md` 声称本模块导出或实现了它们，因此不按文档漂移或内容错放处理；但它可以提醒后续建立跨插件公式校验。

以下四类“遗漏”没有升级为真问题：

- 类型/配置接口（`DailyChallengeOptions`、`DDAOptions`、`LeaderboardOptions`、`MetaProgressionOptions` 等）：用户通常读方法签名即可发现。
- 内部或私有成员：源码已经 `private`。
- 业务侧回调名、虚构示例类、设计反例名。
- `MultiplierKeys`、`DEFAULT_TIERS`：其中 `DEFAULT_TIERS` 已文档化，`MultiplierKeys` 只作建议键名，优先级低。


## 跨单元规律

**真正值得优先补的不是类型名，而是“用户会找不到下手点”的三类 API。**

| 类别 | 高风险单元 | 缺失时的典型踩坑 |
|---|---|---|
| 状态查询 | `daily`、`difficulty`、`leaderboard`、`meta` | UI 显示“7/5”、错误判断 DDA、把 `undefined`/`null` 当数据 |
| 清理与移除 | `leaderboard` | 赛季/测试重置时新建实例，或误以为导入是合并 |
| 存档与迁移 | `daily` | 只保存部分记录，换存档后 attempts/recordMode 不一致 |

`quest` 是反例：它明确把定义、流转、进度、奖励、导入导出、重置分块，用户即使不知道 `destroy()`，也能从 `reset()`、`import()`、回调取消函数找到对应方案。因此后续统一模板不必追求穷举所有 interface，而应**每个模块固定包含“查询 / 清理 / 序列化”三小节**。

**未发现“文档名字不存在于源码”的硬错误。** 但有两个值得关注的近误报：

- `daily` 的 `fromText`：源码可返回 `null`，文档不写判空是“返回值坑”，不是“不存在 API”。
- `difficulty` 的 `multiplier()`：API 存在、文档存在，但正负方向对部分 key 反转，若只在源码注释里说明，仍可能造成严重逻辑错误。

这两个应被视为高于普通“方法漏列”的文档缺陷。


---

## 第 7 批 · 评分匹配 / 工程运维 / 表现外壳（20 个）

**子代理实验的第二轮**：4 个子代理各 4 个 + 我自己 4 个。
特意让两组都审 `observable`，想看结论是否稳定——**结果不稳定**。

| 分组 | 单元 | 结论 |
|---|---|---|
| A（子代理） | entity / adapters / save / observable | 8 项真问题 |
| B（子代理） | elo / matchmaking / matchops / ranking | 8 项真问题 |
| C（子代理） | analytics / anticheat / crash / debug-console | 4 项真问题 |
| D（子代理） | cheatcode / logger / timeutil / observable | 3 项真问题 |
| 我自己 | config / di / scheduler / scheduling | 4 项真问题 |

### 子代理的两处"重复报已修项"

- B 组说"elo 文档写 1500 而源码 1200" —— 我上一轮已加 1200/1500 对比表
- C 组说"debug-console 写 6 个命令实际 4 个" —— 我上一轮已删掉不存在的两个

**它不知道历史，会把已修的当新问题。** 下次要给"已修清单"。

### 同单元两组结论矛盾

A 组说 `observable` 有 4 项真问题；D 组说"无新增，上一轮没遗漏"。

核实后 **A 组对**——`peek()`、`listenerCount`、`invalidate()`、`Change<T>`、
`RefOptions.equals` 全都漏了。

> **D 组被我给的"已知误报清单"带偏了。**
> 我在 prompt 里列了 `flush`/`batchSize` 是内部 API 不用报，
> 它顺带把真正该报的一起放过了。
>
> **排除清单越具体，越容易让它过度放过。下次不给清单，让它全报我来筛。**

### 本批实测证伪的文档

| 单元 | 文档原话 | 实测 |
|---|---|---|
| `cheatcode` | "`GOLD`/`GOLDEN` 短码优先触发" | 注册三命令实测：`gol` 只报未知命令，**从无前缀匹配** |
| `timeutil` | `Countdown` 只写 `start`/`remaining` | 暂停 50s 期间剩余冻结，但 `state()` **仍返回 running** |
| `debug-console` | 源码 JSDoc 列 6 个内置命令 | 实际只注册 4 个（`help`/`history`/`clear`/`find`） |

### ⚠️ 两个"命令模块"的 API 形状不同

这是本批最有价值的发现。库里有两个命令模块，
`CommandDef` 长得几乎一样，**字段名和 `run` 签名却完全不同**：

| | CheatCode | DebugConsole |
|---|---|---|
| 别名 | `aliases` | `alias` |
| 帮助文本 | `desc` | `help` |
| 参数类型 | `int`/`number`/`string`/`bool` | `int`/`float`/`string`/`bool`/`enum` |
| `run` 签名 | `run(ctx)`，参数在 `ctx.args`（**数组**） | `run(args, ctx)`，参数**按名字取** |
| 相似度函数 | `levenshtein` 返回**整数**（越小越像） | `similarity` 返回 **0~1**（越大越像） |

实测同一个 `give sword 5`：

```
CheatCode     ctx.args = ["sword", 5]          ← 位置数组
DebugConsole  a.getString('item') = "sword"    ← 按名字
```

> **复制粘贴命令定义会静默失效。**
> `alias` 少个 `s` 不报错，只是别名不生效；
> `desc` 写成 `help` 不报错，只是帮助文本空白。
> 这类问题不崩，只表现为"功能好像没生效"，调试时极难联想到字段名。

已在两份 README 里**双向**写了对照表。

### 体检脚本的修正（第四轮）

**实现内部接口的公开方法也算进来了。**

`observable/Computed.onRead` 实现的是**非导出**的 `Collector` 接口，
语法上是 `public`，但用户根本不需要知道它怎么收集依赖。

约定：这类方法在 JSDoc 里写 `@internal`，脚本跳过。

> 比在脚本里硬编码"排除 onRead"通用——
> **下一个类似的方法不用再改脚本。**

累计四轮修正：泛型匹配、只扫 README.md、非导出类、private 方法、`@internal`。

### 收尾：全库 API 缺口清零

```
API 缺口（函数/方法）  0 项     ← 从 62 项降到 0
无 README             0 个
测试未引用            0 个
多文档未互链          0 个
```

**剩余 435 项"类型名未文档化"，我判断大部分不该补：**

| 分类 | 数量 | 补不补 |
|---|---|---|
| 数据结构（内部） | 253 | 不补 |
| 配置类型（`*Options`/`*Config`） | 97 | 不补 |
| 联合 / 枚举类型 | 76 | 不补 |
| **作为导出函数返回类型** | **21** | **该补** |
| 常量 | 9 | 不补 |

理由：类型名用 IDE hover 就能拿到；用户实际写代码用的是字面量
（`new Dash({ maxSpeed: 10 })`），不需要知道 `DashOptions` 这个名字。

**例外是"返回类型"**：拿到返回值要存起来 / 传下去时必须写类型标注。
这 21 项留作下一轮。

### 顺带修的两处工程问题

1. **`package.json` 漏注册 `example:basic`** —— 磁盘上有 20 个示例，
   只注册了 19 个。已补上。
2. **`审查记录.md` 有 123 行重复块** —— 上次合并报告时切错位置。**已于 2026-09-05 清理**（同时删掉子代理报告的逐单元细节与引用来源共 515 行，只保留结论）。
   已删，文件从 1086 行降到 963 行。

---

## 补遗 · 审过但漏记的 4 个单元

> **为什么会漏**：这几轮的对话多次因过长被中断，
> 改动已落盘但记录没写完。
>
> 判断依据不是记忆，是**改动痕迹**——README 里出现了
> 只有审查才会产生的产物（双实现对照表、返回值结构章节、
> 密集的 ⚠️ 提示），而记录里只字未提。

| 单元 | 判断依据 |
|---|---|
| `currency` | 有 API 章节 + 10 处 ⚠️ + 222 行（原 62 行） |
| `autoquality` | 有 API 章节 + 4 处 ⚠️ |
| `transition` | 有 API 章节 + 3 处 ⚠️ |
| `diagpack` | 5 处 ⚠️，无 API 章节 |

### 1. `currency` —— ⚠️ 同目录有两套钱包实现

**本批最严重的发现。** 一个目录里有两个文件，
都导出 `CurrencyDef`，**字段完全不同**：

| | `CurrencyWallet.ts`（推荐） | `Currency.ts` 的 `Wallet` |
|---|---|---|
| 主打 | **精度与上下限** | **多货币原子支付** |
| 单货币消费 | `spend()` → `SpendResult`（带 `need`/`have`） | `spend()` → `boolean` |
| 小数精度 | ✅ `precision` | ❌ 只支持整数 |
| 负债（负数） | ✅ `floor` | ❌ |
| 多货币原子 | ❌ 仅 `exchange`（1:1） | ✅ `spendAll` / `addAll` / `canAfford(costs[])` |
| 测试 | ✅ 36 项 | ❌ **无** |

```
CurrencyWallet: { id, name, initial, cap, floor, precision, tracked, data }
Currency.ts   : { id, name, max,      premium,  trackLog,  desc }
```

> **只 import 一个文件**就不会冲突；两边都引会得到同名不同类型的 `CurrencyDef`。

**处理**：源码头 + README 顶部**双向**都加了对照表。
`Currency.ts` 里还写明了"本实现无测试覆盖，用之前先补测试"。

**其余补进去的**：

| 项 | 坑 |
|---|---|
| `get` / `tryGet` / `has` | `get` 未声明货币**抛错**；`tryGet` 返回 0（UI 遍历用）；`has` 返回 false |
| `SpendResult` | `need`/`have` 只在**失败时**有值。提示"还差 X"用 `need`，自己算会算错 |
| `LedgerEntry` | **没有 `currencyId` 字段**，靠 `id` 区分货币 |
| `onInsufficient: 'clamp'` | 会让"钱不够也能买"——有 80 买 100 的东西会扣光 80 并成功 |
| `add` 返回值 | 是**实际增加量**不是新余额。满了返回 0 |
| `trackLog: false` | 不记流水，省内存但**对账查不到** |

### 2. `autoquality` —— 初始档位是个陷阱

补了完整 API 表。最有价值的是这两条：

> ⚠️ **初始档位从最低开始 → 旗舰机永远升不上去。**
> 低档位下帧率永远过剩，达不到升级阈值——
> 玩家会一直看着很差的画面，且**不会自动变好**。

> ⚠️ **冷却期内读 `medianFps` 会得到自相矛盾的输出**
> （"低画质 + 60fps"）。这就是 `fpsValid` 存在的原因。

三种读数分工：`medianFps`（**决策用**，抗单帧卡顿）/
`averageFps` / `lowFps1Percent`。用均值决策会被单帧卡顿误触发降级。

### 3. `transition` —— 一个"转场结束后点不动"的坑

补了 API 表。最隐蔽的一条：

> ⚠️ **`overlayStyle(overlay)` 在 `overlay === 0` 时 `visible` 必须是 `false`。**
>
> 只设 `opacity = 0` 的话，全屏遮罩节点**依然会吃掉触摸事件**——
> 表现为"转场结束了，但整个界面点不动了"。
> 这个症状完全不会让人联想到转场模块。

另外补了与 `scenerouter` 的分工：

| | `transition` | `scenerouter` |
|---|---|---|
| 管什么 | 一次转场的**时序与遮罩** | 界面栈的**进出与转场锁** |
| 层级 | 场景级 | UI 级 |

### 4. `diagpack` —— 脱敏的时机反直觉

> ⚠️ **脱敏必须在序列化之后做**：`redact(safeStringify(data))`。
>
> 不是先脱敏再序列化。在对象上做的话，
> 那些被 `String(v)` 转成的字符串（比如 `Error.stack`）里的手机号就漏了。
> **而 `stack` 恰恰是最容易带敏感信息的地方**
> （`POST /api/user/13800138000`）。

其余两条：

- `valuePattern` 正则**必须带 `g`**，否则只替换第一个（模块内部会自动补，但显式写更清楚）
- `collectEnvironment(env)` **接受 env 对象而不是访问全局**——
  浏览器 / Cocos 原生 / Node 的环境 API 完全不同，
  直接访问会让这个模块无法在 Node 里测试

顺带：输出里带 `timezoneOffsetMin` 是有意的——
玩家的"每天 0 点刷新"问题八成是时区理解不一致，
有 offset 就能直接判断是客户端错了还是服务端错了。

---

**教训**：**判断"审没审过"不能靠记忆，要靠改动痕迹。**
对话中断后，记忆会丢失，但落盘的内容不会。

### 补遗续 · 又发现 6 个同样确凿的

补完上面 4 个后重新扫描，发现遗漏不止这些——
**同一批对话里还有 6 个单元的改动痕迹同样明显。**

| 单元 | 判断依据 |
|---|---|
| `gesture` | 「## API」章节 + 4 处 ⚠️ + 125 行（原 62 行） |
| `rebind` | 「## API」章节 + 6 处 ⚠️ + 136 行（原 63 行） |
| `minimap` | 「## API」章节 + **12 处** ⚠️ + 245 行（原 45 行） |
| `telemetry` | 「## API」章节 + 5 处 ⚠️ + 130 行 |
| `command` | 「## 返回值结构」章节（批量补的产物） |
| `settings` | 「### 存档：SettingsSnapshot」章节 |

**`minimap` 从 45 行涨到 245 行**，是全库单轮增幅最大的一个。

各单元补进去的关键内容：

| 单元 | 最有价值的一条 |
|---|---|
| `gesture` | `totalTurn` **必须带符号**——不分正负的话，抖 20 次每次 180° 累加就是 20π，会被误判成画了 10 个圈 |
| `rebind` | `encodeBinding` **会排序去重修饰键**。否则 `ctrl+shift+s` 和 `shift+ctrl+s` 是两个组合，玩家"明明改了还提示冲突" |
| `minimap` | `worldToMinimap` **内部做了 Y 轴翻转**，调用方直接用。自己手写变换最容易漏这步，症状是"上下颠倒、左右正常" |
| `telemetry` | `shouldFlush()` **要自己每帧调**。本模块不自带定时器，忘了调"超时自动发送"就永不发生 |
| `command` | `CommandStackSnapshot` 的 `undoNames` **栈顶在前**。当成时间正序会显示错"下一步撤销什么" |
| `settings` | `pendingRestart` 要从 `snapshot()` 读，别自己维护列表 |

> **`rebind` 的 `clearAll()` 也值得单独记**：
> 它清空**包括默认在内**的所有绑定，
> 之后玩家没有任何可用按键。
> "重置"应该用 `resetToDefault()`。

---

**至此「审过但漏记」共 10 个单元全部补完。**

剩余 15 个（accessibility / audio / condition / curve / cutscene /
expression / feedback / gameflow / i18n / mmr / noise / progressbar /
reddot / social / spatial / subtitle / tutorial / tween / uinav）
**没有明确的审查产物痕迹**，需要逐个打开文件确认是真的没审过，
还是只改了一两行没留下特征。
