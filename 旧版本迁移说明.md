# 从 v0230 迁移到 v0.39.0

> **这份说明只针对那一个测试项目。**
>
> 背景：v0230 → v0.39.0 之间，`save` 与 `room-graph` 两个模块的对外接口发生过变更。
> 这是很老的版本遗留问题，全库内部（README / examples / tests）**早已完全同步**，
> 目前只有一个早期测试项目仍在用旧接口。本文件就是为它写的。

---

## 0. 先做一件事：确认你是否受影响

在项目里搜这三个字符串：

```bash
grep -rn "\.save(\|\.load(\|new RoomGraph" your-project/
```

- **没有任何输出** → 你不受影响，不用往下看了
- **有输出** → 按下面两节逐个改

⚠️ 注意 `.load(` 会误伤：很多模块有自己的 `load()` 方法
（比如 `replay/ReplayRecorder`、`config` 的 `_source.load`）。
**只有 `SaveManager` 实例上的 `.save()` / `.load()` 才需要改。**

---

## 1. SaveManager：`save` / `load` → `write` / `read`

### 1.1 方法改名

| 旧（v0230） | 新（v0.39.0） |
|---|---|
| `manager.save(slot, data)` | `manager.write(slot, data): boolean` |
| `manager.load(slot)` | `manager.read<T>(slot): Result` |

旧方法**已经不存在了**，调用会直接抛：

```
TypeError: manager.save is not a function
```

### 1.2 ⚠️ 返回值也变了 —— 这是最容易漏的一处

`load()` 大概是直接返回数据；`read()` 返回的是 **Result 风格**：

```ts
{ ok: true; value: T } | { ok: false; error: string }
```

**就算你把方法名改对了，不判 `ok` 还是会出错**——
而且这次不报错，是拿到一个包装对象当数据用，字段全是 `undefined`。
这类 bug 能一路带到线上才发现。

```ts
// ✗ 错：只改了方法名
const data = saves.read<MySave>('slot1');
console.log(data.floor);        // undefined（实际是 { ok, value } 或 { ok, error }）

// ✓ 对
const r = saves.read<MySave>('slot1');
if (!r.ok) {
  console.warn('读档失败：', r.error);
  return;
}
console.log(r.value.floor);     // 正确
```

### 1.3 `read` 可能出现的失败原因

调试时看 `r.error` 就能定位：

| error 文案 | 含义 |
|---|---|
| `槽位 "xxx" 不存在` | 没存过，或 key 前缀不对 |
| `存档解析失败（文件损坏）` | JSON 坏了 |
| `存档格式错误` | 信封结构不对 |
| `存档属于另一个游戏（xxx）` | **`gameId` 不匹配**，见 1.4 |
| `存档校验失败（可能被修改或损坏）` | checksum 对不上 |
| `存档版本 N 高于当前 M（请更新游戏）` | 用新档玩老版本 |
| `缺少 vN → vN+1 的迁移函数` | 版本升了但没注册迁移 |

### 1.4 构造函数参数位置

```ts
// 现在（v0.39.0）
new SaveManager(storage, { gameId: 'your-game', version: 2 })
```

第二参数是**必填**的配置对象，两个必填字段：

| 字段 | 作用 |
|---|---|
| `gameId` | 游戏标识。**不同游戏的存档会被互相拒绝**，改了它旧档就读不出来 |
| `version` | 当前存档版本，配合 `registerMigration` 做逐级迁移 |

⚠️ 如果参数位置传错，**不会报错**，而是 `gameId` 变成 `undefined`——
后果是存档前缀乱掉、或者读到别的游戏的档。这种不报错的错最难查。

```ts
// 迁移函数必须"逐级"，不能跳级
saves.registerMigration(1, 2, (d) => ({ ...d, relics: d.relics ?? [] }));
// registerMigration(1, 3, ...) 会直接抛错（要求 to === from + 1）
```

### 1.5 好消息：旧存档文件本身能读

存档在磁盘上的信封格式是：

```ts
{ v, gameId, savedAt, checksum, data }
```

只要 **`gameId` 和旧项目保持一致**，`read()` 会自动沿着
`registerMigration` 逐级把老数据迁到新版本。

**所以迁移时不要改 `gameId`**，改了旧档就读不出来了（会报"存档属于另一个游戏"）。

### 1.6 IStorage 有个新的可选方法 `commit`

```ts
interface IStorage {
  read(key: string): string | null;
  write(key: string, data: string): void;
  remove(key: string): void;
  keys(): string[];
  commit?(tmpKey: string, key: string): void;   // 新增，可选
}
```

**可选**——不实现也不会报错，会降级成"双档 + 回退"（主档写坏时读回 `__tmp` 备份）。
仍然安全，只是不如 `rename` 干净。想用就实现它：

- Node：`fs.renameSync(tmpPath, keyPath)`
- 浏览器 / 小游戏：先写新 key，成功后删旧 key（尽力而为）

---

## 2. RoomGraph：类 → 函数

### 2.1 调用方式变了

```ts
// ✗ 旧（v0230）：类
const g = new RoomGraph(...);

// ✓ 新（v0.39.0）：函数
import { generateRoomGraph } from './room-graph/RoomGraph';
const g = generateRoomGraph({ depth: 9, rng: new RNG(12345) });
```

`RoomGraph` 这个导出**已经不存在了**（现在导出的是 `generateRoomGraph` 函数）。
用 `new` 会直接崩：

```
TypeError: RoomGraph is not a constructor
```

### 2.2 新签名（选项对象）

```ts
interface RoomGraphOptions {
  depth: number;          // 必填。总层数（含入口层与 Boss 层），最小 3
  width?: LayerWidth;     // 每层节点数，默认 2~4
  rng?: IRandomSource;    // 随机源。不传则用不可复现的默认源
  allowSkip?: boolean;    // 默认 false。开了会出现跨层捷径
  maxOutDegree?: number;  // 默认 2。1=瘦树状，2=经典分叉（推荐）
  minOutDegree?: number;  // 默认 1。设 0 会出死路
}
```

⚠️ `depth` 是**必填且最小 3**：

```ts
generateRoomGraph({ depth: 1001, rng })    // ✗ 抛「层数过大」
generateRoomGraph({ depth: Infinity, rng })// ✗ 抛「层数至少 3…且必须为有限值」
```

### 2.3 返回结构

```ts
interface RoomGraphData {
  readonly nodes: RoomNode[];   // 节点列表
  readonly depth: number;       // 总层数
  readonly layers: number[][];  // 每层的节点 id 列表
}
```

配套的两个函数：

```ts
assignTypes(graph, spec, rng?)  // 给房间分配类型（战斗/商店/Boss…）
diagnose(graph, opts?)          // 体检：连通性、可达性等
```

---

## 3. 迁移检查清单

改完之后逐条过一遍：

- [ ] 搜不到 `.save(` / `.load(`（确认不是别的模块的同名方法）
- [ ] 搜不到 `new RoomGraph`
- [ ] 每处 `read()` 都判了 `if (r.ok)` —— **这条最容易漏**
- [ ] `SaveManager` 构造的第二个参数传了 `{ gameId, version }`
- [ ] **`gameId` 与旧项目保持一致**（否则旧存档读不出来）
- [ ] 需要读旧档的话，`registerMigration` 逐级注册齐全（1→2→3，不能跳）
- [ ] 跑一遍：存 → 退出 → 重进 → 读，确认旧档能正常读出

---

## 4. 如果不想迁移

也可以继续用旧版本——把那个测试项目锁定在 v0230 的那份拷贝上，不更新这两个模块即可。

但要注意：**旧版不会再收到后续修复**。这次精审修掉的 30 个 P0
（数据损坏、经济漏洞、死循环等）都只在 v0.39.0 上，旧版没有。

---

## 5. 一点说明

这类"改了对外接口但不通知"的事，在 **npm 安装**的库里会有版本警告兜底；
这个库是**复制分发**的（第 7 条铁律"复制即可用"），
你手里的代码和仓库完全脱钩，没有自动通知渠道——**README 是唯一的通知方式**。

所以后续如果再有对外接口变更，会在这个文件里追加记录，不再静默修改。
