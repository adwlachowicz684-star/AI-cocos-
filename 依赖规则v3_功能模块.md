# 依赖规则 v3：功能模块

> **文档导航**
> - [`README.md`](./README.md) —— 入口
> - [`轮子清单.md`](./轮子清单.md) —— 找某个功能
> - [`插件脑图.md`](./插件脑图.md) —— 看整体结构
> - [`依赖规则v2`](./依赖规则v2_前置模块与分层.md) + 本文档
>
> v2 讲的是**纵向**关系（前置模块：A 建立在 B 之上）。
> 本文讲**横向**关系（并列聚合：A 和 B 是同一功能的两部分）。
>
> 起因是你提出的第二个观察：
> 「如果多个小方法组合成一个大模块，则可以打包视作一个模块——
> 例如技能的目标释放和弹道释放，两者是并列的，但是同一个功能的两部分。」
>
> 这一条比 v2 更重要。因为它解决的不是"能不能依赖"，
> 而是**"一个模块应该有多大"**——这是拆库时真正的难题。

---

## 一、v2 留下的缺口

v2 确立的规则是「必需才依赖，可选就注入」，依赖方向只能向上。

它解释了**纵向**关系：

```
ds（数据结构）
  ↑
pathfind（需要优先队列）
```

但它对**横向**关系无话可说。于是现实中出现了这个问题：

```
hitbox/       判定形状
projectile/   弹道飞行
indicator/    范围指示
telegraph/    攻击预警
skill-caster/ 技能释放
```

这五个是**同一个功能的五个部分**，但在库里是五个平级目录，
彼此零依赖、零关联。使用者要做一个技能系统，得自己知道
"要把这五个一起拖进来"。

**这就是过度拆分。**

v2 只告诉了你"谁可以依赖谁"，
没告诉你"哪些东西本来就该在一起"。
结果是：为了遵守"禁止横向依赖"，
把高内聚的功能拆成了五个互不相识的孤儿。

---

## 二、核心概念：功能模块（Feature Module）

> **一个功能模块 = 一组高内聚的插件，对外表现为一个完整功能**

关键区分：

| | 前置关系（v2） | 并列聚合（v3） |
|---|---|---|
| 形态 | A → B，纵向 | A ↔ B，横向 |
| 语义 | B 建立在 A 之上 | A 和 B 是同一功能的两部分 |
| 例子 | `pathfind → ds` | 目标释放 ↔ 弹道释放 |
| 去掉一个 | B 无法工作 | 功能缺一半 |
| 规则 | 必需才依赖 | **打包成一个模块** |

你举的例子正好是后者：

```
        技能释放（功能）
        ╱     │      ╲
   目标释放  弹道释放  时间轴释放
   hitShape  projectile  timeline
```

三者地位对等，谁也不是谁的前置。
它们共同回答"技能怎么落地"这一个问题。
**去掉任何一个，技能系统残缺；只留任何一个，也叫技能系统——但只能做一半的技能。**

---

## 三、三条判据：什么该打包在一起

借用软件工程里的**包内聚性原则**，翻译成游戏开发的语言：

### 1. REP —— 一起被复用的，放一起

> 我会单独想要 `hitbox` 吗？还是每次要它时都同时想要 `projectile`？

如果你从来没有过"只要判定形状、不要弹道"的需求，
那它们就不该是两个可獨立複用的单位。

**反例**：`ds`（数据结构）显然应该独立——
做排行榜要优先队列，做寻路也要，但它们互不相干。

### 2. CCP —— 一起变化的，放一起

> 改技能系统时，`hitbox` 和 `projectile` 会一起改吗？

会。比如加"技能可以穿透"这个特性：
弹道要加 `pierce`，判定要去重。
改一个需求，动两个目录 → 它们是一个模块。

**这条最实用。** 想想上次改功能时你动了几个目录，
那些目录就该是一个模块。

### 3. CRP —— 不要把用户用不到的强塞给他

> 有人只想用 `telegraph`（比如陷阱预警，不需要技能系统）吗？

有。所以不能把 `telegraph` 完全吞进 `skill-caster` 内部。

**这条是刹车。** 它防止你把整个库揉成一坨。

### 三条冲突时怎么办

REP 和 CCP 会让模块变大，CRP 会让模块变小。实践中的取舍：

- **前中期听 CCP**（一起变化的一起放），模块宁可大一点
- **明确有人只想要其中一部分时，听 CRP**，把那部分独立出去
- **REP 用于验证**：如果拆出来后没人单独用过，说明拆错了

---

## 四、模块内的依赖规则

**模块内可以自由依赖，模块间受 v2 约束。**

```
┌─ 模块：skill ──────────────────────┐
│  skill-caster ←── 对外入口(Facade) │
│      ↓ 依赖                         │
│  hitbox   projectile   indicator    │  ← 模块内可横向依赖
│      ↓         ↓           ↓        │
│  telegraph ────────────────┘        │
└─────────────────────────────────────┘
        ↓ 跨模块只能依赖对方的 Facade 或 L0/L1
┌─ 模块：combat ─────────────────────┐
│  damage-pipeline ←── Facade         │
└─────────────────────────────────────┘
```

### 为什么允许模块内横向依赖

因为它们是同一个功能的内部实现细节。
`skill-caster` 依赖 `hitbox`，就像 `HashMap` 依赖 `Hasher`——
**这是实现，不是耦合**。

对外，**整个模块只暴露 Facade**。
外部不需要知道里面有五个文件还是五十个。

### Facade 的作用

```typescript
// ✅ 外部这样用
import { SkillCaster, type SkillDef } from './skill';

// ❌ 外部不该这样
import { HitboxWorld } from './skill/hitbox/Hitbox';
import { ProjectileWorld } from './skill/projectile/Projectile';
```

Facade 的三个职责：
1. **收窄 API 面** —— 只导出外部需要的
2. **隐藏实现** —— 内部怎么拆是内部的事，改了不影响外部
3. **提供组合** —— 把五件事的正确用法固定下来（这是最大的价值）

第 3 条最值钱。五个插件各自正确，但**用错顺序照样出 bug**。
Facade 把正确的组合方式写死，外部就不会用错。

---

## 五、当前的模块划分

定义在 `_kitmeta.json` 的 `modules` 字段，`check-deps.js` 会校验。

**现状（2026-09-04）：9 个模块，收录 61 个插件，另 58 个未归类。**

| 模块 | 数量 | 包含 | 为什么是一伙的 |
|---|---|---|---|
| **roguelike** 肉鸽核心 | 12 | room-graph / wave-spawner / meta / affix / loot / daily / leaderboard / runscope … | 一局的完整循环 |
| **skill** 技能系统 | 9 | skill-caster / skill-player / hitbox / projectile / indicator / telegraph / attribute / **skill-queue** | 三种释放模式是并列关系，共同回答"技能怎么落地" |
| **combat** 战斗数值 | 9 | damage-pipeline / element / buff / scoring / difficulty / attack-token / perception / targeting / combo | 伤害、元素、增益、评分构成一套反馈闭环 |
| **content** 内容系统 | 9 | inventory / card / dialogue / quest / shop / craft / gacha / setbonus / turn | 都是数据驱动的内容层 |
| **tooling** 开发工具 | 9 | logger / config / debug-console / command / crash / binary / snapshot / replay / scheduler | 开发期用的，不进游戏运行时 |
| **ai** AI 行为 | 7 | behavior-tree / fsm / steering / pathfind / grid / fov / dungeon | 决策、移动、寻路、感知缺一不成 AI |
| **presentation** 演出 | 3 | cutscene / transition / camera | 过场与转场 |
| **ops** 运维 | 2 | diagpack / telemetry | 线上诊断与埋点 |
| **audio** 音频 | 1 | audio | — |

> ⚠️ **未归类的 58 个不是遗漏，是还没轮到。**
>
> 分类的价值在于指导"复制时该拿哪一堆"。
> 已经成体系的（技能、战斗、肉鸽、AI）先归好；
> 剩下的一次性全塞进某个模块，等于没分类——
> 那是凑数字，不是整理。
>
> 什么时候补：**下次要动那个插件时顺手归类**。
> 别为了凑覆盖率做一次性大改。

---

## 六、对你当前项目的直接影响

### 1. 不用急着重构目录

现有目录结构保持不变——**模块是逻辑概念，不强制要求物理目录**。
在 `_kitmeta.json` 里声明归属即可，`check-deps.js` 认这个声明。

理由是：大规模移动文件会打断你正在写的主项目，
而收益（目录好看）小于风险。

### 2. 新功能按模块建

以后加新插件时，先问：**它属于哪个模块？**

- 属于已有模块 → 放进该模块，允许依赖模块内其他插件
- 不属于任何模块 → 独立，且大概率会成为新模块的种子

### 3. 复制时的单位从"插件"变成"模块"

以前问："复制 `hitbox` 要改几行？"
现在问：**"复制 `skill` 模块要改几行？"**

后者才是真实场景——你不会只想复制一个判定形状。
按模块计量，才能反映真实的复用成本。

### 4. Facade 是可选的

现有插件没有强制 Facade（因为历史原因）。
新建模块应该有。老模块**在下次需要动它时**再补——
不要为了凑规范做一次性大改。

---

## 七、三条规则的完整形态

| 版本 | 解决的问题 | 规则 |
|---|---|---|
| v1 | 混乱依赖 | 禁止一切横向依赖 |
| v2 | 该复用的没复用 | **「必需」才依赖，「可选」就注入；依赖只能向上，不能成环** |
| v3 | 过度拆分 | **一起变化的打包成模块；模块内可自由依赖，模块间走 Facade** |

一句话总结：

> **先按功能聚成模块（v3），再排模块之间的层级（v2）。**
> **内聚优先于分层。**

---

## 八、一句话记忆

| 问题 | 答案 |
|---|---|
| A 和 B 是上下关系吗？ | 是 → v2：必需才依赖 |
| A 和 B 是同一功能的两部分吗？ | 是 → v3：打包成模块 |
| 有人会单独要 A 吗？ | 会 → CRP 刹车，让它保持独立 |
| 改 A 的时候会一起改 B 吗？ | 会 → CCP，它们该在一起 |
