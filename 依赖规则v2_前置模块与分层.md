# 依赖规则 v2：前置模块与分层

> **文档导航**
> - [`README.md`](./README.md) —— 入口
> - [`轮子清单.md`](./轮子清单.md) —— 找某个功能
> - [`插件脑图.md`](./插件脑图.md) —— 看整体结构
> - 本文档 + [`依赖规则v3`](./依赖规则v3_功能模块.md)
>
> 本文是「插件之间禁止横向依赖」这条铁律的**升级**，不是放宽。
>
> 起因是你提出的一个观察：
> 「如果多个功能可以以某一个功能为中心支点向外扩展，
> 那么可以打包成一起是合理的——例如技能模块借用了自动选择的方法，
> 则自动选择就变为技能模块的前置模块。」
>
> 这个观察是对的。它指出了 v1 规则的一个真实缺陷。

---

## 一、v1 规则错在哪

v1 的规定是：

> 插件之间禁止横向 import。唯一例外是 `_core` 与 `ds`（零依赖纯基础设施）。

### `depends` 字段的口径

`_kitmeta.json` 里每个单元的 `depends` 登记的是：

> **本模块的源码实际 import 了什么**

它**不是**"这个模块通常会和谁一起用"。后者写在 README 里。

| 情况 | 该不该登记 |
|---|---|
| 源码 `import { clamp } from '../_core/math'` | ✓ 登记 `_core` |
| README 示例里 import 了别家，但源码没有 | ✗ 不登记（写进 README） |
| 通过结构化类型接收（duck typing），不 import | ✗ 不登记 |
| 注释里写了 `import ...` 作为**反例** | ✗ 不登记 |

> ⚠️ **`crash` 曾踩过最后一条。**
> 源码注释里有这么一段：
>
> ```typescript
> // 如果这里 `import { LogEntry } from '../logger/Logger'`，就把两者绑死了
> ```
>
> 这是解释"为什么**不能**这么写"的反例，
> 早期登记时被误当成真实依赖写进了 `depends`。已移除，
> 理由补进了 `crash/README.md`。

这个口径由 `node scripts/check-deps.js` 的**第 5 项**自动检查，
不一致会报错。自动修法：

```bash
node scripts/check-deps.js --fix
```

> **`--fix` 会直接改写 `_kitmeta.json`**，改前建议先 `git diff` 看看。
> 它保留精确写法（如 `_core/math`），只做两件事：
> 删掉源码里没有的、补上源码里有但没登记的。

执行下来，当前库的实际依赖图是这样的：

```
30 个插件 → 全部只指向 _core / ds
28 个插件 → 出度为 0（谁都不依赖）
```

**没有任何一个插件依赖另一个插件。**

这个结果本身暴露了问题：它意味着所有插件被压成了完全扁平的一层，
而真实世界里它们显然不是平级的——
`pathfind` 需要优先队列，`dungeon` 需要并查集，
`skill-caster` 需要判定形状。有些依赖是**天然的**。

v1 把「禁止循环和禁止混乱依赖」误写成了「禁止一切依赖」。
后果有两个：

1. **该复用的没复用** —— 明明已有现成能力，却因为规则不敢用
2. **更糟的：悄悄重复实现** —— 为了不 import，把工具函数复制一份到另一个插件里

第 2 条比横向依赖危险得多。横向依赖至少是显式的，
复制出去的代码会**各自演化、各自出 bug**。

---

## 二、v2 的核心区分：必需依赖 vs 可选增强

这是本文唯一真正重要的概念。

### 前置模块（Prerequisite）—— 可以依赖

**B 的实现建立在 A 之上，且没有 A 就不能工作。**

| 例子 | 为什么是前置 |
|---|---|
| `pathfind` → `ds` | 需要 BinaryHeap，没有就实现不了 A* |
| `dungeon` → `ds` | 需要 DisjointSet 做连通性检查 |
| `damage-pipeline` → `_core` | 需要 clamp/round |

判据：**把 A 拿掉，B 立刻无法编译或无法工作。**

### 可选增强（Enhancement）—— 必须注入

**B 可以用 A 做得更好，但没有 A 也能正常工作。**

| 例子 | 为什么是注入 |
|---|---|
| `crash` → `logger` | 崩溃报告**可以**带日志，没日志照样能报 |
| `skill-caster` → `targeting` | 技能**可以**自动选目标，也可以朝指定方向放 |
| `damage-pipeline` → `logger` | 想打日志就注入一个回调，管线本身不需要认识日志系统 |

判据：**把 A 拿掉，B 照样能用，只是少个功能。**

### 回到你举的例子

> 技能模块借用了自动选择的方法，则自动选择就变为技能模块的前置模块

这句话成立与否，取决于一个设计决定：

- **如果每个技能都必须先选目标**（没有目标就放不出来）
  → `targeting` 是前置模块，`skill-caster → targeting` 合法
- **如果只有部分技能需要选目标**（AOE 和方向技能不需要）
  → 应该注入 `ITargetProvider`，保持零依赖

**我倾向第二种。** 理由是：肉鸽里 AOE 技能、位移冲刺、自身 buff
都不需要目标。一旦把 targeting 变成必需依赖，
那些不需要选目标的技能就被迫背上了一个用不上的模块。

而且注入的成本极低——一个接口，十行代码：

```typescript
interface ITargetProvider {
  pick(caster: unknown): number | null;
}

// 需要自动选目标时：
new SkillCaster({ targetProvider: myTargeting });
// 不需要时：
new SkillCaster({});
```

**结论**：你的判断标准是对的（一个模块以另一个为中心向外扩展就该打包），
只是要再加一条 —— **"必需"才依赖，"可选"就注入。**

---

## 三、四条判据（可操作）

一条依赖 `B → A` 是否合法，问四个问题：

### 1. A 能独立使用吗？

A 单独拿出去，能解决一个完整问题吗？

- `ds.BinaryHeap` → 能（任何需要优先队列的地方）
- `targeting.TargetSelector` → 能（镜头锁定、UI 焦点、塔防选敌）

如果 A 只有在 B 的上下文里才有意义，那它不是前置模块，
而是 B 的**内部实现**——应该合并进 B，不要拆成两个插件。

### 2. A 知道 B 的存在吗？

**A 里一旦出现 B 的概念，就是设计错了。**

```
✅  ds 里没有 "寻路" 这个词        → pathfind → ds 合法
❌  targeting 里出现 "技能" 这个词  → 反向耦合，必须改成注入
```

这条最容易在日常迭代中被破坏：
为了省事在 A 里加一个 `if (fromSkillCaster)` 的特判，
就悄悄把 B 的概念灌进了 A。

### 3. 依赖图是无环的吗？

A → B → C → A 是最典型的架构腐化。

自动化检测已内建，见第五节。

### 4. 复制成本可以接受吗？

这是最根本的判据，v1 和 v2 共用：

> **把 B 复制到另一个项目，需要改几行？需要带上几个前置？**

- 带 1~2 个你本来就想要的前置 → 合理
- 带 5 个你根本不关心的前置 → 说明 B 的粒度定错了

`pathfind` 依赖 `ds`：复制寻路时带上数据结构库，
这是任何项目都乐意接受的。
但如果 `pathfind` 依赖了 `logger`、`config`、`crash` 三个无关模块，
复制时就变成了「我只是想要个 A*，结果拖进来半个游戏框架」。

---

## 四、新的分层

```
┌──────────────────────────────────────────────────────┐
│ L0  基础设施        零依赖，纯函数 / 纯结构            │
│     _core（math / types）                             │
│     ds（BinaryHeap / DisjointSet / QuadTree / Hash）  │
├──────────────────────────────────────────────────────┤
│ L1  原子件          只能依赖 L0                        │
│     rng  event-bus  pool  logger  scheduler  config   │
│     input  curve  result  signal  expression  binary  │
│     grid  hex  noise  fov                             │
├──────────────────────────────────────────────────────┤
│ L2  复合件          可依赖 L0–L1                       │
│     damage-pipeline  fsm  behavior-tree  buff         │
│     pathfind  dungeon  steering  inventory  card      │
├──────────────────────────────────────────────────────┤
│ L3  域件            域内可依赖「前置模块」              │
│     技能域   skill-caster（核心）                      │
│               ← indicator / telegraph / hitbox        │
│     肉鸽域   room-graph（核心）                        │
│               ← wave-spawner / affix / meta           │
└──────────────────────────────────────────────────────┘
```

**规则只有一条：箭头只能向上，且不能成环。**

### 域（Domain）的定义

> 一个域 = 一个核心插件 + 若干个围绕它的前置/扩展插件

识别方法：如果几个插件**总是一起被使用**，
且其中一个明显是「其他几个存在的理由」，
它们就构成一个域。

```
         skill-caster（核心：为什么需要其他几个）
              ↑            ↑
        indicator      telegraph
     （给技能显示范围）（给技能做预警）
```

注意 `indicator` 和 `telegraph` **自身也能独立使用**
（比如陷阱预警不需要技能系统），所以它们是合法的前置模块，
而不是 skill-caster 的内部实现。

---

## 五、自动化检测

`_kitmeta.json` 里为每个插件标注 `layer` 与 `domain`，
用脚本定期跑三项检查：

| 检查 | 失败意味着 |
|---|---|
| **环检测** | 架构腐化，必须立刻修 |
| **层违规** | 依赖了更高层，说明方向搞反了 |
| **L0 纯净度** | 基础设施里混进了业务概念 |
| **LAYERS 覆盖面** | 有插件目录没登记分层 → **它的分层从未被检查过** |

> **最后一项是后来加的。**
> 曾经 `LAYERS` 表漏了 11 个目录，它们的分层一直没被检测——
> 检查工具却在报告"全部通过"。
>
> **漏检比不检查更危险：它给人一种虚假的安全感。**
> 所以加了反向核对（`[2d] 覆盖面`）：扫到的目录必须都在 LAYERS 里。

```bash
node scripts/check-deps.js
```

**当前状态（2026-09-05 复核）：49 个节点，47 条边，0 环，0 违规。**

`_kitmeta.json` 的 `depends` 已与源码 import 完全对齐（详见上面第 5 项检查）。

其中只有一条是真正的「插件依赖插件」：

```
skill-queue(L3) → skill-caster(L2)
```

其余 46 条全部指向 `_core` / `ds`（L0 基础设施）。

> **这意味着：目前只有一个前置模块关系被实际建立。**
> 下面「四、新的分层」里的域结构（`skill-caster ← indicator/telegraph/hitbox`）
> 是**设计意图**——说明"如果要依赖，应该长这样"，
> 而**不是**当前的 import 关系。
>
> 现状之所以如此扁平，是因为早期把"禁止混乱依赖"误读成了"禁止一切依赖"，
> 于是该复用的也没复用。这是需要逐步修正的历史问题，不是目标状态。

---

## 六、一句话总结

| | v1 | v2 |
|---|---|---|
| 规则 | 禁止一切横向依赖 | 允许向上的依赖，禁止向下和成环 |
| 判据 | 「是不是 `_core`」 | 「是不是必需的前置模块」 |
| 结构 | 完全扁平 | 分层 + 域 |
| 可选功能 | 也只能注入 | 仍然只能注入 |

**「必需」才依赖，「可选」就注入。依赖只能向上，不能成环。**

---

## 七、给后续开发的直接影响

1. **不再为了规避依赖而复制代码** —— 需要什么就依赖什么，只要方向对
2. **新增插件时标注 `layer` 和 `domain`** —— 写进 `_kitmeta.json`
3. **可选能力一律走接口注入** —— 这条没变，且更重要了，
   因为现在有了「可以依赖」的口子，要防止滥用
4. **每次加依赖前问一遍第三节的四个问题**
