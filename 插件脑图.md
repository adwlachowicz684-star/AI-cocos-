# cocos-kit 插件脑图

> 由 `_kitmeta.json` + 源码扫描**自动生成**，不要手改。
> 重新生成：`python3 scripts/gen-mindmap.py`（数据源为 `_kitmeta.json` 与源码扫描）。
>
> **读法**
>
> - 缩进层级 = 从属关系，可直接被 XMind / 幕布 / markmap 导入
> - **加粗**的是插件单元；其下依次是 作用 / 功能 / 前置依赖 / 接口
> - 接口下的缩进项是**方法**，破折号后是说明
>
> **说明的来源优先级**（保证不编造）：
>
> 1. 该插件 `README.md` 的 API 表格（人工写的准确说明）
> 2. 源码里的 JSDoc 首行
> 3. 方法名前缀推断的类别（查询 / 设置 / 判断 / 添加 / 移除…）
>
> ⚠️ 只写"查询""设置"这类**类别词**的，说明该方法既无文档注释、
> 名字也不在 README 的 API 表里——类别是可靠的，具体语义请读源码。
>
> **文档导航**
> - [`README.md`](./README.md) —— 入口、理念、快速开始
> - [`轮子清单.md`](./轮子清单.md) —— 找某个功能
> - 本文档 —— 看整体结构
> - 119/119 单元已全部交付（开发进度类文档已随收口移除）
> - [`依赖规则v2`](./依赖规则v2_前置模块与分层.md) / [`v3`](./依赖规则v3_功能模块.md)

---

## 视图一：按领域与功能模块（主视图）

```text
cocos-kit（119 个单元 / 119 个目录，一一对应）
│
├─【基础设施】2 个 —— 零依赖。其余一切的地基
│   ├─ _core
│   ├─ ds
│
├─【地基】12 个 —— 不装这些，后面的一切都会歪
│   ├─ 〔功能模块·开发工具〕3 个组合而成
│   │   ├─ config
│   │   ├─ logger
│   │   ├─ scheduler
│   ├─ di
│   ├─ event-bus
│   ├─ input
│   ├─ number-roller
│   ├─ pool
│   ├─ result
│   ├─ rng
│   ├─ scheduling
│   ├─ signal
│
├─【通用工具】12 个 —— 通用小件，按需取用
│   ├─ 〔功能模块·开发工具〕2 个组合而成
│   │   ├─ binary
│   │   ├─ snapshot
│   ├─ condition
│   ├─ curve
│   ├─ expression
│   ├─ i18n
│   ├─ noise
│   ├─ observable
│   ├─ save
│   ├─ spatial
│   ├─ timeutil
│   ├─ tween
│
├─【算法与地图】7 个 —— 不认识你的游戏，只认数字和坐标
│   ├─ 〔功能模块·AI 与地图〕5 个组合而成
│   │   ├─ dungeon
│   │   ├─ fov
│   │   ├─ grid
│   │   ├─ pathfind
│   │   ├─ steering
│   ├─ 〔功能模块·肉鸽〕1 个组合而成
│   │   ├─ room-graph
│   ├─ pathfinding
│
├─【数值与战斗】16 个 —— 打得到、打得中、打得疼
│   ├─ 〔功能模块·技能〕9 个组合而成
│   │   ├─ attribute
│   │   ├─ hitbox
│   │   ├─ indicator
│   │   ├─ projectile
│   │   ├─ skill-caster
│   │   ├─ skill-player
│   │   ├─ skill-queue
│   │   ├─ skill-variant
│   │   ├─ telegraph
│   ├─ 〔功能模块·战斗〕6 个组合而成
│   │   ├─ attack-token
│   │   ├─ buff
│   │   ├─ combo
│   │   ├─ damage-pipeline
│   │   ├─ element
│   │   ├─ targeting
│   ├─ bullet-pattern
│
├─【移动与碰撞】4 个 —— 从「方向」到「角色真的动起来」
│   ├─ collision
│   ├─ dash
│   ├─ joystick-mover
│   ├─ mover
│
├─【AI与感知】3 个 —— 敌人怎么发现你、怎么决策
│   ├─ 〔功能模块·AI 与地图〕2 个组合而成
│   │   ├─ behavior-tree
│   │   ├─ fsm
│   ├─ 〔功能模块·战斗〕1 个组合而成
│   │   ├─ perception
│
├─【内容与掉落】9 个 —— 打完之后掉什么、掉多少
│   ├─ 〔功能模块·肉鸽〕4 个组合而成
│   │   ├─ affix
│   │   ├─ blessing
│   │   ├─ curse
│   │   ├─ loot
│   ├─ 〔功能模块·内容与经济〕4 个组合而成
│   │   ├─ craft
│   │   ├─ gacha
│   │   ├─ setbonus
│   │   ├─ shop
│   ├─ rarity
│
├─【玩法框架】8 个 —— 具体玩法的骨架
│   ├─ 〔功能模块·内容与经济〕4 个组合而成
│   │   ├─ card
│   │   ├─ dialogue
│   │   ├─ inventory
│   │   ├─ turn
│   ├─ 〔功能模块·肉鸽〕2 个组合而成
│   │   ├─ interact
│   │   ├─ wave-spawner
│   ├─ builder
│   ├─ objective
│
├─【进度与统计】11 个 —— 跨局的东西：存档、成就、评分
│   ├─ 〔功能模块·肉鸽〕5 个组合而成
│   │   ├─ daily
│   │   ├─ leaderboard
│   │   ├─ meta
│   │   ├─ runscope
│   │   ├─ score
│   ├─ 〔功能模块·战斗〕2 个组合而成
│   │   ├─ difficulty
│   │   ├─ scoring
│   ├─ 〔功能模块·内容与经济〕1 个组合而成
│   │   ├─ quest
│   ├─ 〔功能模块·开发工具〕1 个组合而成
│   │   ├─ replay
│   ├─ achievement
│   ├─ stats
│
├─【表现与外壳】18 个 —— 玩家能感知、但说不清的「感觉」
│   ├─ 〔功能模块·演出〕3 个组合而成
│   │   ├─ cutscene
│   │   ├─ minimap
│   │   ├─ transition
│   ├─ 〔功能模块·音频〕1 个组合而成
│   │   ├─ audio
│   ├─ 〔功能模块·运维〕1 个组合而成
│   │   ├─ autoquality
│   ├─ accessibility
│   ├─ camera
│   ├─ currency
│   ├─ feedback
│   ├─ gesture
│   ├─ progressbar
│   ├─ rebind
│   ├─ reddot
│   ├─ scenerouter
│   ├─ settings
│   ├─ subtitle
│   ├─ tutorial
│   ├─ uinav
│
├─【评分与匹配】6 个 —— 多人对战：分数、撮合、对局运维
│   ├─ elo
│   ├─ matchmaking
│   ├─ matchops
│   ├─ mmr
│   ├─ ranking
│   ├─ social
│
├─【工程与运维】9 个 —— 开发期与线上：调试、埋点、崩溃
│   ├─ 〔功能模块·开发工具〕3 个组合而成
│   │   ├─ command
│   │   ├─ crash
│   │   ├─ debug-console
│   ├─ 〔功能模块·运维〕1 个组合而成
│   │   ├─ diagpack
│   ├─ analytics
│   ├─ anticheat
│   ├─ cheatcode
│   ├─ gameflow
│   ├─ telemetry
│
├─【适配与实体】2 个 —— 轮子之间的轴，以及「这个 id 是谁」
│   ├─ adapters
│   ├─ entity
│
└─ 共 14 个领域
```

---

## 基础设施（2 个）

> 零依赖。其余一切的地基

- **_core**
  - 作用：第 0 层：纯函数（clamp / lerp / smoothDamp / Easing 30+ / 角度）
  - 功能：提供 `FixedRandomSource`、`clamp`、`clamp01`、`clampNum`
  - 前置依赖：无  · 分层 L0
  - 接口：1 个类 / 34 个方法 / 8 个类型 / 4 个常量
    - class `FixedRandomSource`
      - `next()` — 取下一个值（**到末尾后循环**，不是返回 undefined）
      - `calls()` — 已取用的次数（**测试断言用**）
      - `reset()` — 回到开头
    - 导出函数
      - `clamp()` — 夹紧。`clamp01` 是 `clamp(v, 0, 1)` 的快捷版
      - `clamp01()` — 夹紧。`clamp01` 是 `clamp(v, 0, 1)` 的快捷版
      - `clampNum()` — **容量字段兜底**：非有限值回退，有限超界截断（**有上界**）
      - `numOr()` — **普通配置兜底**：只兜非有限值，**不定上界**
      - `safeDt()` — **dt 守卫**：只有"有限且为正"返回 true
      - `maxOf()` — 操作
      - `minOf()` — 操作
      - `lerp()` — 插值 / **反向**插值（"v 在 a→b 里的位置"）
      - `inverseLerp()` — 插值 / **反向**插值（"v 在 a→b 里的位置"）
      - `remap()` — 区间重映射
      - … 另有 21 个方法
    - type `EasingName` — 类型定义
    - type `IVec2` — 二维向量接口 `{ x, y }`
    - type `IRect` — **角点表示法** `{ minX, minY, maxX, maxY }`
    - type `IRectSized` — **位置+尺寸表示法** `{ x, y, w, h }`
    - type `IDisposable` — 可释放接口（**`destroy()`**，不是 `dispose()`）
    - type `Partial` — 把 T 的所有属性变为可选（配置对象的部分覆盖）
    - type `IRange` — 区间 `{ min, max }`
    - type `IRandomSource` — 随机源接口（`next(): number`）。**所有随机逻辑依赖它而不是 `RN
    - const `DEG2RAD`
    - const `RAD2DEG`
    - const `Easing`
    - const `MathRandomSource`
- **ds**
  - 作用：第 0 层：数据结构（BinaryHeap / LazyHeap / DisjointSet / QuadTree / SpatialHash）
  - 功能：提供 `BinaryHeap`、`LazyHeap`、`DisjointSet`、`QuadTree`
  - 前置依赖：无  · 分层 L0
  - 接口：5 个类 / 41 个方法 / 3 个类型
    - class `BinaryHeap`
      - `size()` — 操作
      - `isEmpty()` — 判断
      - `peek()` — ds —— 游戏开发常用数据结构
      - `push()` — 入堆 O(log n)
      - `pop()` — 出堆 O(log n)
      - `remove()` — 移除特定元素 O(n)
      - `removeWhere()` — 按条件移除第一个匹配的元素
      - `drain()` — 堆化后按序取出全部（会**清空**堆）
      - `toArray()` — 转换
      - `clear()` — 移除
    - class `LazyHeap`
      - `size()` — 操作
      - `isEmpty()` — 判断
      - `push()` — 入堆 O(log n)
      - `pop()` — 出堆 O(log n)
      - `remove()` — 移除特定元素 O(n)
      - `clear()` — 移除
    - class `DisjointSet`
      - `size()` — 操作
      - `componentCount()` — 连通分量数（全部连通时为 1）
      - `find()` — 找根（带路径压缩）
      - `union()` — 合并。返回是否已连通（true = 本次真的合并了）
      - `connected()` — 是否连通
      - `groups()` — 所有连通分量
      - `componentSize()` — 某个连通分量的大小
      - `reset()` — 重置为初始状态
    - class `QuadTree`
      - `count()` — 计算
      - `insert()` — 【⚠️ 全库有 3 个同名 `Rect`，坐标系不同，别混用】
      - `query()` — 放置到子树（不增加计数），供 _insert 和 _split 共用 */
      - `queryCircle()` — 圆形范围查询（半径）
      - `remove()` — 移除特定元素 O(n)
      - `clear()` — 移除
    - class `SpatialHash`
      - `cellSize()` — 操作
      - `count()` — 计算
      - `insert()` — 【⚠️ 全库有 3 个同名 `Rect`，坐标系不同，别混用】
      - `update()` — 每个 id 当前所在的格子（用于快速更新） */
      - `remove()` — 移除特定元素 O(n)
      - `queryNeighbors()` — 查询周围 radius 格内的所有 id（含自己所在格）
      - `queryRect()` — 查询一个像素矩形范围内的 id
      - `queryCell()` — 同格的所有 id
      - `clear()` — 移除
    - 导出函数
      - `rectsOverlap()` — 操作
      - `pointInRect()` — 点是否在矩形内
    - type `CompareFn` — 类型定义
    - type `Rect` — 类型定义
    - type `QuadItem` — 类型定义

---

## 地基（12 个）

> 不装这些，后面的一切都会歪

- 〔功能模块·开发工具〕3 个单元组合
  - **config**
    - 作用：配置加载、索引、全量校验、热重载
    - 功能：提供 `MemoryTableSource`、`ConfigLoader`、`Validator`
    - 前置依赖：无  · 分层 L1
    - 接口：3 个类 / 20 个方法 / 6 个类型
      - class `MemoryTableSource`
        - `set()` — 设置
        - `load()` — 加载单张表
      - class `ConfigLoader`
        - `loadAll()` — 加载所有表 + 跨表引用检查
        - `load()` — 加载单张表
        - `get()` — 按 id 取，**找不到抛错**
        - `find()` — 找不到返回 undefined
        - `all()` — 整表（只读）
        - `where()` — 过滤
        - `count()` — 查询
        - `has()` — 查询
        - `loadedTables()` — **已加载的表名列表**（调试用）
        - `issues()` — 校验问题列表
        - … 另有 4 个方法
      - class `Validator`
        - `validateTable()` — 校验一张表，返回全部问题
        - `validateRow()` — 校验单行（编辑器即时校验用）
        - `checkReferences()` — **跨表引用完整性**
        - `format()` — 格式化为可读文本
      - type `ITableSource` — 类型定义
      - type `ConfigLoaderOptions` — 类型定义
      - type `FieldType` — 类型定义
      - type `FieldSchema` — 类型定义
      - type `TableSchema` — 类型定义
      - type `ValidationIssue` — 类型定义
  - **logger**
    - 作用：分级日志 + 快速失败断言（环形缓冲保留崩溃现场）
    - 功能：提供 `AssertionError`、`Assert`、`Logger`、`ModuleLogger`
    - 前置依赖：_core  · 分层 L1
    - 接口：4 个类 / 33 个方法 / 2 个类型 / 1 个常量
      - class `AssertionError`
      - class `Assert`
        - `isTrue()` — Assert —— 快速失败的断言工具
        - `isFalse()` — 判断
        - `notNull()` — 非空 + 类型收窄
        - `isNull()` — 值必须为空（用于"这个对象应该已被释放"之类的检查）
        - `range()` — 数值区间
        - `finite()` — **非 NaN / 非 Infinity**
        - `index()` — 数组越界
        - `notEmpty()` — 数组/字符串非空
        - `found()` — 查找必须成功，返回值
        - `unreachable()` — 穷尽性检查
        - … 另有 1 个方法
      - class `Logger`
        - `setLevel()` — 级别控制
        - `level()` — 最低**输出**级别。**低于此级别的只写缓冲，不输出到控制台**
        - `setModuleLevel()` — 级别控制
        - `clearModuleLevel()` — 取消某个模块的单独级别，回落到全局
        - `addSink()` — 添加输出目标，返回取消函数
        - `log()` — 写入一条日志
        - `trace()` — 操作
        - `debug()` — 操作
        - `info()` — 操作
        - `warn()` — 操作
        - … 另有 7 个方法
      - class `ModuleLogger`
        - `trace()` — 操作
        - `debug()` — 操作
        - `info()` — 操作
        - `warn()` — 操作
        - `error()` — 操作
      - type `LogEntry` — 类型定义
      - type `LoggerOptions` — 类型定义
      - const `LogLevel`
  - **scheduler**
    - 作用：统一时间源 —— 真暂停/慢动作/顿帧的唯一实现路径
    - 功能：提供 `Scheduler`、`TimeScale`
    - 前置依赖：_core  · 分层 L1
    - 接口：2 个类 / 34 个方法 / 3 个类型
      - class `Scheduler`
        - `update()` — 同样 3 秒冷却，有的快有的慢
        - `everyFrame()` — ✅
        - `everyFrameUnscaled()` — ❌
        - `delay()` — 延时，返回取消函数
        - `delayUnscaled()` — 延时，返回取消函数
        - `repeat()` — 定时重复
        - `repeatUnscaled()` — 定时重复（**不受 timeScale 影响**）
        - `pause()` — 暂停
        - `unpause()` — 暂停
        - `paused()` — 显式暂停（优先于所有层）
        - … 另有 9 个方法
      - class `TimeScale`
        - `add()` — 添加层（同 id 覆盖）
        - `remove()` — 查询与移除
        - `has()` — 查询与移除
        - `get()` — 查询与移除
        - `suspend()` — 挂起（保留但不生效）
        - `resume()` — 挂起（保留但不生效）
        - `pause()` — 暂停
        - `unpause()` — 暂停
        - `paused()` — 显式暂停（优先于所有层）
        - `update()` — 同样 3 秒冷却，有的快有的慢
        - … 另有 5 个方法
      - type `TickCallback` — 类型定义
      - type `SchedulerOptions` — 类型定义
      - type `TimeScaleOptions` — 类型定义
- **di**
  - 作用：轻量依赖注入容器（循环依赖检测 + fork）
  - 功能：提供 `DIContainer`、`createContainer`
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 15 个方法 / 2 个类型
    - class `DIContainer`
      - `register()` — 注册工厂。`opts.lifetime`: `singleton`(默认) / `transient` / `ove
      - `value()` — 注册常量
      - `instance()` — 注册常量
      - `disposable()` — 注册带 `destroy()` 的单例
      - `has()` — 是否已注册（**不会触发工厂**）
      - `get()` — 解析（**循环依赖会报错**；未注册会报错并给出"你是不是想找 xxx"）
      - `tryGet()` — 解析，未注册返回 `undefined` 而不是抛错
      - `fork()` — 派生子容器（**测试换依赖的标准做法**）
      - `keys()` — 已注册的 key 列表
      - `size()` — 注册项数量
      - … 另有 4 个方法
    - 导出函数
      - `createContainer()` — 添加
    - type `Lifetime` — 类型定义
    - type `RegisterOptions` — 类型定义
- **event-bus**
  - 作用：类型安全的发布订阅
  - 功能：提供 `EventBus`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 8 个方法 / 1 个类型
    - class `EventBus`
      - `listenerCount()` — 监听器总数（排查泄漏）
      - `on()` — 订阅，返回取消函数（幂等）
      - `once()` — 只触发一次
      - `emit()` — 发布（快照遍历 + 错误隔离）
      - `off()` — 移除该事件的所有监听器
      - `offAll()` — 移除全部
      - `has()` — 是否有监听器
      - `destroy()` — 清空
    - type `EventBusOptions` — 类型定义
- **input**
  - 作用：统一输入抽象：键鼠 / 触摸 / 手柄 →「动作名 → 状态」
  - 功能：提供 `InputBuffer`、`CoyoteTimer`、`InputManager`
  - 前置依赖：_core  · 分层 L1
  - 接口：3 个类 / 41 个方法 / 5 个类型 / 1 个常量
    - class `InputBuffer`
      - `window()` — InputBuffer —— 输入缓冲
      - `tick()` — 每帧推进
      - `press()` — 记录一次输入
      - `peek()` — 是否有待消费的该动作（**不消费**）
      - `consume()` — 消费该动作
      - `consumeAny()` — 消费任意动作（返回被消费的动作名）
      - `clear()` — 清空某个动作
      - `pendingCount()` — 待消费数量
      - `matchSequence()` — 检查最近的输入序列是否匹配
      - `consumeSequence()` — 消费一个搓招（匹配成功则清空相关输入）
      - … 另有 3 个方法
    - class `CoyoteTimer`
      - `setGrounded()` — 清理过期项 */
      - `tick()` — 每帧推进
      - `canJump()` — 现在能否跳
      - `consumeJump()` — 消费跳跃（消费后本次土狼时间作废）
      - `left()` — 剩余时间（调试）
    - class `InputManager`
      - `bindButton()` — 绑定按钮动作（可绑多个键）
      - `bindAxis()` — 绑定轴（四方向各一组键）
      - `rebind()` — 改键
      - `getBinding()` — 获取某动作当前的绑定
      - `exportBindings()` — 存档 / 读档
      - `importBindings()` — 存档 / 读档
      - `resetBindings()` — 恢复默认
      - `beginFrame()` — 手柄摇杆完全没反应
      - `endFrame()` — **按一次攻击连放好几次**
      - `setKeyDown()` — 按键状态
      - … 另有 13 个方法
    - type `BufferedInput` — 类型定义
    - type `InputBufferOptions` — 类型定义
    - type `KeyCode` — 类型定义
    - type `AxisBinding` — 类型定义
    - type `InputManagerOptions` — 类型定义
    - const `Key`
- **number-roller**
  - 作用：数字滚动核心逻辑（渲染层需自己写）
  - 功能：提供 `NumberRollerCore`
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 11 个方法 / 1 个类型
    - class `NumberRollerCore`
      - `set()` — 设置目标
      - `snapTo()` — 立即到位
      - `update()` — 每帧推进
      - `display()` — 当前显示值
      - `displayInt()` — 当前显示值
      - `target()` — 操作
      - `isRolling()` — 是否滚动中（**用于跳过 Label 赋值**）
      - `formatted()` — 格式化字符串（千分位/符号/小数）
      - `abbreviated()` — 缩写（1.2K / 3.4M / 2.5B）
      - `reset()` — 归零并停止滚动（不触发回调）
      - … 另有 1 个方法
    - type `NumberRollerOptions` — 类型定义
- **pool**
  - 作用：泛型对象池（三个注入钩子 + 泄漏追踪）
  - 功能：提供 `Pool`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 8 个方法 / 1 个类型
    - class `Pool`
      - `idle()` — 统计
      - `prewarm()` — 预热 n 个
      - `get()` — 取出（自动调用 onGet）
      - `put()` — 归还（自动调用 onPut）
      - `putAll()` — 归还（自动调用 onPut）
      - `clear()` — 清空空闲对象
      - `dump()` — 泄漏报告（需 trackLeaks）
      - `destroy()` — 清空
    - type `PoolOptions` — 类型定义
- **result**
  - 作用：显式错误处理（Result 类型）
  - 功能：提供 `Ok`、`Err`、`ok`、`err`
  - 前置依赖：无  · 分层 L1
  - 接口：2 个类 / 22 个方法 / 1 个类型
    - class `Ok`
      - `isOk()` — 判断（兼类型收窄）
      - `isErr()` — 判断（兼类型收窄）
      - `map()` — 变换
      - `mapErr()` — 变换
      - `flatMap()` — 变换
      - `unwrapOr()` — 安全取值
      - `unwrap()` — 抛异常
      - `match()` — 分支处理
    - class `Err`
      - `isOk()` — 判断（兼类型收窄）
      - `isErr()` — 判断（兼类型收窄）
      - `map()` — 变换
      - `mapErr()` — 变换
      - `flatMap()` — 变换
      - `unwrapOr()` — 安全取值
      - `unwrap()` — 抛异常
      - `match()` — 分支处理
    - 导出函数
      - `ok()` — 构造
      - `err()` — 构造
      - `attempt()` — 包装抛异常的函数
      - `all()` — 组合
      - `any()` — 组合
      - `fromNullable()` — 从可空值构造
    - type `Result` — 类型定义
- **rng**
  - 作用：可复现随机数 + 人类可读种子编码
  - 功能：提供 `RNG`、`encode`、`decode`、`random`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 16 个方法 / 2 个常量
    - class `RNG`
      - `state()` — 内部状态（存档用）
      - `next()` — [0,1)
      - `range()` — [min,max) 浮点
      - `rangeInt()` — [min,max] 整数
      - `int()` — [0,n) 整数
      - `chance()` — 概率
      - `bool()` — 概率
      - `pick()` — 取样
      - `shuffle()` — 原地洗牌（Fisher-Yates）
      - `sample()` — 取样
      - … 另有 2 个方法
    - 导出函数
      - `encode()` — 数字 → 'WORD-NN'
      - `decode()` — 'WORD-NN' → 数字（非法返回 null）
      - `random()` — 随机可读种子
      - `daily()` — 按日期生成固定种子（**所有玩家必须用同一时区偏移**）
    - const `CAPACITY`
    - const `Seed`
- **scheduling**
  - 作用：分帧调度器（按时间预算切分重活）
  - 功能：提供 `FrameScheduler`
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 11 个方法 / 3 个类型
    - class `FrameScheduler`
      - `budgetMs()` — 操作
      - `pendingCount()` — 状态查询
      - `isBusy()` — 状态查询
      - `schedule()` — 注册分步任务，返回 id。`step` 返回 true = 完成
      - `scheduleBatch()` — 批量处理（最常用）
      - `cancel()` — 取消
      - `cancelAll()` — 取消
      - `flush()` — 卡住好几秒（靠 guard 撑到 100 万次）
      - `update()` — 每帧驱动
      - `lastFrameMs()` — **本帧已用时间（毫秒）**（性能面板用）
      - … 另有 1 个方法
    - type `FrameSchedulerOptions` — 类型定义
    - type `TaskOptions` — 类型定义
    - type `StepContext` — 类型定义
- **signal**
  - 作用：轻量信号（类型安全、可持有）
  - 功能：提供 `Signal`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 6 个方法 / 1 个类型
    - class `Signal`
      - `add()` — 添加监听，返回取消函数
      - `once()` — 一次性监听
      - `emit()` — 触发
      - `listenerCount()` — 监听者数量（**排查泄漏**）
      - `clear()` — 移除所有监听
      - `destroy()` — 移除
    - type `Listener` — 类型定义

---

## 通用工具（12 个）

> 通用小件，按需取用

- 〔功能模块·开发工具〕2 个单元组合
  - **binary**
    - 作用：位级二进制序列化（schema 驱动，存档压缩 90%）
    - 功能：提供 `Schema`、`BitWriter`、`BitReader`、`RecordArray`
    - 前置依赖：无  · 分层 L1
    - 接口：4 个类 / 24 个方法 / 3 个类型
      - class `Schema`
        - `fixedBits()` — 定长部分的总位宽（**不含** `string` / `raw` 的变长部分）
        - `minBytes()` — 最少占用字节数
        - `encode()` — 对象 → `Uint8Array`。**缺的字段用 `def` 填**，不报错
        - `decode()` — `Uint8Array` → 对象
        - `describe()` — 人类可读的位布局（调试时看每个字段占几位）
      - class `BitWriter`
        - `writeBits()` — `readBits(bits)`
        - `toBytes()` — 已写入的总位数（调试用） */
        - `bitLength()` — `position` / `remainingBits`
      - class `BitReader`
        - `position()` — 操作
        - `remainingBits()` — 操作
        - `readBits()` — 操作
      - class `RecordArray`
        - `encode()` — 对象 → `Uint8Array`。**缺的字段用 `def` 填**，不报错
        - `decode()` — `Uint8Array` → 对象
      - 导出函数
        - `uint()` — `uint(12)`
        - `int()` — `int(9)`
        - `bool()` — `bool()`
        - `float()` — `float(-500,500,0.05)`
        - `enumeration()` — `enumeration(['a','b'],'a')`
        - `string()` — `string(64)`
        - `raw()` — `raw(16)`
        - `schema()` — 操作
        - `utf8Encode()` — UTF-8 编解码（`string()` 字段内部用）
        - `utf8Decode()` — UTF-8 编解码（`string()` 字段内部用）
        - … 另有 1 个方法
      - type `FieldDef` — 类型定义
      - type `SchemaFields` — 类型定义
      - type `SchemaOptions` — 类型定义
  - **snapshot**
    - 作用：快照/diff/补丁/撤销栈
    - 功能：提供 `UndoStack`、`deepClone`、`diffSnapshots`、`createPatch`
    - 前置依赖：_core  · 分层 L1
    - 接口：1 个类 / 13 个方法 / 4 个类型
      - class `UndoStack`
        - `push()` — 记录一个可回到的状态（**在改动之前调**）
        - `undo()` — 撤销，返回上一个状态（**不能撤销时返回 `undefined`**）
        - `redo()` — 重做，返回下一个状态（同理）
        - `canUndo()` — 是否可撤销 / 重做
        - `canRedo()` — 是否可撤销 / 重做
        - `undoDepth()` — 栈深度
        - `redoDepth()` — 栈深度
        - `clear()` — 清空两个栈
        - `destroy()` — 释放
      - 导出函数
        - `deepClone()` — 结构化深拷贝（**支持循环引用**）
        - `diffSnapshots()` — 差异比对
        - `createPatch()` — 生成补丁
        - `applyPatch()` — 应用补丁（不改 base）
      - type `DiffEntry` — 类型定义
      - type `DiffResult` — 类型定义
      - type `Patch` — 类型定义
      - type `UndoStackOptions` — 类型定义
- **condition**
  - 作用：条件引擎（成就/任务/解锁）
  - 功能：提供 `ConditionEngine`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 16 个方法 / 5 个类型
    - class `ConditionEngine`
      - `register()` — 注册定义
      - `unregister()` — 注销定义，返回是否成功
      - `setStat()` — 设置 / 累加统计项
      - `addStat()` — 设置 / 累加统计项
      - `getStat()` — 读单个统计项（不存在返回 `0`，不抛错）
      - `stats()` — 全部统计项的只读快照
      - `evaluate()` — 检查单个，返回 `{progress, completed}`
      - `check()` — 检查全部，**返回新完成的 id 数组**
      - `isCompleted()` — 是否已完成
      - `progress()` — 进度 0–1（UI 用）
      - … 另有 6 个方法
    - type `CompareOp` — 类型定义
    - type `LogicOp` — 类型定义
    - type `Condition` — 类型定义
    - type `ConditionDef` — 类型定义
    - type `ConditionResult` — 类型定义
- **curve**
  - 作用：关键帧曲线（线性/平滑/阶梯）
  - 功能：提供 `Curve`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 16 个方法 / 3 个类型
    - class `Curve`
      - `fromArray()` — Curve —— 关键帧曲线
      - `constant()` — 常数值曲线
      - `linear01()` — 线性 0→1
      - `addKey()` — 添加关键帧（自动排序）
      - `keyCount()` — 关键帧数量
      - `duration()` — 时间范围
      - `startTime()` — 时间范围
      - `endTime()` — 时间范围
      - `minValue()` — 值域
      - `maxValue()` — 值域
      - … 另有 6 个方法
    - type `InterpMode` — 类型定义
    - type `Keyframe` — 类型定义
    - type `TangentMode` — 类型定义
- **expression**
  - 作用：表达式求值器（配置里写公式，非 eval）
  - 功能：提供 `Expression`
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 7 个方法 / 1 个类型 / 1 个常量
    - class `Expression`
      - `maxDepth()` — Expression —— 表达式求值器
      - `source()` — 原始表达式
      - `evaluate()` — 求值。未定义变量当 0
      - `evaluateStrict()` — 严格求值，未定义变量抛错
      - `variables()` — 列出用到的变量名（**检查配置拼写**）
      - `collectVars()` — 操作
      - `toString()` — 转换
    - type `ExpressionOptions` — 类型定义
    - const `DEFAULT_MAX_DEPTH`
- **i18n**
  - 作用：本地化（复数/插值/回退/覆盖率）
  - 功能：复数
  - 功能：插值
  - 功能：回退
  - 功能：热重载
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 13 个方法 / 3 个类型
    - class `I18N`
      - `addLocale()` — 添加（**合并**到已有同语言包）
      - `setLocaleTable()` — 替换整个包（**热重载**）
      - `hasLocale()` — 该语言包是否已添加
      - `locales()` — 已添加的全部语言
      - `setLocale()` — 切换（不存在则保持，返回 false）
      - `locale()` — 当前语言
      - `onChange()` — 语言切换回调（**UI 要自己刷新**）
      - `t()` — 翻译
      - `has()` — 是否存在
      - `coverage()` — 覆盖率 `{total, translated, missing}`
      - … 另有 3 个方法
    - type `PluralForm` — 类型定义
    - type `LocaleTable` — 类型定义
    - type `I18NOptions` — 类型定义
- **noise**
  - 作用：确定性噪声（Value + FBM + Ridged）
  - 功能：提供 `PerlinNoise`、`SimplexNoise`、`ValueNoise`、`WorleyNoise`
  - 前置依赖：无  · 分层 L1
  - 接口：5 个类 / 16 个方法 / 1 个类型
    - class `PerlinNoise`
      - `noise2D()` — noise —— 程序化噪声（地形 / 纹理 / 随机分布）
      - `noise1D()` — 1D 噪声
    - class `SimplexNoise`
      - `noise2D()` — noise —— 程序化噪声（地形 / 纹理 / 随机分布）
      - `noise3D()` — 3D 噪声（用于体积雾、3D 地形）
    - class `ValueNoise`
      - `noise2D()` — noise —— 程序化噪声（地形 / 纹理 / 随机分布）
    - class `WorleyNoise`
      - `noise2D()` — noise —— 程序化噪声（地形 / 纹理 / 随机分布）
    - class `Noise`
      - `noise2()` — 原始 2D 噪声
      - `fbm()` — 分形噪声，值域 [-1, 1]
      - `fbm01()` — 分形噪声归一化到 [0, 1]
      - `ridged()` — 山脊噪声，值域 [0, 1]，偏向高值
      - `heightMap()` — 生成高度图
    - 导出函数
      - `fbm2D()` — 操作
      - `ridged2D()` — 操作
      - `generateHeightmap()` — 操作
      - `classifyHeightmap()` — 操作
      - `islandMask()` — 判断
    - type `FbmOptions` — 类型定义
- **observable**
  - 作用：UI 自动刷新的地基
  - 功能：提供 `Observable`、`Computed`、`Subscription`、`batch`
  - 前置依赖：无  · 分层 L1
  - 接口：3 个类 / 32 个方法 / 4 个类型
    - class `Observable`
      - `value()` — 操作
      - `trackRead()` — 操作
      - `enqueue()` — 添加
      - `peek()` — 当前正在求值的收集器栈（computed 可嵌套） */
      - `update()` — 函数式更新
      - `subscribe()` — 订阅变更
      - `once()` — 只订阅一次
      - `dispose()` — 操作
      - `listenerCount()` — 操作
      - `disposed()` — 操作
      - … 另有 2 个方法
    - class `Computed`
      - `onRead()` — observable/Observable.ts —— 响应式数据（UI 自动刷新的地基）
      - `value()` — 操作
      - `trackRead()` — 操作
      - `peek()` — 当前正在求值的收集器栈（computed 可嵌套） */
      - `subscribe()` — 订阅变更
      - `invalidate()` — 强制重算（下次读取时生效）
      - `dispose()` — 操作
      - `listenerCount()` — 操作
      - `onDepChange()` — 有订阅者时激活：真正订阅下层，才能收到冒泡 */
      - `_addDependent()` — 操作
      - … 另有 1 个方法
    - class `Subscription`
      - `size()` — 已加入数量
      - `closed()` — 是否已关闭
      - `add()` — 加入一个取消函数
      - `unsubscribeAll()` — 全部取消并关闭
    - 导出函数
      - `batch()` — 批内多次修改只通知一次
      - `flush()` — **立即**执行队列中的通知（通常由 `batch` 自动调）
      - `batchSize()` — 操作
      - `ref()` — 创建。**用工厂函数，不用 `new`**
      - `computed()` — 创建。**用工厂函数，不用 `new`**
    - type `Change` — 类型定义
    - type `Listener` — `(change: Change<T>) => void`
    - type `Unsubscribe` — `() => void`
    - type `RefOptions` — 类型定义
- **save**
  - 作用：存档（版本迁移 + 原子写 + 校验和）
  - 功能：提供 `MemoryStorage`、`SaveManager`
  - 前置依赖：无  · 分层 L1
  - 接口：2 个类 / 15 个方法 / 4 个类型
    - class `MemoryStorage`
      - `read()` — 读 + 自动迁移，返回 `{ok,value}` 或 `{ok:false,error}`
      - `write()` — 原子写，返回是否成功
      - `remove()` — 移除
      - `keys()` — 操作
      - `commit()` — SaveManager —— 存档读写（版本迁移 + 原子写 + 多槽位）
    - class `SaveManager`
      - `registerMigration()` — 注册迁移（**必须 to === from+1**）
      - `write()` — 原子写，返回是否成功
      - `read()` — 读 + 自动迁移，返回 `{ok,value}` 或 `{ok:false,error}`
      - `has()` — 槽位管理
      - `deleteSlot()` — 槽位管理
      - `listSlots()` — 列出所有槽位
      - `meta()` — 元信息（不迁移，存档界面用）。**`null` 有两种含义，见下**
      - `clearAll()` — **清空全部槽位**（登出、换账号、测试清理）
      - `version()` — 操作
      - `destroy()` — 释放迁移表（**换存档体系时调**）
    - type `IStorage` — 类型定义
    - type `SaveEnvelope` — 类型定义
    - type `SaveManagerOptions` — 类型定义
    - type `MigrationFn` — 类型定义
- **spatial**
  - 作用：空间哈希（范围查询加速）
  - 功能：提供 `SpatialHash`
  - 前置依赖：无  · 分层 L2
  - 接口：1 个类 / 11 个方法 / 1 个类型
    - class `SpatialHash`
      - `itemCount()` — 统计（调试用）
      - `cellCount()` — 统计（调试用）
      - `update()` — **"明明站在爆炸里却没受伤"**，极难定位
      - `insert()` — 插入
      - `remove()` — 移除
      - `clear()` — 清空全部条目**和格子**（`_cells.clear()` + `_items.clear()`）
      - `get()` — 查询
      - `queryCircle()` — 圆形范围查询
      - `queryRect()` — 矩形查询
      - `queryNearest()` — 最近的 K 个
      - … 另有 1 个方法
    - type `SpatialHashOptions` — 类型定义
- **timeutil**
  - 作用：时区 / 日界 / 体力周期 / 服务器时间校准 / 倒计时
  - 功能：提供 `ClockSync`、`Countdown`、`startOfDay`、`startOfNextDay`
  - 前置依赖：无  · 分层 L1
  - 接口：2 个类 / 26 个方法 / 2 个类型 / 2 个常量
    - class `ClockSync`
      - `synced()` — 是否已校准
      - `offset()` — 偏移量（服务器时间 − 本地时间）
      - `sync()` — **标准校准**，补偿半个 RTT
      - `syncSimple()` — 简化校准，**忽略 RTT**
      - `now()` — 估算的服务器时间
      - `detectClockJump()` — 本地时钟是否被大幅改动（默认容差 60 秒）
      - `reset()` — 重置为未同步（**重新登录时调**）
    - class `Countdown`
      - `durationMs()` — 总时长
      - `start()` — 从头开始
      - `startWith()` — **从剩余时间开始**（存档恢复用）
      - `pause()` — 暂停 / 恢复
      - `resume()` — 暂停 / 恢复
      - `remaining()` — 剩余毫秒（**永不为负**）
      - `progress()` — 进度 0..1
      - `isFinished()` — 是否结束
      - `state()` — `'waiting'` / `'running'` / `'finished'`
      - `reset()` — 重置为未同步（**重新登录时调**）
    - 导出函数
      - `startOfDay()` — 当天 00:00 的时间戳
      - `startOfNextDay()` — 次日 00:00（做**倒计时终点**用这个）
      - `dayIndex()` — 第几天（从 epoch 起）。存它，别存时间戳
      - `isNewDay()` — 跨天了吗。每日奖励、每日任务用它
      - `msUntilNextDay()` — 距次日 00:00 还有多少毫秒
      - `ticksSince()` — 离线一天回来直接满体力。用 `maxTicks`——这是设计决定不是优化
      - `monotonicNow()` — 单调时钟（不受系统时间回拨影响）。**算间隔用它**，不要用 `Date.now()`
      - `formatDuration()` — `01:23:45`；`showHoursAlways` 控制是否总是显示小时
      - `formatDurationCN()` — `1小时23分`；`maxUnits` 控制精度（默认 2 个单位）
    - type `Zone` — 时区偏移（`Zones.UTC` 等）
    - type `CountdownState` — `'waiting'` / `'running'` / `'finished'`（*
    - const `Zones`
    - const `DAY_MS`
- **tween**
  - 作用：补间动画（纯逻辑，不持有目标）
  - 功能：提供 `Tween`、`TweenRunner`
  - 前置依赖：_core  · 分层 L1
  - 接口：2 个类 / 21 个方法 / 1 个类型
    - class `Tween`
      - `delay()` — 延迟开始（**溢出会补偿，不丢时间**）
      - `ease()` — fn)`
      - `loop()` — `'none'` / `'repeat'` / `'pingpong'`
      - `onUpdate()` — 每帧回调（p=缓动后，raw=线性）
      - `onComplete()` — 完成回调（只触发一次）
      - `update()` — 每帧推进，自动清理完成的
      - `complete()` — 跳到终态并触发完成
      - `kill()` — 终止（不触发完成、不跳终态）
      - `done()` — 状态查询与复用
      - `killed()` — 是否被 `kill()` 终止过
      - … 另有 3 个方法
    - class `TweenRunner`
      - `add()` — 添加
      - `to()` — 添加
      - `delay()` — 延迟开始（**溢出会补偿，不丢时间**）
      - `update()` — 每帧推进，自动清理完成的
      - `count()` — 数量（**只增不减 = 泄漏**）
      - `killAll()` — 批量操作
      - `completeAll()` — 批量操作
      - `destroy()` — 释放
    - type `LoopMode` — 类型定义

---

## 算法与地图（7 个）

> 不认识你的游戏，只认数字和坐标

- 〔功能模块·AI 与地图〕5 个单元组合
  - **dungeon**
    - 作用：地牢生成（BSP / Room / Cellular / Maze 四种 + 死图自检）
    - 功能：提供 `DungeonBase`、`BSPDungeon`、`RoomDungeon`、`CellularDungeon`
    - 前置依赖：ds  · 分层 L2
    - 接口：5 个类 / 22 个方法 / 6 个类型 / 1 个常量
      - class `DungeonBase`
        - `width()` — 操作
        - `height()` — 操作
        - `rooms()` — 操作
        - `roomCount()` — 操作
        - `tileAt()` — 操作
        - `setTile()` — 设置
        - `isWalkable()` — 判断
        - `floorCount()` — 地板格子总数
        - `floodFill()` — 洪水填充，返回连通区域
        - `isFullyConnected()` — 所有地板是否连通成一个区域
        - … 另有 6 个方法
      - class `BSPDungeon`
        - `generate()` — 房间最小尺寸（含墙） */
      - class `RoomDungeon`
        - `generate()` — 房间最小尺寸（含墙） */
      - class `CellularDungeon`
        - `generate()` — 房间最小尺寸（含墙） */
      - class `MazeDungeon`
        - `generate()` — 房间最小尺寸（含墙） */
      - 导出函数
        - `rectCenter()` — 房间中心（**向下取整**，保证落在格子上）
        - `rectsOverlap()` — 两矩形是否重叠（**`padding` 会加在两侧**，见下）
      - type `Rect` — 类型定义
      - type `Room` — 类型定义
      - type `BSPOptions` — 类型定义
      - type `RoomDungeonOptions` — 类型定义
      - type `CellularOptions` — 类型定义
      - type `MazeOptions` — 类型定义
      - const `Tile`
  - **fov**
    - 作用：视野与战争迷雾（Shadowcasting + Raycasting）
    - 功能：提供 `VisibilityMap`、`Shadowcasting`、`Raycasting`、`makeWallTest`
    - 前置依赖：无  · 分层 L1
    - 接口：3 个类 / 18 个方法 / 1 个类型
      - class `VisibilityMap`
        - `inBounds()` — 操作
        - `set()` — 设置
        - `mark()` — 操作
        - `has()` — 判断
        - `clear()` — 移除
        - `count()` — fov —— 视野计算（战争迷雾）
        - `toArray()` — 导出为坐标数组
      - class `Shadowcasting`
        - `compute()` — 当前帧可见 */
        - `canSee()` — 递归投射一个八分体的一行
        - `hasExplored()` — 是否探索过
        - `isSymmetric()` — 对称性检查
        - `resetExplored()` — 重置探索记录（换关卡时）
        - `hasLineOfSight()` — 有视线（LOS）——简化的两点判定
        - `width()` — 操作
        - `height()` — 操作
      - class `Raycasting`
        - `compute()` — 当前帧可见 */
        - `canSee()` — 递归投射一个八分体的一行
      - 导出函数
        - `makeWallTest()` — 操作
      - type `IVec2` — 类型定义
  - **grid**
    - 作用：二维网格 + 建造放置 + 六边形坐标
    - 功能：提供 `Grid`、`GridPlacement`、`hex`、`hexAdd`
    - 前置依赖：_core  · 分层 L1
    - 接口：2 个类 / 38 个方法 / 4 个类型
      - class `Grid`
        - `index()` — Grid —— 二维网格 + 建造放置 + 六边形坐标
        - `inBounds()` — 操作
        - `get()` — 查询
        - `set()` — 设置
        - `setStrict()` — 强制设置（越界抛错，用于发现逻辑错误）
        - `clear()` — 清空为 undefined
        - `fillAll()` — 全部填充
        - `fill()` — 填充矩形区域
        - `neighbors4()` — 四邻居（存在的才返回）
        - `neighbors8()` — 八邻居
        - … 另有 5 个方法
      - class `GridPlacement`
        - `define()` — 操作
        - `canPlace()` — 占地宽（格） */
        - `place()` — 放置
        - `at()` — 某格被谁占用
        - `isFree()` — 某格是否空着
        - `remove()` — 拆除
        - `move()` — 移动到新位置（拖动已放置的建筑）
        - `get()` — 查询
        - `placedCount()` — 操作
        - `freeCount()` — 空闲格子数
        - … 另有 3 个方法
      - 导出函数
        - `hex()` — 操作
        - `hexAdd()` — 操作
        - `hexEquals()` — 操作
        - `hexNeighbors()` — 操作
        - `hexDistance()` — 操作
        - `hexRing()` — 操作
        - `hexSpiral()` — 操作
        - `hexToPixel()` — 操作
        - `pixelToHex()` — 操作
        - `hexRound()` — 操作
      - type `Cell` — 类型定义
      - type `PlacementDef` — 类型定义
      - type `Placed` — 类型定义
      - type `Hex` — 类型定义
  - **pathfind**
    - 作用：寻路（A* + PathSmoother + FlowField 流场）
    - 功能：提供 `AStar`、`PathSmoother`、`FlowField`
    - 前置依赖：ds  · 分层 L2
    - 接口：3 个类 / 18 个方法 / 4 个类型
      - class `AStar`
        - `width()` — 操作
        - `height()` — 操作
        - `inBounds()` — 操作
        - `at()` — pathfind —— A* 寻路 + 流场
        - `isWalkable()` — 判断
        - `find()` — 寻路
      - class `PathSmoother`
        - `hasLineOfSight()` — 用视线检测（LOS）去掉路径上的冗余拐点
        - `smooth()` — 平滑路径
      - class `FlowField`
        - `width()` — 操作
        - `height()` — 操作
        - `isBuilt()` — 判断
        - `build()` — 流场寻路
        - `distanceAt()` — 某格到目标的距离（-1 = 不可达）
        - `reachable()` — 是否可达
        - `hasNext()` — 是否有下一步（false = 已到达或不可达）
        - `directionAt()` — 下一步的位移
        - `tracePath()` — 从某点一路走到目标的完整路径（调试/可视化用）
        - `destroy()` — 移除
      - type `IPoint` — 类型定义
      - type `GridMap` — 类型定义
      - type `AStarOptions` — 类型定义
      - type `PathResult` — 类型定义
  - **steering**
    - 作用：转向行为（7 种单体 + Flock 群体 + 队形）
    - 功能：提供 `Flock`、`v2`、`createAgent`、`seek`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 16 个方法 / 4 个类型
      - class `Flock`
        - `compute()` — 分离：小于此距离则互相推开 */
      - 导出函数
        - `v2()` — 操作
        - `createAgent()` — 添加
        - `seek()` — 全速接近。**不要在到达时用**，会抖动
        - `flee()` — 逃离（带恐慌距离）
        - `arrive()` — **到达，带减速。日常用这个**
        - `pursuit()` — 追击，预判目标未来位置（抄近路拦截）
        - `evade()` — 预判版逃跑
        - `wander()` — 游荡（伪随机但平滑）
        - `obstacleAvoid()` — 反应式避障
        - `formationOffset()` — 转换
        - … 另有 5 个方法
      - type `IVec2` — 类型定义
      - type `Agent` — 类型定义
      - type `WanderState` — 类型定义
      - type `FlockOptions` — 类型定义
- 〔功能模块·肉鸽〕1 个单元组合
  - **room-graph**
    - 作用：房间图生成：分层DAG、路径不交叉、类型分配、路径热度分析
    - 功能：提供 `generateRoomGraph`、`assignTypes`、`diagnose`
    - 前置依赖：_core  · 分层 L2
    - 接口：0 个类 / 8 个方法 / 8 个类型 / 1 个常量
      - 导出函数
        - `generateRoomGraph()` — 操作
        - `assignTypes()` — 设置
        - `diagnose()` — 操作
        - `pathHeat()` — 操作
        - `pathLengths()` — 操作
        - `findPath()` — 查询
        - `findBestPath()` — 查询
        - `graphToString()` — 操作
      - type `RoomNode` — 类型定义
      - type `RoomGraphData` — 类型定义
      - type `LayerWidth` — 类型定义
      - type `RoomGraphOptions` — 类型定义
      - type `TypeRule` — 类型定义
      - type `TypeRuleContext` — 类型定义
      - type `TypeSpec` — 类型定义
      - type `Diagnosis` — 类型定义
      - const `RoomTypes`
- **pathfinding**
  - 作用：网格 A* + 路径平滑
  - 功能：提供 `GridGraph`、`findPath`、`smoothPath`
  - 前置依赖：无  · 分层 L2
  - 接口：1 个类 / 11 个方法 / 2 个类型
    - class `GridGraph`
      - `inBounds()` — 坐标是否在范围内
      - `isWalkable()` — 判断
      - `setWalkable()` — 设置可走性
      - `setWalkableRect()` — 设置可走性
      - `setCost()` — 地形代价（默认 1）
      - `getCost()` — 地形代价（默认 1）
      - `fillWalkable()` — 整体填充（**初始化时先全设 false 再挖房间**）
      - `walkableCount()` — 可走格子数（**自检用**：为 0 说明地图生成有问题）
      - `destroy()` — 移除
    - 导出函数
      - `findPath()` — **A*，找不到返回 `null`**
      - `smoothPath()` — 敌人沿格子边缘锯齿移动，很机械
    - type `GridPoint` — 类型定义
    - type `AStarOptions` — 类型定义

---

## 数值与战斗（16 个）

> 打得到、打得中、打得疼

- 〔功能模块·技能〕9 个单元组合
  - **attribute**
    - 作用：属性容器（基础值 + add/mul/override 修正器 + 缓存）
    - 功能：提供 `AttributeSet`、`summarizeModifiers`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 19 个方法 / 4 个类型
      - class `AttributeSet`
        - `define()` — AttributeSet —— 属性容器（基础值 + 修正器）
        - `has()` — 是否定义过
        - `ids()` — 全部属性 id
        - `setBase()` — 读写**基础值**（不含修正器）
        - `getBase()` — 读写**基础值**（不含修正器）
        - `add()` — 添加一个，返回**移除函数**
        - `addAll()` — 批量添加，返回**一次性移除全部**的函数
        - `removeBySource()` — 按来源移除
        - `clearByTag()` — 按标签移除（如 buff 全部到期）
        - `removeWhere()` — 按条件移除，返回移除数量
        - … 另有 8 个方法
      - 导出函数
        - `summarizeModifiers()` — 计算
      - type `AttrModifierType` — 类型定义
      - type `AttributeDef` — 类型定义
      - type `AttributeModifier` — 类型定义
      - type `AttributeChange` — 类型定义
  - **hitbox**
    - 作用：判定形状与重叠检测（circle/rect/sector/capsule + 空间哈希）
    - 功能：提供 `HitboxWorld`、`applyShapeOffset`、`hitboxCenter`、`boundingRadius`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 21 个方法 / 5 个类型
      - class `HitboxWorld`
        - `add()` — 增删
        - `remove()` — 增删
        - `update()` — 更新位置与朝向
        - `setEnabled()` — 启停单个判定框（**比反复 add/remove 便宜**）
        - `get()` — 取判定框
        - `count()` — 判定框总数（**调试用**：只增不减 = 实体死了没 `remove`）
        - `query()` — 查询：给定形状与掩码，返回所有命中的判定框
        - `applyShapeOffset()` — 把 `offset` 按朝向旋转后加到位置上，返回实际中心
        - `lineOfSightBlocked()` — 两点之间是否被挡住（视线 / 弹道遮挡）
        - `clear()` — 清空 / 销毁
        - … 另有 1 个方法
      - 导出函数
        - `applyShapeOffset()` — 把 `offset` 按朝向旋转后加到位置上，返回实际中心
        - `hitboxCenter()` — 取判定框的实际中心（`out` 可选，复用对象避免分配）
        - `boundingRadius()` — 外接圆半径（**粗筛用**：先比距离再算精确相交，快很多）
        - `samplePoints()` — 取形状的采样点（调试画线、自定义判定用）
        - `containsPoint()` — 点是否在形状内
        - `shapesOverlap()` — 两个形状是否重叠（**含旋转**，检测逻辑本体）
        - `circle()` — radius
        - `rect()` — halfW / halfH
        - `sector()` — radius + angleDeg
        - `capsule()` — radius + height
      - type `ShapeKind` — 类型定义
      - type `Shape` — 类型定义
      - type `Hitbox` — 类型定义
      - type `HitResult` — 类型定义
      - type `HitboxWorldOptions` — 类型定义
  - **indicator**
    - 作用：技能指示器（只算参数不画，与判定共用形状）
    - 功能：提供 `SkillIndicator`、`toQueryArgs`、`inRing`、`snapAngle`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 7 个方法 / 7 个类型
      - class `SkillIndicator`
        - `configure()` — 运行期改配置（**换技能**时用）
        - `config()` — 读当前配置（只读）
        - `compute()` — 计算指示器参数，返回 `IndicatorResult`
        - `centerFor()` — 把配置转成判定形状 */
      - 导出函数
        - `toQueryArgs()` — 把结果转成 `hitbox` 的 `query()` 参数（**省得手写四个参数**）
        - `inRing()` — 环形二次过滤（`ring` 指示器必配，见下）
        - `snapAngle()` — 朝向吸附
      - type `IndicatorKind` — 类型定义
      - type `IndicatorShape` — 类型定义
      - type `IndicatorConfig` — 类型定义
      - type `ISnapTarget` — `{ x, y, radius?, id? }`
      - type `ITargetProvider` — `findNearest(x, y, r)`
      - type `IPlacementValidator` — `isValid(x, y, shape)`
      - type `IndicatorResult` — 类型定义
  - **projectile**
    - 作用：弹道系统（5 种运动模式，碰撞通过接口注入）
    - 功能：提供 `ProjectileSystem`、`createHitboxSweepProvider`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 8 个方法 / 7 个类型
      - class `ProjectileSystem`
        - `count()` — 存活数量（**调试用**：一直涨说明忘了 `kill`）
        - `all()` — 取全部（只读）
        - `spawn()` — 发射一枚
        - `tick()` — 推进并做扫掠检测（`provider` 见上）
        - `kill()` — 手动销毁一枚（**打中后要调**，否则它继续飞）
        - `clear()` — 清空 / 销毁
        - `destroy()` — 清空 / 销毁
      - 导出函数
        - `createHitboxSweepProvider()` — 添加
      - type `CollisionHit` — 类型定义
      - type `ICollisionProvider` — 类型定义
      - type `ProjectileMode` — 类型定义
      - type `ProjectileSpawn` — 类型定义
      - type `ExpireReason` — 类型定义
      - type `Projectile` — 类型定义
      - type `ProjectileSystemOptions` — 类型定义
  - **skill-caster**
    - 作用：技能释放编排器（★用户点名，全部能力注入，零插件依赖）
    - 功能：提供 `SkillCaster`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 16 个方法 / 17 个类型
      - class `SkillCaster`
        - `learn()` — 学会一个技能（重复 learn 同 id 会覆盖并重置充能）
        - `forget()` — **连定义一起删**（源码 `_skills.delete`）。之后 `has(id)` 为 false
        - `getDef()` — 取技能定义
        - `has()` — 是否已学会
        - `skillIds()` — 已注册的全部技能 id
        - `cooldownLeft()` — 剩余 CD 秒数
        - `cooldownRatio()` — 剩余 CD **比例** 0..1（画扇形遮罩直接用）
        - `resetCooldown()` — 只把 CD 归零，**保留定义**。不传 id = 全部重置（换关卡时用这个）
        - `chargesLeft()` — 剩余充能层数
        - `busy()` — 当前是否正在施法
        - … 另有 6 个方法
      - type `SkillShape` — 类型定义
      - type `CasterHit` — 类型定义
      - type `IHitboxProvider` — 类型定义
      - type `ICasterProjectile` — 类型定义
      - type `IProjectileProvider` — 类型定义
      - type `ICasterDamage` — 类型定义
      - type `IDamageProvider` — 类型定义
      - type `IResourceProvider` — 类型定义
      - … 另有 9 个类型
  - **skill-player**
    - 作用：时间轴驱动的技能播放器 + 冷却
    - 功能：提供 `Cooldown`、`SkillHandle`、`SkillPlayer`、`Track`
    - 前置依赖：_core  · 分层 L2
    - 接口：4 个类 / 38 个方法 / 8 个类型 / 1 个常量
      - class `Cooldown`
        - `ready()` — 查询
        - `charges()` — 查询
        - `progress()` — 归一化进度
        - `remaining()` — 查询
        - `trigger()` — 消耗一个充能，返回是否成功
        - `tick()` — 每帧推进（dt 需缩放）
        - `reset()` — 立即充满
        - `applyReduction()` — 冷却缩减的正确公式
        - `normalized()` — 归一化进度 0~1（**1 = 就绪**）
        - `destroy()` — 清空
      - class `SkillHandle`
        - `cancelled()` — 是否已被取消
        - `cancel()` — 取消这次播放
        - `pause()` — 控制
        - `resume()` — 控制
        - `time()` — 已播时间
        - `progress()` — 归一化进度
      - class `SkillPlayer`
        - `register()` — 注册事件处理器
        - `unregister()` — 反注册事件处理器
        - `play()` — 播放，返回 handle
        - `tick()` — 每帧推进（dt 需缩放）
        - `seek()` — 跳转（编辑器预览）
        - `pause()` — 控制
        - `resume()` — 控制
        - `stop()` — 控制
        - `state()` — 当前状态（`idle` / `playing` / `paused`）
        - `currentTime()` — 当前播放位置（秒）
        - … 另有 4 个方法
      - class `Track`
        - `from()` — Track —— 技能时间轴（纯数据 + 求值）
        - `toJSON()` — 序列化
        - `fromJSON()` — 序列化
        - `validate()` — 校验：返回错误列表（空数组 = 合法）
        - `query()` — 区间查询（out 可复用，零分配）
        - `events()` — 所有事件（只读）
        - `frameToTime()` — 从帧号换算时间（美术标注用）
        - `progress()` — 归一化进度
      - type `CooldownOptions` — 类型定义
      - type `EventHandler` — 类型定义
      - type `SkillState` — 类型定义
      - type `SkillPlayerOptions` — 类型定义
      - type `TrackEventType` — 类型定义
      - type `TrackEvent` — 类型定义
      - type `TrackData` — 类型定义
      - type `TrackSchema` — 类型定义
      - const `TRACK_SCHEMA_VERSION`
  - **skill-queue**
    - 作用：技能排队与 CD 补发：提前点击自动延后释放 / 失败分类 / 窗口自检
    - 功能：提供 `SkillQueue`、`defaultRetryable`、`suggestWindow`
    - 前置依赖：_core、skill-caster  · 分层 L3
    - 接口：1 个类 / 13 个方法 / 8 个类型
      - class `SkillQueue`
        - `window()` — 失败原因（与 CastResult.reason 对齐） */
        - `capacity()` — 操作
        - `stats()` — 统计：`requested / cast / rejected / nearMiss / maxWaited`
        - `request()` — 请求释放。**ctx 强烈建议传函数**
        - `tick()` — 每帧推进。**必须在 `caster.tick()` 之后**；暂停传 0
        - `poll()` — 从输入源拉取（固化 peek→request→consume）
        - `pending()` — 待释放请求（按优先级排序，带 `waited`）
        - `has()` — 数量 / 是否排队中
        - `count()` — 数量 / 是否排队中
        - `clear()` — 清空（换关卡、死亡、被沉默）
        - … 另有 1 个方法
      - 导出函数
        - `defaultRetryable()` — 操作
        - `suggestWindow()` — 操作
      - type `IQueuedCaster` — 类型定义
      - type `IInputSource` — 类型定义
      - type `QueueFailReason` — 类型定义
      - type `CastContextLike` — 类型定义
      - type `QueuedCast` — 类型定义
      - type `RejectReason` — 类型定义
      - type `SkillQueueHooks` — 类型定义
      - type `SkillQueueOptions` — 类型定义
  - **skill-variant**
    - 作用：技能变体：补丁+条件+深拷贝，遗物改造技能（★用户点名「技能模块」的扩展）
    - 功能：提供 `SkillVariantSystem`、`applyPatch`、`deepClone`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 9 个方法 / 6 个类型
      - class `SkillVariantSystem`
        - `register()` — 注册变体定义
        - `registerAll()` — 注册变体定义
        - `unregister()` — 反注册，返回是否真的删掉了
        - `get()` — 查询
        - `all()` — 操作
        - `apply()` — 应用变体，返回新对象（**深拷贝，不改原配置**）
        - `preview()` — **预览**效果，不真正应用（调试/GM 指令用）
      - 导出函数
        - `applyPatch()` — 设置
        - `deepClone()` — 操作
      - type `PatchOp` — 类型定义
      - type `Patch` — 类型定义
      - type `VariantCondition` — 类型定义
      - type `VariantDef` — 类型定义
      - type `SkillVariantOptions` — 类型定义
      - type `VariantContext` — 类型定义
  - **telegraph**
    - 作用：攻击预警（windup/active/recover，判定位置在 active 锁定）
    - 功能：提供 `TelegraphSystem`、`phaseDuration`、`telegraphProgress`、`isDangerous`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 11 个方法 / 4 个类型
      - class `TelegraphSystem`
        - `count()` — 当前活跃数量（**调试用**：应该会回落，一直涨说明泄漏）
        - `all()` — 取全部（只读）
        - `spawn()` — 创建一个预警，返回 `Telegraph`
        - `tick()` — 推进（**dt 必须来自统一时间源，暂停时传 0**）
        - `cancel()` — 取消单个
        - `cancelAll()` — 取消全部
        - `clear()` — 清空（**换关卡时必须调**，见下）
        - `destroy()` — 释放（含清空所有预警）
      - 导出函数
        - `phaseDuration()` — 当前阶段的总时长（`windup`/`active`/`recover` 各自的秒数）
        - `telegraphProgress()` — 0..1 进度，**渲染层画填充动画用这个**（见上）
        - `isDangerous()` — 是否已进入危险期（蓄力过 70%）
      - type `TelegraphShape` — 类型定义
      - type `TelegraphConfig` — 类型定义
      - type `TelegraphPhase` — 类型定义
      - type `Telegraph` — **单个**攻击的预警与判定窗口
- 〔功能模块·战斗〕6 个单元组合
  - **attack-token**
    - 作用：攻击令牌：限制同时攻击数，防围殴秒杀 + 超时兜底
    - 功能：敌人轮流上，玩家能逐个格挡 / 闪避
    - 功能：视觉上变成「围而不攻」的包抄圈，压迫感反而更强
    - 功能：玩家能读懂「谁要打我了」
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 17 个方法 / 5 个类型
      - class `AttackTokenSystem`
        - `activeCount()` — 持有数量
        - `queuedCount()` — 排队数量
        - `maxConcurrent()` — 1 = 回合感强（只狼式决斗）；**2 = 推荐**；3 = 混乱（只有杂兵海合适）；≥4 基本失去意义
        - `hasToken()` — 是否已拿到令牌（**正在攻击中**）
        - `isQueued()` — 是否在排队
        - `requestState()` — `'queued'` / `'holding'` / `null`（**没在等也没拿到**）
        - `activeIds()` — 当前持有令牌的实体 id 列表
        - `freeSlots()` — 剩余可用令牌数（**调试用**：长期为 0 说明有泄漏）
        - `request()` — 申请 / 释放令牌
        - `release()` — 申请 / 释放令牌
        - … 另有 7 个方法
      - type `TokenRequestState` — 类型定义
      - type `TokenRequest` — 类型定义
      - type `ReleaseReason` — 类型定义
      - type `AttackTokenOptions` — 类型定义
      - type `TokenStats` — 类型定义
  - **buff**
    - 作用：Buff 生命周期（叠层/tick/驱散/存档）
    - 功能：提供 `BuffSystem`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 17 个方法 / 5 个类型
      - class `BuffSystem`
        - `register()` — 注册定义
        - `has()` — 查询
        - `stacks()` — 查询
        - `remain()` — 查询
        - `active()` — 所有生效中的 buff
        - `count()` — 当前生效的 buff **种类数**（不是总层数）
        - `apply()` — 应用，返回实际层数
        - `remove()` — 移除（默认全移除）
        - `removeByTag()` — 批量驱散（跳过 `dispellable: false`）
        - `dispelAll()` — 批量驱散（跳过 `dispellable: false`）
        - … 另有 7 个方法
      - type `StackMode` — 类型定义
      - type `BuffDef` — 类型定义
      - type `BuffInstance` — 类型定义
      - type `BuffChangeKind` — 类型定义
      - type `BuffChange` — 类型定义
  - **combo**
    - 作用：连招输入：前缀等待/窗口超时结算/优先级/条件
    - 功能：提供 `ComboSystem`、`validateCombos`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 9 个方法 / 5 个类型
      - class `ComboSystem`
        - `moveCount()` — 已配置的招式数
        - `buffered()` — combo/ComboSystem.ts —— 连招（输入序列 → 招式）
        - `pending()` — 是否正在等待后续输入
        - `input()` — 输入一个键，返回**已成立**的招式（没成立返回 null）
        - `tick()` — 每帧推进，返回**因超时而结算**的招式
        - `cancel()` — 取消当前连招（被打断、切武器时）
        - `reset()` — 重置状态
        - `bufferedInput()` — 当前缓冲的输入（**调试用**：看连招卡在哪一步）
      - 导出函数
        - `validateCombos()` — 判断
      - type `MoveDef` — 类型定义
      - type `ComboContext` — 类型定义
      - type `MoveTriggered` — 类型定义
      - type `ComboSystemOptions` — 类型定义
      - type `ComboValidation` — 类型定义
  - **damage-pipeline**
    - 作用：分阶段伤害计算 + 数值修正框架 + 伤害契约
    - 功能：提供 `DamagePipeline`、`ModifierSet`
    - 前置依赖：_core  · 分层 L2
    - 接口：2 个类 / 18 个方法 / 7 个类型
      - class `DamagePipeline`
        - `addStage()` — 插入阶段（order 小的先执行）
        - `removeStage()` — 移除
        - `calculate()` — **纯计算**，返回 DamageResult
        - `apply()` — 计算 + 写入 + 通知
        - `simulate()` — 批量模拟（无副作用）
        - `onResult()` — 订阅结果（返回取消函数）
        - `destroy()` — 清空
        - `createDefault()` — 创建一条标准管线
      - class `ModifierSet`
        - `add()` — 添加一个，返回**移除函数**
        - `remove()` — 按 id 移除
        - `removeBySource()` — 按 source 移除，返回移除数量（**换装备用这个**）
        - `clearByTag()` — 按标签批量移除（「移除全部 debuff」）
        - `clearTagEverywhere()` — 跨所有属性按标签移除
        - `get()` — 取该属性的最终值
        - `count()` — 修正器条数
        - `dump()` — 取全部条目（**调试面板用**，看每个加成的来源）
        - `clear()` — 清空（不传 attr = 全清）
        - `destroy()` — 清空
      - type `DamageStage` — 类型定义
      - type `DamagePipelineOptions` — 类型定义
      - type `DamageContext` — 类型定义
      - type `DamageResult` — 类型定义
      - type `IDamageable` — 类型定义
      - type `ModifierType` — 类型定义
      - type `ModifierEntry` — 类型定义
  - **element**
    - 作用：元素反应：附着/衰减/反应/配置校验（效果由data携带）
    - 功能：提供 `ElementSystem`、`validateReactions`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 11 个方法 / 5 个类型
      - class `ElementSystem`
        - `elementOf()` — 当前附着的元素
        - `gaugeOf()` — 附着量（**元素量表**）
        - `ratioOf()` — 附着量比例 0..1（**UI 显示元素图标的填充**）
        - `peek()` — 取完整状态（元素 + 量表，不修改）
        - `apply()` — 附着元素，**返回反应结果**（没触发反应时 `null`）
        - `clear()` — 清除单个目标的元素
        - `set()` — 直接设置（**测试与 GM 指令用**，正常流程走 `apply`）
        - `clearAll()` — 清除全部（**换关卡调**）
        - `tick()` — 推进衰减
        - `activeCount()` — 有元素附着的目标数
      - 导出函数
        - `validateReactions()` — 判断
      - type `ElementDef` — 类型定义
      - type `ReactionDef` — 类型定义
      - type `ReactionResult` — 类型定义
      - type `ElementSystemOptions` — 类型定义
      - type `ElementValidation` — 类型定义
  - **targeting**
    - 作用：目标锁定：多评分器 + 迟滞 + 粘性 + 宽限期
    - 功能：玩家想打正前方的怪，系统却锁了身后 1 米处的 → 角色突然回头，玩家完全懵掉
    - 功能：两个怪距离几乎相同时，锁定在两者间**疯狂闪烁** → 角色来回扭头
    - 功能：目标短暂被柱子遮挡一帧，锁定就丢失 → 闪烁
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 14 个方法 / 5 个类型
      - class `TargetSelector`
        - `current()` — 操作
        - `mode()` — 操作
        - `setMode()` — 设置
        - `addScorer()` — targeting/TargetSelector.ts —— 目标锁定与切换
        - `clearScorers()` — 移除
        - `update()` — 每帧更新锁定目标
        - `lock()` — ② 在全部候选中找最优
        - `unlock()` — 解除锁定
        - `cycle()` — 按方向切换目标
        - `aimAngle()` — 在 hard 模式下，获取朝向目标的角度
      - 导出函数
        - `byDistance()` — 1
        - `byAngle()` — 1
        - `byStickiness()` — **10**
        - `byWeight()` — 操作
      - type `TargetCandidate` — 类型定义
      - type `TargetScorer` — 类型定义
      - type `TargetingContext` — 类型定义
      - type `TargetSelectorOptions` — 类型定义
      - type `LockMode` — 类型定义
- **bullet-pattern**
  - 作用：弹幕发射器：环形/螺旋/扇形/镜像 + 序列编排
  - 功能：形状
  - 功能：编排
  - 前置依赖：_core  · 分层 L2
  - 接口：2 个类 / 21 个方法 / 6 个类型 / 1 个常量
    - class `BulletPattern`
      - `addEmitter()` — 添加一个发射器
      - `setTypeId()` — 运行中换弹型（**换阶段改弹幕外观**）
      - `play()` — 整体启停
      - `stop()` — 整体启停
      - `playing()` — 整体启停
      - `reset()` — 全部重置
      - `setEmitterActive()` — 单独启用/停用某个发射器
      - `restartEmitter()` — 重置并重新开始某个发射器
      - `isEmitterDone()` — 该发射器是否已打完
      - `allDone()` — **全部**发射器是否已打完（波次结算用）
      - … 另有 1 个方法
    - class `SequencePlayer`
      - `start()` — 启停
      - `stop()` — 整体启停
      - `running()` — 是否在播 / 是否播完
      - `finished()` — 是否在播 / 是否播完
      - `tick()` — 推进（**整个项目只调一次**）
    - 导出函数
      - `compileShape()` — 形状编译成函数（自定义形状时用）
      - `speedAt()` — 取第 index 发的速度（`speed` 可以是数组，做「逐发加速」）
      - `angleToVec()` — 操作
      - `normalizeAngle2Pi()` — 归一化到 `[0, 2π)`
      - `lerpAngle()` — 角度插值，**走最短弧**（不会绕远）
    - type `BulletSpawn` — 类型定义
    - type `ShapeContext` — 类型定义
    - type `ShapeFn` — 类型定义
    - type `ShapeSpec` — 类型定义
    - type `EmitterOptions` — 类型定义
    - type `SequenceStep` — 类型定义
    - const `Shapes`

---

## 移动与碰撞（4 个）

> 从「方向」到「角色真的动起来」

- **collision**
  - 作用：碰撞检测：SAT / 扫掠 / moveAndSlide / 射线（防高速穿墙）
  - 功能：提供 `CollisionGrid`、`circle`、`aabb`、`obb`
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 31 个方法 / 13 个类型 / 1 个常量
    - class `CollisionGrid`
      - `nextId()` — 操作
      - `clear()` — 移除
      - `insert()` — 添加
      - `query()` — collision/Collision.ts —— 碰撞检测与解析（物理阻挡层）
      - `bucketCount()` — 操作
    - 导出函数
      - `circle()` — 操作
      - `aabb()` — 操作
      - `obb()` — 操作
      - `capsule()` — 操作
      - `aabbFromSize()` — 操作
      - `closestPointOnSegment()` — 点到线段的最近点（**写入 out，避免分配**）
      - `distSqPointSegment()` — 点到线段的**距离平方**
      - `shapeCenter()` — 形状中心（排序、粗筛用）
      - `boundingRadius()` — 包围圆半径（**粗筛用**）
      - `circleCircle()` — 圆 vs 圆
      - … 另有 16 个方法
    - type `ShapeKind` — 类型定义
    - type `CircleShape` — `kind:'circle'`, `x`, `y`, `r`
    - type `AabbShape` — `kind:'aabb'`, `x`, `y`, `hw`, `hh`
    - type `ObbShape` — `kind:'obb'`, `x`, `y`, `hw`, `hh`, `rot`（
    - type `CapsuleShape` — `kind:'capsule'`, `x0`,`y0`,`x1`,`y1`, `r`
    - type `Shape` — 类型定义
    - type `PolygonShape` — `kind:'polygon'`, `x`, `y`, `rot`, `pts`（*
    - type `AnyShape` — 类型定义
    - … 另有 5 个类型
    - const `LAYER`
- **dash**
  - 作用：冲刺（衰减曲线 + 无敌帧 + 取消后摇）
  - 功能：提供 `DashController`、`shouldSpawnGhost`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 20 个方法 / 2 个类型
    - class `DashController`
      - `state()` — 状态（`idle` / `dashing` / `cooldown` …）
      - `active()` — 是否在冲刺中
      - `deltaX()` — 本帧位移
      - `deltaY()` — 本帧位移
      - `invulnerable()` — 当前是否无敌（**按 `invulnerableRatio` 算**）
      - `progress()` — 0..1 进度
      - `cooldownLeft()` — 剩余 CD / 剩余充能
      - `chargesLeft()` — 剩余 CD / 剩余充能
      - `ready()` — 现在能不能冲
      - `dirX()` — 冲刺方向（残影/特效用）
      - … 另有 9 个方法
    - 导出函数
      - `shouldSpawnGhost()` — 判断
    - type `DashOptions` — 类型定义
    - type `DashState` — 类型定义
- **joystick-mover**
  - 作用：轮盘移动（Core 纯逻辑 + Mover 为 Cocos 薄适配层）
  - 功能：提供 `JoystickCore`、`JoystickMover`
  - 前置依赖：_core  · 分层 L3
  - 接口：2 个类 / 14 个方法 / 3 个类型
    - class `JoystickCore`
      - `onDown()` — 按下，返回是否接受（多点触摸会拒绝第二个）
      - `onMove()` — 移动
      - `onUp()` — 抬起（自动归零）
      - `reset()` — 清空状态（**换关卡 / 复用组件时用**）
      - `evaluate()` — 计算输出（返回复用对象，零 GC）
      - `center()` — UI 定位用
      - `knob()` — UI 定位用
      - `isActive()` — 是否有触点
      - `setAxis()` — 外部驱动（键盘/手柄）
      - `destroy()` — 重置
    - class `JoystickMover`
      - `output()` — 当前输出（`JoystickOutput \
      - `direction()` — 操作
      - `onLoad()` — Cocos 生命周期，**由引擎调用，不要手动调**
      - `onDestroy()` — Cocos 生命周期，**由引擎调用，不要手动调**
    - type `JoystickMode` — 类型定义
    - type `JoystickOptions` — 类型定义
    - type `JoystickOutput` — 类型定义
- **mover**
  - 作用：角色移动：加速度 / 击退 / 撞墙投影 / 手感预设
  - 功能：提供 `CharacterMover`、`clampSpeed`、`lerpVelocity`、`stoppingTime`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 16 个方法 / 3 个类型 / 2 个常量
    - class `CharacterMover`
      - `addImpulse()` — 施加外力（击退、爆炸、传送带）
      - `clearImpulse()` — 清空外力与击退态
      - `externalSpeed()` — 外力大小（**诊断用**）
      - `hasExternal()` — 是否有明显外力在作用
      - `speed()` — 不含外力 / 含外力的速度大小
      - `totalSpeed()` — 不含外力 / 含外力的速度大小
      - `facing()` — 朝向（弧度；静止时保持上一次）
      - `update()` — 主循环（⚠️ 缩放时间）
      - `teleport()` — 瞬移并清空速度
      - `stop()` — 只清速度，不动位置
      - … 另有 3 个方法
    - 导出函数
      - `clampSpeed()` — 把速度限制在 `max` 内，**按比例缩放**
      - `lerpVelocity()` — 速度朝目标插值（进/出载具的过渡）
      - `stoppingTime()` — 从当前速度刹停还需多久（AI 预判、刹车距离 UI）
    - type `IMoveSolver` — 类型定义
    - type `MoveOutcome` — 类型定义
    - type `MoverConfig` — 类型定义
    - const `NullSolver`
    - const `MoverPresets`

---

## AI与感知（3 个）

> 敌人怎么发现你、怎么决策

- 〔功能模块·AI 与地图〕2 个单元组合
  - **behavior-tree**
    - 作用：行为树（组合/装饰/叶子节点 + 黑板）
    - 功能：提供 `Selector`、`Sequence`、`Parallel`、`Inverter`
    - 前置依赖：_core  · 分层 L2
    - 接口：11 个类 / 44 个方法 / 4 个类型 / 1 个常量
      - class `Selector`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `runningIndex()` — 当前 running 节点的位置
        - `destroy()` — 递归销毁所有节点
      - class `Sequence`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `runningIndex()` — 当前 running 节点的位置
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Parallel`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Inverter`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Succeeder`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Repeater`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `CooldownDecorator`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `remain()` — 已用 / 剩余时间（`Timeout`/`Wait` 节点用）
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Condition`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Action`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `destroy()` — 递归销毁所有节点
      - class `Wait`
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `elapsed()` — 已用 / 剩余时间（`Timeout`/`Wait` 节点用）
        - `destroy()` — 递归销毁所有节点
      - class `BehaviorTree`
        - `root()` — 操作
        - `lastStatus()` — 状态与统计
        - `tickCount()` — 状态与统计
        - `tick()` — 高刷屏上 AI 反应快一倍
        - `reset()` — 重置树（切换 AI 类型、复活时）
        - `clearBlackboard()` — 清空黑板
        - `recordNode()` — 调试追踪
        - `getNodeStatus()` — 按名字取节点状态（**调试面板用**）
        - `trackedNodes()` — 调试追踪
        - `destroy()` — 递归销毁所有节点
      - type `Blackboard` — 类型定义
      - type `IBTNode` — 类型定义
      - type `BTChild` — 类型定义
      - type `BehaviorTreeOptions` — 类型定义
      - const `BTStatus`
  - **fsm**
    - 作用：有限状态机（显式转换 + 不可达检测）
    - 功能：提供 `StateMachine`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 9 个方法 / 2 个类型
      - class `StateMachine`
        - `start()` — 触发初始状态的 enter
        - `current()` — 当前状态 / 停留时长
        - `timeInState()` — 当前状态 / 停留时长
        - `can()` — 能否转换
        - `transitionTo()` — 强制转换，受 transitions 约束
        - `update()` — 每帧推进，update 返回状态名则转换
        - `findUnreachable()` — **返回不可达状态列表**
        - `reset()` — 回到初始状态
        - `destroy()` — 释放（**之后 onChange 不再触发**）
      - type `StateHooks` — 类型定义
      - type `StateMachineOptions` — 类型定义
- 〔功能模块·战斗〕1 个单元组合
  - **perception**
    - 作用：感知系统：视觉/听觉/记忆/警觉度/同伴延迟警报
    - 功能：隔着墙也能「看见」你
    - 功能：从背后走过去它也能立刻察觉
    - 功能：一旦进入范围就永久锁定，躲到天涯海角也甩不掉
    - 功能：一个怪发现你，全场怪**瞬间**都知道
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 18 个方法 / 7 个类型 / 2 个常量
      - class `PerceptionSystem`
        - `addPerceiver()` — 无障碍视线（测试 / 开阔场地用） */
        - `addTarget()` — 注册/更新一个可被感知的目标（玩家、诱饵）
        - `removeTarget()` — 移除
        - `removePerceiver()` — 移除
        - `setPosition()` — 更新位置
        - `stateOf()` — 操作
        - `isAware()` — 判断
        - `targetOf()` — 操作
        - `alertOf()` — 操作
        - `lastKnownPosition()` — 最后已知位置（用于"去最后看到的地方搜索"）
        - … 另有 7 个方法
      - 导出函数
        - `soundFalloff()` — 操作
      - type `StimulusType` — 类型定义
      - type `Stimulus` — 类型定义
      - type `PerceiverConfig` — 类型定义
      - type `PerceptionState` — 类型定义
      - type `PerceptionEvent` — 类型定义
      - type `ILineOfSight` — 类型定义
      - type `PerceptionSystemOptions` — 类型定义
      - const `OpenLineOfSight`
      - const `PerceptionPresets`

---

## 内容与掉落（9 个）

> 打完之后掉什么、掉多少

- 〔功能模块·肉鸽〕4 个单元组合
  - **affix**
    - 作用：词条系统：roll、冲突组、槽位限制、聚合、配置校验
    - 功能：提供 `AffixSystem`、`validateAffixPool`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 9 个方法 / 9 个类型 / 1 个常量
      - class `AffixSystem`
        - `defCount()` — 已注册的词缀定义数
        - `maxAffixes()` — 操作
        - `roll()` — affix/AffixSystem.ts —— 词条系统（随机属性）
        - `reroll()` — 重铸（洗练）单条词条
        - `rerollAll()` — 重铸整件装备的所有词条
        - `aggregate()` — 把一组词缀的效果**聚合成** `Map<属性, 数值>`
        - `compute()` — 用聚合结果算最终值（不知道 base 时传 0 只看 add）
        - `describe()` — 人类可读的词条描述（业务可覆盖 `format`）
      - 导出函数
        - `validateAffixPool()` — 判断
      - type `AffixOp` — 类型定义
      - type `RarityDef` — 类型定义
      - type `AffixDef` — 类型定义
      - type `Affix` — 类型定义
      - type `AffixSum` — 类型定义
      - type `AffixSystemOptions` — 类型定义
      - type `RollOptions` — 类型定义
      - type `RerollOptions` — 类型定义
      - … 另有 1 个类型
      - const `DefaultRarities`
  - **blessing**
    - 作用：祝福堆叠：软上限递减 + 互斥（双向）+ 三选一抽取
    - 功能：提供 `BlessingSystem`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 18 个方法 / 6 个类型
      - class `BlessingSystem`
        - `all()` — 操作
        - `def()` — 操作
        - `add()` — 第 4 节
        - `remove()` — 第 4 节
        - `clear()` — 清空全部持有（**开新局 / 换存档时必须调**）
        - `stacks()` — 某祝福的层数
        - `has()` — 判断
        - `count()` — **种类数**，不是总层数
        - `effectiveStacks()` — 递减后的**有效**层数
        - `effectsOf()` — 效果列表
        - … 另有 8 个方法
      - type `BlessingOp` — 类型定义
      - type `BlessingEffect` — 类型定义
      - type `BlessingDef` — 类型定义
      - type `BlessingSystemOptions` — 类型定义
      - type `BlessingStack` — 类型定义
      - type `StatEffect` — 类型定义
  - **curse**
    - 作用：诅咒：增益+代价触发时机+移除条件，canRemove/remove 一致
    - 功能：提供 `CurseSystem`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 21 个方法 / 7 个类型
      - class `CurseSystem`
        - `all()` — 操作
        - `def()` — 操作
        - `add()` — curse —— 诅咒系统（带代价的强化）
        - `remove()` — 【⚠️ 曾经的 bug：allowDuplicate 形同虚设】
        - `canRemove()` — 检查是否能移除（UI 显示用，不实际移除）
        - `clear()` — 清空全部（**开新局时必须调**）
        - `has()` — 是否持有某个诅咒
        - `active()` — 当前生效的诅咒（`ActiveCurse[]`）
        - `count()` — 计算
        - `tick()` — 每帧（持续掉血）
        - … 另有 11 个方法
      - type `CurseOp` — 类型定义
      - type `CurseEffect` — 类型定义
      - type `CurseTrigger` — 类型定义
      - type `CurseCost` — 类型定义
      - type `CurseDef` — 类型定义
      - type `CurseSystemOptions` — 类型定义
      - type `ActiveCurse` — 类型定义
  - **loot**
    - 作用：PRD / ShuffleBag / WeightedTable / LootTable / Chest（随机与掉落）
    - 功能：提供 `Chest`、`LootTable`、`PRD`、`ShuffleBag`
    - 前置依赖：_core  · 分层 L2
    - 接口：5 个类 / 51 个方法 / 7 个类型
      - class `Chest`
        - `state()` — 操作
        - `current()` — Chest —— 开箱 / 三选一
        - `rerollsLeft()` — 操作
        - `canReroll()` — 是否还能 reroll（**UI 的 reroll 按钮**）
        - `taken()` — 操作
        - `roll()` — 掷出选项
        - `reroll()` — 重掷（排除当前选项）
        - `take()` — 选择第 index 个
        - `reset()` — 重置（开启下一个宝箱时）
        - `addOwned()` — 更新已拥有列表（拿到新遗物后调用）
        - … 另有 3 个方法
      - class `LootTable`
        - `entry()` — LootTable —— 掉落表
        - `withPity()` — 设置保底
        - `entryCount()` — 掉落条目数
        - `sinceRare()` — 距离上次稀有的次数（调试/UI 显示"再 X 次必出"）
        - `pityRemaining()` — 距离保底还差几次（未启用保底返回 -1）
        - `roll()` — 掷出选项
        - `resetPity()` — 重置保底计数（存档读取、换关时）
        - `exportPity()` — 序列化保底状态（存档用）
        - `importPity()` — 操作
        - `destroy()` — 移除
      - class `PRD`
        - `fromChance()` — PRD —— 伪随机分布（Pseudo-Random Distribution）
        - `c()` — C → 实际长期频率 */
        - `failCount()` — 连续未出**稀有**的次数（保底计数）
        - `chance()` — 本次的实际触发概率
        - `roll()` — 掷出选项
        - `reset()` — 重置（开启下一个宝箱时）
        - `setFailCount()` — 手动设置保底计数（**调试 / 存档迁移**）
      - class `ShuffleBag`
        - `add()` — ShuffleBag —— 洗牌袋（无放回抽取）
        - `remove()` — 移除元素
        - `clear()` — 移除
        - `capacity()` — 理论上的总格子数
        - `remaining()` — 当前袋子剩余（0 表示下次 draw 会重洗）
        - `draw()` — 抽一个
        - `drawMany()` — 抽多个（不重洗则受剩余数量限制）
        - `reshuffle()` — 手动重洗（新轮次开始时用）
        - `lastDrawn()` — 上次抽到的元素（调试用）
        - `destroy()` — 移除
      - class `WeightedTable`
        - `add()` — ShuffleBag —— 洗牌袋（无放回抽取）
        - `setWeight()` — WeightedTable —— 加权随机表
        - `remove()` — 移除元素
        - `clear()` — 移除
        - `count()` — 计算
        - `totalWeight()` — 转换
        - `pick()` — 抽一个
        - `pickMany()` — 抽 n 个（可重复）
        - `pickUnique()` — 抽 n 个不重复的（三选一的核心）
        - `toArray()` — 导出（调试与存档用）
        - … 另有 1 个方法
      - type `ChestState` — 类型定义
      - type `ChestOptions` — 类型定义
      - type `ChestSnapshot` — 类型定义
      - type `LootEntry` — 类型定义
      - type `LootDrop` — 类型定义
      - type `PityOptions` — 类型定义
      - type `ShuffleBagOptions` — 类型定义
- 〔功能模块·内容与经济〕4 个单元组合
  - **craft**
    - 作用：合成系统（嵌套材料树/原子性/品质随机）
    - 功能：提供 `CraftSystem`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 12 个方法 / 7 个类型
      - class `CraftSystem`
        - `setRNG()` — 设置随机源
        - `define()` — 定义配方
        - `defineAll()` — 定义配方
        - `unlock()` — 图纸
        - `isUnlocked()` — 图纸
        - `recipeIds()` — 全部配方 id
        - `byStation()` — 按工作台筛选
        - `canCraft()` — 检查，返回 `{ok, missing, maxCount}`
        - `findCraftable()` — 反向查询
        - `totalMaterials()` — 展开材料树
        - … 另有 2 个方法
      - type `IInventory` — 类型定义
      - type `RecipeInput` — 类型定义
      - type `RecipeOutput` — 类型定义
      - type `Recipe` — 类型定义
      - type `MissingMaterial` — 类型定义
      - type `CanCraftResult` — 类型定义
      - type `CraftResult` — 类型定义
  - **gacha**
    - 作用：抽卡保底：软/硬保底 + 50/50 + 十连保底 + 批量模拟
    - 功能：提供 `GachaPity`、`simulate`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 12 个方法 / 6 个类型
      - class `GachaPity`
        - `pityCount()` — gacha/GachaPity.ts —— 抽卡与保底
        - `toHardPity()` — 距硬保底还差几抽（无硬保底返回 null）
        - `currentRate()` — 当前出货概率（含软保底提升）
        - `totalPulls()` — 转换
        - `guaranteedLimited()` — 下次出金是否必定是限定（50/50 保底）
        - `pull()` — 单抽
        - `pullTen()` — 十连（**内部会走保底逻辑**，别自己循环 `pull()`）
        - `pullN()` — 任意次数连抽
        - `snapshot()` — 操作
        - `restore()` — 操作
        - … 另有 1 个方法
      - 导出函数
        - `simulate()` — 计算
      - type `GachaItem` — 类型定义
      - type `RarityConfig` — 类型定义
      - type `PullResult` — 类型定义
      - type `GachaOptions` — 类型定义
      - type `GachaSnapshot` — 类型定义
      - type `SimulationReport` — 类型定义
  - **setbonus**
    - 作用：装备套装：N 件套、阈值叠加/仅最高档、部位去重
    - 功能：提供 `SetBonusSystem`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 13 个方法 / 9 个类型
      - class `SetBonusSystem`
        - `equip()` — SetBonus —— 装备套装效果
        - `unequip()` — 脱下某个槽位，返回被脱下的装备
        - `unequipById()` — 按 id 脱下（**不知道在哪个槽时用**）
        - `clear()` — 全部脱下
        - `equipped()` — 操作
        - `countFor()` — 某套装已装备的**不同部位**数
        - `countDistinctIds()` — 按不同装备 id 计数（同名装备只算 1 件）
        - `activeThresholds()` — 当前**已激活**的档位
        - `effects()` — 原始效果列表
        - `summary()` — 已聚合的 `{'atk.mul': 1.375}`
        - … 另有 3 个方法
      - type `SlotId` — 类型定义
      - type `EffectOp` — 类型定义
      - type `StatEffect` — 类型定义
      - type `Threshold` — 类型定义
      - type `SetDef` — 类型定义
      - type `EquipItem` — 类型定义
      - type `SetBonusOptions` — 类型定义
      - type `ActiveThreshold` — 类型定义
      - … 另有 1 个类型
  - **shop**
    - 作用：商店与经济（折扣/多货币/库存）
    - 功能：价格浮动（不同商店 / 不同时间）
    - 功能：折扣（会员、促销、砍价技能）
    - 功能：库存（限量商品）
    - 功能：刷新（Roguelike 每层重刷货架）
    - 功能：买不起的原因：**钱不够** 还是 **没库存**，提示完全不同
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 23 个方法 / 7 个类型
      - class `Shop`
        - `defineItem()` — 定义商品
        - `defineAll()` — 定义商品
        - `hasItem()` — 查商品是否存在 / 取定义
        - `getItem()` — 查商品是否存在 / 取定义
        - `setPricing()` — 定价钩子
        - `priceOf()` — 单价
        - `totalPriceOf()` — 批量总价（`quantity` **必填**）
        - `stock()` — 上架。`count` 为 -1 = 无限
        - `unstock()` — 下架
        - `stockOf()` — 库存查询
        - … 另有 13 个方法
      - type `TradeSide` — 类型定义
      - type `ShopItem` — 类型定义
      - type `PriceContext` — 类型定义
      - type `PricingFn` — 类型定义
      - type `StockEntry` — 类型定义
      - type `BuyFailureReason` — 类型定义
      - type `TradeResult` — 类型定义
- **rarity**
  - 作用：稀有度不只是个排序——还牵扯掉落权重、UI 颜色、保底归属、排序比较。
  - 功能：提供 `Rarity`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 16 个方法 / 2 个类型 / 2 个常量
    - class `Rarity`
      - `all()` — 全部稀有度（**按声明顺序**）
      - `allAscending()` — 全部稀有度，**按 `order` 升序**
      - `highest()` — 最高 / 最低档
      - `lowest()` — 最高 / 最低档
      - `get()` — 查询
      - `getOrFallback()` — 取值，未知 id **回退到最低档**（不抛错）
      - `isRarer()` — a 是否比 b 稀有
      - `atLeast()` — a 是否达到 b 的稀有度（含等于）
      - `compare()` — 排序比较器（最稀有的在前）
      - `weight()` — 权重 0 会让 `roll` 永远选不到它，通常是配表笔误
      - … 另有 6 个方法
    - type `RarityDef` — 类型定义
    - type `RandomSource` — 类型定义
    - const `CommonRarity`
    - const `SimpleRarity`

---

## 玩法框架（8 个）

> 具体玩法的骨架

- 〔功能模块·内容与经济〕4 个单元组合
  - **card**
    - 作用：牌库/手牌/弃牌堆（Deck）
    - 功能：提供 `Deck`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 29 个方法 / 3 个类型
      - class `Deck`
        - `handLimit()` — 操作
        - `drawPile()` — 抽牌堆
        - `hand()` — 操作
        - `discardPile()` — 弃牌堆
        - `exhaustPile()` — 消耗堆（**本局不会再回来**）
        - `top()` — Deck —— 牌库 / 手牌 / 弃牌堆
        - `totalCards()` — 统计与快照
        - `isHandFull()` — 手牌是否满了
        - `setDeck()` — 设置牌库（默认记为主牌库）
        - `reset()` — 回到主牌库
        - … 另有 19 个方法
      - type `DeckOptions` — 类型定义
      - type `CardZone` — 类型定义
      - type `DeckSnapshot` — 类型定义
  - **dialogue**
    - 作用：对话节点图（条件分支 + 完整性校验）
    - 功能：提供 `DialogueGraph`、`DialogueRunner`
    - 前置依赖：无  · 分层 L2
    - 接口：2 个类 / 19 个方法 / 4 个类型
      - class `DialogueGraph`
        - `node()` — 注册（重复 id 抛错）
        - `nodes()` — 注册（重复 id 抛错）
        - `has()` — 判断
        - `nodeIds()` — 全部节点 id
        - `validate()` — 跳转到不存在的节点（运行时表现为"对话说到一半卡住"，且只在特定分支触发）
        - `findUnreachable()` — 死内容（写了但没人能到达的对话）
        - `start()` — 创建运行器
        - `getNode()` — 取节点定义
        - `destroy()` — 清空
      - class `DialogueRunner`
        - `isDone()` — 状态与走过的路径
        - `currentNodeId()` — 当前节点 id（**存档用**：恢复对话时 `jumpTo` 回来）
        - `history()` — 状态与走过的路径
        - `current()` — 当前展示内容（渲染层直接读）
        - `advance()` — 推不动，玩家以为卡了
        - `choose()` — 选选项，返回是否成功
        - `jumpTo()` — 强制跳转（任务系统插入对话）
        - `end()` — 强制结束
        - `advanceToChoice()` — 连续播到有选项的节点（**有安全上限**）
        - `destroy()` — 清空
      - type `DialogueState` — 类型定义
      - type `Choice` — 类型定义
      - type `DialogueNode` — 类型定义
      - type `DialogueView` — 类型定义
  - **inventory**
    - 作用：背包与物品栏（堆叠/交换/拆分/整理）
    - 功能：提供 `Inventory`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 25 个方法 / 5 个类型
      - class `Inventory`
        - `define()` — 注册物品定义（**必须带 name**）
        - `getDef()` — 取物品定义（**拿 name / icon / stackable**）
        - `size()` — 操作
        - `slots()` — 操作
        - `usedSlots()` — 已占用格数
        - `isFull()` — 满 / 空
        - `isEmpty()` — 满 / 空
        - `firstEmpty()` — 第一个空格下标（**-1 = 满了**）
        - `count()` — 查询
        - `slotsUsedBy()` — 某物品占了几格
        - … 另有 15 个方法
      - type `ItemDef` — 类型定义
      - type `Slot` — 类型定义
      - type `InventoryChangeKind` — 类型定义
      - type `InventoryChange` — 类型定义
      - type `InventoryOptions` — 类型定义
  - **turn**
    - 作用：回合制系统（先攻/行动点/死亡跳过）
    - 功能：提供 `TurnSystem`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 23 个方法 / 3 个类型
      - class `TurnSystem`
        - `addUnit()` — 添加。重复返回 false
        - `killUnit()` — 死亡 / 复活
        - `reviveUnit()` — 死亡 / 复活
        - `skipUnit()` — 跳过本回合（眩晕）
        - `grantExtraTurn()` — 额外回合（连击）
        - `has()` — 判断
        - `unitCount()` — 参战单位数
        - `start()` — 开始（排序）。没单位会抛错
        - `endTurn()` — 结束当前，返回是否成功
        - `skip()` — 跳过当前（不触发 onUnitEnd）
        - … 另有 13 个方法
      - type `TurnUnit` — 类型定义
      - type `TurnPhase` — 类型定义
      - type `TurnEvents` — 类型定义
- 〔功能模块·肉鸽〕2 个单元组合
  - **interact**
    - 作用：环境互动：目标选择（朝向分档优先）+ 条件 + 用完禁用
    - 功能：玩家同时靠近两个物件时，交互提示闪烁
    - 功能：明明面朝宝箱，却触发了身后的门
    - 功能：交互后物件没禁用，能反复开同一个箱子
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 17 个方法 / 4 个类型
      - class `InteractSystem`
        - `register()` — 注册
        - `registerAll()` — 注册
        - `unregister()` — 反注册，返回是否真的删掉了
        - `setDisabled()` — 手动启停（**比反复 register/unregister 便宜**）
        - `clear()` — 清空全部注册
        - `get()` — 取单个 / 取全部
        - `all()` — 取单个 / 取全部
        - `usedCount()` — 已交互次数（对照 `maxUses`）
        - `findFocus()` — 选一个**最优**目标（排序见第 3 节）
        - `focused()` — 当前焦点物件（`null` = 没有）
        - … 另有 5 个方法
      - 导出函数
        - `defaultDistance()` — 默认距离函数，**支持 2D/3D**（`z` 可选）
        - `alignmentTo()` — 朝向对齐度（点积，1 = 完全正对）
      - type `Interactable` — 类型定义
      - type `InteractContext` — 类型定义
      - type `InteractSystemOptions` — 类型定义
      - type `InteractCandidate` — 类型定义
  - **wave-spawner**
    - 作用：波次刷怪：三重超时兜底，保证永远不会卡住
    - 功能：提供 `WaveSpawner`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 16 个方法 / 8 个类型
      - class `WaveSpawner`
        - `state()` — 当前状态（`idle` / `spawning` / `fighting` / `cleared` …）
        - `currentWaveIndex()` — 当前第几波（**从 0 还是 1 开始，看配置**）
        - `aliveCount()` — 存活敌人数
        - `pendingCount()` — 还没生成的敌人数
        - `totalTime()` — 转换
        - `waveTime()` — 操作
        - `finished()` — 全部波次是否结束
        - `aliveHandles()` — 存活敌人的句柄列表（**批量加 buff / 全体强制击杀用**）
        - `start()` — 开始（从头）
        - `reset()` — 重置到第 0 波（**重开关卡**）
        - … 另有 6 个方法
      - type `SpawnHandle` — 类型定义
      - type `WaveEntry` — 类型定义
      - type `WaveDef` — 类型定义
      - type `ISpawnSink` — 类型定义
      - type `RemoveReason` — 类型定义
      - type `WaveSpawnerOptions` — 类型定义
      - type `FallbackInfo` — 类型定义
      - type `WaveState` — 类型定义
- **builder**
  - 作用：建造系统：原子性放置、旋转占位、返还策略、原地升级
  - 功能：提供 `Builder`、`rotateCell`、`blueprintCells`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 18 个方法 / 12 个类型
    - class `Builder`
      - `update()` — 推进建造进度。**`now` 是外部传入的时间戳**
      - `now()` — 当前时间
      - `getBlueprint()` — 蓝图定义
      - `allBlueprints()` — 蓝图定义
      - `getBuilding()` — 已放置的建筑
      - `allBuildings()` — 已放置的建筑
      - `buildingAt()` — 查询某格
      - `countOf()` — 某蓝图已建了几个（**限建造上限**用）
      - `buildingCount()` — 建筑总数
      - `preview()` — 纯查询，返回错误原因 + 占位 + 冲突对象
      - … 另有 6 个方法
    - 导出函数
      - `rotateCell()` — 操作
      - `blueprintCells()` — 操作
    - type `Cell` — 类型定义
    - type `Direction` — 类型定义
    - type `ResourceId` — 类型定义
    - type `Blueprint` — 类型定义
    - type `PlacedBuilding` — 类型定义
    - type `PlaceError` — 类型定义
    - type `PlaceResult` — 类型定义
    - type `RemoveError` — 类型定义
    - … 另有 4 个类型
- **objective**
  - 作用：关卡目标：5 种类型 + 前置依赖 + 循环检测
  - 功能：分阶段关卡（先开门，再清场）的激活顺序容易乱
  - 功能：可选目标失败不该导致整关失败，但容易写成会的
  - 功能：循环依赖会让关卡永远不开始
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 16 个方法 / 6 个类型 / 1 个常量
    - class `ObjectiveSystem`
      - `status()` — 某目标的状态（`inactive` / `active` / `done` / `failed`）
      - `progress()` — 某目标的当前进度值
      - `ratio()` — 某目标的进度比例 0..1（**做进度条**）
      - `finished()` — 结果查询
      - `failed()` — 结果查询
      - `failedId()` — 结果查询
      - `totalTime()` — 已总用时
      - `active()` — 当前激活的目标（UI 显示用）
      - `primary()` — 主目标（**UI 高亮的那个**）
      - `all()` — 操作
      - … 另有 6 个方法
    - type `ObjectiveKind` — 类型定义
    - type `ObjectiveStatus` — 类型定义
    - type `ObjectiveDef` — 类型定义
    - type `ObjectiveState` — 类型定义
    - type `ObjectiveEvent` — 类型定义
    - type `ObjectiveSystemOptions` — 类型定义
    - const `Objectives`

---

## 进度与统计（11 个）

> 跨局的东西：存档、成就、评分

- 〔功能模块·肉鸽〕5 个单元组合
  - **daily**
    - 作用：每日挑战：时区、种子雪崩、分享回填、连续打卡
    - 功能：提供 `DailyChallenge`、`dateKeyOf`、`hashDateKey`、`seedToText`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 19 个方法 / 5 个类型
      - class `DailyChallenge`
        - `todayKey()` — 当天的日期键
        - `entryFor()` — 取当天 / 指定日期的题目
        - `fromText()` — 文本 → 题目，**可能返回 `null`**（见上）
        - `today()` — 取当天 / 指定日期的题目
        - `recordMode()` — 操作
        - `attemptsOn()` — 查询
        - `recordOf()` — 取某天的记录（`undefined` = 没玩过）
        - `hasPlayedToday()` — 按种子抽 modifiers
        - `submit()` — 提交成绩（**绑定开始日期**，见 §5）
        - `streak()` — 连续打卡天数
        - … 另有 5 个方法
      - 导出函数
        - `dateKeyOf()` — `Date` → `'YYYY-MM-DD'` 字符串键
        - `hashDateKey()` — 判断
        - `seedToText()` — 操作
        - `isValidSeedText()` — 校验种子文本（**只判断，不解析**）
      - type `DateKey` — 类型定义
      - type `DailyModifier` — 类型定义
      - type `DailyChallengeOptions` — 类型定义
      - type `DailyEntry` — 类型定义
      - type `DailyRecord` — 类型定义
  - **leaderboard**
    - 作用：排行榜：并列名次、只留最好成绩、分页、快照
    - 功能：提供 `Leaderboard`、`mergeLeaderboards`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 16 个方法 / 6 个类型
      - class `Leaderboard`
        - `size()` — 当前条目数
        - `capacity()` — 操作
        - `submit()` — Leaderboard —— 排行榜
        - `submitAll()` — 批量提交（初始化 / 从服务器拉取）
        - `ranked()` — a 是否优于 b */
        - `page()` — 分页
        - `rankOf()` — 查某个玩家的名次（`null` = 榜上无名）
        - `around()` — 某玩家周围的榜单（"附近的人"）
        - `top()` — 取前 n 名（**做"前 100 名"展示**）
        - `snapshot()` — 快照
        - … 另有 5 个方法
      - 导出函数
        - `mergeLeaderboards()` — 集合操作
      - type `RankMode` — 类型定义
      - type `SortOrder` — 类型定义
      - type `ScoreEntry` — 类型定义
      - type `RankedEntry` — 类型定义
      - type `LeaderboardOptions` — 类型定义
      - type `LeaderboardPage` — 类型定义
  - **meta**
    - 作用：元进度（局外成长）：解锁树、循环依赖检测、效果聚合、存档容错
    - 功能：局内收集"黑暗" → 局外升级武器天赋
    - 功能：局内收集"宝石" → 局外装修主城（解锁新功能）
    - 功能：送"蜜露"给 NPC → 解锁剧情与遗物
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 23 个方法 / 9 个类型
      - class `MetaProgression`
        - `nodeCount()` — 节点总数
        - `allNodes()` — 全部节点
        - `level()` — 操作
        - `maxLevel()` — 操作
        - `isUnlocked()` — 是否已解锁
        - `currency()` — 查余额
        - `nextCost()` — 下一级的价格，**满级返回 `null`**
        - `canUnlock()` — 能否解锁（含原因）
        - `addCurrency()` — 增加货币（负数会被 clamp 到 0）。单货币时可省略 id
        - `unlock()` — 解锁 / 升级一个节点
        - … 另有 11 个方法
      - 导出函数
        - `defaultCostCurve()` — 操作
        - `costAt()` — 操作
      - type `EffectOp` — 类型定义
      - type `MetaEffect` — 类型定义
      - type `CostCurve` — 类型定义
      - type `MetaNode` — 类型定义
      - type `AggregatedEffect` — 类型定义
      - type `MetaProgressionOptions` — 类型定义
      - type `RejectReason` — 类型定义
      - type `MetaSnapshot` — 类型定义
      - … 另有 1 个类型
  - **runscope**
    - 作用：局内/局外/会话三域数据隔离，越界抛错防串档
    - 功能：局内金币被当成永久货币花掉
    - 功能：死亡后没清干净，下一局带着上局的遗物开局
    - 功能：存档时把整局数据写进了永久槽
    - 功能：玩家中途退出，永久货币丢了
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 2 个方法 / 5 个类型 / 1 个常量
      - class `ScopedStore`
      - 导出函数
        - `key()` — 操作
        - `keys()` — 操作
      - type `Scope` — 类型定义
      - type `KeyDef` — 类型定义
      - type `StoreSchema` — 类型定义
      - type `ScopedStoreOptions` — 类型定义
      - type `ScopedSnapshot` — 类型定义
      - const `SCOPES`
  - **score**
    - 作用：有状态评分追踪器：实时累积+可配置评级+三种评级模式+StarRating
    - 功能：提供 `ScoreSystem`、`StarRating`
    - 前置依赖：_core  · 分层 L2
    - 接口：2 个类 / 15 个方法 / 5 个类型 / 2 个常量
      - class `ScoreSystem`
        - `set()` — 设置 / 累加指标
        - `add()` — 设置 / 累加指标
        - `get()` — 查询
        - `reset()` — 期望是 0
        - `startTimer()` — **归零**并重新开始计时（不是"启动"）
        - `tick()` — 时间倒流
        - `elapsed()` — 已累计时间
        - `breakdown()` — 各指标得分明细（**做结算面板的逐项展示**）
        - `total()` — 总分（0~100+，**可能超过 100**，超额奖励见第 6 节）
        - `grade()` — 评级
        - … 另有 2 个方法
      - class `StarRating`
        - `addCondition()` — 加条件，返回 `this`（链式）
        - `evaluate()` — 评定，返回达成数
        - `max()` — 最大星级
      - type `MetricDirection` — 类型定义
      - type `MetricDef` — 类型定义
      - type `GradeDef` — 类型定义
      - type `ScoreSystemOptions` — 类型定义
      - type `MetricScore` — 类型定义
      - const `DEFAULT_GRADES`
      - const `Metrics`
- 〔功能模块·战斗〕2 个单元组合
  - **difficulty**
    - 作用：难度档位 + DDA 动态难度（±15% 平滑调节）
    - 功能：提供 `DifficultySystem`、`computePerformance`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 15 个方法 / 3 个类型 / 2 个常量
      - class `DifficultySystem`
        - `currentTierId()` — 当前档位（id / 完整对象）
        - `currentTier()` — 当前档位（id / 完整对象）
        - `tiers()` — 操作
        - `setTier()` — 设置
        - `ddaEnabled()` — 自适应难度是否开启
        - `setDDAEnabled()` — 设置
        - `ddaValue()` — 当前 DDA 调节量（**调试用**：一直是 0 说明没生效）
        - `smoothedPerformance()` — 平滑后的玩家表现
        - `resetDDA()` — 移除
        - `report()` — 上报一段游戏表现
        - … 另有 4 个方法
      - 导出函数
        - `computePerformance()` — 计算
      - type `DifficultyTier` — 类型定义
      - type `PerformanceSignal` — 类型定义
      - type `DDAOptions` — 类型定义
      - const `MultiplierKeys`
      - const `DEFAULT_TIERS`
  - **scoring**
    - 作用：战斗评分：方向归一/权重/曲线/评级/短板诊断
    - 功能：提供 `ScoringSystem`、`normalize`、`gapToPerfect`、`gradeOf`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 10 个方法 / 7 个类型 / 2 个常量
      - class `ScoringSystem`
        - `metricCount()` — 操作
        - `evaluate()` — scoring/ScoringSystem.ts —— 战斗评分与评级
        - `score()` — 【为什么除以 totalWeight 而不是配置的总权重】
        - `grade()` — 便捷：直接算评级
        - `weakPoints()` — 诊断：哪些指标拖后腿
        - `distribution()` — 批量统计（平衡用）
      - 导出函数
        - `normalize()` — 单项归一化到 0..1（**处理方向差异**）
        - `gapToPerfect()` — 距满分的差距（"再快 3 秒就满分"）
        - `gradeOf()` — 总分 → 等级
        - `toNextGrade()` — 距下一档还差多少分
      - type `MetricDirection` — 类型定义
      - type `MetricDef` — 类型定义
      - type `MetricValue` — 类型定义
      - type `MetricScore` — 类型定义
      - type `Grade` — 类型定义
      - type `ScoreResult` — 类型定义
      - type `ScoringOptions` — 类型定义
      - const `DEFAULT_THRESHOLDS`
      - const `CombatMetrics`
- 〔功能模块·内容与经济〕1 个单元组合
  - **quest**
    - 作用：任务系统（目标追踪/任务链/防重复领奖）
    - 功能：提供 `QuestSystem`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 22 个方法 / 6 个类型
      - class `QuestSystem`
        - `define()` — 定义（后者返回错误列表，会校验前置）
        - `defineAll()` — QuestSystem —— 任务系统
        - `status()` — 状态查询
        - `get()` — 查询
        - `available()` — 列表
        - `active()` — 列表
        - `completed()` — 列表
        - `isClaimed()` — 状态查询
        - `accept()` — 流转
        - `abandon()` — 流转
        - … 另有 12 个方法
      - type `QuestStatus` — 类型定义
      - type `ObjectiveType` — 类型定义
      - type `Objective` — 类型定义
      - type `QuestDef` — 类型定义
      - type `QuestEvent` — 类型定义
      - type `QuestProgress` — 类型定义
- 〔功能模块·开发工具〕1 个单元组合
  - **replay**
    - 作用：输入录制与回放（deltaOnly，体积极小）
    - 功能：提供 `ReplayRecorder`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 20 个方法 / 3 个类型
      - class `ReplayRecorder`
        - `seed()` — 操作
        - `frame()` — 操作
        - `isRecording()` — 是否正在录制（**UI 的录制指示灯**）
        - `isPlaying()` — 是否已载入回放数据（`_data !== null`）
        - `keyframeCount()` — 关键帧数（**体积的直接指标**）
        - `recordedFrames()` — 已录帧数
        - `start()` — 开始/停止录制
        - `stop()` — 开始/停止录制
        - `record()` — 记录一帧（**frame 必须是固定步长的逻辑帧**）
        - `export()` — 导出
        - … 另有 10 个方法
      - type `InputFrame` — 类型定义
      - type `ReplayOptions` — 类型定义
      - type `ReplayData` — 类型定义
- **achievement**
  - 作用：成就看起来简单：if (kills >= 100) unlock('kill100')。
  - 功能：提供 `Achievement`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 17 个方法 / 4 个类型
    - class `Achievement`
      - `def()` — 操作
      - `isUnlocked()` — 是否已解锁
      - `unlockedCount()` — 已解锁数量
      - `visible()` — achievement/Achievement.ts —— 成就系统
      - `unlocked()` — 已解锁的成就定义列表
      - `points()` — 已获得 / 全部点数
      - `totalPoints()` — 已获得 / 全部点数
      - `completion()` — 完成度 0..1
      - `requirementsMet()` — 前置成就是否都达成了
      - `check()` — 检查**全部**成就，返回**本次新解锁的**列表
      - … 另有 7 个方法
    - type `AchievementContext` — 类型定义
    - type `AchievementDef` — 类型定义
    - type `AchievementProgress` — 类型定义
    - type `AchievementOptions` — 类型定义
- **stats**
  - 作用：结算界面的数据来源
  - 功能：忘了 `Math.min` 就把"最短时间"变成"最后一次时间"
  - 功能：想分武器统计时才发现问题，改起来要动所有记录点
  - 功能：`Infinity` 直接渲染成 "-∞"
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 19 个方法 / 5 个类型
    - class `Stats`
      - `record()` — stats/Stats.ts —— 统计追踪
      - `add()` — 累加（sum 的语义化别名）
      - `set()` — 直接设置（覆盖聚合逻辑）
      - `get()` — max 无记录时是 `-Infinity`（正确的单位元），但直接渲染是 "-∞"
      - `tryGet()` — 取值，不存在返回 `0`（**不抛错**）
      - `display()` — max 无记录时是 `-Infinity`（正确的单位元），但直接渲染是 "-∞"
      - `has()` — 是否有记录
      - `derived()` — 操作
      - `derivedIds()` — 全部派生指标 id
      - `byTag()` — 按标签分组统计
      - … 另有 8 个方法
    - 导出函数
      - `makeKey()` — 操作
    - type `Aggregation` — 类型定义
    - type `StatDef` — 类型定义
    - type `Tags` — 类型定义
    - type `DerivedFn` — 类型定义
    - type `StatsOptions` — 类型定义

---

## 表现与外壳（18 个）

> 玩家能感知、但说不清的「感觉」

- 〔功能模块·演出〕3 个单元组合
  - **cutscene**
    - 作用：演出编排：时间轴播放、seek 重放、跳过应用终态、门控与超时
    - 功能：提供 `Cutscene`、`Timeline`
    - 前置依赖：_core  · 分层 L2
    - 接口：2 个类 / 20 个方法 / 6 个类型
      - class `Cutscene`
        - `def()` — 演出定义（只读）
        - `on()` — 注册某类 step 的回调
        - `play()` — 播放 / 推进
        - `update()` — 播放 / 推进
        - `state()` — 当前状态（`CutsceneState`）
        - `time()` — 当前时间（毫秒）
        - `duration()` — 总时长（毫秒）
        - `progress()` — 进度 0~1
        - `finished()` — 是否已播完
        - `timedOut()` — 是否曾因超时而放行
        - … 另有 5 个方法
      - class `Timeline`
        - `add()` — 串行添加（接在上一个之后）
        - `with()` — 并行添加（与上一个同时开始，**总时长取 max**）
        - `wait()` — 门控
        - `gap()` — 空档
        - `build()` — 生成 `CutsceneDef`
      - type `StepKind` — step 类型（`on(kind, handler)` 的 kind）
      - type `CutsceneStep` — 单个 step
      - type `CutsceneDef` — 演出定义（`build()` 的产物，也是构造参数）
      - type `StepHandler` — step 回调，签名 `(step, ctx) => void`
      - type `CutsceneState` — 播放状态
      - type `CutsceneCut` — `update()` / `seek()` / `skip()` 的返回（本帧的变更
  - **minimap**
    - 作用：小地图坐标变换与布局：跟随/固定、旋转、边缘钳制、战争迷雾
    - 功能：提供 `Minimap`、`FogMap`、`normalizeAngle`
    - 前置依赖：_core  · 分层 L2
    - 接口：2 个类 / 16 个方法 / 6 个类型
      - class `Minimap`
        - `scale()` — 自动
        - `config()` — 当前配置
        - `worldToMinimap()` — 世界 → 小地图坐标
        - `minimapToWorld()` — 小地图 → 世界坐标（**点击传送 / 标记用**）
        - `isInView()` — 某点是否在可视范围内
        - `clampToEdge()` — `true`
        - `layout()` — 一次算出全部图标
        - `createFogMap()` — 【图标朝向：以小地图的"上"为 0，顺时针为正】
      - class `FogMap`
        - `resolution()` — 格子数（`Math.max(1, resolution)`）
        - `reveal()` — 迷雾位图
        - `isRevealed()` — 某点是否已探索
        - `coverage()` — 已探索比例 0~1
        - `clear()` — 清空（**新一局调用**）
        - `toBytes()` — 序列化（存档用）
        - `fromBytes()` — 转换
      - 导出函数
        - `normalizeAngle()` — 把角度规范到 **(-π, π]**
      - type `Vec2` — `{ x, y }`
      - type `MinimapMode` — `'fixed'`（整张地图完整显示）/ `'follow'`（玩家居中，地图滚动）
      - type `MinimapShape` — `'rect'`（方形关卡）/ `'circle'`（开放世界，视野是圆）
      - type `MinimapConfig` — 类型定义
      - type `MinimapEntity` — 输入：`id` / `pos` / `rotation?` / `kind` / `
      - type `MinimapIcon` — 输出：`id` / `kind` / `x` / `y` / `rotation` 
  - **transition**
    - 作用：场景转场：out→load→in 三段式、重入保护、加载超时、进度防假死
    - 功能：提供 `Transition`、`overlayStyle`
    - 前置依赖：_core  · 分层 L2
    - 接口：1 个类 / 11 个方法 / 4 个类型
      - class `Transition`
        - `start()` — 发起，转场中返回 `false`
        - `update()` — 推进（真实时间）
        - `state()` — 完整状态快照
        - `phase()` — 操作
        - `busy()` — 是否转场中
        - `error()` — 操作
        - `shouldLockInput()` — 是否应锁输入（**失败时也是 true**）
        - `reset()` — 回到 idle
        - `retry()` — 重试失败的加载
        - `forceComplete()` — 强制完成（调试用）
      - 导出函数
        - `overlayStyle()` — 操作
      - type `TransitionPhase` — 类型定义
      - type `TransitionConfig` — 类型定义
      - type `TransitionState` — 类型定义
      - type `SceneLoader` — 类型定义
- 〔功能模块·音频〕1 个单元组合
  - **audio**
    - 作用：音效播放（并发/去重/优先级/音量混合）+ 分层背景音乐（水平重混）
    - 功能：提供 `AudioManager`、`BgmStack`
    - 前置依赖：_core  · 分层 L2
    - 接口：2 个类 / 37 个方法 / 11 个类型 / 1 个常量
      - class `AudioManager`
        - `update()` — 暂停时延迟音效永远不触发，恢复后一次性涌出
        - `tick()` — 按增量推进（与 `update` 二选一）
        - `now()` — 当前时间与帧号
        - `frame()` — 当前时间与帧号
        - `play()` — 播放，返回 `AudioHandle \
        - `stop()` — 按句柄 id 停
        - `stopSound()` — 按 soundId 停**所有**同名实例，返回停掉的数量
        - `stopCategory()` — 停一整类，返回停掉的数量
        - `stopAll()` — 上个场景的循环音效跟着进新场景
        - `setMasterVolume()` — 主音量
        - … 另有 12 个方法
      - class `BgmStack`
        - `setState()` — 早期版本会重启过渡，音量卡在 0（现已修：同状态一律返回 false）
        - `state()` — 当前 / 上一个状态
        - `previousState()` — 当前 / 上一个状态
        - `inTransition()` — 是否在淡变中
        - `transitionProgress()` — 淡变进度 0~1
        - `update()` — 暂停时延迟音效永远不触发，恢复后一次性涌出
        - `layerVolume()` — 某一层**当前**的目标音量
        - `layers()` — 全部层的状态
        - `playingLayers()` — 正在出声的层 / 静音的层
        - `silentLayers()` — 正在出声的层 / 静音的层
        - … 另有 5 个方法
      - type `AudioCategory` — 音效分类（ui / sfx / bgm / voice …）
      - type `AudioPriority` — 优先级常量（`LOW` / `NORMAL` / `HIGH` / `CRITICA
      - type `PlayOptions` — `play()` 的选项（音量 / 循环 / 延迟 / 优先级 …）
      - type `AudioHandle` — 类型定义
      - type `AudioManagerConfig` — 构造配置（`maxVoices` / 分类默认音量 …）
      - type `LayerName` — BGM 的层名与状态名（**由你的配表决定**）
      - type `StateName` — BGM 的层名与状态名（**由你的配表决定**）
      - type `BgmLayerConfig` — 层配置与状态混音表
      - … 另有 3 个类型
      - const `PRIORITY`
- 〔功能模块·运维〕1 个单元组合
  - **autoquality（单元名 auto-quality）**
    - 作用：帧率监测与自动画质降级：中位数抗离群、迟滞防横跳、冷却防连降
    - 功能：提供 `AutoQuality`、`FpsMeter`
    - 前置依赖：_core  · 分层 L2
    - 接口：2 个类 / 20 个方法 / 4 个类型
      - class `AutoQuality`
        - `update()` — 每帧推进
        - `medianFps()` — 中位数
        - `averageFps()` — 平均
        - `lowFps1Percent()` — 最差 1% 帧
        - `setManualLevel()` — 手动控制
        - `unlock()` — 手动控制
        - `locked()` — 手动控制
        - `level()` — 当前档位
        - `tier()` — 当前档位
        - `settings()` — 当前档位
        - … 另有 4 个方法
      - class `FpsMeter`
        - `tick()` — 推进/控制
        - `median()` — 独立的帧率采样器
        - `average()` — 操作
        - `lowFps1Percent()` — 最差 1% 帧
        - `settled()` — 设置
        - `reset()` — 移除
      - type `QualityLevel` — 类型定义
      - type `QualityTier` — 类型定义
      - type `AutoQualityConfig` — 类型定义
      - type `AutoQualityState` — 类型定义
- **accessibility**
  - 作用：可访问性常被当成"发布前再说"的事，
  - 功能：提供 `Accessibility`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 31 个方法 / 3 个类型
    - class `Accessibility`
      - `reduceMotion()` — 三项主要设置
      - `colorBlind()` — 三项主要设置
      - `fontScale()` — 三项主要设置
      - `highContrast()` — 高对比 / 字幕 / 字幕说话人
      - `subtitles()` — 高对比 / 字幕 / 字幕说话人
      - `subtitleSpeaker()` — 高对比 / 字幕 / 字幕说话人
      - `oneHanded()` — `'off' \
      - `longPressMs()` — 长按阈值（毫秒） / 震屏强度倍率
      - `shakeScale()` — 长按阈值（毫秒） / 震屏强度倍率
      - `apply()` — 设置
      - … 另有 21 个方法
    - type `ColorBlindMode` — 类型定义
    - type `AccessibilityOptions` — 类型定义
    - type `EffectKind` — 类型定义
- **camera**
  - 作用：打击感的三大件：顿帧、震屏、粒子。
  - 功能：提供 `CameraFollow`、`CameraShake`、`computeCameraBounds`、`applyShakePreset`
  - 前置依赖：_core  · 分层 L2
  - 接口：2 个类 / 24 个方法 / 8 个类型 / 1 个常量
    - class `CameraFollow`
      - `x()` — 操作
      - `y()` — 操作
      - `lookAheadX()` — 操作
      - `lookAheadY()` — 操作
      - `inDeadZone()` — 操作
      - `bounds()` — 操作
      - `setBounds()` — camera/CameraFollow.ts —— 相机跟随（纯逻辑层）
      - `update()` — 每帧更新
      - `snapTo()` — 立即吸附到目标（切场景、读档用）
      - `addOffset()` — 外部施加的偏移（震屏用）
      - … 另有 1 个方法
    - class `CameraShake`
      - `strengthScale()` — 全局强度倍率（**可读写**）——接无障碍的 `shakeScale`
      - `offsetX()` — 本帧偏移量，直接加到相机位置上
      - `offsetY()` — 本帧偏移量，直接加到相机位置上
      - `offsetRotation()` — 本帧偏移量，直接加到相机位置上
      - `active()` — 是否还在震
      - `sourceCount()` — 当前叠加了几个震源
      - `punch()` — 打一拳（**方向性**：传 `dirX`/`dirY` 则定向，否则全向随机）
      - `addTrauma()` — 按创伤值累加（**0~1，会平方**，更接近"越猛越夸张"）
      - `tick()` — 推进（**用未缩放的 dt**，见上文）
      - `stop()` — 立即停止
      - … 另有 1 个方法
    - 导出函数
      - `computeCameraBounds()` — 计算
      - `applyShakePreset()` — 设置
    - type `Vec2` — 类型定义
    - type `Rect` — 类型定义
    - type `FollowOptions` — 类型定义
    - type `FollowSnapshot` — 类型定义
    - type `ShakeNoise` — 类型定义
    - type `CameraShakeOptions` — 类型定义
    - type `ShakePunch` — 类型定义
    - type `ShakePresetName` — 类型定义
    - const `SHAKE_PRESETS`
- **currency**
  - 作用：金币、钻石、体力、活动代币……手写的 player.gold += 10 会遇到三个必然问题…
  - 功能：提供 `Wallet`、`CurrencyWallet`
  - 前置依赖：_core  · 分层 L1
  - 接口：2 个类 / 39 个方法 / 10 个类型
    - class `Wallet`
      - `get()` — 查询（见下文 `get` vs `tryGet`）
      - `tryGet()` — 查询（见下文 `get` vs `tryGet`）
      - `has()` — 是否已声明
      - `def()` — 取定义
      - `currencyIds()` — 全部货币 id（**返回 `string[]` 新数组**，不是只读视图）
      - `space()` — 剩余空间（无上限的返回 Infinity）
      - `isFull()` — 上限 / **剩余空间** / 是否满
      - `add()` — 满了再加返回 0，可用来提示"体力已满"
      - `spend()` — 查询抛错（代码错误，必须立刻暴露）；消费返回 reason（配表错误不该让玩家崩溃）
      - `set()` — 直接设置（**不走上限检查之外的校验**）
      - … 另有 10 个方法
    - class `CurrencyWallet`
      - `has()` — 是否已声明
      - `ids()` — 全部货币 id
      - `def()` — 取定义
      - `get()` — 查询（见下文 `get` vs `tryGet`）
      - `tryGet()` — 查询（见下文 `get` vs `tryGet`）
      - `cap()` — 上限 / **剩余空间** / 是否满
      - `isFull()` — 上限 / **剩余空间** / 是否满
      - `room()` — 上限 / **剩余空间** / 是否满
      - `add()` — 满了再加返回 0，可用来提示"体力已满"
      - `spend()` — 查询抛错（代码错误，必须立刻暴露）；消费返回 reason（配表错误不该让玩家崩溃）
      - … 另有 9 个方法
    - type `CurrencyDef` — 类型定义
    - type `CurrencyEntry` — 类型定义
    - type `AffordResult` — 类型定义
    - type `WalletOptions` — 类型定义
    - type `Cost` — 类型定义
    - type `CurrencyDef` — 类型定义
    - type `CurrencyOptions` — 类型定义
    - type `LedgerEntry` — 类型定义
    - … 另有 2 个类型
- **feedback**
  - 作用："打中了"这件事需要被感觉到而不只是看到。
  - 功能：提供 `HitFeedback`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 10 个方法 / 5 个类型 / 1 个常量
    - class `HitFeedback`
      - `output()` — 读当前输出（**不推进**）
      - `timeScale()` — 单项读数
      - `shake()` — 单项读数
      - `flash()` — 单项读数
      - `lastKnockback()` — 上一次的击退量
      - `activeCount()` — 当前活跃的实例数
      - `play()` — 触发一次反馈
      - `update()` — 推进（**必须用真实 dt**），返回 `FeedbackOutput`
      - `clear()` — 全部清空
      - `takePopupPayloads()` — 取出本帧的飘字数据（**取走即清空**）
    - type `HitKind` — 类型定义
    - type `FeedbackLayer` — 类型定义
    - type `HitProfile` — 类型定义
    - type `FeedbackOutput` — 类型定义
    - type `HitFeedbackOptions` — 类型定义
    - const `DEFAULT_PROFILES`
- **gesture**
  - 作用：swipe / drag / tap / longPress / circle / pinch
  - 功能：抖动被当成滑动（手指微动几个像素就翻页了）
  - 功能：圆圈被当成长按
  - 功能：累积转角不分正负（来回抖动累加出很大角度，被判成画圈）
  - 功能：双指起始距离为 0（`scale = d1/d0` 得到 Infinity）
  - 前置依赖：无  · 分层 L1
  - 接口：2 个类 / 16 个方法 / 6 个类型
    - class `GestureRecognizer`
      - `active()` — 是否正在跟踪一次手势
      - `pointCount()` — 已采集点数
      - `points()` — 操作
      - `down()` — 触摸序列
      - `move()` — 触摸序列
      - `up()` — 触摸序列
      - `cancel()` — 取消当前手势（`up` 不再产出结果）
      - `isLongPressSoFar()` — **当前是否已构成长按**
    - class `DoubleTapDetector`
      - `tap()` — 记录一次点击，返回是否构成双击
      - `reset()` — 重置状态
    - 导出函数
      - `pathLength()` — 路径总长度
      - `totalTurn()` — **累计转角（弧度，带符号）**
      - `boundsOf()` — 轨迹包围盒
      - `dirOf()` — 由位移判断方向
      - `recognize()` — 操作
      - `recognizePinch()` — 操作
    - type `Point` — `{ x, y, t }`（`t` 是毫秒时间戳）
    - type `GestureKind` — `'swipe'` / `'drag'` / `'tap'` / `'longPre
    - type `Direction` — `'left'` / `'right'` / `'up'` / `'down'`
    - type `Gesture` — 识别结果：`kind` / `dir?` / `turns?` / 缩放倍数
    - type `RecognizeOptions` — 配置
    - type `RecognizerOptions` — 配置
- **progressbar**
  - 作用：血条、经验条、加载条。看起来是"画一个矩形填到 ratio"，
  - 功能：提供 `ProgressBar`、`fillPixels`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 25 个方法 / 4 个类型
    - class `ProgressBar`
      - `value()` — 真实值与范围
      - `min()` — 真实值与范围
      - `max()` — 真实值与范围
      - `ratio()` — 真实值占比 0~1
      - `displayRatio()` — **显示值**占比（缓动后）——渲染用这个
      - `trailRatio()` — 延迟条占比
      - `isEmpty()` — 是否空 / 满
      - `isFull()` — 是否空 / 满
      - `state()` — `'empty' \
      - `segmentCount()` — 分段总数 / 当前段 / 该段填充度
      - … 另有 14 个方法
    - 导出函数
      - `fillPixels()` — 操作
    - type `FillDirection` — 类型定义
    - type `ProgressBarOptions` — 类型定义
    - type `BarState` — 类型定义
    - type `BarSnapshot` — 类型定义
- **rebind**
  - 作用：设置界面里的"按键设置"
  - 功能：编码归一化
  - 功能：冲突处理
  - 功能：保留键
  - 功能：反向索引清理
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 16 个方法 / 5 个类型 / 2 个常量
    - class `Rebind`
      - `bindingOf()` — 设置
      - `actionOf()` — rebind/Rebind.ts —— 按键重映射
      - `has()` — 是否已有绑定 / 全部动作名
      - `actions()` — 是否已有绑定 / 全部动作名
      - `isReserved()` — 该键是否被保留
      - `bind()` — 绑定一个动作
      - `unbind()` — 解除绑定
      - `resetToDefault()` — 恢复默认
      - `clearAll()` — 清空**全部**绑定
      - `exportState()` — 存档（**字符串形式**）
      - … 另有 2 个方法
    - 导出函数
      - `encodeBinding()` — 编码成**规范字符串**
      - `decodeBinding()` — 解码
      - `prettyKey()` — 单键展示：`'arrowup'` → `'↑'`
      - `formatBinding()` — 展示用：`'Ctrl+S'`
    - type `Modifier` — `'ctrl'` / `'shift'` / `'alt'` / `'meta'`
    - type `Binding` — `{ key, mods? }`
    - type `ConflictPolicy` — 类型定义
    - type `RebindOptions` — 类型定义
    - type `BindingMap` — `Record<string, Binding>`
    - const `MODIFIERS`
    - const `DEFAULT_RESERVED`
- **reddot**
  - 作用："邮件有新的"、"任务能领了"、"商店上新了"——
  - 功能：提供 `RedDot`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 13 个方法 / 1 个类型
    - class `RedDot`
      - `set()` — 直接设置数量
      - `add()` — 写成 `max(0, cur) + delta` 时 cur=3、delta=-10 会得到 -7 并抛错
      - `clear()` — 清除**某个路径及其全部子路径**
      - `clearAll()` — 清空整棵树
      - `own()` — **该节点自己的数，不含后代**
      - `get()` — 聚合后的数量（**含所有后代**）
      - `has()` — 该路径是否存在（有记录）
      - `any()` — 该路径或任一后代 > 0
      - `children()` — 直接子路径列表
      - `activePaths()` — 所有 > 0 的路径（**刷新 UI 用这个**）
      - … 另有 3 个方法
    - type `RedDotOptions` — 类型定义
- **scenerouter**
  - 作用："主菜单 → 关卡 → 结算 → 回主菜单"，中间夹着加载界面和转场动画。
  - 功能：提供 `SceneRouter`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 11 个方法 / 4 个类型
    - class `SceneRouter`
      - `state()` — 操作
      - `current()` — 当前场景名（**未就绪时为 `null`**）
      - `phase()` — 操作
      - `busy()` — 是否正在切场（out / load / in 任一阶段）
      - `history()` — 返回栈（只读）
      - `go()` — scenerouter/SceneRouter.ts —— 场景路由
      - `goTo()` — 便捷方法
      - `back()` — 返回上一场景
      - `tick()` — 每帧驱动
      - `notifyLoaded()` — 重复调用不造成状态错乱
      - … 另有 1 个方法
    - type `RoutePhase` — 类型定义
    - type `RouteRequest` — 类型定义
    - type `SceneRouterOptions` — 类型定义
    - type `RouteState` — 类型定义
- **settings**
  - 作用：游戏设置：范围校验 / 默认值迁移 / 分组 / 变更通知
  - 功能：提供 `Settings`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 18 个方法 / 5 个类型
    - class `Settings`
      - `has()` — 是否有这个设置项
      - `keys()` — 全部键
      - `def()` — 取定义（`undefined` = 不存在）
      - `keysOfGroup()` — 按分组取键 / 全部分组名
      - `groups()` — 按分组取键 / 全部分组名
      - `get()` — 取值（泛型）
      - `num()` — **类型化读取**，省掉断言
      - `bool()` — **类型化读取**，省掉断言
      - `str()` — **类型化读取**，省掉断言
      - `set()` — 否则拖动滑块会疯狂触发
      - … 另有 8 个方法
    - type `SettingValue` — 类型定义
    - type `SettingKind` — 类型定义
    - type `SettingDef` — 类型定义
    - type `SettingsOptions` — 类型定义
    - type `SettingsSnapshot` — 类型定义
- **subtitle**
  - 作用：字幕与 SRT：纯查询播放 + 时间轴解析 + 跳过回填
  - 功能：同一时刻可能有多条（画外音 + 对话），不做区分会互相覆盖
  - 功能：玩家点跳过，当前这条要立刻结束并进入下一条
  - 功能：字幕必须跟着"播放到第几毫秒"走，自己维护计时器会在暂停/变速后漂移
  - 前置依赖：无  · 分层 L2
  - 接口：1 个类 / 11 个方法 / 3 个类型
    - class `SubtitleTrack`
      - `lines()` — 全部字幕行（**已排序**）
      - `duration()` — 最后一条的结束时间
      - `at()` — 该时刻应显示的字幕（**返回数组**，见坑表格）
      - `speakersAt()` — 该时刻的说话人列表（**立绘高亮用**）
      - `nextIndex()` — 下一条的下标（`-1` = 后面没有了）
      - `skipToNext()` — 下一条的**开始时间**（跳过空档）
      - `advance()` — 同上，返回下一个时间点
      - `allSpeakers()` — 全部说话人 id
      - `lineCountOf()` — 某说话人有几句
    - 导出函数
      - `parseSRT()` — 转换
      - `toSRT()` — 转换
    - type `SubtitleLine` — 类型定义
    - type `SubtitleTrackOptions` — 类型定义
    - type `ActiveSubtitle` — 类型定义
- **tutorial**
  - 作用：引导是玩家看到的第一段内容，也是最容易做砸的。
  - 功能：提供 `Tutorial`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 17 个方法 / 4 个类型
    - class `Tutorial`
      - `state()` — 当前状态
      - `index()` — 当前步骤下标 / 步骤对象（**全完成时为 null**）
      - `current()` — 当前步骤下标 / 步骤对象（**全完成时为 null**）
      - `progress()` — 完成进度 0~1
      - `elapsed()` — 当前步已用时 / 剩余时间（**无超时限制时为 Infinity**）
      - `remaining()` — 当前步已用时 / 剩余时间（**无超时限制时为 Infinity**）
      - `start()` — 从头开始 / 从指定步骤开始
      - `startAt()` — 从头开始 / 从指定步骤开始
      - `update()` — 驱动等待与超时（**每帧调**）
      - `tap()` — 等待点击
      - … 另有 7 个方法
    - type `TutorialEventKind` — 类型定义
    - type `TutorialStep` — 类型定义
    - type `TutorialOptions` — 类型定义
    - type `TutorialState` — 类型定义
- **uinav**
  - 作用：打开背包 → 点物品 → 弹确认框。按返回键应该退一层，
  - 功能：提供 `UINav`
  - 前置依赖：无  · 分层 L1
  - 接口：1 个类 / 15 个方法 / 4 个类型
    - class `UINav`
      - `useClock()` — uinav/UINav.ts —— UI 导航栈
      - `current()` — 栈顶项（`null` = 空栈）
      - `depth()` — 当前深度
      - `isEmpty()` — 是否空栈
      - `stack()` — 整个栈（`readonly T[]`）
      - `locked()` — **是否处于转场锁**
      - `contains()` — 某界面是否在栈中
      - `indexOf()` — 在栈中的位置（`-1` = 不在）
      - `push()` — 入栈，返回是否成功（**转场锁期间返回 false**）
      - `pop()` — 不抛错，反复 pop 是安全的
      - … 另有 5 个方法
    - type `NavAction` — 类型定义
    - type `NavChange` — 类型定义
    - type `DuplicatePolicy` — 类型定义
    - type `UINavOptions` — 类型定义

---

## 评分与匹配（6 个）

> 多人对战：分数、撮合、对局运维

- **elo**
  - 作用：ELO 与 Glicko-2 评分（K 值三段 / RD 不确定度 / 保守分）
  - 功能：提供 `expectedScore`、`ratingGapFor`、`kFactor`
  - 前置依赖：_core  · 分层 L2
  - 接口：0 个类 / 19 个方法 / 8 个类型 / 3 个常量
    - 导出函数
      - `expectedScore()` — 期望胜率 / 反查分差
      - `ratingGapFor()` — 期望胜率 / 反查分差
      - `kFactor()` — 当前玩家用的 K 值（新手/常规/高手三档）
      - `rate1v1()` — 1v1 结算
      - `rateMultiplayer()` — 多人结算，返回**新分数数组**
      - `rateMultiplayerDetailed()` — 多人结算的**明细**：`rating` / `delta` / `expected` / `actual` / `
      - `describeMatchup()` — 从分数反推"大约能赢多少"的**直观描述**（调试 / UI 用）
      - `averageRating()` — 组队平均分（**显示队伍实力**）
      - `createGlickoPlayer()` — 创建玩家
      - `ratePeriod()` — 操作
      - … 另有 9 个方法
    - type `EloConfig` — 类型定义
    - type `EloPlayer` — 类型定义
    - type `EloOutcome` — 类型定义
    - type `Elo1v1Result` — 类型定义
    - type `GlickoPlayer` — 类型定义
    - type `GlickoResult` — 类型定义
    - type `GlickoConfig` — 类型定义
    - type `GlickoSave` — 存档结构：`{ r, rd, sigma }`
    - const `GLICKO_DEFAULT_RATING`
    - const `GLICKO_DEFAULT_RD`
    - const `GLICKO_DEFAULT_SIGMA`
- **matchmaking**
  - 作用：撮合与分队：双向范围 + 窗口放宽 + 超时兜底 + 黑店不拆
  - 功能：提供 `Lobby`、`Matchmaker`、`matchQuality`、`estimateWaitMs`
  - 前置依赖：_core  · 分层 L2
  - 接口：2 个类 / 44 个方法 / 13 个类型
    - class `Lobby`
      - `capacity()` — 当前人数 / 上限
      - `size()` — 当前人数 / 上限
      - `isFull()` — 满 / 空
      - `isEmpty()` — 满 / 空
      - `hostId()` — 房主 id，**空房间是 `null`**
      - `state()` — `'open'` / `'starting'` / `'closed'`
      - `revision()` — **版本号，每次状态变化 +1**（脏检查用）
      - `players()` — 玩家列表 / **槽位列表**（含 `ready`、`isHost`、`joinedAt`）
      - `slots()` — 玩家列表 / **槽位列表**（含 `ready`、`isHost`、`joinedAt`）
      - `has()` — 单项查询
      - … 另有 19 个方法
    - class `Matchmaker`
      - `matchSize()` — 一局需要几人（`teamSize × teamsPerMatch`）
      - `queueSize()` — 队列中的**人数**
      - `waiting()` — 等待中的 id 列表
      - `matchesMade()` — 累计撮合成功次数（统计用）
      - `waitOf()` — 某人已等待毫秒（**UI 显示"已等待 1:23"**）
      - `enqueue()` — **这一批人是一支队伍**
      - `enqueueAll()` — 按 `partyId` 分组，无 partyId 的各自独立
      - `dequeue()` — 取消排队 / 清空队列
      - `clear()` — 取消排队 / 清空队列
      - `tick()` — 推进（**撮合在内部自动发生**，没有单独的公开入口）
    - 导出函数
      - `matchQuality()` — 对局质量 **0~1**
      - `estimateWaitMs()` — 估算等待毫秒（UI 显示"预计等待 45 秒"）
      - `balanceTeams()` — 分队（**主力**）
      - `splitIntoTwoTeams()` — 简单对半分（**不保证均衡**）
      - `validateTeamAssignment()` — 校验硬约束（黑店、人数、角色配额）
    - type `LobbyPlayer` — 房间
    - type `LobbyConfig` — 房间
    - type `LobbyState` — 房间
    - type `LobbySlot` — 房间
    - type `LobbyJoinError` — 类型定义
    - type `LobbyJoinResult` — 类型定义
    - type `QueueEntry` — 撮合：队列项 / 配置
    - type `MatchmakerConfig` — 撮合：队列项 / 配置
    - … 另有 5 个类型
- **matchops**
  - 作用：对局运维：重连 / 投降（冻结票数）/ 观战
  - 功能：提供 `ReconnectTracker`、`SpectateSession`、`Surrender`、`shouldAbortMatch`
  - 前置依赖：_core  · 分层 L2
  - 接口：3 个类 / 47 个方法 / 13 个类型
    - class `ReconnectTracker`
      - `register()` — 登记本局玩家
      - `has()` — 判断
      - `stateOf()` — 单项查询
      - `graceFor()` — 单项查询
      - `remainingMs()` — 单项查询
      - `snapshot()` — 全部条目（**做重连倒计时 UI**）
      - `disconnectedCount()` — 统计
      - `forfeitedCount()` — 统计
      - `markDisconnected()` — 标记断线 / 重连
      - `reconnect()` — 标记断线 / 重连
      - … 另有 3 个方法
    - class `SpectateSession`
      - `start()` — 发起 / 投票
      - `end()` — 操作
      - `isActive()` — 对局是否还在进行
      - `join()` — 进出观战
      - `leave()` — 进出观战
      - `switchTarget()` — 切换视角（`number` 选手位 / `'director'`）
      - `count()` — 计算
      - `capacity()` — 观众上限 / 观众列表
      - `delayMs()` — 操作
      - `list()` — 观众上限 / 观众列表
      - … 另有 6 个方法
    - class `Surrender`
      - `setTeam()` — 设置队伍 / **更新在线名单**
      - `setConnected()` — 设置队伍 / **更新在线名单**
      - `markMatchStart()` — 标记开局时刻（**`too-early` 判定靠它**）
      - `start()` — 发起 / 投票
      - `vote()` — 发起 / 投票
      - `tick()` — 推进，返回**本帧新判负的 id 列表**
      - `cancel()` — 取消投票 / 清空状态
      - `status()` — 当前状态（`SurrenderStatus`）
      - `remainingMs()` — 单项查询
      - `state()` — 操作
    - 导出函数
      - `shouldAbortMatch()` — 是否该直接结束对局（而不是继续少打多）
      - `forfeitMultiplier()` — 断线惩罚倍率（**信誉分系统用**）
      - `isDelaySafe()` — 该延迟是否安全（**配置校验用**）
      - `recommendDelay()` — 按游戏类型推荐延迟
      - `pickDirectorShot()` — 导演视角自动切换：**给每个画面打分取最高**
      - `popularityTier()` — 观战人数分档（UI 显示"热门对局"）
      - `formatDelay()` — 延迟格式化成人类可读文本
      - `surrenderDiscount()` — 投降的扣分减免系数
    - type `ReconnectConfig` — `graceMs`（默认 120s）、`maxAttempts`（默认 3）、`te
    - type `ReconnectState` — `'online'` / `'disconnected'` / `'forfeite
    - type `ReconnectEntry` — `id` / `state` / `disconnectedAt` / `attem
    - type `ReconnectVerdict` — 类型定义
    - type `SpectatorVisibility` — 见第 3 节的降级说明
    - type `SpectateConfig` — `delayMs`（默认 120s）、`allVisionMinDelayMs`（默
    - type `Spectator` — 类型定义
    - type `JoinError` — `'full'` / `'duplicate'` / `'not-started'`
    - … 另有 5 个类型
- **mmr（单元名 team-mmr）**
  - 作用：组队匹配分：carry 效应、组队加成、分差限制、散人补齐
  - 功能：提供 `teamMmr`、`partyPenalty`、`validateParty`
  - 前置依赖：_core  · 分层 L2
  - 接口：0 个类 / 6 个方法 / 5 个类型
    - 导出函数
      - `teamMmr()` — 操作
      - `partyPenalty()` — 操作
      - `validateParty()` — 判断
      - `fillFromPool()` — 操作
      - `winProbability()` — 操作
      - `suggestedWindow()` — 操作
    - type `MmrPlayer` — 类型定义
    - type `MmrStrategy` — 类型定义
    - type `TeamMmrConfig` — 类型定义
    - type `TeamMmrResult` — 类型定义
    - type `PartyViolation` — 类型定义
- **ranking**
  - 作用：段位与赛季：掉段保护 / 软重置 / 赛季奖励
  - 功能：提供 `RankProgress`、`SeasonRewardDistributor`、`tierOf`、`romanNumeral`
  - 前置依赖：_core  · 分层 L2
  - 接口：2 个类 / 21 个方法 / 11 个类型 / 1 个常量
    - class `RankProgress`
      - `current()` — 当前段位信息，**保护期内返回"已掉出线但受保护"的状态**
      - `shieldGames()` — 操作
      - `update()` — ranking/RankTier.ts —— 段位与赛季
      - `exportState()` — 存档
      - `importState()` — 存档
    - class `SeasonRewardDistributor`
      - `resolve()` — 按赛季最高段位结算
      - `grant()` — 批量发放
      - `forceGrant()` — **强制补发**（管理员手动干预）
      - `isGranted()` — 该赛季是否已发放过
      - `grantedCount()` — 已发放数量
      - `resetSeason()` — 清空发放记录（**仅限测试/回滚**）
      - `exportState()` — 存档
      - `importState()` — 存档
    - 导出函数
      - `tierOf()` — 分数 → 段位信息
      - `romanNumeral()` — 转罗马数字，**n 是 0-based 索引**（`0 → I`、`1 → II`、`4 → V`）
      - `rankScore()` — 段位 → 可比较的**标量**（**排序用这个**）
      - `softReset()` — 单个分数软重置
      - `softResetAll()` — 批量，返回**新数组，不改原数组**
      - `defaultRankConfig()` — 默认配置
      - `tierDistribution()` — 段位分布统计
      - `diagnoseDistribution()` — 段位分数线是否合理
    - type `TierDef` — 类型定义
    - type `RankTierConfig` — 类型定义
    - type `TierInfo` — 类型定义
    - type `RankEvent` — 类型定义
    - type `RankUpdate` — 类型定义
    - type `SeasonResetConfig` — 类型定义
    - type `RewardTier` — 类型定义
    - type `RewardBasis` — 类型定义
    - … 另有 3 个类型
    - const `DEFAULT_TIERS`
- **social（单元名 report）**
  - 作用：举报：信誉加权、冷却与每日上限、恶意举报识别
  - 功能：提供 `ReportCenter`、`severityWeighted`、`updateCredibility`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 18 个方法 / 5 个类型 / 1 个常量
    - class `ReportCenter`
      - `submit()` — 提交举报，返回 `{ result, ticket }`
      - `weightOf()` — 单条举报的权重（**受举报者信誉影响**）
      - `statsOf()` — 该目标的汇总统计
      - `needsAction()` — 是否已达到处理阈值
      - `remainingToAction()` — 还差多少权重才触发处理
      - `reject()` — 驳回某条举报，返回是否成功
      - `rejectedCount()` — 该举报者被驳回过多少次
      - `isAbusiveReporter()` — **是否属于滥用举报**
      - `clearFor()` — 清除某目标的所有记录，返回清了几条
      - `size()` — 当前票据总数
      - … 另有 6 个方法
    - 导出函数
      - `severityWeighted()` — 操作
      - `updateCredibility()` — 推进/控制
    - type `ReportReason` — 类型定义
    - type `ReportTicket` — 类型定义
    - type `ReportConfig` — 类型定义
    - type `SubmitResult` — 类型定义
    - type `ReportStats` — 类型定义
    - const `REASON_SEVERITY`

---

## 工程与运维（9 个）

> 开发期与线上：调试、埋点、崩溃

- 〔功能模块·开发工具〕3 个单元组合
  - **command**
    - 作用：命令模式 + 撤销重做（事务/合并/回滚）
    - 功能：撤销建造，但资源没退还
    - 功能：撤销删除，但选中状态没恢复
    - 功能：连点两下撤销，状态错乱（没有事务边界）
    - 前置依赖：_core  · 分层 L3
    - 接口：1 个类 / 20 个方法 / 3 个类型
      - class `CommandStack`
        - `canUndo()` — 是否可撤销 / 重做
        - `canRedo()` — 是否可撤销 / 重做
        - `undoDepth()` — 栈深度
        - `redoDepth()` — 栈深度
        - `nextUndoName()` — 下一步的名字（`null` = 没有）
        - `nextRedoName()` — 下一步的名字（`null` = 没有）
        - `inTransaction()` — 是否在事务中
        - `do()` — 执行并入栈（**已在 `do()` 内部执行过**）
        - `undo()` — 撤销 / 重做一步，返回是否成功
        - `redo()` — 撤销 / 重做一步，返回是否成功
        - … 另有 9 个方法
      - 导出函数
        - `setValueCommand()` — 设置
      - type `ICommand` — 类型定义
      - type `CommandStackOptions` — 类型定义
      - type `CommandStackSnapshot` — 类型定义
  - **crash**
    - 作用：崩溃捕获与上报（面包屑/去重/指纹/采样）
    - 功能：提供 `CrashReporter`、`makeFingerprint`、`normalizeMessage`、`safeStringify`
    - 前置依赖：_core  · 分层 L3
    - 接口：1 个类 / 21 个方法 / 6 个类型
      - class `CrashReporter`
        - `setContext()` — 整体替换
        - `setContextValue()` — 单个设置
        - `removeContextValue()` — 单个移除
        - `context()` — 读取（只读）
        - `bindLogSource()` — 绑定日志源（**注入，不直接持有 Logger**）
        - `setMinLogLevel()` — 只收集此级别以上的日志
        - `leave()` — 留痕
        - `breadcrumbs()` — 读取 / 数量
        - `breadcrumbCount()` — 读取 / 数量
        - `clearBreadcrumbs()` — 清空（**换关卡 / 换账号时调**）
        - … 另有 7 个方法
      - 导出函数
        - `makeFingerprint()` — 算指纹（**去重靠它**）
        - `normalizeMessage()` — 消息规范化：抹掉数字与长十六进制
        - `safeStringify()` — 安全序列化（循环引用 / BigInt / Date）
        - `reportText()` — 把报告格式化成可读文本
      - type `ILogEntryLike` — **只依赖 logger 的类型，不 import 实现**
      - type `CrashSeverity` — `'fatal'` / `'error'` / `'warning'`
      - type `Breadcrumb` — 面包屑：`time` / `category` / `message` / `lev
      - type `CrashReport` — 报告主体（含 `severity`、面包屑、指纹等）
      - type `ICrashTransport` — 上报通道接口（自己接 HTTP / 文件就实现它）
      - type `CrashReporterOptions` — 构造配置
  - **debug-console**
    - 作用：运行时调试控制台（参数校验/补全/历史/模糊建议）
    - 功能：提供 `CommandError`、`DebugConsole`、`usageOf`、`tokenize`
    - 前置依赖：_core  · 分层 L3
    - 接口：2 个类 / 19 个方法 / 6 个类型
      - class `CommandError`
      - class `DebugConsole`
        - `enabled()` — 运行时可改
        - `enabledWasExplicit()` — 用户是否显式设置过 enabled（测试与自检用）
        - `register()` — 注册（**返回 `this`，可链式**）
        - `unregister()` — 注销，返回是否真的删了
        - `registerAll()` — 注册（**返回 `this`，可链式**）
        - `has()` — 是否已注册
        - `list()` — 命令列表。**默认不含 `hidden` 的**
        - `onOutput()` — 订阅输出 / 清屏
        - `onClear()` — 订阅输出 / 清屏
        - `execute()` — 执行一行，返回**是否被识别为命令**
        - … 另有 6 个方法
      - 导出函数
        - `usageOf()` — 生成用法串：`give <item:string> [count:int]`
        - `tokenize()` — 分词（**支持引号**）
        - `similarity()` — 编辑距离相似度 **0~1**
      - type `ArgType` — 类型定义
      - type `ArgDef` — 类型定义
      - type `CommandDef` — 类型定义
      - type `ParsedArgs` — 类型定义
      - type `CommandContext` — 类型定义
      - type `DebugConsoleOptions` — 类型定义
- 〔功能模块·运维〕1 个单元组合
  - **diagpack（单元名 diag-pack）**
    - 作用：诊断包：安全序列化、序列化后脱敏、分区配额、永不抛错
    - 功能：提供 `DiagCollector`、`safeStringify`、`redact`、`collectEnvironment`
    - 前置依赖：无  · 分层 L2
    - 接口：1 个类 / 9 个方法 / 6 个类型 / 1 个常量
      - class `DiagCollector`
        - `section()` — 注册分区（**注册时不执行**，`generate()` 时才跑）
        - `removeSection()` — 移除分区，返回是否成功
        - `sectionNames()` — 已注册的分区名列表
        - `generate()` — 生成诊断报告
        - `generateJson()` — 生成并序列化为 JSON 文本
        - `summarize()` — 生成人类可读的摘要
      - 导出函数
        - `safeStringify()` — 操作
        - `redact()` — 操作
        - `collectEnvironment()` — 操作
      - type `SectionName` — 类型定义
      - type `SectionProvider` — 类型定义
      - type `RedactRule` — 类型定义
      - type `DiagPackConfig` — 类型定义
      - type `DiagSection` — 类型定义
      - type `DiagnosticReport` — 类型定义
      - const `DEFAULT_REDACTIONS`
- **analytics（单元名 abtest）**
  - 作用：A/B 实验：稳定分组、实验间独立、显著性检验、样本量预判
  - 功能：提供 `Experiment`、`bucketHash`、`bucketOf`、`assign`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 22 个方法 / 7 个类型
    - class `Experiment`
      - `name()` — 实验名 / **去重后**的参与人数
      - `size()` — 实验名 / **去重后**的参与人数
      - `variantOf()` — 分组查询（**幂等**）
      - `trackBinary()` — 记二值结果
      - `trackValue()` — 记数值结果
      - `result()` — 出结论（`ExperimentResult`）
      - `reset()` — 清空参与记录
    - 导出函数
      - `bucketHash()` — 哈希（带 finalizer，见第 3 节）
      - `bucketOf()` — 分桶到 **0~99**
      - `assign()` — 分配 `'control'` / `'treatment'`
      - `checkBalance()` — 分组均衡性检查
      - `emptyStats()` — 空统计对象
      - `recordBinary()` — 记一次**二值**结果（转化 / 未转化）
      - `recordValue()` — 记一次**数值**结果（时长、金额）
      - `conversionRate()` — 转化率
      - `mean()` — 均值 / **样本方差（除以 n−1）**
      - `variance()` — 均值 / **样本方差（除以 n−1）**
      - … 另有 5 个方法
    - type `ExperimentConfig` — 类型定义
    - type `Variant` — 类型定义
    - type `Stats` — 类型定义
    - type `ZTestResult` — 类型定义
    - type `ExperimentResult` — 类型定义
    - type `BalanceReport` — 类型定义
    - type `ExperimentOptions` — 类型定义
- **anticheat（单元名 anti-cheat）**
  - 作用：反作弊：速度/统计离群/行为指纹三线索，单项证据不足以封号
  - 功能：提供 `SpeedChecker`、`binomialZ`、`isStatisticalOutlier`、`intervalRegularity`
  - 前置依赖：_core  · 分层 L2
  - 接口：1 个类 / 13 个方法 / 6 个类型
    - class `SpeedChecker`
      - `strikes()` — 当前连续异常次数
      - `maxObserved()` — **观测到的最大速度**（调阈值用）
      - `push()` — 上报一次位置，返回 `SpeedViolation` 或 `null`
      - `windowHits()` — 【滑动窗口：抓"隔几帧来一次"的作弊】
      - `reset()` — 重置
    - 导出函数
      - `binomialZ()` — 命中率的 Z 分数（**原则二要用**）
      - `isStatisticalOutlier()` — 是否统计离群
      - `intervalRegularity()` — 点击间隔规律性（**脚本特征**）
      - `looksLikeScript()` — 是否像脚本
      - `suspicionScore()` — 操作
      - `actionFor()` — 分数 → 建议动作
      - `accuracyZ()` — 操作
      - `normalizeScore()` — 归一化到 **0~1**（与 `clamp01` 同义，此处为语义清晰）
    - type `SpeedSample` — `x` / `y` / `t`（毫秒）
    - type `SpeedViolation` — `speed` / `allowed` / `exceeded` / `flagge
    - type `SpeedCheckerConfig` — 类型定义
    - type `CheatEvidence` — `speedViolations?` / `accuracyZ?` / `inter
    - type `SuspicionBreakdown` — `speed` / `accuracy` / `interval` / `repor
    - type `SuspicionResult` — 类型定义
- **cheatcode**
  - 作用：开发期你一定需要：跳关、加钱、无敌、解锁全部。
  - 功能：提供 `CheatCode`、`tokenize`、`levenshtein`
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 16 个方法 / 6 个类型
    - class `CheatCode`
      - `enabled()` — 总开关状态
      - `enabledWasExplicit()` — 用户是否显式设置过 enabled（测试与自检用）
      - `register()` — 注册
      - `registerAll()` — 注册
      - `unregister()` — 注销，返回是否真的删了
      - `has()` — 是否已注册 / 命令列表
      - `commands()` — 是否已注册 / 命令列表
      - `complete()` — 前缀补全候选
      - `help()` — 生成帮助文本。**默认不含 `hidden`**
      - `execute()` — 执行，返回 `ExecuteResult`
      - … 另有 4 个方法
    - 导出函数
      - `tokenize()` — 分词（**支持引号**）
      - `levenshtein()` — 编辑距离（**整数，不是 0~1 的相似度**）
    - type `ArgType` — `'int'` / `'number'` / `'string'` / `'bool
    - type `ArgDef` — `{ name, type?, optional?, default?, desc?
    - type `CommandContext` — `{ args, raw }` —— **`args` 是数组，按位置取**
    - type `CommandDef` — `{ name, aliases?, desc?, args?, hidden?, 
    - type `ExecuteResult` — 类型定义
    - type `CheatCodeOptions` — 类型定义
- **gameflow**
  - 作用：主菜单 → 游戏中 → 暂停 → 结算 → 回主菜单
  - 功能：从暂停直接跳结算，忘了清理战斗数据
  - 功能：结算界面按返回，回到了**还是暂停着的**界面
  - 功能：两次快速点击，同一个状态被进入了两次
  - 前置依赖：无  · 分层 L2
  - 接口：1 个类 / 14 个方法 / 4 个类型
    - class `GameFlow`
      - `current()` — 当前状态 id
      - `context()` — 上下文对象（`when` 回调的入参）
      - `history()` — 历史栈（**有上限，默认 32**）
      - `timeInState()` — 在当前状态停留了多久
      - `states()` — 全部状态定义
      - `isTransitioning()` — 是否正在切换中
      - `get()` — 取某个状态定义（`undefined` = 不存在）
      - `availableTargets()` — 当前状态**所有合法**的下一站
      - `canGoTo()` — 只查询不改变状态
      - `goTo()` — 需要重入用 `{ allowSelf: true }`
      - … 另有 4 个方法
    - type `FlowTransition` — 类型定义
    - type `FlowStateDef` — 类型定义
    - type `GameFlowOptions` — 类型定义
    - type `GoToOptions` — 类型定义
- **telemetry**
  - 作用：游戏上线后你要知道：玩家卡在第几关？哪个遗物从没人选？
  - 功能：一局几百个事件，每个都发请求 → 流量和电量爆炸
  - 功能：网络失败就丢了 → 数据不准
  - 前置依赖：_core  · 分层 L1
  - 接口：1 个类 / 13 个方法 / 4 个类型
    - class `Telemetry`
      - `useClock()` — **注入时间源**
      - `enabled()` — 总开关
      - `sessionId()` — 当前会话 id
      - `buffered()` — 当前缓冲条数
      - `stats()` — 四个计数
      - `willSample()` — 这个事件是否会被采集
      - `capture()` — 上报一条
      - `shouldFlush()` — 是否**该**发了（攒够条数或超时）
      - `flush()` — 尽力发送
      - `newSession()` — 换会话（序号归零）
      - … 另有 2 个方法
    - 导出函数
      - `hashString()` — 字符串哈希到 **[0, 1)**（采样用）
    - type `TelemetryEvent` — `{ name, props, seq, t }`。**`props` 已合并通用属
    - type `TelemetrySender` — `(batch) => Promise<boolean> \
    - type `TelemetryOptions` — 类型定义
    - type `TelemetryStats` — `captured` / `sent` / `failed` / `dropped`

---

## 适配与实体（2 个）

> 轮子之间的轴，以及「这个 id 是谁」

- **adapters**
  - 作用：模块间适配器：地牢→网格 / 命中→施法 / 掉落→入包（消除重复胶水）
  - 功能：提供 `toGrid2D`、`toFlatGrid`、`wallTestFrom2D`
  - 前置依赖：无  · 分层 L2
  - 接口：0 个类 / 8 个方法 / 5 个类型
    - 导出函数
      - `toGrid2D()` — 转换
      - `toFlatGrid()` — 转换
      - `wallTestFrom2D()` — 操作
      - `toCasterHits()` — 全部转换，默认按距离升序
      - `nearestCasterHits()` — 只取最近 N 个
      - `uniqueEntityHits()` — 按实体去重——**同一实体的多个判定框只保留最近的一个**
      - `flattenDrops()` — 操作
      - `applyDropsToInventory()` — 设置
    - type `ITileSource` — 类型定义
    - type `IHitLike` — 类型定义
    - type `ICasterHitLike` — 类型定义
    - type `ILootDropLike` — `id` / `count` / `fromPity?` / `children?`
    - type `DroppedItem` — `id` / `count` / `fromPity` / `path`
- **entity**
  - 作用：统一实体注册表：id 版本号防 ABA / 判定框别名映射 / 死亡归宿
  - 功能：提供 `EntityRegistry`、`EntityIndex`、`idIndex`、`idGeneration`
  - 前置依赖：无  · 分层 L1
  - 接口：2 个类 / 35 个方法 / 6 个类型 / 2 个常量
    - class `EntityRegistry`
      - `spawn()` — 注册，返回 id
      - `get()` — 取记录（后者排除尸体）
      - `getAlive()` — 取记录，排除尸体
      - `getEntity()` — 取业务对象
      - `exists()` — 存在 / 存活
      - `isAlive()` — 存在 / 存活
      - `fromAlias()` — 判定框 id → 实体 id
      - `entityFromAlias()` — 判定框 id → 业务对象
      - `fromCollider()` — 碰撞体 id → 实体 id
      - `addTag()` — 单个标签
      - … 另有 17 个方法
    - class `EntityIndex`
      - `bind()` — 绑定
      - `idOf()` — 取 id，**未绑定返回 `INVALID_ID`**
      - `unbind()` — 解绑，返回是否真的解了
      - `has()` — 是否已绑定
    - 导出函数
      - `idIndex()` — id 编解码
      - `idGeneration()` — id 编解码
      - `makeId()` — id 编解码
      - `isValidId()` — 合法性（不含存在性）
    - type `DeathReason` — 类型定义
    - type `EntityRecord` — 类型定义
    - type `SpawnOptions` — 类型定义
    - type `DeathHandler` — 类型定义
    - type `SpawnHandler` — 类型定义
    - type `DestroyHandler` — 类型定义
    - const `SLOT_CAPACITY`
    - const `INVALID_ID`

---

## 视图二：按依赖前置（被依赖者为父，依赖者为子）

> 这一视图回答：**改了某个模块，会波及谁**。
> 依赖数据由扫描源码 `import` 得出，不是 `_kitmeta.json` 的 `depends` 字段——
> 后者有 72 个单元登记为空，与实际不符。

```text
依赖树（共 75 条依赖边，45 个单元无前置依赖）

_core（被 72 个单元依赖）
   ├─ affix
   ├─ analytics
   ├─ anticheat
   ├─ attack-token
   ├─ audio
   ├─ autoquality
   ├─ behavior-tree
   ├─ buff
   ├─ builder
   ├─ bullet-pattern
   ├─ camera
   ├─ card
   ├─ cheatcode
   ├─ collision
   ├─ combo
   ├─ command
   ├─ craft
   ├─ crash
   ├─ currency
   ├─ cutscene
   ├─ daily
   ├─ damage-pipeline
   ├─ dash
   ├─ debug-console
   ├─ di
   ├─ difficulty
   ├─ element
   ├─ elo
   ├─ expression
   ├─ feedback
   ├─ fsm
   ├─ gacha
   ├─ grid
   ├─ hitbox
   ├─ indicator
   ├─ input
   ├─ inventory
   ├─ joystick-mover
   ├─ leaderboard
   ├─ logger
   ├─ loot
   ├─ matchmaking
   ├─ matchops
   ├─ minimap
   ├─ mmr
   ├─ mover
   ├─ number-roller
   ├─ objective
   ├─ perception
   ├─ progressbar
   ├─ projectile
   ├─ ranking
   ├─ replay
   ├─ room-graph
   ├─ scenerouter
   ├─ scheduler
   ├─ scheduling
   ├─ score
   ├─ scoring
   ├─ skill-caster
   ├─ skill-player
   ├─ skill-queue
   ├─ snapshot
   ├─ social
   ├─ steering
   ├─ targeting
   ├─ telegraph
   ├─ telemetry
   ├─ transition
   ├─ tutorial
   ├─ tween
   └─ wave-spawner

ds（被 2 个单元依赖）
   ├─ dungeon
   └─ pathfind

skill-caster（被 1 个单元依赖）
   └─ skill-queue

无前置依赖的单元（45 个）：
  _core、accessibility、achievement、adapters、attribute、binary
  blessing、condition、config、curse、curve、diagpack
  dialogue、ds、entity、event-bus、fov、gameflow
  gesture、i18n、interact、meta、noise、observable
  pathfinding、pool、quest、rarity、rebind、reddot
  result、rng、runscope、save、setbonus、settings
  shop、signal、skill-variant、spatial、stats、subtitle
  timeutil、turn、uinav
```
