# 异常数值穿透修复计划

> 来源：四份《逐模块精审报告》（第一批 ~ 第四批，基线 v0.23.0）
> 核实环境：v0.35.0（与报告基线差 12 个版本，**所有结论均已重新实测**）
> 核实日期：2026-09-06

---

## 一、核实结论

### 1.1 六条核心缺陷实测坐实

| # | 缺陷 | 报告定位 | v0.35.0 实测 | 证据 |
|---|---|---|---|---|
| **P0-1** | `room-graph` depth=Infinity | OOM 崩溃 | ✅ **坐实** | 进程 13s 内堆涨到 2GB 被杀 |
| **P1-1** | `mover` 坐标永久 NaN | 角色消失 | ✅ **坐实** | `x=NaN`，再正常 100 帧仍 NaN |
| **P1-2** | 角度归一化死循环 | `perception` | ✅ **坐实，且根因更深** | 见 1.2 |
| **P1-3** | `buff` 死循环 | 全库唯一无 guard | ✅ **坐实** | 退出码 124（超时） |
| **M2-1** | `command` limit=NaN | 栈无上限 | ✅ **坐实** | `_limit=NaN` |
| **M2-2** | `replay` maxFrames=NaN | 录制无上限 | ✅ **坐实** | `_maxFrames=NaN` |

### 1.2 比报告更深一层的发现

报告把「角度归一化死循环」定位在 `perception/Perception.ts:532`。
**实测根因在 `_core/math.ts:101`**：

```typescript
export function normalizeAngleRad(a: number): number {
  let x = a;
  while (x > Math.PI) x -= Math.PI * 2;     // ← 无 guard
  while (x < -Math.PI) x += Math.PI * 2;    // ← 无 guard
  return x;
}
```

`Infinity - 2π === Infinity` → 循环条件恒真 → **永不退出**（实测退出码 124）。

**影响面从 1 个模块扩到所有调用方**：

```
normalizeAngleRad(Infinity)   → 死循环
angleDiffRad(from, Infinity)  → 死循环（内部调用 normalizeAngleRad）
```

`perception` 只是暴露点之一。修在 `_core` 才能一次根除。

有限值行为正常（`0 → 0`、`100 → -0.5309`）、`NaN` 不死循环（返回 NaN，属静默失效）。

### 1.3 与报告数字有出入的部分（如实记录）

| 项 | 报告 | 本次扫描 | 说明 |
|---|---|---|---|
| dt 守卫缺失 | **29 处** | **9 处** | 我的正则更窄：只匹配 `tick/update/advance/step` 且 `dt` 参与算术 |
| `Math.max(常数, opts.x)` | **42 处** | **185 处** | 我的正则更宽：含 `Math.max(1, x ?? n)` 各种变体 |
| 循环上界非 `.length` | 30 处 | 239 处（含 while 无 guard **70 处**） | 同上 |

**两个数字都不要当准数**。真实值取决于「什么算命中」，而这一点没定义清楚。
**以实际修复时逐处核对为准。**

### 1.4 本次扫描命中的 9 处 dt 守卫

| 模块 | 位置 | 现状 | 盲区 |
|---|---|---|---|
| `mover` | `CharacterMover.ts:311` | `dt <= 0` | NaN / Infinity 穿透 |
| `camera` | `CameraFollow.ts:263` | `dt <= 0` | 同上 |
| `skill-caster` | `SkillCaster.ts:595` | `dt <= 0` | 同上 |
| `element` | `ElementSystem.ts:357` | `!(dt > 0)` | Infinity 穿透 |
| `progressbar` | `ProgressBar.ts:268` | `!(dt > 0)` | 同上 |
| `input` | `InputBuffer.ts:337` | **无守卫** | 全穿透 |
| `projectile` | `Projectile.ts:412` | **无守卫** | 全穿透 |
| `skill-player` | `Cooldown.ts:76` | **无守卫** | 全穿透 |
| `tween` | `Tween.ts:130` | **无守卫** | 全穿透 |

---

## 二、修复策略

### 核心决策：用工具函数收口，而不是逐处手写

**理由**：这个模式已在不同模块独立出现 9+ 次，靠人工 review 杜绝不了。
每处手写 `if (!Number.isFinite(dt) || dt <= 0) return;` 早晚又会漏。

在 `_core` 提供两个函数，之后所有点统一调用：

```typescript
/** dt 守卫：非有限值或非正数时返回 false */
export function safeDt(dt: number): boolean {
  return Number.isFinite(dt) && dt > 0;
}

/** 配置项兜底：非有限值时回退到默认值，并钳制到 [min,max] */
export function clampNum(
  v: unknown, min: number, max: number, fallback: number
): number {
  const n = Number(v);
  if (!Number.isFinite(n)) return fallback;
  return Math.min(max, Math.max(min, n));
}
```

### 死循环用「改算法」而不是「加 guard」

`normalizeAngleRad` 的循环改成 **O(1) 取模**，天然无死循环可能：

```typescript
const TAU = Math.PI * 2;
export function normalizeAngleRad(a: number): number {
  if (!Number.isFinite(a)) return 0;              // 与"入口校验"方案一致
  return ((a + Math.PI) % TAU + TAU) % TAU - Math.PI;
}
```

比加 guard 更优：**不存在"guard 上限设多少才够"的问题**。

### OOM 用「入口校验 + 上界」双重

```typescript
if (!Number.isFinite(totalDepth) || totalDepth < 3) throw ...;
if (totalDepth > 1000) throw ...;   // 防 1e9 撑爆内存
```

---

## 三、分批计划

### ✅ 批次 1 — 崩溃与死循环（P0/P1，4 处） **已完成**

| # | 位置 | 改法 | 状态 |
|---|---|---|---|
| 1 | `_core/math.ts` normalizeAngleRad | 改 O(1) 取模 + 非有限值返回 0 | ✅ |
| 2 | `buff/BuffSystem.ts` update | `safeDt` 守卫 + while 加 `guard++ < 64` | ✅ |
| 3 | `room-graph/RoomGraph.ts` depth | `isFinite` + 上界 1000 | ✅ |
| 4 | `mover/CharacterMover.ts` update | `safeDt(dt)` | ✅ |

**实测验收（全部通过）**：

```
room-graph depth=Infinity  → 抛"必须为有限值"（原 OOM）
room-graph depth=1001      → 抛"层数过大"（新增上界）
normalizeAngleRad(Infinity) → 0（原死循环，退出码 124）
buff.update(Infinity)       → 正常返回，onTick 被截断到 64 次
mover.update(NaN)           → 坐标不变，之后能继续正常移动
```

**反向验证（注入故障确认测试有效）**：

```
回退 4 处修复 → 测试精准失败 4 项 + buff 死循环退出码 124
  ✗ 非有限角度返回 0，不死循环
  ✗ 异常 dt 不改变任何状态（mover）
  ✗ 非有限 depth 抛错（room-graph）
  ✗ 超上界 depth 抛错（room-graph）
恢复后 3304 项全通过
```

**新增测试**：`tests/run_dtguard.ts`（11 项），已接入 `npm run verify`。

**三处行为变更已写入文档**（用户可见）：

| 模块 | 变更 |
|---|---|
| `buff` | 长卡顿帧的 onTick 截断在 64 次（`tickInterval=0.1` 时是 6.4 秒） |
| `room-graph` | `depth > 1000` 现在会抛错（原本允许） |
| `_core` | `normalizeAngleRad(±π)` 现在返回 `-π`（原 `+π`）；两处调用方都用 `Math.abs`，无影响 |

### ✅ 批次 2 — dt 守卫统一 **已完成**

最终落地 **29 个模块**（多于 1.4 节扫描出的 9 处——扫描器有盲区，见下）。

**两种写法，按方法结构选，不能一刀切**：

| 写法 | 适用 | 判据 |
|---|---|---|
| `if (!safeDt(dt)) return;` | 方法开头就是时间推进 | 提前 return 不会跳过任何输出 |
| `const step = safeDt(dt) ? dt : 0;` | 方法开头有**每帧必须执行**的逻辑 | 提前 return 会破坏语义 |

用第二种的有 4 处，都是因为开头有每帧输出要清零：

```
dash         _dx/_dy 是每帧位移增量，提前 return 会残留 → 角色自己滑一段
CameraShake  _ox/_oy/_orot 同理，提前 return → 镜头歪着回不来
fsm          _timeInState 之后还要把 dt 透传给业务 update
number-roller  _current 是本帧显示值，应保持上次值而不是整个跳过
targeting    _grace 递减在方法中段，提前 return 会跳过目标切换
```

**两处「看起来有守卫、实际挡不住」**：

```typescript
if (dt > 0) this._startTime += dt;   // Infinity > 0 为 true → 计时变 Infinity
```

出现在 `scenerouter` / `score` / `tutorial`，已改为 `safeDt(dt)`。
`NaN > 0` 为 false 所以能挡住 NaN，但挡不住 Infinity——
这种"挡了一半"的守卫比没有守卫更容易让人误以为安全。

**验收**：29 个模块 × (5 种异常 dt × 状态不变 + 异常后可恢复)，共 91 项，见 `tests/run_dtguard2.ts`。

**反向验证**：把 `safeDt` 改成恒 `true` 后 **56 项精准变红**，覆盖全部 29 个模块；还原后 3395 项全绿。

> 【注】3395 是当时的测试总数，后续批次补充后已增至 3447。
> 此处保留原始数字，是为了让「56 项变红」这个判据可追溯——
> 数字会随时间变化，但「反向验证必须变红」这条规则不变。
（首次反向验证只红了 8 项——当时 9 个模块只加了 `import { safeDt }` 而守卫代码没写，
编译期因"已导入未使用"报错但 JS 仍生成，于是测试全绿而守卫根本不存在。
**编译报错但产物照出，会让"测试通过"变成假信号。**）

**扫描器的盲区**（1.4 节只报 9 处的原因）：

- 返回类型是对象联合时正则提前终止（`{v:number} | null`）
- 只扫 `update/tick/step` 三个约定名，其他命名漏掉

所以最终修的是 29 处而不是 9 处。**扫描器用于兜住新增代码，不用于确认存量已清零。**

### ✅ 批次 3 — 上限字段（M2，2 处 + 工具） **已完成**

| # | 位置 | 改法 | 状态 |
|---|---|---|---|
| 1 | `command/CommandStack.ts:116` | `clampNum(opts.limit, 1, 1e6, 100)` | ✅ |
| 2 | `replay/ReplayRecorder.ts:115` | `clampNum(opts.maxFrames, 1, 1e7, 200_000)` | ✅ |
| 3 | `_core/math.ts:49` 新增 `clampNum` | — | ✅ |

**为什么 `Math.max` 兜底挡不住 NaN**：

```typescript
Math.max(1, opts.limit ?? 100)   // limit=NaN → Math.max(1, NaN) === NaN
```

兜底对 NaN 完全无效。裁剪判定 `length > NaN` 恒为 false → 栈永不裁剪。
表现不是崩溃，是**内存随操作次数无限增长**。

`clampNum` 先判 `Number.isFinite` 再裁剪，非有限值直接回退默认值。

**验收**：`limit=NaN` → 100（栈会裁剪）；`maxFrames=NaN` → 200000。

### ✅ 批次 4 — 静态扫描规则固化 **已完成**

落地 `scripts/scan-dt-guard.py`，已接入 `verify.sh` 第 11 步。

**只在存量清零后才接入**——接入前跑过存量确认 0 处命中，
所以现在的 0 是"真的没有"，不是"脚本没在工作"。

**接入前做过注入验证**（永远返回 0 的检查等于没有检查）：

```
把 score 的 safeDt(dt) 改回 dt > 0  → 精准报出 1 处（score/ScoreSystem.ts:227）
还原                                 → 归零
```

**已知盲区**（写在脚本文件头）：返回类型为对象联合时正则提前终止。
所以**扫描器用于兜住新增代码，不用于确认存量已清零**——
批次 2 最终靠人工 + 测试覆盖到 29 个模块，远超扫描器报出的 9 处。

原计划的后两条规则（循环上界 `isFinite`、`while` 无 guard）**未做**：
批次 1 已用"改算法"根治了死循环（`normalizeAngleRad` 改 O(1) 取模、
`buff` 加 `guard++ < 64`、`room-graph` 加 `isFinite` + 上界），
存量已清零，再加扫描器的边际收益低于维护成本。

### 批次 5 — `Math.max` 系统性替换（**已完成**）

不做 185 处一次性替换，只处理**参数可能来自外部输入**的那一批。

**实际处理 63 处**（不是 185，也不是报告说的 29/42——那两个数字口径不同），分两类改法：

| 类型 | 处数 | 改法 | 理由 |
|---|---|---|---|
| 容量/上限类 | 23 | `clampNum(v, min, max, fb)` | Infinity 让裁剪判定恒 false → 内存无限增长 |
| 普通配置类 | 40 | `numOr(v, fb)` + 原 `Math.max/min` | 定上界会误伤合法大值（`transitionMs: 600_000`） |

**为什么分两种**：给普通配置定上界是"猜业务上限"，猜错就静默改掉用户配置，
比 NaN 穿透更隐蔽。所以脚本只强制"必须用这两个函数之一"，不强制选哪个。

**固化**：`scripts/scan-num-guard.py`，已接入 `verify.sh` 第 12 步。
接入前存量 0，注入探针验证过四种反向用例（numOr 已包裹 / 非外部来源 /
行注释 / 块注释）均正确放过。

**回归**：`tests/run_numguard.ts` 34 项。反向验证（把两个函数退化成
`return Number(v)`）后 **64 项精准变红**，还原后全绿。

---

### ✅ 批次 6 — 批次 5 之后的补充项（**已完成**）

批次 5 收尾后，又逐条复核了当初"已知不修"清单里的 4 项，
结果 **3 项被推翻处理、1 项维持原判**（详见第五章）。本批记录这些补充改动。

| 项 | 处理 | 测试 | 位置 |
|---|---|---|---|
| `gesture` 异常时间戳 | ✅ 改代码（`dtValid`） | 4 项 | `tests/run_batch13.ts:2214~2250` |
| `scoring` 权重 NaN | ✅ 改代码（构造期抛错） | 2 项 | `tests/run_batch9.ts:1756 / 1776` |
| `loot` 坏 rng | ✅ 只补文档（不改代码） | — | `loot/README.md` |
| `expression` `maxDepth` 非法值 | ✅ 补测试（实现早已收口） | 1 项 | `tests/run_fixregress.ts:1321` |

**`gesture` 的具体症状**（4 项测试覆盖 4 种坏输入）：

```typescript
// 修复前：Math.max(1, NaN) === NaN
//   → speed = path / NaN = NaN
//   → 所有速度比较恒 false → 手势识别静默失效，永远识别不出 swipe
const dtValid = Number.isFinite(rawDt) && rawDt > 0;
const dt = dtValid ? Math.max(1, rawDt) : 0;
const speedOf = (d: number): number => (dtValid ? d / dt : 0);
```

四种坏输入：`NaN` / `Infinity` / **时间戳反了** / **全相同**。
后两种不是"脏数据"，是**正常业务场景**（数据乱序、单次点击），
但同样会让 speed 失真，所以一并处理。

**`expression` 这条值得单独说**：它属于"**早已修好但测试没锁住**"。
`maxDepth` 早就是 `clampNum` 收口的，深度测试也有 9 项——
但那 9 项只覆盖了 0 和负数，**没覆盖 NaN / ±Infinity**。
而 `depth > NaN` 恒为 false，一旦 `maxDepth` 变 NaN，
深度检查**彻底失效**，几千项式子会直接 RangeError 栈溢出。
本批补 1 项测试堵住这个缺口。

> 顺带纠一个我自己的错：我曾判断"Expression 深度上限没有测试"。
> 实际有 9 项，只是 describe 标题是中文「表达式 · 深度上限」，
> 而我 grep 时只搜了英文 `Expression` —— **中英混用的库，
> 只 grep 一种语言就会漏检**。

---

## 四、验证方法

### 每条修复必须配反向验证

这是我们在多轮修复中确立的做法——**补完测试后临时回退修复，确认测试变红**。

```
注入故障 → 测试应失败 → 恢复 → 测试应通过
```

只加测试不回退验证，等于不知道测试有没有真的在测。

### 死循环测试必须用子进程 + 超时

死循环会让测试进程永久卡住，不能直接在主测试进程里跑：

```bash
timeout 5 node check_xxx.js
echo $?    # 124 = 超时 = 死循环
```

### OOM 测试要限制内存

`room-graph` 的 OOM 会吃掉 2GB。测试时用 `--max-old-space-size=256` 限制，
避免把开发环境搞挂。

---

## 五、已知不修的项（及理由）

### 5.1 最终状态：4 项中 3 项已处理，仅 1 项维持不修

这张清单写于计划制定时，当时判断"发现问题但不值得在模块内修"。
后续逐条复核时，**其中 3 项被推翻**，最终只有 1 项维持原判。

| 项 | 原判定 | 最终状态 | 变更原因 |
|---|---|---|---|
| `gesture` `Math.max(1, dt)` | 列入批次 5 | ✅ **已修** | 本就不是"不修"，只是排期靠后 |
| `scoring` weight=NaN | 侥幸安全，不修 | ✅ **已修** | **原判定错误**，见 5.2 |
| `loot` rng 返回异常值 | 不修，可补文档 | ✅ **文档已补** | 维持"不改代码"，但按原计划补了文档 |
| `curve.evaluate(NaN)` | 不修 | ⏸ **维持不修** | 理由见 5.3 |

---

### 5.2 记录一次判断被推翻：`scoring` 不是"侥幸安全"

**原判定的推理链**：

```
末尾有 `totalWeight > 0 ? ... : 0`
  → NaN 会让 totalWeight 变 NaN，NaN > 0 为 false
    → 走 else 分支返回 0
      → "没有实际危害，侥幸安全"
```

**这个推理漏了一层**——运行期还有一处兜底：

```typescript
numOr(def.weight, 1)   // NaN → 静默改成 1
```

所以真实行为不是"返回 0"，而是：

| 现象 | 原以为 | 实际 |
|---|---|---|
| 构造期 | 放行 | 放行 |
| 运行期权重 | — | **NaN 被静默改成 1** |
| 总分 | 异常（0） | **看起来正常** |
| 报错 | 无 | 无 |

**危害等级完全变了**：原判定认为"无实际危害"，实际是**静默改配置**——
`weight: total / count`（count 为 0 → NaN）这种写法，
构造不报错、运行不报错、总分看起来正常，但权重结构已经错了。

这比直接抛错难查得多，属于我们一直在抓的**静默降级**族。

**改法**：构造期校验 `Number.isFinite && >= 0`，非法值直接抛错。
测试 2 项，其中一项专门防"矫枉过正"（未填 / 正常值不受影响）。

**留着这段记录的价值**：它比结论本身更有用。
"侥幸安全"这种判断**只扫了一层兜底就下结论**，
而真正的失效路径藏在第二层兜底里。
以后再遇到"反正有兜底"的推理，要问一句：**兜底把值改成了什么？**

---

### 5.3 维持不修：`curve.evaluate(NaN)`

| 维度 | 说明 |
|---|---|
| 现象 | 传入 NaN 时返回 NaN，一路传播 |
| 为什么不修 | 入参防护属**调用方责任** |
| 已有防护 | 构造期校验已很严格（控制点坐标、数量均校验） |
| 若加防护的代价 | 返回 0 会把"算错了"伪装成"值为 0"，更难发现 |

**判定依据**：这里 NaN 传播是**可见的**（画布上不出现或位置异常），
而返回 0 是**不可见的**。保留可见的失败，优于制造静默的降级。

这与 `safeDt` 系列不同：dt 是**每帧自动传入**的引擎时间，
异常值来自环境而非调用方意图；`evaluate` 的入参是**调用方显式写出**的。

---

### 5.4 `loot` 坏 rng：维持不改代码，文档已补

**不改代码的理由**：`IRandomSource` 契约要求返回 `[0,1)`，
防的是"注入了坏依赖"，属调用方责任。
若在库内给每个返回值加校验，等于**替调用方隐藏集成错误**。

**已补的文档**（`loot/README.md`）：

- 症状表新增一行：「注入的 rng 返回 NaN」→「掉落永远为空 / PRD 永远不中，且不报错」
- 新增「坏 rng 的症状：静默失效」小节
- 附排查代码片段

**为什么这比加校验更有用**：症状是"永远不中"，
没人会想到是 rng 的问题。文档能让人在排查时
**第一时间想到检查 rng**，而加校验只是让"永远不中"变成"永远不中但不报错"——
一样的静默，还多了一层无用的防御。

---

## 六、诚实声明

### 6.1 本计划的覆盖范围（更新至 v0.39.0）

- 起步时**只核实了 6 条核心缺陷 + 9 处 dt 守卫位置**
- 批次 1~6 完成后，实际处理范围扩大到：
  **4 处崩溃/死循环 + 29 个模块 dt 守卫 + 63 处配置项收口 +
  4 项原"已知不修"复核 + `expression` 深度上限补测**
- 四份报告中的其余条目（约 40 条 S3/M2）**未逐条重新实测**，
  其结论基于 v0.23.0，**可能已被修复，也可能仍存在**
- 扫描数字与报告有出入（见 1.3），**以逐处核对为准**
- `perception` 本体未实测（根因已在 `_core` 证实，修 `_core` 即可覆盖）

### 6.2 本轮暴露的自身方法缺陷

这些不是"库的缺陷"，而是**我在核实与修复过程中犯的错**。
记下来的价值高于结论本身——它们会重复发生。

| # | 错误 | 后果 |
|---|---|---|
| 1 | 断言`maxDepth` 无测试，实际有 9 项 | 差点重复造轮子；根因是**只 grep 英文**漏掉中文 describe |
| 2 | 判断 Expression"深度上限未修" | 实际早已修好；**没验证就沿用旧结论** |
| 3 | 判定 `scoring` "侥幸安全、无危害" | 实际是**静默改配置**；只看了一层兜底就下结论 |
| 4 | 报"3451 项测试" | 实测 3447；**凭印象报数** |
| 5 | README 三处测试数各不相同（3447/3395/3190） | 文档自相矛盾；缺乏单一事实来源 |

**贯穿这几条的教训**：
"文档里的数字"和"我说过的话"都不能作为依据，**只有重新跑出来的才算**。
本计划里凡是标注「实测」的，都注明了样本量或来源行号，就是为了能证伪。

### 6.3 已知未做

- `curve.evaluate(NaN)` 维持不修（理由见 5.3）
- 表达式栈溢出的**根治**（改迭代实现）——仅在 5000+ 项表达式时触发，
  当前靠 `maxDepth` 在安全区内给明确报错；要彻底解决需重写 parser
- 引擎侧实测（需本地 Cocos Creator）
