# cocos-kit 引擎实测任务（第四轮）

> **面向**：通过 Cocos MCP 在真实 Creator 环境执行
> **对应版本**：v0.39.0（实测执行于 v0.34.0 包，结论已回填）
> **生成日期**：2026-09-06
> **实测回填**：2026-09-06（第四轮报告已回，B 组与 A-4 结论已并入）
> **执行前必读**：第 2 节的「基线核对」——数字对不上就先别往下测

---

## 这份文件的可信度从哪来

**里面所有 TypeScript 代码片段，我都在 Node 侧跑过一遍才写进来。**

理由：这个项目反复栽在"测试代码里的方法名是猜的"。
写这份文件时我又猜错了 4 次（`TierInfo.name` 实际是 `.label`、
`describe()` 不是 `debugDump()`、`DiagPack` 不叫 `DiagnosticsPack`、
`DailyModifier` 必填 `effects`）——**全被编译器拦下**。

如果我不实跑，这些错会原样交给你，你会以为是自己环境的问题。

**Node 侧已验的项，本文给出「期望值」；真机结果不符即为差异，需要记录。**

---

## 零、执行前必做（三件事）

### 0.1 基线核对

```bash
npm install
./verify.sh
```

| 项 | 期望值 |
|---|---|
| 测试通过数 | **3,395 项**，失败 0 |
| 示例可运行 | 21 / 21 |
| 检查项 | 依赖 / 断链 / 同名导出 / 随机源 / 实测数字 / 文档反查 全绿 |

**⚠️ 如果测试数不是 3447，先别往下测。**

历史数字参考：3395 → 3447（批次 5 及后续补充）。
若数字对不上，最常见原因是 `.build/` 里只有部分产物（未全量重编），
重新 `npm install && npm run build` 后再确认。
数字对不上 = 编译产物不是最新的 = 测出来的结论不可信。

### 0.2 不要把这份文件里的代码粘进生产项目

这些片段是**验证用的探针**，里面有 `console.log`、有副作用、有临时变量。
验完就删。

另外，片段里的 import 路径写的是 `cocos-kit/xxx/yyy`，
**这是示意路径**，实际要按你自己项目里的放置位置改
（比如你把它放 `assets/scripts/` 下，就得写 `./cocos-kit/xxx/yyy` 或配好的别名）。

### 0.3 三类信息必须原样记录

1. **实际输出**（不要只写"通过/失败"）
2. **环境信息**（Canvas 配置、DPR、时区、Cocos 版本）
3. **复现步骤**（失败时）

---

## 一、A 组 · 已修复，需真机验证（最重要）

这四项我在 Node 侧都改完并补了回归测试。
**但改动涉及运行时行为，需要真机确认没有环境差异。**

---

### A-1 【P0】`debug-console` 的 `enabled` 开关

**背景**：外部安全审查发现，这个模块原先**完全没有开关**
（源码里 0 处 `enabled` / `permission`）。
只要创建了实例，任何注册过的命令就一定能执行。
现在补了 `enabled`，默认 `true`（改默认值是破坏性变更）。

**代码**：

```typescript
import { DebugConsole } from 'cocos-kit/debug-console/DebugConsole';

let ran = 0;
const c = new DebugConsole({ enabled: false });
c.register({ name: 'boom', help: 'x', run: () => { ran++; return 'ok'; } });

const r1 = c.execute('boom');
// 【必须立刻取值】见下方 ⚠️
const snapRan1 = ran;
const snapHist1 = c.history.length;

c.enabled = true;
const r2 = c.execute('boom');
const snapRan2 = ran;

c.enabled = false;
const r3 = c.execute('boom');
const snapRan3 = ran;
const snapHist3 = c.history.length;
```

**⚠️ 一个我踩过的坑（会让你得出完全相反的结论）**

我第一版写成"先做完所有操作，再统一打印"：

```typescript
const r1 = c.execute('boom');   // 关闭时
c.enabled = true;
c.execute('boom');              // 开启后
console.log(ran);               // ← 读到的是 1，不是 0！
```

结果打印出 `ran = 1`，看起来像"关闭了却仍执行"——**差点误报为 bug**。
实际是打印时机太晚，读到的是后面那次成功执行的计数。

**必须在每步操作后立刻快照。**

**期望值（Node 侧实测）**：

| 检查 | 期望 |
|---|---|
| `r1`（关闭时 execute） | `false` |
| `snapRan1`（run 被调用次数） | `0` |
| `snapHist1`（history.length） | `0` ← **历史也不记录** |
| `r2`（开启后 execute） | `true` |
| `snapRan2` | `1` |
| `r3`（再关闭） | `false` |
| `snapRan3` | `1`（没再涨） |
| `snapHist3` | `1` |

---

### A-2 【P0】`cheatcode` 的未显式传参警告

**背景**：同上，默认开启会把作弊码带到线上。
但**默认值没改成 `false`**——那是破坏性变更，
现有用户的作弊码会**静默失效且无提示**。

改成：只有「没显式传 `enabled` 且最终为 `true`」时警告一次。
**显式传 `true` 的人说明想清楚了，不打扰。**

**代码**：

```typescript
import { CheatCode } from 'cocos-kit/cheatcode/CheatCode';

// 拦截 console.warn 来计数
const warns: string[] = [];
const orig = console.warn;
console.warn = (...a: any[]) => { warns.push(a.join(' ')); };

const k1 = new CheatCode({});
const k2 = new CheatCode({ enabled: true });
const k3 = new CheatCode({ enabled: false });

console.warn = orig;
```

**期望值（Node 侧实测）**：

| 检查 | 期望 |
|---|---|
| `warns.length` | `1`（只有 `{}` 那次） |
| `k1.enabled` / `k1.enabledWasExplicit` | `true` / `false` |
| `k2.enabled` / `k2.enabledWasExplicit` | `true` / `true` |
| `k3.enabled` / `k3.enabledWasExplicit` | `false` / `true` |

**同时验 `DebugConsole` 的同一行为**（A-1 的代码里补三行）：

```typescript
const c2 = new DebugConsole({});                  // 应警告
const c3 = new DebugConsole({ enabled: true });   // 不应警告
const c4 = new DebugConsole({ enabled: false });  // 不应警告
```

期望：`c2.enabledWasExplicit === false`，`c3` / `c4` 均为 `true`。

---

### A-3 【P1】`ranking` 段位往返

**背景**：`exportState()` 原先存的是 `floor`（段位下限），两个后果：

1. 小段**降一级**（白银 I → 白银 II）—— `tierOf(floor)` 有浮点误差
2. 段内**进度归零**（1600 分存成 1583.33，恢复后 progress 从 20% 变 0%）

现在改存**玩家真实分数**。

**代码**：

```typescript
import { RankProgress, defaultRankConfig } from 'cocos-kit/ranking/RankTier';

const cfg = defaultRankConfig();
const rp = new RankProgress(cfg, 1200);
rp.update(1150);
const st = rp.exportState();

console.log('label    =', rp.current!.label);
console.log('floor    =', rp.current!.floor);
console.log('progress =', rp.current!.progress.toFixed(4));
console.log('state    =', JSON.stringify(st));

const rp2 = new RankProgress(cfg);
rp2.importState(st!);
console.log('还原 label    =', rp2.current!.label);
console.log('还原 progress =', rp2.current!.progress.toFixed(4));

// 旧存档兼容：老版本存的是 floor
const rp3 = new RankProgress(cfg);
rp3.importState({ rating: rp.current!.floor, shield: 3, belowLine: true });
console.log('旧存档 label  =', rp3.current!.label);
```

**期望值（Node 侧实测）**：

```
update(1150):
  current.label    = 青铜 I
  current.floor    = 800
  current.progress = 0.8750
exportState()      = {"rating":1150,"shield":3,"belowLine":true}
                     ↑ rating 是【真实分数 1150】，不是 floor 800
importState 后:
  current.label    = 青铜 I
  current.progress = 0.8750
段位一致 = ✅
进度一致 = ✅

旧存档兼容（存 floor=800）:
  label = 青铜 I   ← 不能降级
```

**注意字段名**：是 `TierInfo.label`，**不是** `.name` / `.divisionLabel`。

---

### A-4 【P2】`steering` 分离间距数量级

Node 侧实测（8 单位 × 8 种子 × 180 帧，`Flock` 默认参数）：

| 配置 | 末帧最近间距 | 均值 |
|---|---|---|
| **有分离**（`separationWeight=1.5`） | 12.33 ~ 19.60 | **17.64** |
| **无分离**（`separationWeight=0`） | 0.03 ~ 1.22 | **0.37** |

真机跑群集时**顺带看一眼数量级**。
如果明显不是 17 量级，说明我的积分方式
（力 → 加速度 → 速度 → 位置，dt=1/60）与引擎侧惯用写法不同，需要对齐口径。

> 顺带说明：这个数字上一版文档写的是 **1.77**（差 10 倍，小数点写错）。
> 它在文档里躺了很久没人发现——**因为没标样本量，无法复现、也就无法证伪**。

### ✅ 实测结论（2026-09-06，Cocos Creator 3.8.8）

实测 **92.49**（有分离）/ 77.14（无分离），与 Node 侧的 17.64 差 5 倍。
**判定：积分方式一致，不是 bug。** 但原因值得记一笔——

**Node 侧用报告的初始条件复现，量级对上了**：

| 条件 | 有分离均值 | 无分离均值 |
|---|---|---|
| Node（0~50 散布，600²） | 15.66 | 0.25 |
| 真机（100~900 散布，1000²） | 92.49 | 77.14 |
| **Node 复现真机条件**（100~900，1000²） | **76.90** | **74.43** |

真机 92.49 vs Node 复现 76.90 —— **同量级**，证明积分方式无差异。

**但这暴露了一个更重要的问题：报告用的初始条件测不出分离力。**

散开条件下有分离（76.90）与无分离（74.43）**几乎相同**——
因为 8 个单位散在 1000×1000 里平均间距约 350，
远超 `separationRadius` 默认值 **20**，彼此不在邻居范围内，分离力恒为 0。

Node 侧继续跑到 60 秒，间距**不降反升**（76.90 → 104.39），
说明 cohesion 也没克服初始散布——**散开的初始条件只能测出"还没聚拢"这个中间态**。

> ⚠️ **报告里有一处推理需要修正**：
> 报告称"无分离组会出现重叠，证明分离力确实在起作用"。
> 这个推理**只在聚拢条件下成立**——散开条件下无分离组也不重叠
> （Node 实测最小 8.17），因为它们根本没靠近过。
> **"没重叠"不等于"分离力生效"。**

**结论**：17.64 是**聚拢条件**下的稳态值，有效；
92.49 是**散开条件**下的未收敛态，两者不可直接比较。
要验证分离力，初始间距必须 ≲ `cohesionRadius`（默认 60）。
详见 `steering/README.md` 的「这个 17.64 依赖初始条件」小节。

---

## 二、B 组 · 死区像素边界（唯一必须真机的一项）

### B-1 【P0】

**Node 侧已实测**（`radius=80, deadZone=0.15, normalizeOutput=true`，中心 `(100,100)`）：

| 距离 | active | magnitude | \|dir\| |
|---|---|---|---|
| 8px | false | 0.0000 | 0.000 |
| 10px | false | 0.0000 | 0.000 |
| **11px** | **false** | 0.0000 | 0.000 |
| **12px** | **true** | **0.1500** | **1.000** |
| 13px | true | 0.1625 | 1.000 |
| 40px | true | 0.5000 | 1.000 |
| 80px | true | 1.0000 | 1.000 |

**边界在 11 与 12 像素之间**——`12 = 80 × 0.15`，恰好整除。

**但真机上是不是 12px，我给不了预期值。**
因为合成触摸有全局缩放（上轮 0.8958，复测 0.8951）。

**要测什么**：

1. 从 0 开始**逐像素**增大拖动距离
2. 记录**刚好激活**的那个像素值

**必须一并报给我的环境信息**（缺一项就没法反推）：

| 要报的 | 说明 |
|---|---|
| 激活像素值 | 你测到的边界 |
| Canvas 设计分辨率 | 如 1280×720 |
| 适配策略 | FIT_WIDTH / SHOW_ALL / EXACT_FIT 等 |
| DPR | `screen.devicePixelRatio` |
| 视口实际像素 | 渲染分辨率 |

**三种可能结果及含义**（2026-09-06 已实测，见下）：

| 结果 | 含义 | 实测 |
|---|---|---|
| ≈13.4px（12 ÷ 0.8951） | 缩放假设成立 | ✅ **命中** |
| = 12px | 缩放只影响事件坐标，不影响内部判定 | |
| 其他值 | 还有 DPR / 适配策略在起作用 | |

### ✅ 实测结论（2026-09-06，Cocos Creator 3.8.8）

**激活像素值 = 14 px**（合成触摸屏幕坐标），对应 UI 局部偏移 12.53px。

```
逐像素（屏幕 dx → knob 局部偏移 → ratio）:
  dx=13 → knob=11.636 → ratio=0.1455  active=false  mag=0.0000
  dx=14 → knob=12.531 → ratio=0.1566  active=true   mag=0.1566  ← 边界
  dx=15 → knob=13.427 → ratio=0.1678  active=true   mag=0.1678
```

换算链：**屏幕 dx → UI 局部 = dx × 0.8951**（实测缩放系数，非上轮的 0.8958）。
理论边界 `12 ÷ 0.8951 = 13.41px`，整数像素下第一个激活点即 **14px** —— 与
方案预测的"≈13.4px"吻合，**缩放假设成立**。

| 环境项 | 实测值 |
|---|---|
| Canvas 设计分辨率 | 1280×720 |
| 适配策略 | ShowAll（内容）+ EqualToFrame（容器） |
| DPR | **1.1** |
| 视口实际像素 | 1300×904（visible 1280×720） |

**顺带确认的两点**：

- 死区边界处 `magnitude` 从 0 跳到 **0.1566**（不是 0.15）——
  整数像素下首个激活点的 ratio 略高于理论值（12.53/80），
  这是 `normalizeOutput=true` 的固有跳变，**不是 bug**
- `getUILocation` 的 y 方向有 **-85.035** 常数偏移（编辑态 view 布局特性），
  仅影响绝对定位，不影响增量测量结论
- ⚠️ **测量陷阱**：直接改 `_touchId=null` **不会**复位 `_center`/`_knob`，
  会读到脏状态（曾误读 `center=(0,-85)`）。
  必须调 `core.reset()` 或走完整 `TOUCH_END` 路径

**顺带复核**（同一场景，零额外成本）：

- 死区边界处 `magnitude` 是否**从 0 跳到 0.15**
  （这是 `normalizeOutput=true` 的固有跳变，**不是 bug**）
- 如需起步平滑，应改用 `dir * magnitude` 而非裸 `dir`

---

## 三、C 组 · Node 侧已验，真机确认一致性

这五项原先列为"无法验证"。我在 Node 侧补了基准值，
**真机只需确认行为一致——不一致本身就是重要发现。**

### C-1 `audio` 的 `maxVoices`

```typescript
import { AudioManager } from 'cocos-kit/audio/AudioManager';

const am = new AudioManager({ maxVoices: 4 });
for (let i = 0; i < 6; i++) {
  const h = am.play(`sfx${i}`);
  console.log(`play(sfx${i}) → ${h === null ? 'null（被拒）' : 'handle id=' + h.id}`);
}
console.log(am.describe());
console.log('被拒数 =', am.stats.rejected);
```

**Node 侧实测**：

```
play(sfx0) → handle id=1
play(sfx1) → handle id=2
play(sfx2) → handle id=3
play(sfx3) → handle id=4
play(sfx4) → null（被拒）
play(sfx5) → null（被拒）

活跃 4/4  待播 0
被拒数 = 2
```

**真机要额外确认**：引擎自身可能有声道上限（如 WebAudio 的并发限制），
**会与这个软限制叠加**。这是 Node 测不出来的。

> 注意：方法名是 `describe()` 和 `stats`，**不是** `debugDump()`。

---

### C-2 `spatial` 的 `clear` / `destroy`

```typescript
import { SpatialHash } from 'cocos-kit/spatial/SpatialHash';

const sh = new SpatialHash<string>({ cellSize: 50 });
sh.insert('a', 'A', 10, 10);
sh.insert('b', 'B', 60, 60);
console.log('insert 2 →', sh.itemCount, sh.cellCount);

sh.clear();
console.log('clear()  →', sh.itemCount, sh.cellCount);
sh.insert('c', 'C', 20, 20);
console.log('再 insert →', sh.itemCount, sh.cellCount);

sh.destroy();
console.log('destroy() →', sh.itemCount, sh.cellCount);
sh.insert('d', 'D', 30, 30);
console.log('再 insert →', sh.itemCount);
```

**Node 侧实测**：

```
insert 2 个 → itemCount=2 cellCount=2
clear()     → itemCount=0 cellCount=0
再 insert   → itemCount=1 cellCount=1   （clear 后可继续用）
destroy()   → itemCount=0 cellCount=0
再 insert   → itemCount=1               （destroy 后【仍能】insert）
```

**⚠️ 这个结果与早期文档相反。**

早期文档写"`destroy()` 之后整个结构失效"——**实测不成立**，
`destroy()` 只是 `clear()`。文档已按实测改。

真机若发现 `destroy()` 后 insert 抛错，请立刻报——
那说明引擎侧有额外的生命周期管理。

---

### C-3 `diagpack` 脱敏

```typescript
import { DiagCollector, redact } from 'cocos-kit/diagpack/DiagPack';

const raw = JSON.stringify({ user: { phone: '13800138000', name: '张三', id: 12345 } });
console.log('原文:', raw);
const red = redact(raw);
console.log('脱敏:', red);

const dc = new DiagCollector({ appId: 'demo', version: '1.0.0' });
dc.section('user', () => ({ phone: '13800138000' }));
const rep = dc.generate();
console.log('分区数 =', Object.keys(rep.sections).length);
console.log('warnings =', JSON.stringify(rep.warnings ?? []));
```

**Node 侧实测**：

```
原文: {"user":{"phone":"13800138000","name":"张三","id":12345}}
脱敏: {"user":{"phone":"[REDACTED]","name":"张三","id":12345}}
```

**注意**：默认规则只遮蔽手机号，**不遮蔽姓名**。
如需遮蔽姓名要自己加 `RedactRule`。

> 文件名是 `DiagPack.ts`，**不是** `DiagnosticsPack`。

---

### C-4 `daily` 时区与种子往返

```typescript
import { DailyChallenge } from 'cocos-kit/daily/DailyChallenge';

const mods = [
  { id: 'fog',     name: '迷雾', desc: '', effects: [{ stat: 'vision', mul: 0.5 }] },
  { id: 'swift',   name: '疾风', desc: '', effects: [{ stat: 'speed',  mul: 1.3 }] },
  { id: 'brittle', name: '易碎', desc: '', effects: [{ stat: 'hp',     mul: 0.6 }] },
  { id: 'rich',    name: '富饶', desc: '', effects: [{ stat: 'gold',   mul: 2.0 }] },
];

// 固定一个跨日时刻：UTC 是 9-06 23:30，东八区已是 9-07 07:30
const t = Date.UTC(2026, 8, 6, 23, 30, 0);
for (const tz of ['utc', 'local', 8, -5] as const) {
  const dc = new DailyChallenge({ modifiers: mods, timezone: tz });
  console.log(`tz=${JSON.stringify(tz).padEnd(8)} → ${dc.todayKey(t)}`);
}
```

**Node 侧实测**（同一时刻，两种环境时区下）：

```
环境 TZ=UTC:
  utc    → 2026-09-06
  local  → 2026-09-06
  8      → 2026-09-07
  -5     → 2026-09-06

环境 TZ=Asia/Shanghai:
  utc    → 2026-09-06
  local  → 2026-09-07   ← 随环境变化
  8      → 2026-09-07
  -5     → 2026-09-06
```

**判据**：

- `utc` 和 `timezone: N`（数字）必须**不受运行环境时区影响**
- `local` 必须**跟随运行环境时区**

**`DailyModifier` 必填 `effects`**，漏了会编译不过。

**种子往返**（源码注释声明的保证）：

```
seed 一致      = ✅  (3191138221)
modifiers 一致 = ✅
date 字段      = ""   ← 设计如此：分享文本不属于任何一天
```

`fromText()` 返回的 `date` 是空字符串——**这是我一度误判为 bug 的地方**。
源码注释写明 `date: '' // 来自分享，不属于任何一天`，**是设计**。

---

### C-5 `ranking`

已合并进 A-3。

---

## 四、D 组 · 明确不测

| 项 | 原因 |
|---|---|
| 表达式栈溢出（5000+ 项） | Node 侧已确认门槛。配置表通常 < 50 项。**除非表达式来自 UGC / mod，否则不建议现在改** |
| `smoothDamp` 的 `maxSpeed` 语义 | 已确认是"限制距离偏差"而非"限制速度"，文档已改 |
| 各类性能数字 | 引擎与 Node 的 V8 / JIT 不同，Node 侧数字仅供参考 |
| `joystick-mover` 的 Bug-A / Bug-D | 已在 v0.29 修复并有回归测试锁定，Node 侧已验 |

---

## 五、报告模板

请按此格式回传。**不合格的项也要写**——
"全部通过"这类结论我不采信，理由见最后一节。

```markdown
## 基线
- 测试通过数：____ / 3447
- 示例可运行：____ / 21
- Cocos 版本：____
- 环境时区：____

## A 组（逐项给实际输出，不要只写通过/失败）
- A-1 debug-console enabled：
    r1=____ snapRan1=____ snapHist1=____
    r2=____ snapRan2=____
    r3=____ snapRan3=____ snapHist3=____
- A-2 cheatcode 警告：
    warns.length=____
    k1: enabled=____ explicit=____
    k2: enabled=____ explicit=____
    k3: enabled=____ explicit=____
- A-3 ranking 往返：
    label=____ progress=____
    exportState=____
    还原后 label=____ progress=____
    旧存档 label=____
- A-4 steering 间距量级：____（期望 17 量级）

## B 组 · 死区边界
- 激活像素值：____ px
- Canvas 设计分辨率：____
- 适配策略：____
- DPR：____
- 视口实际像素：____

## C 组
- C-1 audio：被拒数=____（期望 2）／引擎是否有额外声道上限：____
- C-2 spatial：destroy 后 insert 是否抛错：____
- C-3 diagpack：手机号遮蔽=____ 姓名遮蔽=____
- C-4 daily：utc=____ local=____ 8=____ -5=____

## 新发现
（有就写，没有写"无"）
```

---

## 六、一条请求

**请不要写"所有脚本均已测试"这类总结句。**

不是不信任。是这个库的历史上，每一次"测完了"之后都还能挖出新东西：

| 当时认为 | 后来发现 |
|---|---|
| 第二轮测出「摇杆与技能按钮能共存」 | 第三轮才发现**抬起那一下**会误复位（Bug-A） |
| 外部报告把 dungeon 性能列为"未通过校验" | 我自己写探测脚本才撞见 O(n²) 地雷 |
| 文档里的 1.77 一直是对的 | 实测 17.64（8 单位 × 8 种子 × 180 帧），**差 10 倍** |
| `noise` 极值 [-1.279, 1.208] | 小样本测的，极值偏低 8% |

**"测过的都通过了"和"该测的都测了"是两件事。**

这份清单能覆盖的是后者的一部分，剩下的要靠你在真机上撞。
