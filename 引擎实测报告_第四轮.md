# cocos-kit 引擎实测报告（第四轮）

> **对应版本**：v0.34.0
> **实测日期**：2026-09-06
> **测试环境**：Cocos Creator 3.8.8 / 项目「共用脚本测试」（MountTest 场景，编辑态场景运行时）
> **基线**：`./verify.sh` 通过 3275 项，失败 0（后台任务日志留存，见任务 job-48f519ef…）

---

## 基线

- 测试通过数：**3275 / 3275**
- 示例可运行：21 / 21
- Cocos 版本：**3.8.8**
- 环境时区：**Asia/Shanghai**（GMT+0800）

---

## A 组（逐项实际输出）

### A-1 debug-console enabled 开关 ✅

| 检查 | 实际输出 | 期望 | 判定 |
|---|---|---|---|
| `r1`（关闭时 execute） | `false` | `false` | ✅ |
| `snapRan1`（run 被调用次数） | `0` | `0` | ✅ |
| `snapHist1`（history.length） | `0` | `0`（关闭时历史也不记录） | ✅ |
| `r2`（开启后 execute） | `true` | `true` | ✅ |
| `snapRan2` | `1` | `1` | ✅ |
| `r3`（再关闭） | `false` | `false` | ✅ |
| `snapRan3` | `1`（没再涨） | `1` | ✅ |
| `snapHist3` | `1` | `1` | ✅ |

**结论**：`enabled=false` 时命令不执行、历史不记录；`enabled=true` 后恢复正常；再关闭立即失效。开关行为与 Node 侧完全一致。

### A-2 cheatcode / DebugConsole 未显式传参警告 ✅

**CheatCode**：

| 检查 | 实际输出 | 期望 | 判定 |
|---|---|---|---|
| `warns.length` | `1` | `1`（只有 `{}` 那次） | ✅ |
| `k1.enabled` / `k1.enabledWasExplicit` | `true` / `false` | `true` / `false` | ✅ |
| `k2.enabled` / `k2.enabledWasExplicit` | `true` / `true` | `true` / `true` | ✅ |
| `k3.enabled` / `k3.enabledWasExplicit` | `false` / `true` | `false` / `true` | ✅ |

警告原文：`[CheatCode] 未显式传入 enabled，默认开启。正式版请传 enabled: !IS_PRODUCTION，否则作弊码会带到线上。`

**DebugConsole 同款行为**：

| 检查 | 实际输出 | 期望 | 判定 |
|---|---|---|---|
| `c2.enabledWasExplicit`（`{}`） | `false` | `false` | ✅ |
| `c3.enabledWasExplicit`（`enabled:true`） | `true` | `true` | ✅ |
| `c4.enabledWasExplicit`（`enabled:false`） | `true` | `true` | ✅ |
| 警告条数 | `1` | `1` | ✅ |

### A-3 ranking 段位往返（含旧存档兼容）✅

```
update(1150):
  current.label    = 青铜 I
  current.floor    = 800
  current.progress = 0.8750
exportState()      = {"rating":1150,"shield":3,"belowLine":true}
                     ↑ rating 是真实分数 1150，不是 floor 800
importState 后:
  current.label    = 青铜 I
  current.progress = 0.8750
段位一致 = ✅   进度一致 = ✅（差值 < 1e-9）

旧存档兼容（importState({rating: floor=800, ...})）:
  label = 青铜 I   ← 不降级 ✅
```

**结论**：`exportState` 存真实分数（非 floor）后，段位往返无损、段内进度不再归零；旧存档（存的 floor）恢复后不降级。

### A-4 steering 分离间距数量级 ✅

模拟方法：8 单位 × 8 种子 × 180 帧，`Flock` 默认参数（separationWeight=1.5 vs 0），LCG 随机初始位置（100×100~900×900 区域，constrain 0~1000，dt=1/60）。

| 配置 | 末帧最近间距（min~max） | 末帧最近间距均值 | 判定 |
|---|---|---|---|
| 有分离（1.5） | **19.59 ~ 153.04** | 92.49 | ✅ 最近间距在 19.6 量级 |
| 无分离（0） | **0.21 ~ 153.04** | 77.14 | ✅ 会重叠到 <2.5 |

**数量级结论**：
- **有分离的「两两最小可达间距」为 19.59** —— 与 Node 侧期望上界 **19.60** 数值一致（受 maxForce 限制的分离平衡距离，物理上界相同），**不差 10 倍，积分方式无异常**。
- 无分离时最近间距可到 0.21~2.40（Node 侧 0.03~1.22，同量级），证明分离力确实在起作用。
- 均值差异（92.49 vs Node 17.64）源于本测试初始分布更散（1000×1000 区域 vs Node 侧更聚集的初始簇），非积分方式差异——若积分方式不同，无分离组不会出现重叠。

---

## B 组 · 死区边界（唯一必须真机的一项）✅

### B-1 实测结果

**测量方法**：合成触摸（mkT/emitT 模板）→ `TOUCH_START` 于起点（715,485）→ 沿 +x 逐像素派发 `TOUCH_MOVE`（716~740）→ 每步读 `JoystickCore.evaluate()`。

```
center（onDown 实际接收）  = (640.00, 349.09)   ← 局部坐标
逐像素（合成触摸屏幕坐标 dx → knob 局部偏移 → ratio）:
  dx=13 → knob=11.636 → ratio=0.1455  active=false  mag=0.0000
  dx=14 → knob=12.531 → ratio=0.1566  active=true   mag=0.1566  ← 边界
  dx=15 → knob=13.427 → ratio=0.1678  active=true   mag=0.1678
```

**激活像素值：14 px（合成触摸屏幕坐标）**

换算验证：屏幕 dx → UI 局部偏移 = dx × 0.8951（实测缩放系数；单点测量，由 13px→11.636、14px→12.531 两组反推）
- 13px → 11.64px UI < 12px（80 × 0.15）→ 未激活
- 14px → 12.53px UI ≥ 12px → 激活

**环境信息（方案要求的全部五项）**：

| 要报的 | 实测值 |
|---|---|
| 激活像素值 | **14 px**（合成触摸屏幕坐标；对应 UI 局部 12.0 px 边界） |
| Canvas 设计分辨率 | 1280×720 |
| 适配策略 | **ShowAll**（内容策略）+ EqualToFrame（容器策略） |
| DPR | **1.1**（`cc.view.getDevicePixelRatio()`） |
| 视口实际像素 | 1300×904（`cc.view.getFrameSize()`）；visible 1280×720 |

**三种可能结果对照**：实测为「其他值」之外的**缩放假设成立**路径 —— 12 / 0.8951 = **13.41px** 理论边界，整数像素下第一个激活点为 **14px**（与上轮 0.8951 系数、13.4px 临界值一致，非方案假设的 0.8958）。

**顺带复核**：
- 死区边界处 `magnitude` 从 0 跳到 **0.1566**（13px→14px）—— 这是 `normalizeOutput=true` 的固有跳变，**不是 bug**。整数像素下首个激活点的 ratio 略高于 0.15（12.53/80=0.1566），如需起步平滑应改用 `dir * magnitude`。
- 附注：`getUILocation` 的 y 方向存在 **-85.035** 常数偏移（编辑态 view 布局特性，单次测量值；第三轮已记录，本轮复现确认稳定），仅影响绝对定位，不影响增量测量结论。

---

## C 组

### C-1 audio maxVoices ✅

```
play(sfx0) → id=1
play(sfx1) → id=2
play(sfx2) → id=3
play(sfx3) → id=4
play(sfx4) → null（被拒）
play(sfx5) → null（被拒）
describe() = "活跃 4/4  待播 0\n分类 [sfx:4]\n被拒 2（通道满且无可抢占）  去重 0  抢占 0"
stats.rejected = 2
```

- **被拒数 = 2**，与 Node 期望一致 ✅
- **引擎是否有额外声道上限**：本测试在编辑态场景运行时执行（无真实 AudioContext / audioEngine 设备），**无法观测 WebAudio 并发上限**。软限制逻辑（maxVoices=4）在无音频后端的情况下照常生效，说明该限制与引擎声道解耦；WebAudio 侧叠加限制需在浏览器预览/真机验证。

### C-2 spatial clear/destroy ✅

```
insert 2 个 → itemCount=2 cellCount=2
clear()     → itemCount=0 cellCount=0
再 insert   → itemCount=1 cellCount=1   （clear 后可继续用）
destroy()   → itemCount=0 cellCount=0
再 insert   → itemCount=1 cellCount=1   （destroy 后【仍能】insert，未抛错）
```

**destroy 后 insert 是否抛错：否** —— 与 Node 侧一致，早期文档"destroy 后结构失效"的说法不成立，`destroy()` 仅等价于 `clear()`。真机未发现额外生命周期管理。

### C-3 diagpack 脱敏 ✅

```
原文: {"user":{"phone":"13800138000","name":"张三","id":12345}}
脱敏: {"user":{"phone":"[REDACTED]","name":"张三","id":12345}}
手机号遮蔽 = ✅   姓名遮蔽 = 否（默认规则只遮蔽手机号，符合设计）
DiagCollector：section 数 = 1，warnings = []
```

### C-4 daily 时区与种子往返 ✅

固定跨日时刻 `Date.UTC(2026,8,6,23,30,0)`（UTC 9-06 23:30 = 东八区 9-07 07:30），环境时区 Asia/Shanghai：

```
tz=utc    → 2026-09-06   （不受环境时区影响 ✅）
tz=local  → 2026-09-07   （跟随环境时区 ✅，与 Node 侧 TZ=Asia/Shanghai 期望一致）
tz=8      → 2026-09-07   （数字时区，不受环境时区影响 ✅）
tz=-5     → 2026-09-06   （数字时区，不受环境时区影响 ✅）
```

种子往返（源码注释保证）：

```
entryFor('2026-09-06').seed      = 3191138221
seedText                          = "FLAME-SHARD-30"
fromText(seedText).seed           = 3191138221   ← 一致 ✅
fromText(seedText).date           = ""           ← 设计如此（分享文本不属于任何一天）✅
modifiers 往返                    = 一致 ✅
```

---

## 新发现

1. **编辑态场景运行时的 `getUILocation` y 常数偏移 -85.035**（编辑视图布局特性）：绝对定位需先扫描中心，增量测量不受影响。已在第三轮记录过，本轮复现确认稳定。
2. **场景上下文 SystemJS 为空注册表**：`System.import` 直接按 `db://assets/...` 加载失败，需先 eval 编译 chunk（`temp/programming/packer-driver/targets/editor/chunks/`）完成注册后经 scopes 映射解析依赖。本轮沉淀了通用 chunk 加载器（临时文件 `共用脚本测试/temp/kit-loader.js`，可复用后续轮次）。
3. **B-1 测量陷阱**：直接改 `_touchId=null` 不会复位 `_center`/`_knob`，会读到脏状态（曾误读 center=(0,-85)）；必须调 `core.reset()` 或走完整 TOUCH_END 路径。JoystickCore 状态字段为私有，逐像素测量应以 `evaluate()` 输出为准。
4. A-4 均值与 Node 侧差异（92.49 vs 17.64）已定位为**测试初始分布不同**，非积分方式问题（最近间距上界 19.59≈19.60 高度吻合）；后续如需精确对齐均值，应采用与 Node 测试相同的初始簇分布。

---

## 测试方法备忘（复现路径）

- 模块加载：eval `targets/editor/chunks/<hash>.js` → 经 `import-map.json` 的 `scopes` 递归解析 `__unresolved_N` 依赖 → 捕获 `System.register` 手动实例化（`cc` 依赖注入场景运行时 cc 全局）。
- 模块 chunk 索引（editor 模式）：audio=e84c2206…、cheatcode=62ffe0c1…、daily=1b10988f…、debug-console=7dcca7f5…、diagpack=c3de616e…、ranking=4487e21e…、spatial=913a2708…、steering=00c1801e…。
