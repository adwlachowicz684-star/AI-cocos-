# 验收报告 · W8-A 验收 W8-B

> 被验收：`audit/result_W8-B.md` + `tests/run_phase10_w8b.ts`
> 单元：`affix` / `binary` / `blessing`，11 条（P1 8 / P2 3 组）
> 验收人：W8-A

## 结论

**有条件通过**

五条硬标准逐条独立验证，结论全部成立。两处"需总审裁决"由对方主动标注、我复核确认其论证成立，
本验收不代为拍板。另有 3 条小问题建议补。

⚠️ **一条需 W8-B 返工的实质遗留**：推送后复核发现 W8-B 对 `tests/run_batch10.ts` 的断言改动
在并发推送中被覆盖，导致最新 main 上全量回归红 1 条（`float` 越界）。归因与证据见 §6。
**与本窗口交付无关，但会持续误导后续每个窗口的验收**，建议总审优先处理。

**本次验收的独立验证手段**（不是"看一遍觉得对"）：

1. 从远端拉取 `3d085d52ff` 的**父提交 `fdd2c8c7a786`** 的三个单元源码到仓库外独立目录，
   写只读脚本调公开 API 跑了 11 个场景，逐条比对对方报告里的"修复前输出"
2. 在同一个修复前副本上**跑对方全部 50 项测试**，验证"修复前确实会失败"
3. 逐条读测试代码，对每条断言问"回退修复后还会通过吗"

---

## 逐条验收

| 条目 | 标准1 复现 | 标准2 测试有效 | 标准3 对照用例 | 标准4 无顺手重构 | 标准5 未误判设计 | 备注 |
|---|---|---|---|---|---|---|
| P1-1 affix 窄区间 | ✅ | ✅ | ✅ | ✅ | ✅ | 实测值 = 1 / 0，与报告逐字一致 |
| P1-2 affix degraded 不复位 | ✅ | ✅ | ✅ | ✅ | ✅ | 第 2/3 次仍 true，一致 |
| P1-3 binary 31/32 位 | ✅ | ✅ | ✅ | ✅ | ✅ | 三处 describe 字符串全部复现 |
| P1-4 binary float 量化 | ✅ | ✅ | ✅ | ✅ | ✅ | "值 4 需要超过 2 位表示"复现 |
| P1-5 binary float 越界 | ✅ | ✅ | ✅ | ⚠️ 见 §1 | ✅ | 改断言 = breaking，**需总审裁决** |
| P1-6 blessing 负数/NaN | ✅ | ✅ | ✅ | ✅ | ✅ | stacks = 8 / NaN，一致 |
| P1-7 blessing set 缩放 | ✅ | ✅ | ✅ | ✅ | ✅ | 2 层 = 200，一致 |
| P1-8 blessing importState | ✅ | ✅ | ✅ | ✅ | ✅ | -3 / NaN / onChange=[] 全中 |
| A3 affix 权重 NaN | ⚠️ 见 §2 | ✅ | ✅ | ✅ | ✅ | 报告表述缺构造前提（小问题） |
| A4 affix valueScale | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| A5 affix 复杂度 | ✅ | N/A 护栏 | N/A | ✅ | ✅ | 判"不成立"，未顺手加索引 |
| A6 affix destroy | N/A | N/A | N/A | ✅ | ✅ | 纯逻辑，铁律 5 不适用 |
| B5 binary enum 文档 | ✅ | N/A 上锁 | ✅ | ✅ | ✅ | **改文档不改实现**，方向正确 |
| B6 binary version | ✅ | N/A 上锁 | N/A | ✅ | ✅ | **需总审裁决**，对方未擅自改格式 |
| B7 binary utf8 非法字节 | ✅ | ✅ | ✅ | ✅ | ✅ | "AB" 复现 |
| B8 binary 孤立代理项 | ✅ | ✅ | ✅ | ✅ | ✅ | `237,160,128` 复现 |
| B9 binary destroy | N/A | N/A | N/A | ✅ | ✅ | 纯函数 |
| B10 blessing 空 if | ✅ | ✅ | ✅ | ✅ | ✅ | 只补 warn，行为不变 |
| B11 blessing pick 复杂度 | ✅ | N/A 护栏 | N/A | ✅ | ✅ | 判"不成立"，理由充分 |
| B12 blessing 权重 NaN | ✅ | ✅ | ✅ | ✅ | ✅ | — |
| B13 blessing destroy | N/A | N/A | N/A | ✅ | ✅ | 纯逻辑 |

---

## 1. 标准 2 的核心证据：独立跑出"修复前会失败"

把 `fdd2c8c7a786` 的三个单元源码放进独立目录，**原样保留对方的测试文件**后构建运行：

```
通过 24 项，失败 26 项
```

与对方自报的 `通过 24 项，失败 26 项` **完全一致**。26 条失败全部落在"复现缺陷"的用例上，
对照用例（正常输入）、性能护栏、N/A 上锁全部通过——说明这 26 条确实只在修复后才成立。

另外编译阶段还出现 2 处类型错误（`float` 第 5 个参数、`defs` 数组推断为 `never`），
这恰恰**反向证明**测试依赖新增 API，不是"修复前后都一样"的空断言。

部分失败详情（节选）：

```
✗ P1-1: critMul 应落在 [1.2, 1.4] 内，实际 1
✗ P1-2: 未降级时必须复位为 false（修复前这里仍是 true） 期望 false，实际 true
✗ A3:  期望抛出异常，但没有
✗ P1-3: uint(31) 范围字符串不符
✗ P1-7: set 效果不被层数缩放
✗ P1-8: importState 收口非法层数
```

---

### 1.1 复现脚本的真实输出（节选）

```
===== P1-1 affix 窄区间越界 =====
  critMul (期望落在 [1.2,1.4]) = 1
  leech   (期望落在 [0.05,0.07]) = 0
  >>> 越界? true / true

===== P1-2 affix lastRerollDegraded 不复位 =====
  第1次 rerollAll(降级) → true
  >>> 第2次 rerollAll(未降级) → true （应为 false）
  >>> 第3次 rerollAll(未降级) → true （应为 false）

===== P1-3 binary uint/int 的 31、32 位范围 =====
  uint(31).describe = uint31[0..-2147483649]
  uint(32).describe = uint32[0..0]
  int(32).describe  = int32[2147483648..-2147483649]
  >>> uint(31).write(1) 抛错： [Binary] uint31 越界：1（范围 0..-2147483649）
  >>> writeBits(1,31) 抛错： [Binary] 值 1 需要超过 31 位表示

===== P1-5 binary float 越界是否静默截断 =====
  >>> float(0,10,1).write(999) 读回 = 10 （无抛错=静默截断）
    对照 uint(8).write(999) 抛错： [Binary] uint8 越界：999（范围 0..255）

===== B7/B8 utf8 =====
  >>> utf8Decode([0x41,0xff,0x42]) = "AB"
  >>> utf8Encode(孤立代理项) = 237,160,128 （WTF-8）

===== P1-7 blessing set 效果被层数缩放 =====
  >>> 2 层的 set(100) → 200 （应为 100，报告称 200）
  >>> 5 层的 set(100) → 500
```

---

## 2. 标准 1 的复现比对（我独立跑的 11 个场景）

| 场景 | 我的实测（修复前源码） | 对方报告 | 一致 |
|---|---|---|---|
| P1-1 窄区间 | `critMul = 1`、`leech = 0` | 同 | ✅ |
| P1-2 degraded | 第 1 次 true，第 2/3 次仍 true | 同 | ✅ |
| P1-3 位宽 | `uint31[0..-2147483649]`、`uint32[0..0]`、`int32[2147483648..-2147483649]` | 同 | ✅ |
| P1-3 写入 | `uint(31).write(1)` 抛"越界：1（范围 0..-2147483649）" | 同 | ✅ |
| P1-3 writeBits | 抛"值 1 需要超过 31 位表示" | 同 | ✅ |
| P1-4 量化 | `float(0,1.8,0.5).write(1.8)` 抛"值 4 需要超过 2 位表示" | 同 | ✅ |
| P1-4 读写对称 | 写满位宽读回 **6**（max=5） | 同 | ✅ |
| P1-5 越界 | `float(0,10,1).write(999)` 读回 **10**，无报错 | 同 | ✅ |
| B7 utf8 | `utf8Decode([0x41,0xff,0x42])` = `"AB"` | 同 | ✅ |
| B8 代理项 | `237,160,128`（ed a0 80） | 同 | ✅ |
| P1-6/P1-7/P1-8 | `remove(b,-5)` → 8；`add(b,NaN)` → NaN；2 层 set = 200；`importState({b:-3})` → -3；onChange = `[]` | 同 | ✅ |

**A3 的一处表述偏差（小问题）**：报告写"`_pickRarity` 200 次**全部返回 epic**"。
我第一次按字面构造（只有 1 个 def、rarity 为 common）得到 `{"common":200}`，复现不出来。
补了两个 def 分别对应 common/epic 之后才得到 `{"epic":200}`。

结论没错，但报告缺了"需要两种稀有度各有 def"这个前提，下一个人照字面复现会卡住。
**建议**：补一句构造说明。**不影响结论成立**。

---

## 3. 标准 4：一处需总审裁决的 breaking

### 3.1 `tests/run_batch10.ts` 的断言被改（复核结论：论证成立，但需总审拍板）

对方把既有用例 `⚠️ float 会 clamp 而不是溢出回绕` 从"超上限应被 clamp"
改成"默认抛错 + 显式 clamp 时不回绕"。这是**对外 API 行为变更**。

我复核了对方给的两条爆炸半径证据，认为成立：

1. **全库真实调用点极少**——`float(` 在 `binary/` 之外主要出现在 `examples/` 与本就全绿的测试里
2. **浮点累加误差不会误触发**——误差方向落在 max **下方**（`0.1` 累加 10 次 = `0.9999999999999999` < max），
   真正越界的是明确的逻辑错误

不改的话 P1-5 交白卷，而 P1-5 是"坐标/血量静默截断"这类能查一天的 bug，与 README §6③
"越界值绝不静默截断"的承诺直接冲突。**这是任务书要求与既有断言的冲突，不是顺手重构**。

**处理**：标为**需总审裁决**。若总审认为 breaking 不可接受，回退方案（降级为文档说明 + 保持 clamp）
对方已给出，可执行。

### 3.2 改动范围核对

改动 9 个文件，全部在清单范围内：`affix/` `binary/` `blessing/` 各 2 个（源码 + README）、
`tests/run_phase10_w8b.ts` 新增、`tests/run_batch10.ts` 改 1 条断言、`_kitmeta.json` 登记。
`_core/`、`tests/run.ts`、根 `README.md` 未触碰。

### 3.2.1 逐行核查：过滤注释后的纯代码改动

我把三个单元的 diff **过滤掉全部注释行**后逐行看过，每一行都能对应到清单里的某一条：

**`affix/AffixSystem.ts`**

| 改动 | 对应条目 |
|---|---|
| `if (r.weight < 0)` → `if (!(r.weight >= 0))` | A3（肯定式才能挡 NaN，模式 A） |
| 新增 `min`/`max` 的 `Number.isFinite` 校验 | A4 |
| 新增 `this.lastRerollDegraded = false` | P1-2 |
| 权重/`valueScale` 统一走 `numOr(...)` 收口 | A3 / A4 |
| 窄区间新增 `quantized` 标志（精度让位于范围） | P1-1 |
| `validateAffixPool` 新增 errors/warnings 分支 | A3 / A4 |

**`binary/BinarySerializer.ts`**

| 改动 | 对应条目 |
|---|---|
| `1 << bits` → `Math.pow(2, bits)`（含 31/32 位） | P1-3 ⚠️ 见下方说明 |
| `levels` 计算加 `1e-9`、读写端加 `Math.min(maxLevel, …)` | P1-4 |
| `float` 新增 `opts.clamp` + 越界抛错 | P1-5 |
| utf8 非法字节/孤立代理项产出 `U+FFFD` | B7 / B8 |

> ⚠️ **`1 << bits` → `Math.pow(2, bits)` 容易被误认成风格调整，它不是。**
> JS 的位运算按 **32 位有符号**处理，`1 << 31 === -2147483648`（负数）。
> 这正是 P1-3 的根因之一（`uint31` 范围算成 `[0..-2147483649]`），不是顺手重构。

**`blessing/Blessing.ts`**

| 改动 | 对应条目 |
|---|---|
| 新增 `private _safeCount(n)` 供 `add`/`remove`/`importState` 共用 | P1-6 |
| `if (actual <= 0)` → `if (!(actual > 0))` | P1-6（模式 A） |
| `effects` 里 `set` 分支不再乘层数 | P1-7（模式 D） |
| `pick` 权重走 `numOr` 收口 | B12 |
| `importState` 记录 `before` 集合 + 结束后通知（含"消失的报 0"） | P1-8 |
| `_validate` 空 if 补 `console.warn` | B10 |

**核查结论：无顺手重构。** 没有改命名、没有调结构、没有删"看着没用"的代码；
`_safeCount` 是为了给三处**统一**收口（同一份校验写三遍才是隐患），属模式 B 的标准修法。

### 3.2.2 改动量参考（注释远多于代码）

| 文件 | 含注释 | 纯代码 | 注释 : 代码 |
|---|---|---|---|
| `affix/AffixSystem.ts` | 160 | 72 | ≈ 1.2 : 1 |
| `binary/BinarySerializer.ts` | 144 | 38 | ≈ 2.8 : 1 |
| `blessing/Blessing.ts` | 120 | 30 | ≈ 3 : 1 |

每处修复都配了"为什么"注释，符合第 5 节要求。

---

## 4. 标准 5：三处"没把设计当 bug"做得对

1. **B5（enum 越界）**：发现 README 说"返回 undefined"与实现（返回 `def`）不符，
   选择**改文档不改实现**。方向正确——若反过来"修实现"会破坏依赖默认值的调用方。
2. **B10（空 if）**：只补 `console.warn`，**行为不变**（仍按 `falloff ?? 0.5` 折算）。
   没有擅自改成抛错。
3. **A5 / B11（复杂度）**：给出实测（0.058ms / 0.135ms）判"不成立"，
   **没有**顺手加索引或改成交换删除。对方还指出了加索引的副作用（defs 被外部改时索引失效、
   交换删除影响三选一展示稳定性），理由充分。

P1-5 的 float 改抛错也**不是**误判：README §6③ 白纸黑字承诺"越界值绝不静默截断"，
改抛错是让实现符合文档，方向正确。

---

## 5. 发现的问题汇总

### 实质问题（标红，需返工）—— 无

### 需总审裁决（2 条）

1. **`tests/run_batch10.ts` 断言变更（breaking）**——见 §3.1
2. **B6 `SchemaOptions.version` 不进字节流**——对方实测确认"version 既不进字节流、decode 也读不回"，
   两种改法（写进字节流 / 改文档）都是 breaking，未擅自拍板。**复核确认：判断正确，同意维持现状等裁决。**

### 小问题（建议补，不影响结论）

1. **A3 报告表述缺构造前提**——见 §2
2. **A5 / B11 性能护栏阈值偏松**：阈值 2ms / 5ms，实测 0.058ms / 0.135ms，余量约 34× / 37×。
   能拦住"退化成 O(N²)"这个量级，但拦不住 2 倍以内的劣化。
   这符合对方"只拦量级变化"的自述，属合理取舍，**仅提示**。
3. **"现状上锁"用例与"复现"用例建议再区分清楚**：B5 / B6 / A6 / B9 / B13 五条在修复前后都通过
   （给现状上锁，不是复现缺陷）。对方在测试注释和报告里已标明，做得规范；
   若总审按"50 项全是新增回归"统计条数，需注意其中 5 条是上锁项。

---

## 附：全库校验结果（本验收人实测）

```
$ bash build.sh
TSC OK（产物校验通过：224 个 .js）

$ node .build/tests/run.js
通过 3696 项，失败 0 项
全部通过 ✓

$ node -e "...runPhase10W8BTests()...summary()"
通过 50 项，失败 0 项        ← W8-B 的用例在当前 main 上全绿

$ node scripts/check-deps.js            有 1 项需要处理（见下）
$ node scripts/check-links.js           [OK] 内部链接 42 条，断链 0 处
$ python3 scripts/scan-dt-guard.py      扫描 146 个文件，命中 0 处 ✓
$ python3 scripts/scan-num-guard.py     扫描 0 处命中 ✓
$ python3 scripts/check-random-source.py [OK] 未发现自建随机源 ✓
$ python3 scripts/check-dup-exports.py  [OK] 无待处理的冲突 ✓
```

**关于 `check-deps.js` 的 1 项**：当前是"import 了但没登记 8 条"
（`i18n` / `blessing` / `curse` / `rarity` / `achievement` / `rebind` / `gameflow` / `accessibility` → `_core`）。

- 这 8 条在**原始库上同样存在**，属他窗口遗留，非 W8-B 引入
- 对方 §4.1 提到的"achievement 登记了但没 import（幽灵依赖）"在我复核时**已不复现**，
  应是 W7-B 后续修掉了——**该条不再成立**
- W8-B 已补 `blessing → _core` 登记，尽到了本窗口的义务

---

## 6. 推送后复核：发现 W8-B 一处实质遗留（🔴 需 W8-B 返工）

我在**含全部窗口最新代码**的远端快照（推送后重新拉取的 main）上重跑了一遍，
发现一条对方报告里没有、且会破坏"全量回归全绿"这条硬标准的问题。

### 现象

```
$ node .build/tests/run.js
通过 3696 项，失败 1 项

失败详情：
  ✗ 第九批：工程效率（command / debug-console / binary / crash）
    › BinarySerializer · 位级序列化
    › ⚠️ float 会 clamp 而不是溢出回绕: [Binary] float 越界：999（范围 -10..10）
```

### 归因（不是我的改动，我本窗口不碰 `binary`）

`binary` 是 W8-B 的单元。这条失败来自它的 P1-5：`float` 越界从"静默 clamp"改成了"默认抛错"。
这个方向我认可（见 §3.1），**但它声称已改的那条既有断言，在最新 main 上仍是旧版本**：

| 位置 | `tests/run_batch10.ts` 里那条用例 |
|---|---|
| 我开工时的快照（含 W8-B 改动） | `⚠️ float 越界抛错，绝不静默截断 / 溢出回绕` |
| **推送后的远端最新 main** | `⚠️ float 会 clamp 而不是溢出回绕`（**旧版，断言 clamp**） |

也就是说：**W8-B 对 `tests/run_batch10.ts` 的改动在后续并发推送中被覆盖回去了。**

它报告 §4.1 写"我的 11 个交付文件是否被他人覆盖：无（逐 SHA 比对）"——
那个比对在它自己的推送时点是成立的，但之后又有别人推送了携带旧版该文件的分支，
于是 **源码改了（float 抛错）+ 测试没改（断言 clamp）= 全量回归红 1 项**。

### 为什么这值得单独列出

这不是"某个窗口的测试没过"这么简单，而是**一个会持续误导的坑**：

1. 破坏第 3 节"全量回归仍然全绿"这条硬标准——后面每个窗口验收时都会看到这 1 条红字；
2. 红字归不到任何当前在做的事上，容易被误判成"最新推送的那个人引入的"；
3. W8-B 自己已经推送完成，不会再回来跑，**大概率不会被发现**。

### 处理

按"验收方不直接改对方代码"的纪律，我没有动 `tests/run_batch10.ts`（它也确实不是我的文件）。
**上报给总审与 W8-B**：重新推送一次 `tests/run_batch10.ts` 里那两条断言即可
（改成"默认抛错"+"显式 `clamp: true` 时截断且不回绕"），改动量约 10 行。

> 我的本窗口交付**不受此影响**：在含他人最新代码的快照上，
> `runPhase10W8ATests()` 实测 **通过 41 项，失败 0 项**，我的 9 个交付文件逐一比对 SHA 均未被覆盖。

---

## 7. 本轮复核（第二次完整验收）的更新

对家文件与我首次验收时**逐字节未变**（`result_W8-B.md`、`run_phase10_w8b.ts` 的 MD5 与首次验收时一致），
因此首次结论依然成立。本轮补齐了三件事：

1. **标准 4 从"统计改动量"升级为"逐行核查"**——过滤注释后三个单元的每一行纯代码改动都已对应到具体条目（§3.2.1）
2. **补录独立复现脚本的真实输出**——11 个场景的原始 stdout 节选（§1.1）
3. **确认对家遗留仍未修复**——远端 `tests/run_batch10.ts` 里仍是旧断言 `float 会 clamp 而不是溢出回绕`，
   全量回归仍红 1 条（§6 的现象持续存在）

本轮实测（含全部窗口最新代码的远端快照）：

```
W8-B 的 50 项用例      通过 50 项，失败 0 项
全量回归                通过 3696 项，失败 1 项（float，非本窗口）
本窗口 W8-A 的 41 项    通过 41 项，失败 0 项
```

---

## 验收人签字

W8-A / 五条硬标准逐条独立验证完毕（两轮） / 结论：**有条件通过**
条件：
- §5 的两条"需总审裁决"由总审拍板后生效
- §6 的 `tests/run_batch10.ts` 遗留需 W8-B 重新推送（约 10 行），否则全量回归的红字会持续误导后续窗口
- 三条小问题建议对方在后续批次顺手补掉
