# cocos-kit 逐单元精审 · 第 1 批（共 5 批）

> **本文件是自包含任务书。** 你拿到的是一个 Cocos Creator 插件库的精审子任务，
> 之前没有上下文。请先完整读完本文件，再开始动手。

---

## 0. 你的角色与交付纪律

- 你是 **第 1 批审核员**。库里共 119 个单元，另有 4 个窗口在并行审其余批次。
- **总审（我）会逐条再审你的报告**，并给出答复让你返工。所以请严格按第 5 节的格式输出。
- **本轮只审不改**：不要在源码里做任何编辑。你的产出只有一份 Markdown 报告。
  （我复审确认后，会在下一轮给你明确的"哪些改、怎么改"的指令，那时才动代码。）
- **一切结论必须有证据**：行号 + 实测输出，或行号 + 推导。
  **禁止凭感觉下判断，禁止编造未验证的事实。** 拿不准就标"存疑"并写出你的推理链。
- **不要重复劳动**：第 2 节列出了已审结的地基单元和已知全局问题，遇到它们直接引用，不要重审。

### 报告落盘路径（必须遵守）

写入 `/data/workspace/audit/batch1_review.md`，同时把它作为你的最终回复内容返回。

---

## 1. 代码库速览

| 项 | 值 |
|---|---|
| 根目录 | `/data/workspace/AI-cocos--main` |
| 语言 | TypeScript（`strict: true`，`noUnusedLocals`，`noImplicitAny`） |
| 单元数 | 119 个插件 + `_core` / `ds` 两个零依赖基础设施 |
| 全量构建 | `cd /data/workspace/AI-cocos--main && bash build.sh` |
| 全量测试 | `node .build/tests/run.js`（基线：**3453 项通过，0 失败**） |
| 架构元数据 | `_kitmeta.json`（每个单元的 layer / depends / maturity / purpose） |

### 七条铁律（违反即为问题）

1. **零业务依赖**：插件里不得出现任何游戏专属概念
2. **依赖注入**：需要外部对象时由调用方传入，插件内部不得去"找"
3. **单一职责**：一个插件只解决一个问题
4. **配置驱动**：所有可调参数进配置对象
5. **可卸载**：必须提供 `destroy()`，卸载后不留残留
6. **禁止横向依赖**：插件之间不得互相 import（`_core` / `ds` 除外）
7. **每个插件自带 README + 测试 + 示例**

> 判据（rule7 的注脚）：**把这个插件的文件夹复制到另一个 Cocos 项目，需要改几行？答案是 0 才算合格。**

### 现成工具（别重复造）

- `/data/workspace/compact.py` —— 生成去注释的精简源码（保留行号分界），已对全库跑过，
  输出在 `/data/workspace/compact/<单元目录名>.txt`。**精读时先读它**，需要看注释时再回原始文件。
  ```bash
  cd /data/workspace && python3 compact.py <目录名>     # 重新生成某几个单元
  ```
- `/data/workspace/scan_result.json` —— 全库静态扫描画像（代码行、缺陷模式命中、导出符号）
- `/data/workspace/AI-cocos--main/scripts/` —— 仓库自带的校验脚本
  （`check-deps.js` 依赖图、`check-links.js` 文档断链、`scan-dt-guard.py`、
  `scan-num-guard.py`、`scan-silent-degrade.py`、`check-random-source.py`）
- 写验证脚本验证你的猜想（**路径带批次号，避免和别的窗口撞车**）：
  ```bash
  cd /data/workspace && node AI-cocos--main/node_modules/typescript/bin/tsc \
      verify/b1_v1.ts --outDir verify/out1 --module commonjs --target ES2019 --strict --lib ES2019,DOM
  node verify/out1/verify/b1_v1.js
  ```

> ⚠️ **并行纪律（5 个窗口同时跑，务必遵守）**
> - **不要执行 `bash build.sh`**，也不要动 `.build/` —— 它会整体替换产物目录，五个窗口并发会互相踩。
>   （基线测试 3453 项全通过，你要跑回归的话单独喊我，我串行跑。）
> - 你的**验证脚本与产物**一律放 `verify/b1_*` 与 `verify/out1/`，不要碰其他批次的目录。
> - 只写 `/data/workspace/audit/batch1_review.md` 这一个文件，不要改 `audit/` 下的其他文件。
> - **源码一行都不要动**（本轮只审不改）。

---

## 2. 已审结与其他批次的边界（避免重复劳动）

### 已由总审审完的地基单元（**不要再报它们的问题**）

`_core`（math/string/types）· `ds`（DataStructures）· `event-bus` · `pool` · `rng`（RNG/Seed）

这批修掉了 13 个缺陷，其中**会波及你这批单元**的几条，请据此判断你手上的单元有没有同类问题：

| 已修缺陷 | 你要注意的同类风险 |
|---|---|
| `smoothDamp` 的 `maxSpeed` 失效（缺 `target = current - change`） | 调用方若因此做了"二次 clamp 补丁"，现在可能是多余或双重限速 |
| `easing()` 原型链污染（`'toString'` 返回字符串不是数字） | 任何 `Record<string, X>` 按键查表且键来自外部输入的写法 |
| `clampNum` / `numOr` 把 `null` / `''` / `[]` 当成 0 | **全库 100+ 处兜底写法**，你批里若有 `Math.max(1, opts.x ?? 100)` 这类裸写法，就是同类问题 |
| `QuadTree.queryCircle` 退化成 O(k·n) | 任何"先取出 data 再反查坐标/属性"的二次遍历 |
| `SpatialHash` 空桶不回收（内存泄漏） | 任何 `Map<K, Set<V>>` / `Map<K, Array<V>>` 结构，删元素后不删空容器 |
| `LazyHeap.size` 数的是陈旧条目，与 `pop()` 语义不一致 | 任何"惰性删除 / 标记删除"结构，`size`/`isEmpty` 是否对外撒谎 |
| `EventBus` 旧取消函数误删同名新监听器 | 任何 `on()` 返回取消函数的实现 |
| `Pool.active` 被减成负数 | 任何计数型字段（泄漏检测的命脉），是否可能变负 |
| `RNG.sample(arr, -1)` 返回几乎整个数组 | `slice(0, n)` / `splice` 类调用，n 可能是负数或 NaN |

### 已知全局问题（**已在总审台账上，不要重复报，但你批里的新实例要报**）

- **矩形坐标系歧义**：`_core/types.ts` 的 `IRect`（角点）与 `IRectSized`（位置+尺寸）注释口径矛盾
  （一个说"左下"一个说"左上"），`camera` 用 y 向上、`dungeon` 用 y 向下。
  **你批里若有自己定义的 `Rect`，报出它的字段语义**，我统一裁决。
- **`Seed.daily()` 只有 3200 个桶**，日期会碰撞（不同日期同一张地图）。
- **`ds.rectsOverlap` 用 `<`（相切不算相交）vs `_core.rectOverlaps` 用 `<=`（相切算相交）**——相切语义两套。
- **平行实现**：`pathfind` 与 `pathfinding`、`spatial` 与 `ds.SpatialHash`、`pathfinding` 与 `ds.QuadTree`
  疑似重复造轮子。**你批里如果撞上其中一个，请顺带对比另一个，给出"该合并 / 该保留 / 各有分工"的结论。**

---

## 3. 本批单元清单（24 个，5983 行）

| # | 单元 | 目录 | 层 | 代码行 | 文件 | 成熟度 | 风险分 |
|---|---|---|---|---|---|---|---|
| 1 | `entity` | `entity` | L1 | 295 | 1 | beta | 39 |
| 2 | `fov` | `fov` | L1 | 255 | 1 | beta | 41 |
| 3 | `grid` | `grid` | L1 | 333 | 1 | beta | 50 |
| 4 | `i18n` | `i18n` | L1 | 131 | 1 | beta | 40 |
| 5 | `number-roller` | `number-roller` | L1 | 148 | 1 | beta | 19 |
| 6 | `rarity` | `rarity` | L1 | 141 | 1 | beta | 24 |
| 7 | `rebind` | `rebind` | L1 | 243 | 1 | beta | 47 |
| 8 | `telemetry` | `telemetry` | L1 | 177 | 1 | beta | 20 |
| 9 | `abtest` | `analytics` | L2 | 282 | 1 | beta | 40 |
| 10 | `adapters` | `adapters` | L2 | 161 | 1 | beta | 0 |
| 11 | `behavior-tree` | `behavior-tree` | L2 | 298 | 2 | beta | 16 |
| 12 | `bullet-pattern` | `bullet-pattern` | L2 | 323 | 1 | beta | 13 |
| 13 | `dash` | `dash` | L2 | 196 | 1 | beta | 40 |
| 14 | `dungeon` | `dungeon` | L2 | 697 | 1 | beta | 217 |
| 15 | `feedback` | `feedback` | L2 | 253 | 1 | beta | 16 |
| 16 | `fsm` | `fsm` | L2 | 101 | 1 | beta | 12 |
| 17 | `mover` | `mover` | L2 | 285 | 1 | beta | 43 |
| 18 | `objective` | `objective` | L2 | 284 | 1 | beta | 45 |
| 19 | `pathfind` | `pathfind` | L2 | 336 | 1 | beta | 65 |
| 20 | `progressbar` | `progressbar` | L2 | 239 | 1 | beta | 24 |
| 21 | `replay` | `replay` | L2 | 163 | 1 | beta | 23 |
| 22 | `scenerouter` | `scenerouter` | L2 | 156 | 1 | beta | 26 |
| 23 | `steering` | `steering` | L2 | 291 | 1 | beta | 92 |
| 24 | `team-mmr` | `mmr` | L2 | 195 | 1 | beta | 13 |

### 单元详情

#### `entity` — 统一实体注册表：id 版本号防 ABA / 判定框别名映射 / 死亡归宿
- 目录：`entity`　层级：L1　成熟度：beta　代码行：295（原始 580）
- 依赖：无（零依赖）
- 测试：有　示例：**无**
- 扫描命中：双重强转×1, 除零风险×1, 递归调用×16
- 文件：
   - `entity/EntityRegistry.ts`

#### `fov` — 视野与战争迷雾（Shadowcasting + Raycasting）
- 目录：`fov`　层级：L1　成熟度：beta　代码行：255（原始 532）
- 依赖：无（零依赖）
- 测试：有　示例：有
- 扫描命中：异常抛出×1, 递归调用×20
- 文件：
   - `fov/FOV.ts`

#### `grid` — 二维网格 + 建造放置 + 六边形坐标
- 目录：`grid`　层级：L1　成熟度：beta　代码行：333（原始 563）
- 依赖：`_core/types`
- 测试：有　示例：有
- 扫描命中：除零风险×2, 异常抛出×6, 递归调用×18
- 文件：
   - `grid/Grid.ts`

#### `i18n` — 本地化（复数/插值/回退/覆盖率）
- 目录：`i18n`　层级：L1　成熟度：beta　代码行：131（原始 279）
- 依赖：无（零依赖）
- 测试：有　示例：有
- 扫描命中：console输出×2, 除零风险×1, 原型操作×4, 递归调用×7
- 文件：
   - `i18n/I18N.ts`

#### `number-roller` — 数字滚动核心逻辑（渲染层需自己写）
- 目录：`number-roller`　层级：L1　成熟度：beta　代码行：148（原始 268）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：除零风险×2, 异常抛出×1, 递归调用×5
- 文件：
   - `number-roller/NumberRollerCore.ts`

#### `rarity` — 稀有度不只是个排序——还牵扯掉落权重、UI 颜色、保底归属、排序比较。
- 目录：`rarity`　层级：L1　成熟度：beta　代码行：141（原始 272）
- 依赖：无（零依赖）
- 测试：有　示例：有
- 扫描命中：异常抛出×4, 递归调用×10
- 文件：
   - `rarity/Rarity.ts`

#### `rebind` — 设置界面里的"按键设置"
- 目录：`rebind`　层级：L1　成熟度：beta　代码行：243（原始 399）
- 依赖：无（零依赖）
- 测试：有　示例：有
- 扫描命中：异常抛出×6, 原型操作×3, 递归调用×13
- 文件：
   - `rebind/Rebind.ts`

#### `telemetry` — 游戏上线后你要知道：玩家卡在第几关？哪个遗物从没人选？
- 目录：`telemetry`　层级：L1　成熟度：beta　代码行：177（原始 311）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：裸Math.random×1, 异常抛出×1, 递归调用×8
- 文件：
   - `telemetry/Telemetry.ts`

#### `abtest` — A/B 实验：稳定分组、实验间独立、显著性检验、样本量预判
- 目录：`analytics`　层级：L2　成熟度：beta　代码行：282（原始 562）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：除零风险×8, 异常抛出×4, 递归调用×2
- 文件：
   - `analytics/ABTest.ts`

#### `adapters` — 模块间适配器：地牢→网格 / 命中→施法 / 掉落→入包（消除重复胶水）
- 目录：`adapters`　层级：L2　成熟度：beta　代码行：161（原始 375）
- 依赖：无（零依赖）
- 测试：有　示例：**无**
- 扫描命中：无
- 文件：
   - `adapters/Adapters.ts`

#### `behavior-tree` — 行为树（组合/装饰/叶子节点 + 黑板）
- 目录：`behavior-tree`　层级：L2　成熟度：beta　代码行：298（原始 547）
- 依赖：`_core/types`
- 测试：有　示例：有
- 扫描命中：console输出×1, 原型操作×1, 递归调用×5
- 文件：
   - `behavior-tree/BTNode.ts`
   - `behavior-tree/BehaviorTree.ts`

#### `bullet-pattern` — 弹幕发射器：环形/螺旋/扇形/镜像 + 序列编排
- 目录：`bullet-pattern`　层级：L2　成熟度：beta　代码行：323（原始 608）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：非空断言×1, 除零风险×1, 异常抛出×3, 递归调用×2
- 文件：
   - `bullet-pattern/BulletPattern.ts`

#### `dash` — 冲刺（衰减曲线 + 无敌帧 + 取消后摇）
- 目录：`dash`　层级：L2　成熟度：beta　代码行：196（原始 467）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：除零风险×8, 递归调用×4
- 文件：
   - `dash/DashController.ts`

#### `dungeon` — 地牢生成（BSP / Room / Cellular / Maze 四种 + 死图自检）
- 目录：`dungeon`　层级：L2　成熟度：beta　代码行：697（原始 1163）
- 依赖：`ds`
- 测试：有　示例：有
- 扫描命中：除零风险×13, 异常抛出×3, 递归调用×81
- 文件：
   - `dungeon/Dungeon.ts`

#### `feedback` — "打中了"这件事需要被感觉到而不只是看到。
- 目录：`feedback`　层级：L2　成熟度：beta　代码行：253（原始 419）
- 依赖：`_core`
- 测试：有　示例：**无**
- 扫描命中：异常抛出×2, 递归调用×7
- 文件：
   - `feedback/HitFeedback.ts`

#### `fsm` — 有限状态机（显式转换 + 不可达检测）
- 目录：`fsm`　层级：L2　成熟度：beta　代码行：101（原始 218）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：console输出×1, 异常抛出×2, 原型操作×1, 递归调用×2
- 文件：
   - `fsm/StateMachine.ts`

#### `mover` — 角色移动：加速度 / 击退 / 撞墙投影 / 手感预设
- 目录：`mover`　层级：L2　成熟度：beta　代码行：285（原始 604）
- 依赖：`_core/math`
- 测试：有　示例：有
- 扫描命中：除零风险×8, 异常抛出×3, 递归调用×4
- 文件：
   - `mover/CharacterMover.ts`

#### `objective` — 关卡目标：5 种类型 + 前置依赖 + 循环检测
- 目录：`objective`　层级：L2　成熟度：beta　代码行：284（原始 477）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：非空断言×5, 除零风险×3, 异常抛出×3, 递归调用×10
- 文件：
   - `objective/ObjectiveSystem.ts`

#### `pathfind` — 寻路（A* + PathSmoother + FlowField 流场）
- 目录：`pathfind`　层级：L2　成熟度：beta　代码行：336（原始 625）
- 依赖：`ds`
- 测试：有　示例：有
- 扫描命中：除零风险×3, 异常抛出×3, 递归调用×25
- 文件：
   - `pathfind/PathFinder.ts`

#### `progressbar` — 血条、经验条、加载条。看起来是"画一个矩形填到 ratio"，
- 目录：`progressbar`　层级：L2　成熟度：beta　代码行：239（原始 442）
- 依赖：`_core`
- 测试：有　示例：**无**
- 扫描命中：除零风险×1, 异常抛出×4, 递归调用×8
- 文件：
   - `progressbar/ProgressBar.ts`

#### `replay` — 输入录制与回放（deltaOnly，体积极小）
- 目录：`replay`　层级：L2　成熟度：beta　代码行：163（原始 325）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：console输出×1, JSON解析×1, 异常抛出×2, 原型操作×2, 递归调用×4
- 文件：
   - `replay/ReplayRecorder.ts`

#### `scenerouter` — "主菜单 → 关卡 → 结算 → 回主菜单"，中间夹着加载界面和转场动画。
- 目录：`scenerouter`　层级：L2　成熟度：beta　代码行：156（原始 310）
- 依赖：`_core`
- 测试：有　示例：**无**
- 扫描命中：除零风险×1, 递归调用×11
- 文件：
   - `scenerouter/SceneRouter.ts`

#### `steering` — 转向行为（7 种单体 + Flock 群体 + 队形）
- 目录：`steering`　层级：L2　成熟度：beta　代码行：291（原始 597）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：除零风险×23
- 文件：
   - `steering/Steering.ts`

#### `team-mmr` — 组队匹配分：carry 效应、组队加成、分差限制、散人补齐
- 目录：`mmr`　层级：L2　成熟度：beta　代码行：195（原始 406）
- 依赖：`_core`
- 测试：有　示例：有
- 扫描命中：除零风险×3, 异常抛出×1
- 文件：
   - `mmr/TeamMMR.ts`


---

## 4. 审核清单（逐项过，别跳）

按下面的六类逐项检查。**每个单元都要过一遍全部六类**，不能只挑自己擅长的。

### A. 静默错误（本库最痛恨的一类，优先级最高）

这类 bug 的共同点：**不报错、不崩溃，只是结果不对**，排查时没人会想到源头。

- [ ] **NaN / Infinity 穿透**：`Math.max/min` 不做有限性检查（`Math.max(1, NaN) === NaN`）；
      一个 NaN 帧能让坐标永久变 NaN 且不报错
- [ ] **缺值被当成 0**：`Number(null) === 0`、`Number('') === 0`、`Number([]) === 0`；
      容量字段因此归零（撤销栈永不裁剪 → 内存无限增长）
- [ ] **除零 / 退化区间**：`a === b` 时除零；`min > max` 的区间；空数组求最值得 `∓Infinity`
- [ ] **dt 守卫**：负数 / NaN / Infinity 的 dt。
      注意 `dt <= 0` 挡不住 NaN，`!(dt > 0)` 挡不住 Infinity
- [ ] **索引越界静默**：`arr[i]` 越界得 `undefined`，`undefined.xxx` 才崩，中间可能已污染数据
- [ ] **浮点相等**：`===` 比较浮点数；累积误差
- [ ] **空 catch / 静默降级**：`catch {}` 吞掉异常；失败时返回默认值让坏数据看起来正常
- [ ] **`slice`/`splice` 的负数参数**：`slice(0, -1)` 是"去掉最后一个"，不是"取 0 个"
- [ ] **原型链污染**：`Record<string, T>` 按键查表时取到 `Object.prototype` 上的方法

### B. 生命周期与泄漏（rule5）

- [ ] `destroy()` 是否存在、是否清干净：**事件监听、定时器、缓存引用、创建的节点**
- [ ] 最容易漏的是**事件监听**和**定时器**——它们不随对象消失，而是继续持有 `this`
- [ ] `destroy()` 后再次调用其他方法是否安全（幂等 / 状态一致）
- [ ] 长期运行的容器是否只增不减（缓存、历史记录、撤销栈、连接池）
- [ ] 对象池语义：`active` 计数会不会变负；重复归还；借出不还

### C. 契约一致性（注释说谎比没注释更糟）

- [ ] **JSDoc 与实现是否一致**（逐条比对参数、返回值、边界行为）
- [ ] **README 与源码是否一致**（API 表里的函数是否真的存在、签名是否对得上）
- [ ] 是否存在**记录为"设计如此"但实际是 bug** 的行为（本库已出现 2 例，见第 2 节）
- [ ] 文档里写了但源码里没有的 API（**漏写你会去看源码，写错你会信文档**）
- [ ] 同名不同义：同一概念两套命名 / 同一命名两套语义

### D. 架构铁律（rule1-7）

- [ ] rule6 横向依赖：是否 import 了其他插件（允许 `_core` / `ds`）
- [ ] rule1 业务耦合：是否出现游戏专属概念（应通过注入/配置解耦）
- [ ] rule4 配置驱动：魔法数字是否硬编码
- [ ] rule7 是否自带 README + 测试 + 示例

### E. 性能陷阱

- [ ] **O(n²) / 二次遍历**：先取结果再逐个反查（见第 2 节 queryCircle 案例）
- [ ] **展开栈溢出**：`Math.max(...xs)`、`f(...args)`，数组超 10 万项直接 `RangeError`
- [ ] **热路径分配**：每帧 `new` 对象 / `Array.from` / 闭包
- [ ] **无限 while / 死循环**：循环变量非有限值时条件恒真（如 `Infinity - 2π` 仍是 `Infinity`）
- [ ] **递归深度**：无上限的递归

### F. 数值与配置健壮性

- [ ] 容量/上限类字段是否用 `clampNum` 收口（防内存无限增长）
- [ ] 普通配置类字段是否用 `numOr` 收口（只兜非有限值，不定上界以免误伤合法值）
- [ ] 随机数是否走 `IRandomSource` 注入（可复现 + 可测试），而非裸 `Math.random`
- [ ] 存档 / 配置反序列化后的字段是否做了有效性校验

---

## 5. 报告格式（严格遵守，我要拿它做机器比对）

```markdown
# 第 1 批审核报告 · <一句话总结>

## 概览
| 项 | 数 |
|---|---|
| 单元数 / 代码行 | X / Y |
| P0（必须修） | N |
| P1（应该修） | N |
| P2（可选） | N |
| 存疑（需总审裁决） | N |

## 严重度分档
- **P0**：会导致错误结果 / 崩溃 / 泄漏，且**静默**（不报错） —— 必须修
- **P1**：边界条件错误、契约不一致、O(n²) 退化 —— 应该修
- **P2**：可读性、冗余、文档瑕疵 —— 可选

## 单元逐个报告

### <单元名>（<目录>，<代码行> 行，<层级>）
**用途**：<一句话，来自 _kitmeta.purpose>
**结论**：✅ 通过 / ⚠️ 有问题 / ❌ 有 P0

#### P0-1 <问题标题>
- **位置**：`<文件>:<起止行>`
- **现象**：<这段代码现在实际做什么>
- **证据**：<实测输出 / 行号推导 / 最小复现脚本>
- **后果**：<什么场景下暴露，表现为什么症状，为什么难查>
- **建议**：<具体怎么改，写出关键代码>
- **影响面**：<哪些下游单元受影响，有没有调用方已做补丁>

（每个问题一节；无问题的单元写"结论 ✅ 通过"加一两句复核说明即可）

## 跨单元共性问题
<本批内多个单元共有的模式，集中说一次>

## 存疑（需总审裁决）
- **<单元>**：<问题> —— 我倾向 <A/B>，因为 <理由>，但 <不确定之处>
```

**硬性要求：**

1. 每个 P0/P1 **必须有"证据"字段**，且证据是实测输出或行号推导，**不接受"应该""可能"**
2. 每个 P0/P1 **必须有"后果"字段**，说清"什么场景下暴露、表现为什么症状"。
   **说不出后果的问题不要报**——那通常说明它不是问题
3. 报告里**不要写修改方案以外的代码补丁**（本轮只审不改）
4. 若你验证了某个疑似问题**其实不是 bug**，也请写在单元结论里并给出推导
   （总审会据此判断"这里已被查过，不用再查"）

---

## 6. 交付前自检

- [ ] 报告中每个 P0/P1 都有行号 + 证据 + 后果三件套
- [ ] 没有把「已审结的地基单元」的问题重复报一遍
- [ ] 没有把「已知全局问题」当新发现报（但新实例要报）
- [ ] 覆盖了清单里的**全部六类**（A~F），没有只挑擅长的
- [ ] 24 个单元**每个**都有结论（通过也要写）
- [ ] 存疑项单独列在最后一节，没有混进 P0/P1
- [ ] 报告已写入 `/data/workspace/audit/batch1_review.md`

---

## 7. 关于返工

总审会给你的报告逐条反馈，可能出现这三种结论：

- **采纳** → 下一轮给你明确指令后改
- **驳回** → 会写明驳回理由（通常是：证据不足 / 这其实是设计意图 / 后果不成立）。
  **被驳回不要硬顶，按理由重新取证再报**
- **降级/升级** → 严重度判断会调整，以总审口径为准（我掌握全库视角，能看到跨批次的影响面）

开始吧。
