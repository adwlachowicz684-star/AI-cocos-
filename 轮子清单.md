# 轮子清单 · cocos-kit

> 一份速查手册：这些轮子是**做什么的、怎么用、依赖什么、什么环境能跑、怎么装进项目**。
>
> 最后更新：2026-09-05 · 版本 v0.39.0 · **119 个插件单元（119 个目录，一一对应） · 3447 项测试全部通过 · `_kitmeta.depends` 已与源码 import 对齐**
>
> 📖 每个插件目录下还有各自的 `README.md`，里面有完整示例和踩坑清单。本文是**总览**，细节看子文档。
>
> **文档导航**
> - [`README.md`](./README.md) —— 入口、理念、快速开始
> - 本文档 —— 找某个功能（速查表 + 分插件详解 + 83 条缺陷记录）
> - [`插件脑图.md`](./插件脑图.md) —— 看整体结构（树形，含每个单元的接口）
> - [`依赖规则v2`](./依赖规则v2_前置模块与分层.md)（纵向：前置模块）/ [`v3`](./依赖规则v3_功能模块.md)（横向：功能模块）
> - [`审查报告.md`](./审查报告.md) —— 单元体检（自动扫描结果）
> - [`审查记录.md`](./审查记录.md) —— 逐个审查的人工记录（**119/119 完成**）

---

## 0. 三分钟上手

### 环境要求

| 项 | 要求 |
|---|---|
| 语言 | TypeScript 5.x（`strict` 模式） |
| 引擎 | Cocos Creator 3.6+（**但绝大多数插件不需要引擎**） |
| 运行时依赖 | **无**。开发期只依赖 `typescript` 做编译 |
| Node | 用于跑测试（可选，不装也能用） |

### 部署方式：直接复制文件夹

**没有 npm 包、没有构建步骤、没有配置文件。**

```
把 cocos-kit/ 整个目录拖进你的 Cocos 项目的 assets/ 下
→ 删掉 typings/ 目录（那是给 Node 环境做类型检查的桩）
→ 删掉 tests/ 和 examples/（可选，不删也不影响）
→ 用哪个插件就在哪个文件里 import 它
```

```typescript
import { Scheduler } from './cocos-kit/scheduler/Scheduler';
import { DamagePipeline } from './cocos-kit/damage-pipeline/DamagePipeline';
```

> ⚠️ **在 Cocos 里复制文件夹时，`.meta` 文件必须一起复制。**
> Cocos 靠 meta 文件识别资源，漏了的话脚本会丢失引用（表现为"脚本挂不上"）。
> 在操作系统里复制通常没问题；但如果你是从 git 克隆、或用某些同步工具，
> 记得确认 `.meta` 也在。

### 想一眼看全 119 个单元

看 [`插件脑图.md`](./插件脑图.md) —— 树形结构，每个单元含：
作用 / 功能 / 前置依赖 / 接口（接口下列方法并附说明）。

- 导入 XMind / 幕布 / markmap 均可（纯缩进列表）
- 重新生成：`npm run gen:mindmap`
- 有两个视图：按领域与功能模块、按依赖前置

### 验证它没坏

```bash
cd cocos-kit
npm install          # 只装 typescript
npm test             # 期望：通过 3190 项，失败 0 项
npm run demo         # 移动闭环 Demo（摇杆→移动→碰撞→冲刺→技能排队）
npm run example      # 第一批插件的完整链路
npm run example:p0   # P0 地基插件的完整链路
npm run example:batch3   # 第二批：迷你 Roguelike 一层
npm run example:batch4   # 第三批：游戏外壳
npm run example:batch5   # 第四批：据点经营 + 回合制战斗
npm run example:batch6   # 第五批：算法工具（地图 + 寻路 + 怪群）
npm run example:batch7   # 第六批：一场完整的战斗
npm run example:batch8   # 第七批
npm run example:batch9   # 第八批
npm run example:batch10  # 第九批
npm run example:batch11  # 第十批
npm run example:batch12  # 第十一批
npm run example:batch15  # 第十四/十五批：游戏外壳（设置/字幕/引导/路由/血条…）
```

---

## 1. 全景图

```
cocos-kit/
│
├── 【基础设施】零依赖。其余一切的地基，复制任何插件都要带上
│   ├── _core/
│   └── ds/
│
├── 【地基】不装这些，后面的一切都会歪
│   ├── config/
│   ├── di/
│   ├── event-bus/
│   ├── input/
│   ├── logger/
│   ├── number-roller/
│   ├── pool/
│   ├── result/
│   ├── rng/
│   ├── scheduler/
│   ├── scheduling/
│   └── signal/
│
├── 【通用工具】通用小件，按需取用
│   ├── binary/
│   ├── condition/
│   ├── curve/
│   ├── expression/
│   ├── i18n/
│   ├── noise/
│   ├── observable/
│   ├── save/
│   ├── snapshot/
│   ├── spatial/
│   ├── timeutil/
│   └── tween/
│
├── 【算法与地图】不认识你的游戏，只认数字和坐标 —— 最易复用
│   ├── dungeon/
│   ├── fov/
│   ├── grid/
│   ├── pathfind/
│   ├── pathfinding/
│   ├── room-graph/
│   └── steering/
│
├── 【数值与战斗】打得到、打得中、打得疼
│   ├── attack-token/
│   ├── attribute/
│   ├── buff/
│   ├── bullet-pattern/
│   ├── combo/
│   ├── damage-pipeline/
│   ├── element/
│   ├── hitbox/
│   ├── indicator/
│   ├── projectile/
│   ├── skill-caster/
│   ├── skill-player/
│   ├── skill-queue/
│   ├── skill-variant/
│   ├── targeting/
│   └── telegraph/
│
├── 【移动与碰撞】从「方向」到「角色真的动起来」
│   ├── collision/
│   ├── dash/
│   ├── joystick-mover/
│   └── mover/
│
├── 【AI与感知】敌人怎么发现你、怎么决策
│   ├── behavior-tree/
│   ├── fsm/
│   └── perception/
│
├── 【内容与掉落】打完之后掉什么、掉多少
│   ├── affix/
│   ├── blessing/
│   ├── craft/
│   ├── curse/
│   ├── gacha/
│   ├── loot/
│   ├── rarity/
│   ├── setbonus/
│   └── shop/
│
├── 【玩法框架】具体玩法的骨架
│   ├── builder/
│   ├── card/
│   ├── dialogue/
│   ├── interact/
│   ├── inventory/
│   ├── objective/
│   ├── turn/
│   └── wave-spawner/
│
├── 【进度与统计】跨局的东西：存档、成就、评分
│   ├── achievement/
│   ├── daily/
│   ├── difficulty/
│   ├── leaderboard/
│   ├── meta/
│   ├── quest/
│   ├── replay/
│   ├── runscope/
│   ├── score/
│   ├── scoring/
│   └── stats/
│
├── 【表现与外壳】玩家能感知、但说不清的「感觉」
│   ├── accessibility/
│   ├── audio/
│   ├── autoquality/（auto-quality）
│   ├── camera/
│   ├── currency/
│   ├── cutscene/
│   ├── feedback/
│   ├── gesture/
│   ├── minimap/
│   ├── progressbar/
│   ├── rebind/
│   ├── reddot/
│   ├── scenerouter/
│   ├── settings/
│   ├── subtitle/
│   ├── transition/
│   ├── tutorial/
│   └── uinav/
│
├── 【评分与匹配】多人对战：分数、撮合、对局运维
│   ├── elo/
│   ├── matchmaking/
│   ├── matchops/
│   ├── mmr/（team-mmr）
│   ├── ranking/
│   └── social/（report）
│
├── 【工程与运维】开发期与线上：调试、埋点、崩溃、反作弊
│   ├── analytics/（abtest）
│   ├── anticheat/（anti-cheat）
│   ├── cheatcode/
│   ├── command/
│   ├── crash/
│   ├── debug-console/
│   ├── diagpack/（diag-pack）
│   ├── gameflow/
│   └── telemetry/
│
├── 【适配与实体】轮子之间的轴，以及「这个 id 是谁」
│   ├── adapters/
│   └── entity/
│
├── examples/              19 个可运行示例
├── tests/                 3,447 项测试，零依赖迷你框架
├── scripts/               依赖检查 / 接口体检 / 清单生成
└── typings/               ⚠️ 真实 Cocos 项目要删掉
```

---

## 2. 速查表

**成熟度**：`stable` = 有完整测试 + 实际用过；`beta` = 有完整测试但还没在真实项目里跑过

| # | 插件 | 一句话作用 | 依赖 | 引擎耦合 | 测试 | 成熟度 |
|---|---|---|---|---|:---:|---|
| 1 | **Scheduler** | 统一时间源，真暂停/慢动作/顿帧的唯一路径 | `_core/math` | ❌ | 17 | beta |
| 2 | **TimeScale** | 多层时间缩放（相乘合成） | `_core/math` | ❌ | 11 | beta |
| 3 | **Logger** | 分级日志 + 环形缓冲（崩溃现场） | — | ❌ | 7 | beta |
| 4 | **Assert** | 快速失败断言（抓 NaN / 越界） | — | ❌ | 8 | beta |
| 5 | **ConfigLoader** | 配置加载、id 索引、热重载 | `config/Validator` | ❌ | 11 | beta |
| 6 | **Validator** | 全量校验 + 跨表引用完整性 | — | ❌ | 13 | beta |
| 7 | **InputManager** | 键鼠/触摸/手柄 → 动作名 | `_core/*` | ❌ | 20 | beta |
| 8 | **PRD** | 伪随机分布（降低方差） | `_core/types` | ❌ | 6 | beta |
| 9 | **ShuffleBag** | 洗牌袋（一轮内不重复） | `_core/types` | ❌ | 8 | beta |
| 10 | **WeightedTable** | 加权抽取（前缀和+二分） | `_core/types` | ❌ | 7 | beta |
| 11 | **LootTable** | 掉落表（数量/必掉/嵌套/保底） | `_core/types` | ❌ | 9 | beta |
| 12 | **Chest** | 开箱与三选一（去重/reroll） | `_core/types` | ❌ | 11 | beta |
| 13 | **StateMachine** | 有限状态机（显式转换） | — | ❌ | 11 | beta |
| 14 | **BehaviorTree** | 行为树（含组合/装饰/叶子节点） | `_core/types` | ❌ | 11 | beta |
| 15 | **BuffSystem** | Buff 生命周期（叠层/tick/驱散） | — | ❌ | 14 | beta |
| 16 | **Curve** | 关键帧曲线 | — | ❌ | 12 | beta |
| 17 | **Result** | 显式错误处理 | — | ❌ | 10 | beta |
| 18 | **Signal** | 轻量信号（类型安全） | — | ❌ | 9 | beta |
| 19 | **SaveManager** | 存档（迁移/原子写/校验） | — | ❌ | 12 | beta |
| 20 | **NumberRollerCore** | 数字滚动逻辑 | — | ❌ | 12 | beta |
| 21 | **GridGraph + A\*** | 网格寻路与路径平滑 | — | ❌ | 14 | beta |
| 22 | **SpatialHash** | 空间分区（范围查询加速） | — | ❌ | 9 | beta |
| 23 | **Noise** | 确定性噪声（地形/洞穴） | `_core/types` | ❌ | 11 | beta |
| 24 | **ConditionEngine** | 条件引擎（成就/任务） | — | ❌ | 14 | beta |
| 25 | **Tween** | 补间动画（纯逻辑） | `_core/math` | ❌ | 17 | beta |
| 26 | **EventBus** | 全局事件（字符串 key） | — | ❌ | 7 | stable |
| 27 | **Pool\<T\>** | 泛型对象池 + 泄漏追踪 | — | ❌ | 5 | stable |
| 28 | **RNG + Seed** | 可复现随机 + 人类可读种子 | — | ❌ | 17 | stable |
| 29 | **DamagePipeline** | 分阶段伤害计算 | `_core/math` | ❌ | 14 | beta |
| 30 | **Modifier** | 数值修正集（加算/乘算） | — | ❌ | 6 | beta |
| 31 | **SkillPlayer** | 技能时间轴播放器 | `_core/types` | ❌ | 18 | beta |
| 32 | **JoystickCore** | 轮盘纯逻辑 | `_core/*` | ❌ | 11 | beta |
| 33 | **JoystickMover** | 轮盘的 Cocos 组件层 | `cc` | ✅ | — | beta |
| 34 | **DIContainer** | 依赖注入（循环依赖检测 + fork） | — | ❌ | 12 | beta |
| 35 | **FrameScheduler** | 分帧调度（按时间预算切重活） | — | ❌ | 11 | beta |
| 36 | **Inventory** | 背包（堆叠/交换/拆分/整理） | — | ❌ | 24 | beta |
| 37 | **Deck** | 牌库/手牌/弃牌堆 | `_core/types` | ❌ | 15 | beta |
| 38 | **DialogueGraph** | 对话节点图（含完整性校验） | — | ❌ | 15 | beta |
| 39 | **Expression** | 表达式求值（配置里写公式） | — | ❌ | 21 | beta |
| 40 | **Snapshot** | 深拷贝/diff/补丁/撤销栈 | — | ❌ | 16 | beta |
| 41 | **ReplayRecorder** | 输入录制与回放 | — | ❌ | 12 | beta |
| 42 | **I18N** | 本地化（复数/插值/回退） | — | ❌ | 15 | beta |
| 43 | **TurnSystem** | 回合制（先攻/AP/死亡跳过） | — | ❌ | 16 | beta |
| 44 | **Grid** | 二维网格 | — | ❌ | 12 | beta |
| 45 | **GridPlacement** | 建造放置（占位/旋转/回滚） | — | ❌ | 11 | beta |
| 46 | **Hex** | 六边形坐标（axial） | — | ❌ | 9 | beta |
| 47 | **QuestSystem** | 任务（目标/链/防重复领奖） | — | ❌ | 27 | beta |
| 48 | **Shop** | 商店（折扣/多货币/库存） | — | ❌ | 24 | beta |
| 49 | **CraftSystem** | 合成（嵌套树/原子性/品质） | — | ❌ | 21 | beta |
| 50 | **BinaryHeap** | 优先队列（O(log n) 取最值） | — | ❌ | 13 | beta |
| 51 | **LazyHeap** | 惰性删除堆（A* decrease-key） | `ds/BinaryHeap` | ❌ | 4 | beta |
| 52 | **DisjointSet** | 并查集（连通性判定） | — | ❌ | 11 | beta |
| 53 | **QuadTree** | 四叉树（空间查询） | — | ❌ | 9 | beta |
| 54 | **SpatialHash（ds）** | 空间哈希（数字 id，轻量） | — | ❌ | 9 | beta |
| 55 | **AStar** | A* 寻路（地形代价/自定义通行） | `ds/BinaryHeap` | ❌ | 20 | beta |
| 56 | **PathSmoother** | 路径平滑（去锯齿） | — | ❌ | 5 | beta |
| 57 | **FlowField** | 流场（多单位共用路径） | `ds/BinaryHeap` | ❌ | 9 | beta |
| 58 | **Shadowcasting** | 视野 + 战争迷雾 | — | ❌ | 13 | beta |
| 59 | **Raycasting** | 射线视野（对照/调试） | — | ❌ | 4 | beta |
| 60 | **BSPDungeon** | BSP 地牢（死图率 0） | `ds/DisjointSet` | ❌ | 15 | beta |
| 61 | **RoomDungeon** | 随机房间地牢 | `ds/DisjointSet` | ❌ | 5 | beta |
| 62 | **CellularDungeon** | 元胞洞穴（自动清孤岛） | — | ❌ | 6 | beta |
| 63 | **MazeDungeon** | 迷宫（可 braid 加环路） | — | ❌ | 7 | beta |
| 64 | **Steering** | 单体转向 + 物理积分 + 队形 | — | ❌ | 21 | beta |
| 65 | **Flock** | 群体三规则（分离/聚集/对齐） | — | ❌ | 7 | beta |
| 66 | **Noise（统一入口）** | 默认 Simplex + fbm/ridged/heightMap | — | ❌ | 12 | beta |
| 67 | **SimplexNoise** | Simplex 2D/3D | — | ❌ | 4 | beta |
| 68 | **PerlinNoise** | 经典 Perlin | — | ❌ | 5 | beta |
| 69 | **ValueNoise** | 值噪声（最快） | — | ❌ | 3 | beta |
| 70 | **WorleyNoise** | 细胞/Voronoi 噪声 | — | ❌ | 4 | beta |
| 71 | **RoomGraph** | 房间图：分层DAG、路径不交叉、类型分配、热度分析 | — | ❌ | 62 | beta |
| 72 | **WaveSpawner** | 波次刷怪：三重超时兜底，保证永不卡住 | — | ❌ | 38 | beta |
| 73 | **MetaProgression** | 元进度：解锁树、循环依赖检测、存档容错 | — | ❌ | 45 | beta |
| 74 | **AffixSystem** | 词条：roll、冲突组、槽位限制、配置校验 | — | ❌ | 55 | beta |
| 75 | **Observable** | 响应式数据（UI 自动刷新的地基） | — | ❌ | 28 | beta |
| 76 | **CheatCode** | 作弊码命令（调试与测试） | — | ❌ | 26 | beta |
| 77 | **TimeUtil** | 时区/倒计时/服务器时间同步 | — | ❌ | 29 | beta |
| 78 | **UINav** | UI 导航栈（打开/关闭/返回） | — | ❌ | 35 | beta |
| 79 | **Telemetry** | 埋点上报（批量/采样/失败重试） | — | ❌ | 22 | beta |
| 80 | **Rarity** | 稀有度（权重/颜色/排序） | — | ❌ | 22 | beta |
| 81 | **Stats** | 统计追踪（计数/累计/最值） | — | ❌ | 33 | beta |
| 82 | **Achievement** | 成就系统（条件/进度/解锁） | — | ❌ | 30 | beta |
| 83 | **Gesture** | 手势识别（滑动/长按/双指） | — | ❌ | 35 | beta |
| 84 | **Rebind** | 按键重映射（冲突检测/恢复默认） | — | ❌ | 32 | beta |
| 85 | **GameFlow** | 流程状态机（启动→菜单→游戏⇄暂停→结算） | — | ❌ | 40 | beta |
| 86 | **CameraFollow** | 相机跟随（死区/前瞻/边界/瞬移检测） | `_core/math` | ❌ | 26 | beta |
| 87 | **HitFeedback** | 打击反馈（顿帧/震屏/闪白/飘字的时序编排） | `_core/math` | ❌ | 25 | beta |
| 88 | **ProgressBar** | 进度条（分段/延迟条/缓动/阈值滞回） | `_core/math` | ❌ | 36 | beta |
| 89 | **CurrencyWallet** | 多货币钱包（原子消费/精度量化/流水） | — | ❌ | 27 | beta |
| 90 | **Settings** | 游戏设置（范围校验/默认值迁移/分组） | — | ❌ | 26 | beta |
| 91 | **SubtitleTrack** | 字幕（时间轴纯查询/SRT 解析/跳过） | — | ❌ | 24 | beta |
| 92 | **Tutorial** | 新手引导（等待/超时兜底/断点续） | — | ❌ | 23 | beta |
| 93 | **RedDot** | 红点树（父子自动聚合/override） | — | ❌ | 22 | beta |
| 94 | **SceneRouter** | 场景路由（out→load→in 阶段机/返回栈） | — | ❌ | 20 | beta |
| 95 | **Accessibility** | 可访问性（减少动效影响面/色盲/字号） | — | ❌ | 20 | beta |
| 96 | **Elo** | ELO 评分（1v1 / 多人 / 零和 / 分数地板） | `_core/math` | ❌ | 32 | beta |
| 97 | **Glicko2** | Glicko-2（RD 不确定度 / 保守分 / 论文算例校验） | `_core/math` | ❌ | 21 | beta |
| 98 | **Matchmaker** | 撮合队列（双向范围 / 窗口放宽 / 超时兜底） | `_core/math` | ❌ | 33 | beta |
| 99 | **TeamBalancer** | 分队平衡（黑店不拆 / 角色配额 / 三种口径） | `_core/math` | ❌ | 22 | beta |
| 100 | **Lobby** | 房间（房主迁移 / 准备状态 / 开局校验） | — | ❌ | 37 | beta |
| 101 | **RankTier** | 段位与赛季（边界横跳 / 掉段保护 / 软重置） | `_core/math` | ❌ | 41 | beta |
| 102 | **AudioManager** | 音效：并发上限 / 去重 / **优先级抢占** / 三层音量混合 | `_core/math` | ❌ | 25 | beta |
| 103 | **BGMStack** | 分层背景音乐（水平重混，**功率域**等功率交叉淡变） | `_core/math` | ❌ | 18 | beta |
| 104 | **Cutscene** | 演出时间轴：seek 重放 / skip 应用终态 / **门控拦截时间** | `_core/math` | ❌ | 29 | beta |
| 105 | **Transition** | 场景转场：三段式 / 重入保护 / 加载超时 / 进度防假死 | `_core/math` | ❌ | 19 | beta |
| 106 | **Minimap** | 小地图：坐标变换 / 旋转 / 边缘钳制 / 战争迷雾 | `_core/math` | ❌ | 31 | beta |
| 107 | **Builder** | 建造：原子性放置 / 旋转占位 / 返还 / 原地升级 | `_core/math` | ❌ | 34 | beta |
| 108 | **AutoQuality** | 自动画质：中位数抗离群 / 迟滞防横跳 / 冷却防连降 / `fpsValid` | — | ❌ | 31 | beta |
| 109 | **DiagPack** | 诊断包：安全序列化 / 序列化后脱敏 / 分区配额 / 永不抛错 | — | ❌ | 32 | beta |
| 110 | **Collision** | 碰撞：SAT/MTV + **扫掠防穿墙** + **沿墙滑动** + 静态分离 + 射线 | `_core/math` | ❌ | 46 | beta |
| 111 | **CharacterMover** | 角色移动：加减速度分离 / 转身加速 / **撞墙速度投影** / 击退体感解除 | `_core/math` | ❌ | 26 | beta |
| 112 | **Adapters** ★ | 模块间适配器：地牢→网格 / 命中→施法 / 掉落→入包（消除重复胶水） | 无 | ❌ | 29 | beta |
| 113 | **EntityRegistry** ★ | 统一实体注册表：id 版本号防 ABA / 判定框别名映射 / 死亡归宿 / 遍历安全 | 无 | ❌ | 48 | beta |

> **第七批（肉鸽核心）**：这四个**都不依赖任何其他插件**，只吃 `IRandomSource` 注入。

> **⚠️ 两处功能重叠**（都有测试，API 不同，见 §7）：
> - 寻路：`pathfind/`（新，superset，推荐）vs `pathfinding/`（旧，batch3 依赖）
> - 空间哈希：`ds/SpatialHash`（数字 id，轻）vs `spatial/SpatialHash<T>`（字符串 id，带 queryNearest）

---

### ⭐⭐ SkillCaster（★ 用户点名 · 编排器）

**做什么**：把一个技能的完整流程串起来——
检查冷却/资源/距离 → 指示器 → 蓄力 → 判定/发射 → 伤害 → 后摇。

**为什么不 import 任何其他插件**：全部能力通过 `SkillCasterDeps` 注入。
换游戏只要换一批实现，编排逻辑一行不用改。

```typescript
const caster = new SkillCaster({
  hitboxes: world, projectiles: projSys,
  damage: pipeline, resources: myBag,
});

caster.learn({
  id: 'slash', cooldown: 0.8, mask: LAYER_ENEMY,
  windup: 0.12, hitDelay: 0.08, hitWindow: 0.1, recover: 0.2,
  hitShape: { kind: 'sector', radius: 2.5, angleDeg: 100 },
  damage: { raw: 20 },
});

caster.tick(dt);
const r = caster.tryCast('slash', { x, y, facingDeg, aimX, aimY, source: player });
if (!r.ok) showHint(r.reason);
```

**⚠️ `origin` 决定判定中心**（最容易踩的坑）：
`caster`（默认，从自身发出，如扇形挥砍）/ `aim`（落点，如火球爆炸）。
弹道**始终从施法者发射**，不受 origin 影响。

→ 详见 [`skill-caster/README.md`](./skill-caster/README.md)

### Hitbox（判定形状）

**做什么**：「这一刀砍到了谁」。四种形状 + 空间哈希 + 层掩码。

```typescript
const world = new HitboxWorld({ cellSize: 4 });
world.add({ id, shape: circle(0.5), x, y, rotation: 0, layer: LAYER_ENEMY, mask: 0, data: e });
const hits = world.query(sector(2, 100), px, py, facingDeg, MASK);
```

**精度**：circle×任意、rect×rect、capsule×rect/capsule **精确**；
**扇形参与的复杂组合是采样近似**（SAT 处理不了弧形边）。

**⚠️ `offsetX/offsetY` 对查询形状也生效**——剑气用它前移半个长度。

→ 详见 [`hitbox/README.md`](./hitbox/README.md)

### AttributeSet（属性容器）

**做什么**：`最终值 = (base + Σadd) × (1 + Σmul)`，带缓存、来源、标签。

```typescript
attrs.add({ attr: 'atk', type: 'add', value: 5, source: 'sword', tag: 'equip' });
attrs.add({ attr: 'atk', type: 'mul', value: 0.3, tag: 'buff' });
attrs.clearByTag('buff');      // buff 到期
attrs.removeBySource('sword'); // 换下装备
```

**多个 mul 相加而不是连乘**：两个 +50% → 2.0x 而不是 2.25x（防止收益爆炸）。

**与 damage-pipeline/ModifierSet 的区别**：那个服务于一次结算的瞬时修正，
这个是长期持有的属性。同属第 1 层不能横向依赖，所以自带实现——**重复是必要的**。

### Projectile（弹道）

**做什么**：5 种运动模式（linear / accel / parabola / homing / sine），碰撞注入。

```typescript
const sys = new ProjectileSystem({
  collision: createHitboxSweepProvider(world),
  bounds: { minX: -50, minY: -50, maxX: 50, maxY: 50 },
  maxStep: 0.5,     // 防高速穿墙
});
```

**⚠️ 接口要 sweep 不要 point**：子弹一帧走 0.22 米，
0.1 米厚的墙用点检测会直接穿过去（「偶尔穿墙，无法复现」）。

### Telegraph（预警）

**做什么**：windup → active → recover，把「不可预知的死亡」变成「我该躲但没躲好」。

**⚠️ 三条铁律**：
① 视觉与判定用同一份形状
② **active 开始后位置冻结**（否则玩家躲开了判定圈还跟着他）
③ 计时走注入的 dt（不用 setTimeout，否则暂停时蓄力仍在推进）

### SkillIndicator（指示器）

**做什么**：只算参数不画。输出 `Shape` + 位置 + 朝向，渲染层照着画。

**⚠️ 最重要**：指示器、预警、实际判定必须**共用同一份 Shape**，
否则会出现「看着能躲开却被打中」。

超出射程是**钳制**不是失败；支持目标吸附。

### InputBuffer + CoyoteTimer（手感地基）

**做什么**：动作游戏手感的第一块地基。哈迪斯、空洞骑士、鬼泣都靠它。

- 输入缓冲：**提前**按的键，稍后生效（窗口 100~150ms）
- 土狼时间：**延后**才生效的条件，现在仍算数（离地 80~120ms 仍可跳）

**⚠️ 暂停时必须传 dt = 0**，或注入 `now` 时间源接 Scheduler。

### DashController（冲刺）

**做什么**：位移 + 无敌帧 + 取消后摇。

**位移曲线**：`v = v0·(1-t)^2`，前 20% 时间走完 60% 距离——这就是「冲」的感觉。

**⚠️ 末段减速不能对位移打折**：早期实现导致位移先冲到 4.26m 又退回 4.03m
（终点抖一下）。正确做法是让衰减指数平滑增大（数学上保证 ds/du > 0）。

**`canCancel()`**：冲刺必须能取消攻击后摇，否则角色「不听话」。

### CameraShake（震屏）

**做什么**：打击感三件套之一。多震源叠加，各自衰减后向量相加。

**⚠️ 三个坑**：
① **震屏不能影响操作**——只偏相机节点，输入和 UI 不跟着偏
② `strengthScale = 0` 必须完全静止（**可访问性需求**，部分玩家会晕眩）
③ **dt 用未缩放时间**——顿帧时震屏该照常，否则变成缓慢漂移

轨迹**完全确定性**，与 RNG、ReplayRecorder 同一哲学。

---

## 3. 依赖规则（重要）

### 铁律 6：插件之间禁止横向 import

**唯一允许的跨插件依赖是"向下依赖"**，形成一个无环的分层 DAG：

```
第 0 层  _core/          纯函数（clamp / 插值 / Easing / 类型接口）
         ds/             纯数据结构（堆 / 并查集 / 四叉树 / 空间哈希）
              ↑
第 1 层  其他所有插件     只能往下看第 0 层
```

实测依赖图（脚本扫描，无环 ✓）：

| 被依赖方 | 依赖它的插件 |
|---|---|
| `_core` | behavior-tree, card, damage-pipeline, grid, input, joystick-mover, loot, scheduler, skill-player, tween |
| `ds` | pathfind, dungeon |

**第 0 层的准入条件极严**：

> **无状态、无副作用、不需要配置、不 import 引擎、不 import 任何其他插件。**

- `_core` 存的是**纯函数**（给个输入返回一个值）
- `ds` 存的是**纯数据结构**（自己管理数据，不认识"敌人""背包"这些概念）

一旦某个工具需要状态或配置（比如"带缓存的加载器"），
就该独立成第 1 层插件，而不是塞进第 0 层。

> **判据始终不变**：把这个文件夹复制到另一个项目，需要改几行？
> 第 0 层的答案是 0；第 1 层只需要带上它依赖的第 0 层。

**⚠️ 为什么 `ds` 能进第 0 层**：
`pathfind` 需要 `BinaryHeap`（A* 开放列表），`dungeon` 需要 `DisjointSet`（房间连通）。
把堆和并查集各复制一份是另一种浪费——改一处要改两处。
`ds` 自己**零依赖**，所以让它进第 0 层不会引入环。

### 随机源用接口而非具体类

`IRandomSource`（在 `_core/types.ts`）就是为这个存在的：

```typescript
export interface IRandomSource {
  next(): number;   // 返回 [0, 1)
}
```

所以 `loot/` 不依赖 `rng/`，但你能把 `RNG` 传进去：

```typescript
table.pick(new RNG(12345));        // 可复现
table.pick(MathRandomSource);      // 不需要复现时
table.pick(new FixedRandomSource([0.5, 0.9]));   // 测试里精确控制
```

附带好处：**所有随机逻辑都变得可测试**。

### 需要"外部能力"时，注入接口

| 能力 | 接口 | Cocos 侧实现 |
|---|---|---|
| 存储 | `IStorage` | `sys.localStorage` 或文件 |
| 配置数据源 | `ITableSource` | `resources.load(JsonAsset)` |
| 随机 | `IRandomSource` | `RNG` / `MathRandomSource` |

---

## 4. 分插件速查

### ⭐ Scheduler + TimeScale（最关键）

**作用**：所有计时的统一入口。这是「真暂停」的唯一实现路径。

**为什么最关键**：「暂停时 buff 仍在倒计时」是游戏开发最经典、最让玩家愤怒的 bug，
根因永远是某个系统偷偷用了 `setTimeout` / `Date.now()` / 引擎自带的 `schedule`。

```typescript
const sched = new Scheduler();
update(dt: number) { sched.update(dt); }        // 全项目只调这一处

sched.everyFrame((dt) => gameLogic(dt));        // 受暂停影响
sched.everyFrameUnscaled((dt) => uiAnim(dt));   // 不受影响（暂停菜单用这个！）
sched.delay(3, () => spawn());
sched.hitStop(0.08);                            // 顿帧 80ms
sched.slowMotion(0.5, 2);
sched.pause();
```

**两条通道是必须的**：暂停时 scale=0，如果暂停菜单的淡入动画也走缩放通道，
菜单会**自己把自己暂停**，永远淡不进来。

**顿帧参数**：轻击 60ms、重击 110ms、暴击 140ms、击杀 180ms。超过 200ms 就成了"卡"而不是"有力"。

📖 `scheduler/README.md`

---

### Logger + Assert

```typescript
const log = new Logger({ level: LogLevel.Debug, bufferSize: 200 });
const combat = log.module('combat');
combat.info('敌人生成', { id: 'slime', hp: 30 });   // 传对象，别拼字符串
crashReporter.attach(log.exportText());              // 崩溃时导出最近 200 条

Assert.finite(v, '伤害值');        // NaN 是最善于隐藏的 bug
Assert.found(map.get(id), '配置'); // 兼具断言与类型收窄
```

**环形缓冲是崩溃现场的唯一来源**——生产环境没有控制台，玩家只告诉你"闪退了"。
`export()` 已按时间顺序排好（物理顺序是乱的，那样排查会更糟）。

📖 `logger/README.md`

---

### ConfigLoader + Validator

```typescript
const schemas = { enemies: { id: {type:'string',required:true}, hp: {type:'number',min:1,max:9999} } };
const loader = new ConfigLoader(new CocosTableSource(), { tables: schemas });
await loader.loadAll();
const e = loader.get<EnemyCfg>('enemies', 'slime');   // 找不到会抛错
```

**校验器是省钱的关键**：拼错一个 id，没校验会表现成"第 47 层某个敌人不掉东西"，
排查三小时；有校验，启动那一刻就报错，三分钟改完。

**一次报全**（不是遇到第一个就停）——否则你要"改一个→重启→再改一个"循环几十次。

📖 `config/README.md`

---

### InputManager

```typescript
input.beginFrame();                        // ① 重置 + 从按键推导
input.setAxisAnalog('move', padX, padY);   // ② 注入手柄/摇杆（**必须在 beginFrame 之后**）
const dir = input.getAxis('move');         // ③ 读取
input.endFrame();                          // ④ 清除 just 状态（忘了会连放！）
```

**调用顺序是坑**：`setAxisAnalog` 写在 `beginFrame` 前面，模拟量会被清掉，
表现为"手柄摇杆完全没反应但键鼠正常"——很容易误判成手柄没连上。

📖 `input/README.md`

---

### loot（PRD / ShuffleBag / WeightedTable / LootTable / Chest）

你点名的「随机掉落与开宝箱」在这里。

| 类 | 用在哪 |
|---|---|
| `PRD` | 暴击、闪避（**每个角色一个实例**） |
| `ShuffleBag` | 随机音效、房间序列（避免聚集） |
| `WeightedTable` | 刷怪、事件选择（O(log n)） |
| `LootTable` | 掉落（数量区间/必掉/嵌套/**保底**） |
| `Chest` | 三选一奖励界面（去重/reroll/存档） |

**保底为什么必须有**：5% 掉落刷 20 次不出是完全正常的（35.8% 概率）。
玩家不会算概率，只会觉得"游戏在耍我"。保底给的是**确定的希望**。

📖 `loot/README.md`

---

### fsm / behavior-tree

```
先用状态机。当状态超过 10 个，或你开始写
if (state === 'chase' && hasLineOfSight && hp > 30%) 时，换行为树。
```

```typescript
// 状态机：构造时不触发 enter，要显式 start(ctx)
const fsm = new StateMachine<Ctx>({ initial: 'idle', states: {...} });
fsm.start(ctx);
fsm.update(ctx, dt);
fsm.findUnreachable();   // 启动时体检：有没有永远进不去的状态

// 行为树：优先级是显式的树结构，加规则 = 插节点
const tree = new BehaviorTree<Enemy>(new Selector('root', [flee, attack, patrol]));
tree.tick(ctx, dt);      // 必须传真实 dt
```

📖 `fsm/README.md` · `behavior-tree/README.md`

---

### BuffSystem

```
BuffSystem（生命周期）→ onChange → 外部重建 ModifierSet → 数值生效
```

三种叠层模式：`refresh`（重置时长）、`extend`（累加时长）、`independent`（各自计时）。

**坑**：tick 计时器必须**总是**推进，与这一帧有没有传 `onTick` 无关——
否则中毒掉血会忽快忽慢（这个 bug 被测试抓到过）。

📖 `buff/README.md`

---

### DamagePipeline + Modifier

```
(base + Σadd) × (1 + Σmul) → 暴击 → 抗性 → 遗物 → 护甲（除法）→ 真实伤害
```

顺序**写死在测试里**——团队里两人理解不同，数值就会对不上，而且这种错误没有任何报错。

关键接口是 `IDamageable`：管线不认识"敌人"，只认识"能被扣血的东西"。

📖 `damage-pipeline/README.md`

---

### SkillPlayer（Track + Cooldown）

时间轴 + 事件注册表 + `seek()`（技能编辑器的播放头就靠它）。

`Track.frameToTime(frame)` 用于**判定与动画对齐**——差 3 帧玩家就觉得"打空了"。

📖 `skill-player/README.md`

---

### pathfinding

```typescript
const path = findPath(grid, start, goal, { maxNodes: 5000 });
// 找不到返回 null（目标被围住是正常情况）
const smooth = smoothPath(grid, path);   // 去掉锯齿，否则敌人移动很机械
```

**必须设 `maxNodes`**：封闭大地图上寻路会跑很久，表现为"打开某个门时游戏卡住半秒"。

📖 `pathfinding/README.md`

---

### SpatialHash

```typescript
hash.update(e.id, e.x, e.y);          // 移动后**必须**更新
const hits = hash.queryCircle(x, y, 8);
```

**坑**：忘了 update 会表现为"明明站在爆炸范围里却没受伤"，极难定位。

适用门槛：元素 > 100 且查询频繁。< 50 直接遍历更快。

📖 `spatial/README.md`

---

### Noise

```typescript
const hm = noise.heightMap(128, 128, 0.05, { octaves: 5 });   // 已归一化到 [0,1]
const isWall = noise.fbm01(x * 0.1, y * 0.1) > 0.5;
```

**必须归一化**：不归一化的话不同种子值域不同，你的阈值判断
（>0.5 是墙）在不同种子下效果差很多——有时全是墙，有时全是空地。

📖 `noise/README.md`

---

### ConditionEngine

```typescript
engine.register({ id: 'slayer', conditions: [{ stat: 'kill:skeleton', op: '>=', value: 20 }] });
engine.addStat('kill:skeleton', 1);
const newly = engine.check();      // 只返回**本次新完成**的
engine.progress('slayer');         // 0.3（UI 进度条）
```

只管"条件是否满足 + 进度多少"，成就的展示和奖励发放由外部处理。

📖 `condition/README.md`

---

### Tween

```typescript
runner.add(new Tween(0.4).delay(0.3).ease('outBack').onUpdate(p => icon.x = lerp(a, b, p)));
runner.update(dt);   // 用 Scheduler 的 unscaled 通道
```

**Tween 不持有目标对象**——它只给你一个 0→1 的进度，你决定用它算什么。
这是它能补间任何东西（数值、颜色、位置、音量）的原因。

**拼错缓动名会抛错**（正确名是 `outQuad` 不是 `easeOutQuad`）。
静默退化成线性的话，你会花一小时调曲线才发现名字拼错了。

📖 `tween/README.md`

---

### RNG + Seed

```typescript
const rng = new RNG(12345);
rng.fork('loot');        // 独立子流：改了掉落逻辑不影响地图生成
Seed.random();           // 'SHIELD-38'（人类可读，便于分享）
Seed.daily();            // 每日种子（每日挑战）
Seed.encode(n) / decode(s)
```

**`fork()` 是关键**：所有系统共用一个 RNG 序列的话，
你在掉落里多加一次随机调用，整张地图就变了——调试会变成噩梦。

📖 `rng/README.md`

---

### EventBus vs Signal

| | EventBus | Signal |
|---|---|---|
| 作用域 | 全局（字符串 key） | 局部（是一个对象） |
| 用途 | "玩家升级了" | "这个按钮被点了" |

```typescript
bus.on('levelup', fn);        // 返回取消函数
btn.onClick.add(() => {});    // 类型安全，签名写错编译不过
```

📖 `event-bus/README.md` · `signal/README.md`

---

### SaveManager

```typescript
saves.registerMigration(1, 2, d => ({...d, gold: d.coins ?? 0}));   // 必须逐级
const r = saves.read<MySave>('slot1');   // 自动迁移，返回 Result 风格
```

原子写（先写临时 → 覆盖）+ 校验和（检测手改/损坏）+ 版本迁移。

📖 `save/README.md`

---

### NumberRollerCore

```typescript
roller.set(250);          // 从**当前显示值**继续滚，不跳变
roller.update(dt);
label.string = roller.formatted;     // '1,234,567'
roller.abbreviated;                  // '1.2K' / '3.4M'
```

渲染层约 30 行，示例在 `number-roller/README.md`。
**`isRolling` 为 false 时不要写 Label**（Label 赋值会触发重排）。

📖 `number-roller/README.md`

---

### JoystickCore + JoystickMover

你点名的「轮盘移动」。拆两层：

- `JoystickCore`：**纯 TS**，死区/归一化/吸附/多点触摸 → 可完整单测
- `JoystickMover`：Cocos 组件，只做"接事件 + 画 UI"，约 200 行且不含逻辑判断

**它输出的只是方向向量**，不认识"角色"——所以能用在角色、镜头、炮台、菜单光标上。

📖 `joystick-mover/README.md`

---

### DIContainer（依赖注入）

**用在哪**：服务超过 5 个、依赖链超过 2 层时。

```typescript
c.register('rng', (c) => new RNG(c.get<number>('seed')), { lifetime: 'singleton' });
const combat = c.get<CombatService>('combat');

const testC = c.fork();                      // 测试：只换一个依赖
testC.register('rng', () => fakeRng, { override: true });

c.validate();                                // 启动体检
```

**循环依赖会被检测**并报出链 `a → b → a`——不检测的话是
`RangeError: Maximum call stack`，完全看不出是 DI 的问题。

> ⚠️ **别把容器当服务定位器**（业务代码里到处 `get()`）。
> 那会把依赖关系**隐藏**起来，看构造函数看不出依赖什么。
> 只在**组合根**（main.ts）解析一次，然后构造函数传下去。

📖 `di/README.md`

---

### FrameScheduler（分帧调度）

**用在哪**：任何"一次性做完会卡一帧"的活——生成地图、实例化几百个敌人、读档、启动时校验配置表。

```typescript
const fs = new FrameScheduler({ budgetMs: 4 });

fs.scheduleBatch(monsterIds, (id) => spawn(id), {
  onProgress: (done, total) => bar.value = done / total,
  onDone: () => hideLoading(),
});

fs.update();      // 每帧
fs.flush();       // 同步跑完（测试/启动时）
```

**按时间预算切，不按数量**——每项的耗时方差很大，
按数量切会导致帧时间忽长忽短。

> 与 `scheduler/Scheduler` 互补：
> 前者管**什么时候**执行，本类管**一帧之内干多少**。

📖 `scheduling/README.md`

---

### Inventory（背包）

`move` 的三种情况：空 → 搬过去；同类装得下 → 合并；否则 → **交换**。

> 第 3 种为什么是交换而不是失败？玩家拖东西到已占用的格子，
> 期待的是"换位置"，返回失败会以为操作没生效。
> 而写成覆盖会导致**物品凭空消失**——玩家最愤怒的 bug。

`add()` 返回的是**放不下的数量**（不是布尔），
这样你不用再调一次 `remainingSpaceFor()` 去提示"还差多少空间"。

📖 `inventory/README.md`

---

### Expression（配置公式）

```typescript
new Expression('base * (1 + floor * 0.15)').evaluate({ base: 100, floor: 10 });  // 250
new Expression('dmg * 100 / (100 + armor)').evaluate({ dmg: 50, armor: 100 });
new Expression('isCrit ? dmg * 2 : dmg').evaluate({ dmg: 50, isCrit: 1 });
```

**绝不用 `eval` / `new Function`**：配置可能来自网络或玩家（RCE 风险），
而且出错时堆栈完全看不懂。本实现只能做数学和变量查找，**零副作用**。

两个刻意的宽容设计：
- 除零返回 **0** 而不是 NaN（NaN 会沿伤害管线传播，最后表现为"打怪没伤害"）
- 未定义变量当 0（要严格检查用 `evaluateStrict()`）

用 `variables()` 能列出用到的变量，启动时对照检查拼写——
拼错的变量名永远是 0 且没有任何报错。

📖 `expression/README.md`

---

### Snapshot（快照 / diff / 撤销）

```typescript
const diff = diffSnapshots(before, after);       // 哪些字段变了
const patch = createPatch(before, after);        // 只存变化
const restored = applyPatch(before, patch);      // 不改原对象

const undo = new UndoStack<GameState>();
undo.push(state);              // ← 在改动**之前**记录
const back = undo.undo(current);
```

**`deepClone` 是自己实现的**，因为 `JSON.parse(JSON.stringify())` 会：
丢失 `undefined`、把 `Date` 变字符串、把 `Map/Set` 变 `{}`、
**遇到循环引用直接抛异常**（游戏对象互相引用很常见）。

增量存档在**大对象 + 小改动**时效果最好。实测：
200 个敌人的世界状态，改 3 处 → 全量 10420 字节 vs 补丁 146 字节（省 99%）。

📖 `snapshot/README.md`

---

### ReplayRecorder（回放）

录**输入**而不是录状态：一局 10 分钟只要几 KB。

**前提：游戏必须是确定性的**（种子 RNG + 固定步长 + 不用 `Math.random`）。
三者缺一，回放就会**漂移**——前 30 秒一致，之后越走越偏。

种子不匹配时 `load()` **直接报错**，而不是"能播但结果不对"——
后者会让你以为回放是对的，实际是错的。

📖 `replay/README.md`

---

### I18N（本地化）

```typescript
i18n.addLocale('en-US', {
  'ui.start': 'Start Game',
  'item.count_one': '{n} item',     // ← 英文复数
  'item.count_other': '{n} items',
});
i18n.t('item.count', { n: 5 });     // '5 items'
```

中文包不需要写 `_plural`——查不到复数变体会退回 `item.count`。

**缺翻译最后回退到 key 本身而不是空字符串**：
空字符串会让按钮变成一片空白，玩家不知道那是什么；
显示 key 至少能看出"这里缺翻译"，也不阻塞开发。

`coverage()` 会认复数变体（否则 `item.count` 会被误判为未翻译）。

📖 `i18n/README.md`

---

### Deck（卡牌）

`reset()` 用**主牌库**重建，所以战斗中加入的临时卡（事件给的诅咒）
会自动消失——不需要手动清理，也就不会漏。

手牌满时抽不进的牌**留在抽牌堆**，而不是丢弃
（丢弃会让玩家觉得"我的牌莫名其妙少了"）。

📖 `card/README.md`

---

### DialogueGraph（对话）

用**图**而不是树，因为对话经常要回到之前的节点
（"让我再想想"→ 回到选项列表），树里要复制整棵子树。

两个体检方法建议启动时跑一次：

| 方法 | 发现什么 |
|---|---|
| `validate()` | 跳转到不存在的节点（运行时表现为"对话说到一半卡住"，且只在特定分支触发） |
| `findUnreachable(startId)` | 死内容（写了但没人能到达） |

图是**静态共享**的，Runner 是**会话**的——
分开后才能多人/多次同时对话互不干扰。

📖 `dialogue/README.md`

---

### TurnSystem（回合制）

顺序是**数组 + 游标**，不是队列——队列无法表达"插入到中间"和"移除中间的"。
配合**惰性清理**（死了只打标记，遍历结束才真删），
保证遍历过程中修改顺序是安全的。

```typescript
turn.addUnit({ id: 'hero', initiative: 15, actionPoints: 3 });
turn.start();
turn.spendAP(2);      // 不够时返回 false 且**不扣**
turn.killUnit('goblin');   // 轮到时自动跳过，不会崩
turn.skipUnit('slime');    // 眩晕，只跳过一次
turn.grantExtraTurn('boss'); // 连击
```

> **战斗中加人不重排**——重排会让已行动的单位再动一次。
> **同先攻用 id 二级排序**——保证确定性，否则回放对不上。

📖 `turn/README.md`

---

### Grid / GridPlacement / Hex

```typescript
// 网格
const g = new Grid<string>(10, 8);
g.get(99, 99);        // undefined（不崩）
g.neighbors8(3, 4);

// 建造
place.define({ id: 'house', w: 2, h: 2, rotatable: true });
place.place('house', 5, 5);      // 占 4 格
place.move(id, 10, 10);          // 失败时**回滚**
place.remove(id);                // 全部释放

// 六边形
hexDistance({q:0,r:0}, {q:3,r:-1});   // 3
hexSpiral(center, 2).length;          // 19（3r²+3r+1）
```

> ⚠️ **`new Array(n)` 是稀疏数组**，`.filter()` 会跳过空洞。
> `new Array(100).filter(() => true).length === 0`。
> 这个坑不抛异常，只是让计数永远返回 0。

📖 `grid/README.md`

---

### QuestSystem（任务）

设计要点：**上报，而不是注册监听**。
游戏里发生一件事就调 `report()`，系统内部匹配哪些目标关心它。
加新任务不用改任何游戏代码。

```typescript
q.define({ id: 'boss', requires: ['kill_slimes'], objectives: [...], rewards: {...} });
q.report({ type: 'kill', target: 'slime', n: 3 });
q.claim('kill_slimes');   // 只能领一次，第二次返回 null
```

**"完成"和"领奖"分开**：能弹奖励界面给正反馈；
发放失败（背包满）可以稍后重试，而不是丢掉。

📖 `quest/README.md`

---

### Shop（商店）

```typescript
shop.setPricing((base, ctx) => ctx.side === 'buy' ? base * 0.9 : base);
shop.buy('potion', 2, wallet);
// 钱不够 → { ok:false, reason:'insufficient_funds', need:300, have:10 }
// **不扣钱**
```

**为什么返回结构化结果**："买不起"和"没库存"的提示完全不同，
返回 boolean 会逼你再查一次状态——而两次查询之间状态可能已经变了。

计算顺序：`basePrice → markup → sellRatio → pricing 钩子 → 取整`
**取整必须在最后**，中途取整会让多次折扣累积出偏差。

📖 `shop/README.md`

---

### CraftSystem（合成）

**头号事故防护**：扣了材料但产出失败 → 材料凭空消失。
固定五步顺序杜绝它：

```
1. 检查材料（不扣）→ 2. 决定产出 → 3. 先尝试产出
→ 4. 成功才扣材料 → 5. 失败则回滚产出，材料分毫未动
```

```typescript
craft.totalMaterials('iron_sword');  // { iron_ore: 6, wood: 1 }（展开嵌套）
craft.totalMaterials('iron_sword', 1, inv);  // 传背包会**抵扣已有的中间产物**
craft.findCraftable(inv);            // 反向查询：现在能做什么
```

背包通过 `IInventory` 接口接入（只需 `count`/`remove`/`add`），
不绑死任何具体背包实现。

📖 `craft/README.md`

---

### ds（四种数据结构）

**判据：把 `Array` 换成它们能省多少**

| 现搓方案 | 换成 | 省 |
|---|---|---|
| 每次 `sort()` 取最大值 | `BinaryHeap` | O(n log n) → O(log n) |
| 两两比较做碰撞 | `QuadTree` / `SpatialHash` | O(n²) → O(n) |
| 用数组找连通分量 | `DisjointSet` | 查不出 → O(α) |

```typescript
const heap = new BinaryHeap<Task>((a, b) => a.p - b.p);
const uf = new DisjointSet(n);
uf.union(0, 1); uf.connected(0, 2); uf.componentCount;   // 死图检测核心
const qt = new QuadTree<T>({ x: 0, y: 0, w: 800, h: 600 });
const sh = new SpatialHash(64);
```

**坑**：`QuadTree` 分裂时不能重复计数（曾 50 个对象数出 95）；
`rectsOverlap` 的 `padding` 必须两侧都加。

📖 `ds/README.md`

---

### pathfind（A\* / 平滑 / 流场）

**怎么选**：

| 场景 | A* | 流场 |
|---|---|---|
| 1 个单位找一次路 | ✅ | ❌ 算了全场 |
| 100 个单位去同一个点 | ❌ 算 100 次 | ✅ 算 1 次 |
| 目标频繁变动 | ✅ | ❌ 每次重算 |

实测（56×26）：A* 一次 2ms；流场构建 2ms + 每单位 O(1)。

```typescript
const r = finder.find(start, goal);
if (!r.found) return;              // ← **必须检查**，不可达时 path 是空的
const smooth = smoother.smooth(start, r.path);   // 58 步 → 21 步
```

**坑**：`closed` 数组必须存 stamp 值而不是布尔，
否则**同一个实例第二次 `find` 起永远失败**，且不抛异常。

📖 `pathfind/README.md`

---

### fov（视野 / 战争迷雾）

| 算法 | 11×11 空房间、半径 5 的可见格数 |
|---|---|
| **Shadowcasting** | **81**（理论最大值） |
| Raycasting | 21 |

```typescript
fov.compute(px, py, 8);
fov.canSee(x, y);          // 当前可见
fov.hasExplored(x, y);     // 曾经看过 → 灰暗显示
fov.hasLineOfSight(x0, y0, x1, y1);   // 远程攻击 / AI 发现玩家
```

**坑（最值得记的一个）**：八分体坐标变换**别手写成映射函数**。
本库第一版就是这么写的，4 个八分体的 Y 符号写反——
不报错、不崩溃，只是视野从圆形变菱形（81 格只看到 47 格）。
**直接抄标准 2×2 矩阵**，抄错了测试能抓到。

**对称性**：Shadowcasting 不是严格对称的（可能 A 看得见 B，B 看不见 A）。
游戏逻辑层补偿最简单："只要有一方能看见就视为互相可见"。

📖 `fov/README.md`

---

### dungeon（四种关卡生成）

| 算法 | 特点 | 死图率 |
|---|---|---|
| **`BSPDungeon`** | 房间规整、天然连通、**推荐** | **0**（100 次实测） |
| `RoomDungeon` | 随机撒房间 + MST 连接 | 0 |
| `CellularDungeon` | 洞穴感，**必须清孤岛** | 0（已清理） |
| `MazeDungeon` | 完美迷宫，可 braid 加环 | 0 |

**生成后必须自检**——这是发现死图的唯一可靠手段：

```typescript
dungeon.generate();
dungeon.isFullyConnected();      // 地板连成一块？
dungeon.allRoomsReachable();     // 房间互相可达？
dungeon.findFarthestRoom(0);     // 最远房间 → 放出口 / Boss
dungeon.toString();              // ASCII 可视化
```

**上线前跑 10000 次自检，目标死图率 0%。** 手测几十次碰不到极端情况。

**坑**：`MazeDungeon` 尺寸必须是奇数（"格子+墙"表示法，偶数会漏）；
`CellularDungeon` 不清理孤岛的话，宝箱可能生成在走不到的地方。

📖 `dungeon/README.md`

---

### steering（转向 + 群体）

**核心设计：输出"转向力"而不是速度**，多个行为加权叠加，调用方积分。
本库**不碰 dt**。

| 场景 | 用 |
|---|---|
| 日常移动 | `arrive`（带减速，**不要用 seek**，会抖） |
| 追击 | `pursuit`（预判，抄近路） |
| 怪群 | `Flock`（分离 + 聚集 + 对齐） |

```typescript
applyForce(a, flock.compute(a, neighbors));
applyForce(a, arrive(a, playerPos, 4, 0.6));
integrate(a, dt);        // 内部会**清零力**
```

**坑**：
1. 撞墙要**回退位置**，不是只反向速度（否则卡在墙里反复横跳）
2. `obstacleAvoid` 在障碍正前方时差向量是零向量 → 本库已修复（改用侧向力）
3. 邻居查询务必配 `SpatialHash` / `QuadTree`，别 O(n²)

📖 `steering/README.md`

---

### noise（五种噪声 + 分形）

| 类 | 用 |
|---|---|
| **`Noise`** | **统一入口，默认 Simplex，90% 场景用它** |
| `SimplexNoise` | 质量最好，无明显方向性 |
| `PerlinNoise` | 经典（**整数格点会退化成 0**） |
| `ValueNoise` | 最快，块状感更明显 |
| `WorleyNoise` | 细胞 / 龟裂 / 区域划分 |

```typescript
const n = new Noise(seed, rng);     // 传 rng → 绑定主世界种子，可复现
n.fbm01(x, y);                      // 0~1，最常用
n.ridged(x, y);                     // 山脊 → 山脉
n.heightMap(w, h, 0.08);            // 已归一化到 [0,1]
```

| 参数 | 建议 |
|---|---|
| `octaves` | 4~6（超过 8 看不出差别，纯浪费） |
| `persistence` | 0.5（高=毛躁，低=平滑） |
| `lacunarity` | 2.0 |

**坑**：`heightMap` 默认归一化，**别关**——
不同种子的原始范围差异很大，阈值判断会让地形忽大忽小甚至全是墙。

📖 `noise/README.md`

---

## 5. 通用纪律（每天都要守）

### room-graph（房间图）

肉鸽关卡的**结构**层——不是"一堆随机房间"，而是分叉的 DAG。

```typescript
const g = generateRoomGraph({ depth: 9, rng: new RNG(12345) });
assignTypes(g, { weights: {...}, fixed: { 0: RoomTypes.ENTRY } }, rng);
const d = diagnose(g);        // 上线前必跑
console.log(graphToString(g)); // ASCII 调试图
```

**三条硬保证**：无死路、全连通、路径不交叉。

| 违反 | 后果 |
|---|---|
| 死路 | 玩家走进房间出不来 |
| 不连通 | 有房间永远走不到 |
| 交叉 | 地图变 X 形乱麻，玩家分不清走哪条 |

**反直觉的硬规则**：Boss 前一层强制至少一个休息房。
玩家一路打过来血量必然见底，Boss 前没有回血点会造成
"明知道打不过但没资源"的挫败——这不是玩家决策失误，是生成的锅。

**平衡分析必须用 `findBestPath`，不能用 `findPath`**：

| 策略 | 精英数 |
|---|---|
| 全局最优 · 稳健 | 0 个 |
| 贪心 · 稳健 | **2 个** ← 比"激进"还多 |

`findPath` 走到某个房间后可能只剩不想要的分支，
不同策略会走出**完全相同的路线**，让平衡分析失效。

批量自检：500 个种子零死图 / 零不连通 / 零交叉。

---

### wave-spawner（波次刷怪）

**核心承诺：永远不会卡住。**

| 兜底 | 解决什么 |
|---|---|
| `entityTimeout` | 一波里只有 1 个卡住，玩家追着它满地图跑 |
| `waveTimeout` | 整波卡住 |
| `totalTimeout` | 限时挑战硬上限 |

"怪卡在地图外导致门永远打不开"是**每个动作游戏都会遇到的 bug**，
开发期难复现（要怪正好卡进墙缝），上线后必被玩家撞到。

`ISpawnSink.forceKill` 必须实现——那是让卡住的怪消失的唯一途径。
常见做法：淡出销毁，或传送到场地中央（后者更自然）。

`onFallback` 触发说明**有系统性问题**（生成点被占用、寻路失败），
正常游戏里应该永不触发，上线后建议上报服务器。

必设 `maxSpawnsPerFrame`（3~5）——
20 个怪挤在一帧生成会让"进房间卡一下"，
分批生成反而更有压迫感。

---

### meta（元进度 / 局外成长）

"死了也在变强"的机制，肉鸽能让人连打几十小时的关键。

**四种效果运算**：`add` / `mul`（**相加非连乘**）/ `set`（取最大）/ `flag`。
公式 `(base + Σadd) × (1 + Σmul)` 与 `attribute`、`affix` 完全一致——
三处必须统一，否则玩家会发现"面板和实际对不上"。

**两个货币坑**：
1. 单货币可省略 `currency`；多货币不写是歧义 → **构造时立即抛错**
2. `cost` 数组越界时用最后一项，**不是 0**（返回 0 = 白送）

曾经的 bug：注册 `currencies: ['soul']` 但节点没写 `currency`（默认 `'default'`），
`currency()` 对未知货币静默返回 0 →
玩家攒了 320 灵魂却买不起 20 的东西，且无任何报错。

**循环依赖构造时检测**（DFS + 三色标记），发现环直接抛错。
未知前置则忽略——一个笔误不该让游戏起不来。

**存档容错**：删掉的节点静默跳过、超限等级 clamp、未知货币忽略，
全部记在 `RestoreReport` 里。旧存档绝不能让玩家进不去游戏。

---

### affix（词条系统）

装备随机属性，ARPG / 肉鸽构筑深度的核心来源。

**两个必须配的字段**：

| 字段 | 不配会怎样 |
|---|---|
| `group` | 一件装备出现两条"+8% 暴击"和"+12% 暴击"，玩家觉得被占了词条位 |
| `precision` | 百分比取整后只剩 5 个可能值，构筑深度消失 |

**`slots` 的语义**：`slots: ['weapon']` = 只能武器；
**不写 = 通用词条，任何槽位都能出**。
所以"boots 只有 2 个专属词条"不等于只能 roll 出 2 条。

**稀有度双参数**：`weight`（抽到什么品质）+ `valueScale`（数值放大多少）。
有了 valueScale，一个词条定义就能覆盖全部稀有度，
不用为每个稀有度重复配一遍表。

启动时跑 `validateAffixPool`——配置错误不报错，
只表现为"玩家拿到垃圾装备但看不出为什么"。
典型警告：稀有度倒挂（橙装不如蓝装）、槽位词条不足（永远凑不满）。

---

### 七铁律

| # | 铁律 | 判据 |
|---|---|---|
| 1 | 零业务依赖 | 插件里不得出现 Player / Enemy / 遗物 / Boss |
| 2 | 依赖注入 | 需要对象时由调用方传入，不得内部去"找" |
| 3 | 单一职责 | 一个插件只解决一个问题 |
| 4 | 配置驱动 | 所有可调参数进配置对象，禁止硬编码魔数 |
| 5 | 可卸载 | 必须 `destroy()`，不留残留（事件/定时器/节点） |
| 6 | 禁止横向依赖 | 插件间不得互相 import，只允许向下依赖第 0 层（`_core` / `ds`），见 §3 |
| 7 | 每个插件自带 | README + 测试（算法类必须有）+ 示例 |

> **最简判据**：把插件文件夹复制到另一个 Cocos 项目，需要改几行？**答案是 0 才算合格。**

### 三条口诀

1. **需要什么让别人给你，别自己去拿**（依赖注入）
2. **每当参数写了一个具体类型，问自己：我真正需要它的什么能力？**
   `takeDamage(e: Enemy)` → 你要的是"它能被扣血" → 改成 `IDamageable`
3. **出现第三次才抽象**（三次法则）。第二次就抽往往会抽错——
   只有见过三次你才知道真正的共性在哪。

### 机制可复用，内容不复用

| 复用 ✅ | 不复用 ❌ |
|---|---|
| 伤害管线 | 骨王的横扫技能 |
| 行为树框架 | 骷髅兵的行为树配置 |
| 掉落表机制 | 55 件遗物的具体效果 |
| 状态机 | Boss 的三阶段脚本 |

---

## 6. 测试

```bash
npm test     # 1636 项
npm run check:deps   # 依赖规则检测：环 / 层违规 / L0 纯净度
```

### 结构

| 文件 | 内容 |
|---|---|
| `tests/_framework.ts` | 迷你框架（describe / test / assert / eq / near / throws） |
| `tests/run_core.ts` | 第一批插件（86 项） |
| `tests/run_p0.ts` | P0 地基插件（87 项） |
| `tests/run_batch3.ts` | 第二批插件（208 项） |
| `tests/run_batch4.ts` | 第三批插件（141 项） |
| `tests/run_batch5.ts` | 第四批插件（126 项） |
| `tests/run_batch6.ts` | 第五批插件（206 项） |
| `tests/run_batch7.ts` | 第六批插件（131 项） |
| `tests/run_batch8.ts` | 第七批插件（123 项） |
| `tests/run.ts` | 入口 |

各批分布：**第一批 86 / P0 地基 87 / 第二批 208 / 第三批 141 / 第四批 126 / 第五批 206**

### 测试抓到了 21 个真实缺陷

| # | Bug | 现象 |
|---|---|---|
| 1 | `Seed.encode/decode` 往返不一致 | `encode(12345)` → decode 得 2745 |
| 2 | 极高护甲时伤害归零 | 免疫判定写在了取整**之后** |
| 3 | `everyFrame` 被当成一次性任务 | `interval=null` 导致只执行一帧 |
| 4 | `beginFrame` 覆盖模拟量 | 手柄摇杆完全没反应 |
| 5 | `repeat` 浮点累积误差 | 1 秒 / 0.5s 间隔只触发 1 次 |
| 6 | **StateMachine 构造时调 enter** | 传 undefined 进去，启动即崩 |
| 7 | **BuffSystem tick 计时依赖回调** | 中毒掉血忽快忽慢 |
| 8 | **Tween 缓动名拼错静默退化** | 动画"不太对劲"但没报错 |
| 9 | **SaveManager 校验和未保护** | 循环引用导致保存时直接崩溃 |
| 10 | **NumberRoller 缩写阈值 10000** | 1234 显示一长串，UI 放不下 |
| 11 | **FrameScheduler.flush 不移出队列** | 靠 guard 撑到 100 万次，卡好几秒 |
| 12 | **DI 模糊匹配用子串** | 拼写错误给不出提示（`playerServce`） |
| 13 | **I18N coverage 不认复数** | `item.count` 被误判为未翻译 |
| 14 | **稀疏数组的 filter 陷阱** | `new Array(n)` 有空洞，`.filter()` 跳过 → `freeCount` 永远返回 0，**且不抛异常** |
| 15 | **Shop 流水不记货币类型** | 多货币游戏里金币和钻石的账混在一起算 |
| 16 | **TurnSystem 的 guard 没生效** | 声明了死循环防护却没用上，数据异常时会卡死 |
| 17 | **QuadTree 分裂时重复计数** | 插入 50 个对象，`count` 变成 95 |
| 18 | **rectsOverlap 的 padding 只加一侧** | 判定不对称：左边不重叠但右边重叠 |
| 19 | **A* 的 closed 数组用布尔** | **同一实例第一次 find 成功，第二次起永远失败** |
| 20 | **obstacleAvoid 零向量退化** | 障碍正在正前方时差向量为 0 → 避障力 0 → 径直撞上去 |
| 21 | **Shadowcasting 八分体矩阵抄错** | 视野从圆形变菱形，81 格只看到 47 格，**且形状看着"还挺合理"** |

前 16 个见上文，第 17–21 个是第五批（算法层）抓到的。

另外还修了两个 **API 设计问题**（不是崩溃，但用起来别扭）：
- `ItemDef` 缺 `name` 字段——背包 UI 几乎总要显示名字
- `DialogueGraph<S extends Record<string, unknown>>` 约束过严，
  逼用户在自己的状态类型上加索引签名，已放宽为 `object`

### 本轮最值得记住的一个坑：稀疏数组

```typescript
new Array(100).filter(() => true).length    // 0   ← 不是 100！
```

`new Array(n)` 创建的是**稀疏数组**（holes，不是 `undefined` 值）。
`filter` / `map` / `forEach` 会跳过空洞。

这个 bug 藏得极深：它不抛异常、不报 undefined，
只是让 `freeCount` 永远返回 0。如果没有测试，
你会在几周后才在别的地方发现"为什么空格数不对"。

**修复**：`new Array(n).fill(undefined)`，或者干脆用计数循环。

> 时间语义和数值计算的错误**不会抛异常，只会悄悄偏差**。
> 这就是为什么这些必须有测试。

### 第七批（肉鸽核心）最值得记住的三个

**① 已用记录从未更新**（最难发现的一类）

`roll()` 里创建了 `usedDefs` / `usedGroups`，却**从来没往里 add**。

三条症状同时出现，且都不报错：
- `allowDuplicate: false` 形同虚设
- 同组的两条暴击率同时出现
- 池子永远抽不干（每次都在全池选）

靠读代码很难发现——变量声明了、也传进去了。
只有断言"同组不共存"才能逼出来。

**② 波次推进后仍读取已越界的 wave**

```
单体超时清掉最后一个卡住的怪 → _finishWave → _advanceWave
→ waveIndex 已 +1（可能越界）→ 读 _waves[waveIndex] 得到 undefined
→ "Cannot read properties of undefined"
```

只在"有怪卡住"时复现——开发期几乎碰不到，上线后必然出现。
所以每个可能改变 `waveIndex` 的操作后都要重新检查边界。

**③ 贪心路径会让两种策略走出同一条路**

平衡分析用 `findPath` 比较"激进 vs 稳健"，
结果两者完全相同 → 得出"策略无差异"的**错误结论**。

改为 `findBestPath`（枚举后打分）才体现真实差异。

> 累计 **30 个缺陷**（第六批 21 + 第七批 9），全部已修并有测试锁定。

### 第八批（系统与深度）最值得记住的五个

**① 声音衰减：浮点差 0.00028 导致贴脸开枪没反应**

线性衰减下，8 米外的枪声只有 0.73 强度，**永远达不到 1.0 的察觉阈值**——
于是「听到枪声」只会让敌人起疑然后衰减归零，潜行玩法的反馈链断了。

改成二次衰减后，0.5 米处是 0.99972 —— **还是 < 1.0**。
差 0.00028，结果就是「贴着敌人开枪它也不理你」。

最终方案：半径内侧 10% 视为**近场**，响度不打折；之后二次滚降。

> 这种浮点边界问题极难排查：单看每个数字都合理，合起来结果完全错误。

**② Gacha 差一错误：硬保底在第 89 抽触发**

`_doPull()` 在判定**之前**已经把计数推进过了（第 N 抽时 `pityCount === N`），
而 `_rateFor()` 里写成 `n + 1 >= hardPity`，保底就提前了一抽。

有多隐蔽：不报错、不崩溃、玩家几乎察觉不到。
但「抽满 90 必出」是写进规则的**承诺**，
玩家数着抽数对照时会发现对不上 → 信任受损。

**③ `simulate()` 的干涸统计口径与保底语义不一致**

原写法「除了出货的那个稀有度，其余全部 +1」，
于是第 50 抽出五星时四星的 dry 继续累加，报出 `worstDry['r4'] = 23`。

看起来像「四星保底失效了」，但保底其实是好的。
正确语义是「连续多少抽没拿到 **r 及以上**」。

**④ `AttackToken.reset()` 残留令牌**

循环调 `release()`，而 `release()` 内部会把队列里的补上来：

```
maxConcurrent=2，持有 {1,2}，队列 [3]
release(1) → 3 补位 → active={2,3}
release(2) → 队列已空  → active={3}   ← 残留！
```

第二层后果更严重：**3 号从未收到 `onRelease`**，
业务侧以为它还在攻击中，不清理状态。

修法：先把所有请求静默摘下来，再统一通知。

**⑤ `BulletPattern.restartEmitter()` 把 delay 丢了**

构造和 `reset()` 写 `e.time = -delay`，而 `restartEmitter()` 写 `e.time = 0`。
于是同一个「三连发 + 0.5s 前摇」的招式：
第一次播放有前摇，restart 后再播**没有前摇**。

表现为「这个 Boss 的招式时快时慢」，而配置里明明写了 0.5。

> 这类 bug 的形态是**同一份配置在两个代码路径下行为不同**。
> 只有把重置逻辑收敛到一处（`_resetTime()`）才能根治。

**⑥ Scoring 的 par/zero 方向**

`lower-better` 要求 `zero > par`。写反后 span 为负，
兜底逻辑会让「60 秒」和「5 秒」都得 0 分——
玩家发现「我打得飞快却拿 D」，而代码不报任何错。

所以**构造时立即抛错**，而不是静默兜底。

> 累计 **36 个缺陷**（前七批 30 + 第八批 6），全部已修并有测试锁定。

### 第五批（算法层）最值得记住的三个

**① A* 的 stamp 机制（第 19 个）**

```typescript
// ❌ 错：起点在新一轮开始时就带上了上一轮的 closed=1
if (closed[ci] === 1 && stamps[ci] === stamp) continue;
// ✅ 对：closed 也存 stamp 值
if (closed[ci] === stamp) continue;
```

内部用递增 stamp 避免每次寻路都清空数组（省 O(n)）。
但 `closed` 如果存布尔，上一轮的残留会让**起点被直接跳过**。

现象：**同一个 AStar 实例，第一次 `find` 成功，第二次开始永远返回空路径。**
不抛任何异常。

**② Shadowcasting 的八分体矩阵（第 21 个）**

第一版手写成映射函数 `(y, x) => ({ x: y, y: -x })`，
4 个八分体的 Y 符号写反了。

**这个 bug 靠肉眼审查基本发现不了**——
它不报错，视野形状从圆形变成菱形，
而菱形"看起来也像那么回事"。
是"空房间半径 5 应该看到全部 81 格"这条测试把它逼出来的。

**教训**：涉及坐标变换时，抄标准矩阵，别自己推导。

**③ 算法 bug 的共性**

这五个 bug 没有一个会抛异常。它们的共同形态是
**"90% 情况对，10% 情况诡异"**——
而那 10% 往往出现在玩家最意想不到的时刻。

---

## 6.5 第八批速查：系统与深度

这一批的 10 个模块有一个共同主题：**公平性**。

| 模块 | 它在守护什么 | 失败时不报错，只表现为 |
|---|---|---|
| `perception` | 敌人不许隔墙读心、不许背后长眼 | 玩家觉得「敌人开挂」 |
| `attack-token` | 不许被 5 个怪同时秒杀 | 怪站着不动 / 被瞬秒 |
| `bullet-pattern` | 弹幕节奏稳定 | 卡顿后弹幕缺一段 |
| `element` | 反应配置正确 | 「这技能不反应」查不出 |
| `targeting` | 锁定不抖、不锁身后 | 角色莫名回头、来回扭头 |
| `combo` | 按了就要有反应 | 「按了没出招」 |
| `objective` | 关卡按序推进 | 「什么都没发生」 |
| `difficulty` | 隐性调节且不让你察觉 | 难度乱跳 / DDA 没生效 |
| `gacha` | 保底承诺 100% 兑现 | 玩家数着抽数发现对不上 |
| `scoring` | 评级能解释「我哪里没做好」 | 「打得很好却只有 B」 |

### perception（感知）

```typescript
const sys = new PerceptionSystem({ los, jitter: 0.3, allyAlertDelay: 0.8 });
sys.addPerceiver(1, PerceptionPresets.humanoid, x, y, facing);
sys.addTarget(100, px, py);
sys.emitSound(100, px, py, 1.0, 25);
sys.tick(dt);
sys.isAware(1);  sys.lastKnownPosition(1);
```

四通道：视觉（距离+角度+遮挡）、听觉（穿墙衰减）、记忆（最后位置）、传播（同伴**延迟**响应）。
声音衰减用「近场不打折」模型——否则贴脸开枪是 0.99972 < 1.0，敌人不理你。

### attack-token（攻击令牌）

```typescript
const tokens = new AttackTokenSystem({ maxConcurrent: 2, holdTimeout: 3 });
if (tokens.request(id, priority) === 'active') startAttack(id);
tokens.release(id, 'finished');
tokens.remove(id);        // 死亡时统一调用，自动归还
tokens.tick(dt);
```

`maxConcurrent` 推荐 2。**`holdTimeout` 必须设**——否则动画卡死会让所有敌人永久失去攻击能力，且不报错。

### bullet-pattern（弹幕）

```typescript
bp.addEmitter({ id: 'ring', shape: Shapes.ring(12, 6, 'orb'), interval: 1.2, delay: 3 });
bp.play();
for (const s of bp.tick(dt)) spawnBullet(s);   // s.angle / s.speed / s.typeId
```

`delay` 的精确语义：**第一发出现在 `delay + interval`**。
输出是角度不是向量（避免浮点累乘漂移），要向量用 `angleToVec(angle, speed)`。

### element（元素反应）

```typescript
const r = es.apply(targetId, 'fire', 50);
if (r.reactionId) applyEffect(r.data);   // 效果在 data 里，系统不解释
```

**反应定义里没有 `damage` 字段**——这是零业务依赖的关键。
`consumeAll: false` 会残留 base 元素，让反应可以串联（这是玩法深度的来源）。

### targeting（目标锁定）

```typescript
sel.addScorer(byDistance(1)); sel.addScorer(byAngle(1)); sel.addScorer(byStickiness(3));
sel.update(dt, candidates);   // ⚠️ 必须传真实 dt，宽限期才会耗尽
```

`byAngle` 最重要——没有它玩家会锁到身后的怪，角色突然回头。
迟滞 + 粘性叠加，才能完全吸收位置抖动。

### combo（连招）

```typescript
const m = c.input('L');
const timedOut = c.tick(dt);   // ⚠️ 每帧必须调用
```

核心机制：输入是更长招式的**前缀**就等待，否则立即触发；窗口超时强制结算。
`priority` 让遗物强化不用改配置顺序。

### objective（关卡目标）

```typescript
Objectives.kill('clear', 10);
Objectives.after(Objectives.protect('vip', 3), ['clear']);   // 前置依赖
Objectives.optional(Objectives.timeLimit('speed', 90));      // 失败不惩罚
```

`protect` 的初始进度 = target（满血），**进度反向递减**，最容易写反。
循环依赖在构造时抛错。

### difficulty（难度 + DDA）

```typescript
dd.setTier('hard');
dd.report(performance, duration);   // 0~1
dd.multiplier('enemyDamage');       // 基础 × (1 ± 15%)
```

DDA 幅度**钳制在 ±15% 且默认不显示**——玩家发现游戏在放水，成就感会崩塌。
判断 `allowDDA` 必须用 `=== false`，不能用 `!x`（opt-out 布尔的经典陷阱）。

### gacha（抽卡保底）

```typescript
g.pityCount('r5');   g.toHardPity('r5');   g.currentRate('r5');
const report = simulate(opts, 100000);   // 配平用
```

**和 loot/PRD 的区别**：保底是「跨抽累计、出货才重置」；
PRD 是「每次 roll 独立、判定后立即重置」。
拿 PRD 做抽卡，玩家会觉得「根本没保底」。

### scoring（战斗评分）

```typescript
const r = sys.evaluate(values);   // total / grade / gaps
sys.weakPoints(values, 3);        // 结算界面「你该改什么」
```

`direction` 必须显式声明，par/zero 写反**构造时立即抛错**——
不抛错的话「打得越快分越低」，且查不出原因。
推荐 `missingAs: 'skip'`，否则缺指标的关卡会天然低分。

---

## 6.6 第十八批速查：表现与运维层

这一批的 8 个模块有一个共同主题：**玩家能感知到、但说不清的"感觉"**。

| 模块 | 它在守护什么 | 失败时不报错，只表现为 |
|---|---|---|
| `audio` | 预警音不许被爆炸声挤掉 | 玩家死得莫名其妙，认为游戏不公平 |
| `bgm-stack` | 音乐切换点不许突兀 | 音乐比画面早三秒，出戏 |
| `cutscene` | 跳过后画面必须到位 | 镜头停在一个很怪的中间画面 |
| `transition` | 加载不许无限等待 | 玩家以为游戏崩了 |
| `minimap` | 正前方必须在正上方 | 玩家完全没法用，但说不出哪里不对 |
| `builder` | 失败时不许吞资源 | 玩家认为你在偷他东西 |
| `auto-quality` | 画质不许来回横跳 | 画面一直在闪，比稳定 50 帧更糟 |
| `diag-pack` | 诊断包不许带敏感信息 | 一次安静发生的数据泄露事故 |

失败模式高度一致——**都不抛异常**。所以这一批的测试里，
很大一部分断言的不是"能不能跑"，而是**"方向对不对"**：
预警音是否还在？正前方是否显示在上方？跳过之后镜头是否到位？手机号是否真的没了？

### 四个「时间语义」要分清

这批里有四个模块各吃一种时间，用错就是 bug：

| 模块 | 该用哪种时间 | 用错会怎样 |
|---|---|---|
| `audio` 延迟队列 | **真实时间** | 暂停时排队的音效永远不触发 |
| `bgm-stack` 过渡 | **真实时间**（不受 timeScale 影响） | 顿帧时音乐变慢 20 倍，像恐怖片音效 |
| `cutscene` 推进 | **缩放时间** | 暂停了演出还在走 |
| `transition` 三段式 | **真实时间** | 转场期间游戏时间不流动，永远走不完 |

规律：**属于"游戏内容"的用缩放时间（暂停时该停），
属于"系统/表现基础设施"的用真实时间（暂停时该继续）。**

### 五个「先问再做」的接口对

下面这些接口对，执行路径必须**复用**询问路径，
两边各写一遍判断迟早会漂移：

| 询问 | 执行 |
|---|---|
| `preview()` | `place()` |
| `canAfford()` | `purchase()` |
| `canRemove()` | `remove()` |
| `isInView()` | `clampToEdge()` |
| `fpsValid` | `medianFps` |

---

## 6.7 第十九批速查：碰撞与移动

这两个模块补的是**"从摇杆方向到角色真的动起来"**这一段。
它不在画面上，玩家看不见，但每个动作游戏都靠它撑手感。

| 模块 | 它在守护什么 | 失败时不报错，只表现为 |
|---|---|---|
| `collision` | 不许穿墙、不许卡在墙角 | 子弹偶尔穿墙（无法复现） |
| `mover` | 不许"贴纸在冰上滑" | 手感不好，但玩家说不出哪里怪 |

### ⚠️ 两个容易混淆的模块

| | `hitbox`（已有） | `collision`（本批） |
|---|---|---|
| 回答什么 | "这个攻击打中了谁" | "这个角色能不能走到这儿" |
| 要什么结果 | 布尔 | **MTV**（推开多少、朝哪） |
| 重叠时 | 造成伤害 | **不允许重叠**，要分离 |
| 典型形状 | 扇形（攻击范围） | 胶囊、AABB（身体、墙） |

**hitbox 管"打到"，collision 管"撞到"。**

### mover 的三个"必须"

| 必须 | 不做会怎样 |
|---|---|
| **撞墙后投影速度** | 贴墙走然后突然飞出去（表面看不出问题，只在特定几何下触发） |
| **外力衰减 + 限强度** | 连续挨打被弹到地图外 |
| **解除击退 ≠ 外力归零** | 多锁 1 秒控制权，玩家觉得"被打后角色不听话" |

### 时间语义

| 模块 | 该用哪种时间 |
|---|---|
| `mover.update` | **缩放时间**（暂停时角色该停） |
| `audio` 延迟队列 | **真实时间**（暂停时排队的音效仍要触发） |
| `bgm-stack` 过渡 | **真实时间**（顿帧时音乐不该变慢） |
| `transition` 三段式 | **真实时间**（转场期间游戏时间不流动） |

规律：**属于"游戏内容"的用缩放时间，属于"系统/表现基础设施"的用真实时间。**

---

## 6.8 第二十批速查：模块间适配器

> 这一批不是新功能，而是**把 3 处重复胶水正式化**。
> 它们来自 `npm run probe`（接口连通性体检）的实测结论。

### 三处胶水的来源

| 胶水 | 手写 | 用 adapters |
|---|---|---|
| 地牢一维数组 → 二维数组 | 6 行 | `toGrid2D(dun)` |
| HitResult → CasterHit | 4 行 | `toCasterHits(hits)` |
| LootDrop → Inventory | 3 行 + 递归 | `flattenDrops(drops)` |

判据：**换任何项目都要再写一遍的代码，就该是库的一部分。**

### ⚠️ 为什么不是直接改那两个模块

**① 不能让 dungeon 依赖 fov** —— 两者同为第 1 层。
**② 改格式会破坏已有调用方** —— `makeWallTest` 的签名已被 8 处引用。
**③ 适配器是"可选依赖"的正确形态** —— 依赖规则 v2：必需才依赖，可选就注入。

dungeon 不"必需"转给 fov——同一个地牢也可能根本不用视野系统。

### 准入标准

只有满足**全部**条件才配进 `adapters/`：

- ✓ 连接两个及以上已有插件
- ✓ 转换逻辑与具体游戏无关
- ✓ 无状态
- ✓ 不新增业务概念

反例：「把我的 Enemy 类转成 CasterHit」——那是业务代码，不是适配器。

### 关键设计：用结构化类型，不 import 插件

```typescript
export interface ITileSource {
  readonly width: number;
  readonly height: number;
  tileAt(x: number, y: number): number;   // 越界必须返回墙
}
```

适配器一旦 import 具体类，它就从"可选的桥"变成了"硬依赖"。
而 dungeon 只是地牢的**一种**来源。

### 测试抓到的：我自己写错的断言

29 项测试一次通过，没有抓到实现 bug。
但抓到了我自己的一条错误断言：

> 「掉落 → 摊平 → 入包」我预期 3 种物品，实际是 4 种。
> **漏算的正是宝箱本身**——而这恰恰是本模块要解决的核心问题
> （手写递归时最容易漏掉"父项也是物品"）。

断言已修正为 4，并保留注释。这从侧面说明这段逻辑为什么值得做成正式模块。

---

## 6.9 第二十一批速查：统一实体注册表

> 这是体检发现的**头号缺失**：库里没有"根据 id 取实体"的统一入口，
> 而各模块对 id 的假设还不一致。

### 各模块对 id 的说法（实测）

| 模块 | 类型 | 语义 |
|---|---|---|
| `hitbox` | string | **判定框** id（一个实体多个框） |
| `collision` | number | **碰撞体** id |
| `perception` / `attack-token` / `targeting` | number | 实体 id |
| `skill-caster` | string | 命中 id（透传） |

所以"命中了 → 这个 id 是谁"没有地方能回答。

### 设计：映射，而不是统一

不去改 hitbox 的 string，也不去统一 collision 的语义——
那些差异是**真实存在**的。建立映射即可：

```
实体 id (number，带版本号)
   ├── hitbox:     'body' / 'weak'   (string，多对一)
   ├── collision:  1234              (number)
   └── perception / attack-token:    直接复用实体 id
```

### ⚠️ 三个必须记住的点

**① id 带版本号**

```
1. 怪物 A 占用 id 7
2. A 死亡，槽位回收
3. 怪物 B 复用 id 7
4. 技能持有"目标 = 7"的旧引用 → 打在 B 身上
```

症状是"**偶尔打错人**"，无法复现。
现在 `id = index + gen × 1048576`，复用时 `gen + 1`，旧 id 永远查不到。

**② kill 与 destroy 分离**

| 方法 | 做什么 |
|---|---|
| `kill(id)` | 标记死亡 + 触发回调，**保留记录**（尸体要留着掉落） |
| `destroy(id)` | 真正移除并回收槽位 |

`kill` 可重复调用但**回调只触发一次**——
否则"一帧内被两发子弹打死"会掉两份装备，那是玩家会主动刷的漏洞。

**③ 死亡结算的归宿**

`IDamageable.isAlive()` 存在但没人调它，
而 `applyDamage` 的注释明令禁止在里面掉物品。

正确链路：

```
伤害结算 → 业务侧判断 hp<=0 → registry.kill(id)
         → onDeath 回调（掉落 / 成就 / 移除 hitbox / 释放令牌）
```

### 测试抓到的 5 个真 bug（本批）

| # | 缺陷 |
|---|---|
| 1 | **id 用位移打包 → 32 位溢出** —— `gen=2048` 时溢出成负数，实体凭空消失 |
| 2 | **⚠️ `clear()` 重置版本号 → 旧 id 全部复活** —— 换关后技能打在新关的怪身上 |
| 3 | **`spawn` 新槽位硬编码 `gen=0`** —— 绕过第 2 条的修复，让它形同虚设 |
| 4 | **⚠️ 槽位 index 从 0 开始** —— `makeId(0,gen)` 与 `INVALID_ID=0` 冲突，**第一个生成的实体永远查不到**，而后续实体全部正常。第 0 个槽位必须占位不用 |
| 5 | **⚠️ 一个实体多个判定框不去重** —— 一次攻击命中 body+weak 两个框 → 扣两次血。实测 `hp = 100-60-60 = -20` |

> 第 2 和第 3 条是同一个 bug 的两半——只修一个，另一个会让它继续失效。
>
> **第 5 条是在真实链路验证时跑出来的，不是单测跑出来的。**
> 48 项单测全绿，但把 `EntityRegistry` 和 `HitboxWorld` 真接起来一跑，
> 立刻发现一刀扣两次血。
>
> 这正是 `npm run probe` 那套组合检查的价值所在——
> **"各自正确"不等于"能拼起来"。**

修复：新增 `adapters.uniqueEntityHits(hits, entityOf)`。

```typescript
const hits = world.query(sector(2, 90), px, py, facing, MASK);
for (const h of uniqueEntityHits(hits, (id) => reg.fromAlias(id))) {
  // 同一实体只结算一次，且保留距离最近（弱点优先于身体）
}
```

## 6.10 第二十二批速查：技能排队与 CD 补发

> 起因是用户的一个具体问题：
> **"技能还有 0.1 秒 CD，玩家提前点击，能自动释放吗？"**

### 实测：两种接法的差别

CD 1.0s，剩 0.1s 时点击：

| 接法 | 释放次数 |
|---|---|
| `consume()` → `tryCast()` | **1 次** ✗ 输入丢失 |
| `peek()` → `tryCast()` → 成功才 `consume()` | **2 次** ✓ 自动补发 |

```typescript
// ❌ 输入在这里被吃掉
if (buf.consume('fire')) {
  caster.tryCast('fire', ctx);   // CD 还差 0.08s → 失败，但缓冲没了
}

// ✅ 失败不清缓冲，只有真成功（或确定没戏）才清
if (buf.peek('fire') && queue.request('fire', ctx)) {
  buf.consume('fire');
}
```

玩家点了、UI 没反应、技能也没放——**比不点还糟**，他会再点一次。

### 三条核心规则

| 规则 | 违反的后果 |
|---|---|
| 失败时不清缓冲 | 输入丢失，玩家连点 |
| 区分暂时/永久失败 | 蓝不够还一直等，玩家不知道为什么 |
| 延迟释放取**释放时**的位置 | 人走了技能在原地放出来 |

第三条要传**函数**而不是对象：

```typescript
queue.request('fire', () => ({ x: player.x, y: player.y, facingDeg: player.facing }));
//                    ^^^^ 释放那一刻才求值
```

### 失败分类

| 失败原因 | 默认 | 理由 |
|---|---|---|
| `cooldown` / `busy` | **等** | 状态会自己变好 |
| `out-of-range` / `too-close` | **等** | 玩家可能在移动 |
| `resource` | 丢 | 蓝不会自己涨回来，要给明确提示 |
| `unknown-skill` / `invalid` | 丢 | 永远不会成功 |

### nearMiss：窗口配短了会报警

```
CD 0.05s / 后摇 0.15s / 窗口 0.08s：
⚠️ nearMiss ×1 —— fire 等了 0.083s
   意思是「CD 其实早就好了，只是窗口不够等到后摇结束」
```

配窗口用 `suggestWindow(maxLead)`，给 **2 倍余量**：
`suggestWindow(0.1)` → `0.2`，`suggestWindow(0.15)` → `0.3`。

**窗口要覆盖「CD 剩余 + 后摇」，不只是 CD。**

### 依赖：前置模块

```
skill-queue(L3) → skill-caster(L2)
```

按《依赖规则 v2》的判据：技能排队**必须**依赖施法器才能工作，
是必需依赖，不是可选注入。

但对输入源只要求 `peek` / `consume` 两个方法（结构化类型），
**不 import `InputBuffer`** —— 接自己的输入系统也行。

---

## 7. 常见问题

**Q：插件里有 `import { ... } from 'cc'`，还能用吗？**
能，但只有 `joystick-mover/JoystickMover.ts` 一个文件有。其余全部是纯 TS。

**Q：`typings/` 要不要删？**
在**真实 Cocos 项目里必须删**（用引擎自带类型）。只在 Node 环境跑测试时才需要它。

**Q：能只复制一部分插件吗？**
能。每个插件自包含，只依赖 `_core/`。复制时带上 `_core/` 就行。

**Q：怎么给插件加上引擎相关的层？**
参考 `joystick-mover/` 的拆法：纯逻辑 Core（可测）+ 引擎适配层（薄、无逻辑判断）。

**Q：发现某个插件不满足需求怎么办？**
优先改配置（铁律 4），其次用组合（在业务层包一层），
最后才改插件源码——改之前先确认这需求是"机制"还是"内容"，
内容不该进插件。

**Q：为什么有些功能有两份？用哪个？**

这是 v1「禁止一切横向依赖」时期留下的痕迹：为了不 import，
有些代码被**复制**了——这比横向依赖危险得多，
因为复制出去的代码会各自演化、各自出 bug。

现在已全部补齐测试并记录，共 5 处：

| 功能 | A | B | 怎么选 |
|---|---|---|---|
| **寻路** | `pathfind/`（流场、地形代价、飞行无视墙） | `pathfinding/`（自带 GridGraph，更省事） | 要流场/地形代价 → `pathfind`；图省事 → `pathfinding` |
| **空间哈希** | `ds/SpatialHash`（数字 id，轻） | `spatial/SpatialHash<T>`（字符串 id，带 `queryNearest`） | 要"找最近 3 个" → `spatial`；其余 → `ds` |
| **矩形重叠** | `ds/rectsOverlap` | `dungeon/rectsOverlap` | 同签名，`ds` 版是修过 padding 不对称 bug 的 |
| **补丁/深拷贝** | `skill-variant`（**原地修改**） | `snapshot`（**返回新对象**） | 改配置 → `skill-variant`；回滚状态 → `snapshot` |
| **评分** | `score`（**有状态**，实时累积） | `scoring`（**无状态**，一次性） | 边玩边记 → `score`；批量模拟 → `scoring` |

**后两组不是重复，是两种范式**：
- `skill-variant` 改配置（原地），`snapshot` 回滚状态（返回新对象）
- `score` 持续追踪（有状态），`scoring` 纯函数计算（无状态）

前三组的处理原则：**新项目用新版**，旧版保留是因为
`examples/batch3-usage.ts` 依赖它们，强行合并会破坏已有示例和测试。

**Q：算法类的插件为什么测试这么多？**
因为它们**不会抛异常**。第五批 206 项测试抓出了 5 个 bug，
共同形态都是"90% 情况对，10% 情况诡异"：
堆里少一个数、视野缺一圈、第二次寻路必失败……
这些靠肉眼看代码和手玩都发现不了。

---

## 8. 文件统计

> 数字由脚本实测得出，不是估算。

| 项 | 数量 |
|---|---|
| 插件 | **119 个单元**（落在 119 个目录里，**一一对应**） |
| 源码行数 | **59,413 行**（145 个 .ts，不含 tests/examples） |
| 测试行数 | **32,125 行**（25 个 .ts） |
| 示例行数 | **8,700 行**（20 个 .ts） |
| 测试用例 | **3,190 项，全部通过** |
| 示例 | **20 个，全部可运行** |
| 运行时依赖 | **0** |
| 引擎耦合文件 | **1 个**（`JoystickMover.ts`） |
| 文档 | **121 份**插件 README + **7 份**顶层文档 |

> **一个目录 = 一个插件单元，一一对应。**
>
> 一个目录下通常有多个 `.ts` 文件，它们是**同一模块的不同组成部分**，
> 可以自由互相 import——那是**实现细节**，不是"插件间依赖"：
>
> | 目录 | 内含 |
> |---|---|
> | `audio/` | AudioManager + BGMStack |
> | `camera/` | CameraShake + CameraFollow |
> | `ranking/` | RankTier + SeasonReward |
> | `matchops/` | Reconnect / Surrender / Spectate |
> | `elo/` | Elo + Glicko2 |
>
> "禁止横向依赖"约束的是**目录之间**，这才是真正需要守的边界。
>
> ⚠️ 有 **6 个**目录的**单元名**与目录名不同（见全景图里的括号标注），
> 原因是登记时用了更精确的职责名（如 `mmr/` 单元名 `team-mmr`）。
> **按目录名找文件，按单元名理解职责。**
>
> 完整清单：`analytics/`→`abtest`、`anticheat/`→`anti-cheat`、
> `autoquality/`→`auto-quality`、`diagpack/`→`diag-pack`、
> `mmr/`→`team-mmr`、`social/`→`report`。
>
> 【为什么要把完整清单写进来】
> 这里原本写"5 个"，漏了 `social/（report）`——
> 它在全景图里是分支末尾，用的是 `└──` 而不是 `├──`，
> 用 `grep '├──'` 统计时会漏掉。
> **写死数字 + 只举一个例子 = 无法自查**，
> 所以改成"数字 + 完整清单"，任何人 grep 一下就能验。

### 各批测试分布

> 数字取自 `npm test` 的**实际输出**（按批数 ✓ 条数），合计精确等于 3,180。
>
> 重新统计：
> ```bash
> node .build/tests/run.js > /tmp/all.log 2>&1
> python3 scripts/count-suites.py /tmp/all.log
> ```
>
> ⚠️ **不要数源码里 `test(` 的出现次数**——静态数与运行时通过数对不上
> （曾经数出 2552、实际通过 2576，差 24 项且查不出来源）。

| 批次 | 内容 | 测试数 |
|---|---|---|
| 第一批 | 核心（EventBus / Pool / RNG / Damage / Skill / Joystick） | 86 |
| P0 地基 | Logger / Scheduler / Config / Input | 87 |
| 第二批 | 内容与系统（loot / fsm / BT / buff / curve / result / signal / save / roller / pathfinding / spatial / condition / tween） | 208 |
| 第三批 | 基础设施（di / scheduling / inventory / card / dialogue / expression / snapshot / replay / i18n） | 151 |
| 第四批 | 玩法框架（turn / grid / quest / shop / craft） | 126 |
| 第五批 | 算法工具（ds / pathfind / fov / dungeon / steering / noise） | 206 |
| 第六批 | 战斗手感（attribute / hitbox / projectile / indicator / telegraph / skill-caster / input-buffer / dash / camera-shake） | 131 |
| 第七批 | 肉鸽核心（room-graph / wave-spawner / meta / affix） | 123 |
| 第八批 | 系统与深度（perception / attack-token / bullet-pattern / element / targeting / combo / objective / difficulty / gacha / scoring） | 168 |
| 第九批 | 工程效率（command / debug-console / binary / crash） | 114 |
| 第十批 | 玩法补完（Chest 加权 / runscope / setbonus / daily / leaderboard） | 102 |
| 第十一批 | 模块内聚（skill-variant / blessing / curse / interact / score） | 134 |
| 第十二批 | 工程与系统补完（observable / cheatcode / timeutil / uinav / telemetry / rarity / stats / achievement / gesture / rebind） | 282 |
| 第十三批 | GameFlow 孤儿插件收尾 | 40 |
| 第十四批 | 表现层收尾（camera-follow / feedback / progressbar） | 87 |
| 第十五批 | 外壳层（currency / settings / subtitle / tutorial / reddot / scenerouter / accessibility） | 162 |
| 第十六批 | 评分与匹配（elo / glicko2 / matchmaker / team-balancer / lobby / rank-tier） | 154 |
| 第十七批 | 对局生命周期（team-mmr / reconnect / surrender / spectate / report / anticheat / season-reward / abtest） | 215 |
| **第十八批** | **表现与运维层（audio / bgm-stack / cutscene / transition / minimap / builder / auto-quality / diag-pack）** | **219** |
| **第十九批** | **碰撞与移动（collision / mover）** | **72** |
| **第二十批** | **模块间适配器（adapters）** | **33** |
| **第二十一批** | **统一实体注册表（entity）** | **48** |
| **第二十二批** | **技能排队与 CD 补发（skill-queue）** | **29** |
| **第二十三批** | **随机化（模糊）测试（对拍 / 随机采样 / 畸形输入）** | **27** |
| **第二十四批** | **弱测单元补强（队形 / VisibilityMap / SpatialHash / createAgent）** | **30** |
| **合计** | | **3180** |

### 示例

```bash
npm run example         # 第一批：基础链路
npm run example:p0      # P0 地基（真暂停演示）
npm run example:batch3  # 迷你 Roguelike 一层
npm run example:batch4  # 游戏外壳
npm run example:batch5  # 据点经营 + 回合制战斗
npm run example:batch6  # 程序化地形 → 地牢 → 寻路 → 视野 → 怪群（第五批综合）
npm run example:batch7  # 一场完整的战斗：属性 → 判定 → 预警 → 技能 → 冲刺 → 震屏（第六批综合）
npm run example:batch8  # 肉鸽完整闭环：房间图 → 波次战斗 → 词条掉落 → 元进度 → 存档 → 伤害结算（第七批综合）
npm run example:batch9  # 公平性闭环：潜行感知 → 围攻调度 → Boss 弹幕 → 元素反应 → 目标锁定 → 连招 → 关卡目标 → 战斗评分 → 抽卡保底（第八批综合）
npm run example:batch10 # 开发期工具链：编辑器撤销 → 调试控制台 → 存档压缩 → 崩溃还原（第九批综合）
npm run example:batch11 # 肉鸽补完：三选一权重 → 局内外隔离 → 装备套装 → 每日挑战 → 排行榜（第十批综合）
npm run example:batch12 # 模块内聚：技能变体 → 祝福堆叠 → 诅咒代价 → 环境互动（第十一批综合）
npm run example:batch15 # 游戏外壳：设置 → 红点 → 货币 → 血条 → 打击反馈 → 相机 → 字幕 → 引导 → 场景路由（第十四/十五批综合）
npm run example:batch17 # 评分与匹配：ELO → Glicko-2 → 撮合 → 分队 → 房间 → 段位（第十六批综合）
npm run example:batch18 # 对局生命周期：组队分 → 断线 → 投降 → 观战 → 举报 → 反作弊 → 发奖 → A/B（第十七批综合）
npm run example:batch19 # 表现与运维：音效抢占 → 分层音乐 → 演出跳过 → 转场超时 → 小地图朝向 → 建造返还 → 自动降级 → 诊断脱敏（第十八批综合）
npm run example:batch21 # 技能排队：问题复现 → 自动补发 → 延迟求值 → 失败分类 → 窗口自检 → 与 InputBuffer 配合 → 优先级 → 完整一局（第二十二批综合）
```

> 另有 `batch14`、`batch16` 两个编号空缺——批次号与示例号没有逐一对应，
> 内容均以 `batch15 / batch17 / batch18` 为准，不缺内容。

---

## 9. 测试抓到的真实缺陷

> **共 83 条。** 这些是**写测试时由断言逼出来的真实缺陷**——
> 全部属于"不抛异常、只是让游戏偶尔有点怪"的类型，
> 靠读代码和手玩都发现不了。
>
> ⚠️ **编号已于本次整理重排。**
> 原因是历史上有两份文档（轮子清单 / 完成度对照表）各自编号，
> 早就对不上了——同一个 79 号在两份文档里指向两条不同的缺陷。
> 编号既然冲突，作为引用锚点的价值就不存在了，
> 于是按批次数重新连续编号。
>
> 另：**编号 6~13（旧编号）的 8 条已丢失**，两份文档里都无存，无法恢复。

| # | 缺陷 | 现象与后果 |
|---|---|---|
| 1 | **第一批** · `Seed.encode/decode` 往返不一致 | `encode(12345)` → `decode` 得 2745 |
| 2 | **第一批** · 极高护甲时伤害归零 | 免疫判定写在了取整**之后** |
| 3 | **第三批** · `FrameScheduler.flush()` 从不移出队列 | guard 撑到 100 万次才退出，flush 卡住数秒 |
| 4 | **第三批** · DI "你是不是想找 xxx" 对拼写错误无效 | 用子串匹配，`playerServce` 提示不出来 |
| 5 | **第三批** · I18N 覆盖率把已翻译的判成未翻译 | 复数形式键名不同 |
| 6 | **第四批** · 稀疏数组的 `filter` 跳过空洞 | `new Array(100).filter(...).length === 0` |
| 7 | **第四批** · Shop 流水不记货币类型 | 多货币游戏里金币和钻石的账混着算 |
| 8 | **第四批** · TurnSystem 的 guard 没生效 | 声明了死循环防护却没用上 |
| 9 | **第五批** · A\* 的 `closed` 存了布尔 | **同一个实例第二次 `find` 起永远返回空路径** |
| 10 | **第五批** · 八分体矩阵符号写反 | 视野从圆形变菱形（81 格只看到 47 格，且"看着也像那么回事"） |
| 11 | **第五批** · QuadTree 分裂重复计数 | 插 50 个对象，`count` 变成 95 |
| 12 | **第五批** · `rectsOverlap` 的 padding 只加一侧 | 判定不对称 |
| 13 | **第五批** · `obstacleAvoid` 零向量退化 | 障碍在正前方时避障力为 0，径直撞上去 |
| 14 | **第六批** · 冲刺末段对位移打折 | 位移冲到 4.26m 又退回 4.03m，终点抖一下 |
| 15 | **第六批** · 判定写在阶段推进之后 | `hitDelay: 0` 的技能**永远打不中** |
| 16 | **第六批** · `origin` 默认用瞄准点 | 扇形近战全部打空（身前的怪在扇形背后） |
| 17 | **第六批** · 查询形状未应用 offset | 剑气判定建在脚下而不是身前 |
| 18 | **第六批** · `old` 在修改之后才取 | 属性变了但通知永不触发（UI 不刷新，无任何报错） |
| 19 | **第六批** · 测试双比真实实现还宽松 | mock 忽略形状，"打不到远处"这类测试无法成立 |
| 20 | **第七批** · 已用记录从未 add | `allowDuplicate` 形同虚设，同组两暴击率共存，池子永远抽不干 |
| 21 | **第七批** · 四舍五入让值超出上界 | 0.1875 取 2 位 → 0.19 > max，范围契约失效 |
| 22 | **第七批** · 未知前置阻塞解锁 | 配置笔误让节点永远解锁不了（无报错） |
| 23 | **第七批** · 波次推进后读越界 wave | 清完最后一波瞬间崩溃，只在"怪卡住"时复现 |
| 24 | **第七批** · `round` 分配不均导致出度超限 | maxOutDegree=2 却出现出度 3 的扇子形 |
| 25 | **第七批** · `minOutDegree=0` 无效 | 无条件给出边，想要死路（隐藏房间）永远实现不了 |
| 26 | **第七批** · 宽度比例无约束 | 上层 1 个下层 6 个 → 必然死图 |
| 27 | **第七批** · `pathHeat` 只从单个入口走 | 末层多节点时热度算少，平衡数据错误 |
| 28 | **第七批** · 货币名对不上静默失败 | 攒了 320 灵魂买不起 20 的东西，无任何报错 |
| 29 | **第九批** · Gacha 硬保底差一错误 | `n + 1 >= hardPity` → 第 **89** 抽就触发，承诺是第 90 抽 |
| 30 | **第九批** · AttackToken.reset 残留一个令牌 | 循环调 `release()` 会触发 `_promote()` 补位，队列里的被提上来且**从未收到 onRelease** |
| 31 | **第九批** · simulate 干涸统计口径与保底不一致 | 出五星时四星 dry 继续累加，报出 `worstDry['r4']=23` 这种"保底失效"假象 |
| 32 | **第九批** · 崩溃指纹含错误标题行 | `TypeError:TypeError: Cannot read...`，标题含动态内容 → 同一 bug 炸出大量指纹 |
| 33 | **第九批** · 崩溃指纹含行号 | `bossPhaseTwo (game.js:343:15)`，改一次代码就换新指纹，去重失效 |
| 34 | **第九批** · Tab 补全对 `"mode "` 无反应 | `rest === ''` 时令 `argParts = []`，`idx = -1` 取不到参数定义 |
| 35 | **第九批** · uint/int 把 1.5 报成"越界" | 1.5 在范围内却报越界，排查方向跑偏（去查 schema 而非传进来的值） |
| 36 | **第十批** · ordinal 排名写成 else if | 100,100,90 算出 1,2,2——第 2、3 名并列了，而 ordinal 的定义是永不并列 |
| 37 | **第十批** · 可选布尔用 `!x` 判断 | `bestPerPlayer` 未传时是 undefined，`!undefined === true` → 合并出**空榜**且不报错 |
| 38 | **第十一批** · 超出半径的物件被当成有效候选 | "不在范围内"和"满足条件"都返回 null，导致站着能开 10 米外的箱子 |
| 39 | **第十一批** · allowDuplicate 用 Map 存导致覆盖 | 同名诅咒 `set` 覆盖了，count 永远 1——开了开关却没效果 |
| 40 | **第十一批** · patch.add 把"路径不存在"报成"不是数字" | 排查方向跑偏（去查值而不是查路径） |
| 41 | **第十一批** · Blessing 互斥只在声明方后加入时生效 | 先 add('a') 再 add('b') 能共存，反过来却拒绝——同一份配置换顺序结果不同 |
| 42 | **第十一批** · Curse 的 canRemove 与 remove 结论相反 | 没有 removeCondition 的"不可移除"诅咒反而被直接删掉 |
| 43 | **第十八批** · 等功率交叉淡变写在了"插值系数"上，两种曲线结果完全相同 | 直觉做法是给插值系数 k 加曲线再 `from + (to-from)*k`。但 fade-out 层需要 `k = 1-cosθ`、fade-in 层需要 `k = sinθ`——**两者的 k 不一样**，单个 k 无法同时满足。结果 `equal-power` 与 `linear` 的中途输出都是 0.500，所谓"等功率"根本没生效。改为在**功率域**插值 `√(from²cos²θ + to²sin²θ)` 后，两层交叉的最低总能量从 0.707 变成 1.000 |
| 44 | **第十八批** · Cutscene 的门在同一帧被"结束"，门控形同虚设 | wait step 时长为 0，`end === start`，照常判定"到达 end 就算结束"会在**进入的同一帧**标记 exited，时间直接越过门，后面的内容照常播放。修法：wait step 不参与时间到达判定，且 update 先找出下一个门的位置，**本帧最多推进到那里** |
| 45 | **第十八批** · 门被放行后又因"未 exit"重新武装，超时后仍卡死 | `_apply` 里对所有 step 统一写 `if (!shouldExit && s.exited) s.exited = false`，而门的 `shouldExit` 恒为 false → 超时解除的下一帧门又堵上，演出永远播不下去。改为只有"时间真的回退到起点之前"才重新武装 |
| 46 | **第十八批** · AutoQuality 用双向计数器，方向反转时要先"还债" | 快帧时 `strikes--` 减到 -2N，之后慢帧要从 -2N 加回 +N 才触发降级——玩家已经卡了三秒，系统还在还债。改为方向一变就清零，计数才是真正的"连续 N 次"。顺带：最低档/最高档时把计数**钉在阈值上**而非清零，否则每帧都白跑一次 `findIndex` |
| 47 | **第十八批** · 冷却期内的 fps 读数会自相矛盾 | 切换档位会清空采样窗口，缓冲里残留上一档位的帧时间 → 显示"已降到低画质，但帧率 60"。加 `fpsValid` 标志，调试面板必须先判它 |
| 48 | **P0** | `everyFrame` 被当成一次性任务 `interval=null` 导致只执行一帧 |
| 49 | **CameraFollow 的 maxSpeed 限制的是滞后距离，不是速度** | smoothDamp 沿用 Unity 语义，夹的是 `current - target`。目标瞬移 10000 时相机一帧就跳到只剩 1 像素——**maxSpeed 反而让它跳得更快**。修法是在 update 末尾再夹一次真实单帧位移 |
| 50 | **ProgressBar 分段索引用了 floor** | 3 段 300 血时，200 血覆盖满两段，应显示在第 2 段且填满。用 floor 会显示"第 3 段填充 0%"——玩家看到三段都在但第三段空着，看起来像"还有血"，与直觉相反 |
| 51 | **RedDot 根节点的 prefix 是空串不是分隔符** | 写成 `path + '/'` 的话，根路径 `''` 变成 `'/'`，而 `'mail/system'` 不以 `'/'` 开头——**根永远显示 0**，表现为"子项都亮着、总入口没有红点" |
| 52 | **RedDot.add 运算符优先级** | `Math.max(0, cur) + delta` 在 cur=3、delta=-10 时得到 -7，然后被 set() 的校验拒绝并**抛错**。但"扣到 0"是合法意图，不该崩溃。改成先相加再夹 |
| 53 | **Accessibility 导入脏数据** | 手改存档里的 `longPressMs: -999` 会被夹到 200（边界）而不是保留默认 600。这个不算 bug（夹取是合理的），但**文档和示例的预期要写对**——本批修正的是测试断言，不是实现 |
| 54 | **超时兜底只在 `_pickBest` 里放开了范围** | 候选收集阶段的 `_mutuallyAcceptable` 已经把人筛没了，后一道拿到空数组 → 分差 1000 的两人即使等到超时也撮合不到一起。而凌晨人少恰恰是"保证撮合"唯一该起作用的场景 |
| 55 | **撮合时数"组"而不是数"人"** | `candidates.length + 1 < matchSize`。全是散人时组数恰好等于人数，看不出问题；一旦有组队（3 人黑店 + 3 散人 = 4 组 6 人），`4+1 < 6` 判定人不够 → 永远撮不出来 |
| 56 | **`enqueue` 与 `enqueueAll` 语义混淆** | `enqueue` 表示"这批人是一支队伍"，传 10 个散人会直接报"超过每队上限"。这不是实现 bug 而是 API 设计问题，已补 `enqueueAll` 并在报错里提示 |
| 57 | **ELO 取整让低分段"冻住"** | 150 分的人每局理论变化 0.0008 分，取整后恒为 0 → 玩家发现自己"怎么打都不涨分"。解法是 `allowDecimal: true` 内部保留小数 |
| 58 | **matchmaker 的 `as never` 掩盖了类型问题** | `_pickBest` 返回 `[anchor as never, ...picked]`。抽出 `Group` 类型后，`as never` 消失了，顺带发现 `_tryMatch` 里有个没用上的 `pool` 变量 |
| 59 | **裸 FNV-1a 雪崩性不足导致实验间分组高度重合** | 两个"互相独立"的实验，分组合率期望 50%，实测 24%~64% 大幅波动（2 万样本稳定在 35.6%，非随机波动）。同一批用户在所有实验里都在实验组，行为被系统性改变 → **实验互相污染，结论全部不可信**。修法是给 `bucketHash` 加 murmur3 fmix32 收尾，修完各对稳定在 49~51% |
| 60 | **Surrender 的所需票数会随掉线变化** | 5 人需 3 票，投到一半掉 2 人，分母变 3、需求降到 2 → 本来通不过的投票因掉线而通过。**更糟的是可被主动利用**：想投降的人让队友"掉线"就能降低门槛。改为在 `start()` 时冻结需求 |
| 61 | **SeasonReward 的批内去重把首次也判为重复** | `_isDuplicateInBatch` 扫描整个数组判断"有没有重复"，它对**每一次**出现都返回 true（包括首次），结果重复 id 会被整批跳过、一个都拿不到奖励。改为统一由 `done` 集合承担——`done.add()` 在循环内执行，批内去重自动成立 |
| 62 | **Report 的两套"权重"口径不一致** | `weightOf()` 返回 `cred/100`（0.2~1.0，不含理由），`severityWeighted()` 返回 `cred/100 × 理由`（0.2~5.0）。两者都叫"权重"，但 `actionThreshold` 只该比前者。混用会让"1 条作弊举报"（5.0）越过"3 个可信玩家"（3.0）的门槛 |
| 63 | **describeResult 硬编码了样本量门槛** | 文案里写死 `1000`，而 `minSamples` 是可配的。构造时传 500 的话，文案会说"还差 500"却显示门槛 1000。改为把 `minSamples` 放进结果对象 |
| 64 | **功率域插值顺带修掉了"中间音量涨上去"** | 同一条公式下，一个"两层都保持 0.8"的过渡，中间不会涨到 1.13 再落回 0.8。只用系数曲线的版本必然出现这个问题，听起来像音量被推了一下 |
| 65 | **BGMStack 在"插值系数"上做等功率曲线，两层交叉时失效** | fade-out 层需要 `k = 1 - cos θ`，fade-in 层需要 `k = sin θ`，**单个 k 无法同时满足**。但 linear 与 equal-power 在两层交叉时算出完全相同的中间值（都是 0.5），总能量也一样（0.707）——对照组根本没成立。改为在**功率域**插值 `√(from²·cos²θ + to²·sin²θ)` |
| 66 | **BGMStack 过渡途中重复设置同一状态会重启过渡** | 判据写成 `next === state && !inTransition`，意味着过渡中重复调用**会**重启。调用方每帧写 `setState(当前状态)` 的话，过渡永远停在起点、音量卡在 0。改为同状态一律返回 false |
| 67 | **Minimap 旋转分支后又统一翻了一次 Y** | 旋转分支里 `(dx, dy) = (-lateral, -forward)` **已经完成了 Y 翻转**，之后又做 `my = -my` 等于翻两次。表现为观察者的正前方显示在**右边**，整张小地图转了 90°，玩家完全没法用却说不出哪里不对 |
| 68 | **Minimap 圆形半径用了 viewSize.x 而非外接盒的一半** | `viewSize = {200,200}` 的内切圆半径是 100，早期版本当成 200，可视范围变成 4 倍。而 `clampToEdge` 用了另一个口径，于是边界外的图标全挤在角落上 |
| 69 | **`clampToEdge: false` 被静默忽略** | 该方法无条件钳制，而 `layout()` 又用它的结果判断 visible，导致配置开关失效——想关掉钳制的人发现关不掉 |
| 70 | **AutoQuality 冷却期内的 fps 读数是上一档位的残留** | 切换档位会清空采样窗口，缓冲区里剩下的是旧帧时间。直接展示会得出"已降到低画质，但帧率显示 60"这种自相矛盾的输出。新增 `fpsValid` 字段显式标记读数是否可信 |
| 71 | **示例自己都解释不通** | AutoQuality 演示在降级后立刻读 `medianFps`，输出"掉到 35fps → 档位低，帧率 60.0fps"；另一处喂了 240 帧把那帧 500ms 挤出窗口，导致"均值被污染"的对照组根本没成立。示例是给人看的，自相矛盾比没有更糟 |
| 72 | **⚠️ `toPolygon` 返回局部坐标而非世界坐标** | SAT 把两个**不同原点**的形状的局部顶点投影到同一条轴上比较，区间完全错位。**极隐蔽**：形状都在原点附近时结果正确，只有位置拉开距离后才出错——而单元测试很容易只测原点附近。修法：每种形状都加上自己的位置偏移 |
| 73 | **SAT 分离判定用了 `<=`** | `maxA == minB` 是"刚好边贴边"，应算重叠（depth=0，不需推开）。用 `<=` 判分离会让贴墙状态被误判为自由，角色下一帧就能挤进墙里一点点——累积起来就是"偶尔能穿墙" |
| 74 | **退化线段（长度为 0）产生 NaN** | 胶囊两端点重合时，`closestPointOnSegment` 除以长度得 NaN，然后一路传播到位置。表现为"角色突然消失"，不抛任何异常 |
| 75 | **⚠️ 击退态解除复用了 `stopEpsilon`** | 实测（跑了 61 帧，dt=1/60，`maxSpeed=8`、`externalDamping=8`、外力 30）：
外力降到 0.07（完全不可感知）要 0.767s，外力完全归零要 1.017s，
而用 `knockbackThreshold`（= `maxSpeed × 0.25` = 2）时控制权 **0.350s** 就交还了。
若复用 `stopEpsilon`(0.01)，控制权要到 1.017s——**多锁 0.667s**，
正是"被打之后角色不听话"的来源新增 `knockbackThreshold` 分离**数值归零**（数学问题）与**解除击退**（体感问题） |
| 76 | **`_approach` 在接近目标速度时振荡** | 写成"朝目标走 maxDelta"会在 `d` 略大于 `maxDelta` 时来回过冲，表现为接近最高速时速度在抖。改为 `d <= maxDelta` 时直接赋值 |
| 77 | **`Tile` 是 `const enum`，运行期不存在** | 编译期被内联，实测 `Dungeon.Tile === undefined`。下游项目写 `=== Tile.Floor` 会报错，只能硬编码 `=== 1`。本库内部无影响（编译期内联），但对**复用**是致命的。改为普通 `enum`。这是 `npm run probe` 跑出来的第一个东西 |
| 78 | **`EntityRegistry` 槽位 index 从 0 开始** | `makeId(0, gen)` 与 `INVALID_ID=0` 冲突，第一个 spawn 得到的 id 被 `isValidId` 判为非法，**第一个实体永远查不到**——而后续实体全部正常，极易误判成偶然。第 0 个槽位改为占位不用 |
| 79 | **⚠️ 一个实体多个判定框不去重** | 骨王有 body/weak 两框，一次挥砍同时命中 → 扣两次血，实测 `hp = 100-60-60 = -20`。框越多翻倍越狠，表现为「大范围技能伤害莫名翻倍」，策划会以为数值填错。新增 `adapters.uniqueEntityHits()` |
| 80 | **⚠️ id 用位移打包导致 32 位溢出** | `(gen << 20) \ index` 在 `gen=2048` 时溢出成负数（JS 位运算是 32 位有符号），`isValidId` 判定失败 → **实体凭空消失**。实测槽位复用 2048 次后触发。改用乘法编码 |
| 81 | **⚠️ `clear()` 重置版本号，旧 id 全部复活** | 换关卡后 `reg.exists(上一关的id)` 返回 true，上一关残留的技能引用打在新关卡的怪身上。**比普通 ABA 更隐蔽——只在换关时发生**。改为递增所有版本号 |
| 82 | **`spawn` 新槽位硬编码 `gen=0`** | 绕过 `clear()` 保留的版本号，让第 78 条的修复形同虚设。改为读 `this._gens[index]`，且只在 `_gens` 不够长时才扩容 |
| 83 | **⚠️ SkillQueue 的 nearMiss 判定要求 `!busy`** | 判据写成 `cooldownLeft <= 0 && !busy`。但 **busy 是队列自己允许的等待理由**——CD 早就好了、只差后摇没等完，恰恰是最典型的"窗口没算忙碌时间"，却被这个 `!busy` 掩盖。改为只看 CD |

---

## 10. ⚠️ 给使用者的六条提醒

1. **判据只有一条**：把这个文件夹复制到另一个 Cocos 项目，需要改几行？第 0 层答案是 0。

2. **机制可以复用，内容不要复用。**
   伤害管线是机制 → 复用；骨王的横扫技能是内容 → 不复用。
   行为树框架是机制 → 复用；骷髅兵的行为树配置是内容 → 不复用。

3. **三次法则**：第一次直接写，第二次复制忍住，第三次才抽象。
   第二次就抽往往会抽错——只有见过三次你才知道真正的共性在哪。

4. **写完代码必须同时完成三件事**：① 测试 ② README ③ 登记进 `_kitmeta.json`。
   缺任何一件都等于没写完。本项目已**五次**出现"有实现无测试"的孤儿插件。

5. **先做 1 个样板，再批量。** 这一小时能省你 50 小时。

6. **每日构建必须可运行。** 不管做成什么样，下班前必须能跑。

